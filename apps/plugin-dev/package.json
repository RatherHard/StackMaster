{
  "name": "@stackmaster/plugin-dev",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:plugin-site": "node host-mock/plugin-site-server.mjs",
    "build": "vite build && tsc -b --force",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@stackmaster/embed-runtime": "workspace:*",
    "@stackmaster/vm-ui": "workspace:*",
    "@types/node": "^26.4.0",
    "jsdom": "^30.0.1",
    "vite": "^8.3.0",
    "vitest": "^4.1.11"
  }
}
