# Vm 模块设计

| 项 | 值 |
|---|---|
| 地位 | **硬性规范**(2026-09-04 由项目负责人声明) |
| 效力 | 本文件是 VM 模块的设计规范;与已冻结契约(WP-1/协议/双包 Schema)冲突之处,按整改方案执行契约变更 |
| 整改方案 | `docs/develop/Vm 模块设计冲突与整改方案.md`(冲突清单 G1–G4、实施顺序 P0–P4、决策点 D1–D5) |
| 决策状态 | D1–D5 已裁决(2026-09-04):`archBits` ∈ {32, 64};页大小为 4KB 倍数区间;寄存器上限放宽;指令集自由设定 = "指令作用 ↔ 机器码表现"映射可自由设定(移除 `read`/`write`、新增 `leave`;`syscall`/`call` 可调用出题人声明接口);修订计划书 |
| 实施状态 | **契约层整改已落地**(P0–P3,2026-09-04:G1–G4 冲突项全部勾销,WP-1 v1.6 / 双包 Schema v2 定基;详见整改方案 §二、§五);P4 引擎实现归阶段二,按已定稿契约实现 |

## 设计目标

- vm 的架构：32 位还是 64 位应允许出题人指定；
- 寄存器的名称、种类与数量应允许出题人自由设定；
- 虚拟内存设置以 VMA 为一个基本单元，允许出题人自由设定每一个 VMA 的权限（rwx）、类型（栈区、关键区（可以自行命名）、代码区等）和大小（必须为 4KB 的倍数）；
- 指令集应允许出题人自由设定；
- 向前端提供的 VmState 应当预先对选手还未获取到的内存数据进行隐藏，不应当向前端提前发送这些隐藏信息；