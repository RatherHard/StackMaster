# WP-79 决策登记片段(中期 M3;待并入 `docs/develop/权威API语义规约.md` §三·二十三)

> 说明:本文件是主控为统一登记簿预留的**片段文件**(编码 **D-API-134 ~ D-API-136**,由主控按 M3 分派口径预留)。WP-79「最小管理面」按 **D-MP-5 分支 A**(落地 `apps/admin` 只读最小面)的全部定案在此登记;并入主登记簿后本文件可删除。**本片段不修改** `docs/develop/权威API语义规约.md`(禁改面,由主控并入)。

## 工作包定位

D-MP-5(中期决策「管理后台是否落地」)的**分支 A** = 落地 `apps/admin` **只读最小面**:信任域 4 的第三个独立应用,三张读面(题目登记列表 / 裁决查询 / 成绩导出),独立凭证、独立网络域、独立部署,**不入插件链路**。工作包性质是**读取既有权威数据的公开投影**:零新存储面、零新裁决语义、零 app→app 依赖;服务复用一律走**契约复用**(`HostScoresResponseSchema` / `VerdictQueryResponseSchema` / `PublicErrorSchema`),不 import 任何 `apps/session-api` 内部实现。

三条结构底线(计划书 5.2 / 5.3 / 9.2,`docs/项目计划书.md:807`):
**① 凭证独立**(管理面凭证不得复用会话凭证或 `SESSION_API_HOST_BACKEND_TOKEN`);
**② 网络与部署独立**(自有网络域 + 自有发布端口,浏览器无任何可达的插件链路);
**③ 凭证 → 租户绑定白名单**(空即整体 404 同形,fail-closed)。

---

### D-API-134 管理面身份模型 = **独立凭证(摘要比对)+ 租户绑定白名单**;凭证不从 URL / Cookie 呈递

- **决策**:
  1. **独立凭证,零复用**(硬约束,`docs/项目计划书.md:807`):`ADMIN_CREDENTIAL_SHA256`(**只存摘要**,64 位小写十六进制)为唯一凭证事实;呈递形态 = `Authorization: Bearer <明文>`。**不读** `SESSION_API_*` / `VERIFIER_*` 任何环境键;控制台页把凭证保存在**内存**(零 Cookie、零 `localStorage` / `sessionStorage`、零 URL 参数、零 `document.cookie`),`fetch` 显式 `credentials: "omit"`。
  2. **比对形态 = 摘要 + `timingSafeEqual`**:服务端对呈递明文做 sha256 后与配置摘要常数时间比对(**摘要等长 ⇒ 长度侧信道被折叠**;缺失 / 非 Bearer / 空值 / 长度不符 / 取值错误**一律** 401 + `{code:"invalid_input_format",message:"authentication failed"}`,零原因差异)。明文凭证**不进配置、不进日志**(配置面第三道闸拒绝非摘要形态,启动即拒)。
  3. **租户绑定 = 配置白名单 `ADMIN_TENANTS`**(逗号分隔;去空白 → 去空项 → 去重保序;条目须过冻结标识符字符集 ∧ 长度上限,违规拒绝启动;条目数与批量的天花板同属三道闸)。
     - **空 / 缺失 ⇒ 数据面整体 404 同形**(fail-closed,防枚举;这是**合法配置形态**,不报配置错误);空态下**零存储调用**(连查询都不发生)。
     - **单租户绑定 ⇒ 无参数落到该唯一租户**(零仪式形态);**多租户绑定且无参数 ⇒ 404**(不猜、不回退第一个——静默选租户 = 静默改变查询作用域)。
     - `?tenant=` **只能在绑定集合内子选**(收窄),**禁止由查询参数决定租户**;集合外租户与「不存在」**同形** 404(逐字节一致)。
  4. **闸序(冻结)**:凭证(401)→ 频率(429)→ 租户解析(404)→ 参数闸(400)→ 查询 → **审计(披露点)** → 下发。参数错误**不消费**限流预算之外的东西;401 与 404 路径零存储调用。
- **理由**:
  - **摘要而非明文**:管理面凭证是长期共享秘密,配置面只放摘要 ⇒ 配置泄露(日志 / 环境回显 / 进程列表)不等于凭证泄露;且摘要定长使常数时间比较无长度前置条件。
  - **独立凭证是硬门槛而非偏好**:会话凭证属选手(浏览器不可信),宿主令牌属平台服务端;管理面是运维读取面,三者信任级别与轮换节奏都不同——复用任一把都使「谁读走了成绩」在审计面不可归属。
  - **空白名单 = 404 而非 403**:未开通的面不应告诉探测者「这里有个面」;404 同形与 D-API-32(会话定位失败)/ D-API-124(宿主成绩面空白名单)同款 anti-enumeration 形态,使「租户不存在」「租户存在但未绑给我」「面未开通」在响应面**不可区分**。
- **红灯位置**:`apps/admin/test/config.test.ts` 8 例(缺必备键 / 未知保留键 / 越天花板 / 非摘要凭证 / 白名单条目非法 / 白名单超上限 / `PORT=0` 仅测试环境 / 空占位语义 + 解析去重保序);`test/credential.test.ts` 5 例(凭证矩阵 + **源码机检**:代码文本零 `SESSION_API_*` / `VERIFIER_*` 读取、零 `HOST_BACKEND_TOKEN` / `SIGNING_KEY` / `SESSION_CREDENTIAL` 引用);`test/tenant-binding.test.ts` 4 例(空态恒 404 / 单租户回落 / 多租户不猜 / 参数无法自造租户);`test/server.test.ts` 凭证与租户面 5 例(401 同形三态 + 零存储调用 / 集合内子选 / 跨租户与不存在同形 / 空态整体 404 / 多租户无参 404)。
- **影响面**:新建 `apps/admin/src/auth/credential.ts` / `src/auth/tenant-binding.ts` / `src/config.ts`;compose 新增 `admin` 服务与 `admin-net`(**待应用片段**,见末节);管理面**不进入**任何插件链路或浏览器可达面。

### D-API-135 数据面 = **自有只读 PG 角色 `admin_ro` 直连**;服务复用 = 契约复用(零 app→app 依赖);三张读面的上限与截断语义

- **决策**:
  1. **数据面 = 直连 PG 只读角色 `admin_ro`**,**不 import** `apps/session-api` 任何内部实现(禁 app→app 依赖,dependency-cruiser `admin-workspace-deps-allowlist` 强制;`apps/admin` 的工作区依赖面 = `@stackmaster/protocol` + `@stackmaster/challenge-schema`)。**服务复用 = 契约复用**:成绩导出必须**逐字**通过 `HostScoresResponseSchema.parse()`(WP-78 契约,D-API-123),裁决查询逐条通过 `VerdictQueryResponseSchema`,错误体复用 `PublicErrorSchema` 的冻结常量(401 / 404 / 429 / 503 与宿主面同码同文案)。**零第二套契约实现**。
  2. **三张读面(恰三张,零写面)**:
     - `GET /admin/challenges`——题目登记列表(题目标识 / 标题 / 版本链摘要:内容版本 + VM Profile 版本 + 登记时刻);版本链来自 `challenges` ⋈ `challenge_versions`。
     - `GET /admin/verdicts`——裁决查询(按提交定位 / 题目过滤 / **有界时间窗**;pending 态经 `LEFT JOIN verdicts` 表达)。
     - `GET /admin/scores`——**成绩导出,复用 WP-78 契约与游标语义**(`limit + 1` 探测、`nextCursor` = 末行 id、空页 ⇒ `null`)。
  3. **裁决面不发明第二套游标**:裁决查询用**有界时间窗 + 显式 `truncated: boolean`**,而非再造一套 keyset 语义——**游标语义保持唯一**(归 WP-78 成绩面),避免「同一仓内两套分页语义」的契约内部矛盾。`truncated` 是**有界性的事实陈述**(命中上限),不是错误。
  4. **查询参数严格白名单**(未知参数 ⇒ 400,不静默忽略):`tenant` / `limit`(有默认值与天花板,超限 400 而非钳制)/ `cursor` / `submissionId` / `challengeId` / `since` / `until`。畸形游标在路由层拒绝(400),**不进库层 `::uuid` cast**(否则调用方输入错误会以 503 形态呈现)。**零查询参数可扩权**:无法用参数把任意字符串变成合法租户。
  5. **登记列表出口前的公开包契约校验**(`apps/admin/src/challenges/registry-contract.ts`):题目标识 / 内容版本 / VM Profile 版本逐字复用 `@stackmaster/challenge-schema` 的**模式源**(`CHALLENGE_ID_PATTERN_SOURCE` / `SEMVER_PATTERN_SOURCE`,即公开包 JSON Schema 的同一份 `pattern`)**零副本**;库层无 CHECK(001 迁移是自由文本)⇒ 越界值是**真可能落库**的,校验必须落在读面出口。失败形态 = `AdminRegistryContractViolationError`(继承 `AdminStoreError` ⇒ 走既有 **503 存储不可用**冻结路径,零数据下发),错误文案只含**字段路径**、不回显越界值(注入面自伤防护)。这是 admin 进入 `challenge-schema-dependents-restricted` 白名单的**事实依据**(与 verifier 同款:按公开包 Schema 语义消费题目登记元数据)。
  6. **只读面在四层同时成立**(彼此独立):① **端口面**`AdminReadStore` 只有读方法(写方法不在类型上、不在运行时原型面);② **语句面**`assertReadOnlySql` 运行时护栏(前导关键字 ∈ {`SELECT`,`WITH`};写 / DDL / 授权 / 会话态语句逐条红灯;PG 适配器内**每条**数据语句的入参 = 护栏返回值,零 TOCTOU)+ 源码机检(剥注释后的代码文本零写语句形态、零 `client.query` 绕过护栏);③ **授权面**`admin_ro` 对五个只读表 SELECT 在场、INSERT / UPDATE / DELETE **全假**,`audit_log` **零授权**;④ **行级政策面**五表 `ENABLE + FORCE ROW LEVEL SECURITY` + `TO admin_ro` 的**仅 SELECT** 政策(写面无 permissive 政策 ⇒ 默认拒)。
  7. **RLS 谓词 = 租户绑定**:`tenant_id = current_setting('app.tenant_id', true)`,注入点 = 管理面**自有**连接层每事务 `SET LOCAL`(D-API-101 同款形态,自实现,不 import 编排器代码);**GUC 缺失 ⇒ 谓词恒假 ⇒ 零行**(fail-closed,机检:不经连接层直连 `admin_ro` 读 `verdicts` / `challenge_versions` 恒 0 行)。迁移为**新增** `009_admin_row_level_security.sql`(不改 007;末段内置只读政策自检:若出现 TO `admin_ro` 的非 SELECT 政策,迁移确定性失败)。
  8. **零 DDL 于既有表 / 零新表 / 零新列**:本包只新增角色授权脚本与政策迁移(`compose/admin-db-init.sql` + `009_admin_row_level_security.sql`);**不使用 008**(该号为主控预留)。
- **理由**:
  - **契约复用而非代码复用**:管理面与 session-api 属不同信任域、不同部署单元、不同发布节奏;`import` 内部实现会把两个独立部署单元耦合成同一构建图(独立部署退化为名义),而契约是**版本化序列化格式**——复用契约既拿到语义一致性,又保住独立演进。
  - **直连只读角色而非「读 session-api 的 API」**:管理面要的是**运维读取**,经 session-api 会增加一条浏览器同款服务链路(并让管理面读权限继承编排器的写权限面);`admin_ro` 把「管理面能做什么」压到库层一句话:**五个表、只读、只见绑定租户**。
  - **成绩导出逐字过 `HostScoresResponseSchema.parse()`**:这是**唯一出口**(单点退化 + 越界即抛),使「管理面导出」与「宿主面拉取」在字段集 / 游标语义 / 终态耦合上**结构性同源**——两侧任何漂移都在管理面测试红灯,而不是在跨系统对账时才发现。
  - **三张面而非一张**:题目登记(运营要知道平台里有什么题)、裁决查询(运营要知道某次提交为什么这样判)、成绩导出(对账)是三种不同的**最小充分集**,合并成一张宽表必然多带私有面字段。
- **红灯位置**:`test/scores-export.test.ts` 7 例(**契约一致性机检**:逐字过 `HostScoresResponseSchema.parse` / 顶层字段恰 `items`+`nextCursor` / 逐条字段集 = 契约七字段(`Object.keys(HostScoreRecordSchema.shape)` 派生,非手写清单)/ 载荷零 `detail`·`reference`·`tenantId` / 两页拼合零重零漏 + 游标恒为本页末行 id / 空页耦合 + 反例 / 租户隔离 / **存储返回越界裁决字面 ⇒ 导出抛错**);`test/read-only.test.ts` 7 例(护栏语句矩阵 4 正 + 14 反 / 整词匹配不误伤 `updated_at` / PG 适配器零未过闸语句 / 双实现原型面精确等于只读面 / 源码零写语句形态);`test/registry-contract.test.ts` 6 例(模式源**逐字等于**上游导出 / 图案同形正反样本 / 合法条目放行 / 越界题目标识与两个版本字段逐类抛出 / 失败类型继承 `AdminStoreError` 且文案只含字段路径不含越界值);`test/server.test.ts` 三面契约 5 例 + 只读零写 2 例(pending 三字段 / verdicted 五字段 / 时间窗与 `truncated` / 登记列表字段集 / **越界登记值 ⇒ 503 零下发** / 经 HTTP 的成绩载荷同契约);`test/compose-topology.test.ts` 5 例(**容器门控**,真实 PG:最小授权面 + `audit_log` 误写被库层拒 + RLS ENABLE/FORCE + 仅 SELECT 政策 + 三面真跑 + 租户隔离双层与 GUC 缺失零行);`test/persistence-adapters.test.ts` 14 例(**零容器形状层**:假 `PoolClient` 断言事务控制序 `BEGIN → set_config($1,true) → 语句 → COMMIT`、三条数据语句的参数绑定序、行 → 记录映射(`pending` 优先于半个裁决事实 / `epochSeconds` 边界)、`revision` 不可解析 ⇒ `AdminStoreError`、驱动故障 ⇒ 翻译且必 `ROLLBACK`、**每条数据语句 `assertReadOnlySql(sql) === sql`**(幂等 = 确实过闸)+ 全语句零写关键字零 `detail`/`reference` 列、连接池首连失败 ⇒ 拒绝启动);`test/runtime.integration.test.ts` 4 例(**容器门控全装配**:真实池 / 真实 RLS / 真实三表连接 / 真实 HTTP 三面读数 + 指标边界 / 跨租户与匿名冻结同形 / 停机句柄序 / 空白名单 fail-closed 态)。
- **影响面**:新建 `apps/admin/src/persistence/**`(`ports` / `pg-connection` / `pg-read-store` / `memory-read-store` / `read-only-guard`)、`src/scores/export.ts`、`src/challenges/registry-contract.ts`、`src/routes/admin-routes.ts`;新增 `apps/session-api/compose/admin-db-init.sql` + `apps/session-api/migrations/009_admin_row_level_security.sql`;`tooling/dependency-cruiser.cjs` 两处演进(白名单 + 新规则),`tooling/dependency-boundary-self-test.mjs` 同批新增三组期望;测试面新建 `apps/admin/test/persistence-adapters.test.ts`(假 `PoolClient` 形状层)与 `test/runtime.integration.test.ts`(容器门控全装配)。

### D-API-136 管理面查询的账目落点 = **受控日志 + 指标计数**,**不新增审计 kind**(十值封闭集零改动);披露点 fail-closed

- **决策**:
  1. **不新增审计 kind**:审计账 `audit_log.kind` 是**十值封闭集**(D-API-90,库层 CHECK `audit_log_kind_closed_set`)。管理面查询是**运维读取事实**,按 D-API-59 / D-API-92 口径**不进安全事件账**:落点 = **受控 Pino 日志**(结构化:`actor="admin"` / `surface` / `outcome` / `tenantId` / 行数)+ **指标计数器** `admin_queries_total{surface,outcome}`(**标签是有界枚举,零标识符**、零秘密)。
  2. **结构性第二重保证**:`admin_ro` 对 `audit_log` **零授权**(连 SELECT 都没有)⇒ 即使实现被误改为写审计账,库层确定性拒绝(`permission denied for table audit_log`,容器门控用例逐字断言)。**账目归属由设计决定,授权面独立地把误写变成红灯。**
  3. **披露点 fail-closed(不对称是刻意的)**:对**成功披露**路径,审计**先于**下发且**严格**——审计写失败 ⇒ **503 且零数据下发**(数据已被读出但不出进程,容器/服务级用例断言「读过 + 未下发」);对**拒绝**路径(401 / 400 / 404 / 429 / 存储 503)审计是 **best-effort**(拒绝本身不泄露数据,不能因日志面抖动把 401 变成 500)。
  4. **错误体冻结**:401 `{invalid_input_format, authentication failed}` / 404 `{invalid_input_format, resource not found}` / 400 `{invalid_input_format, invalid request}` / 429 `{budget_exhausted, rate limit exceeded}`(与 D-API-50 / D-API-124 同码同文案)/ 503 `{internal_error, storage unavailable}`;审计面另用 `{internal_error, ...}` 同族 503 形态(**处置分歧留主控裁决**,见末节)。**响应体零内部细节**(库错误文本、探针名、存储故障原因一律不透出)。
  5. **安全头与 CSP 分域**:管理面 CSP `frame-ancestors 'none'`(计划书 `:715` 的「`frame-ancestors` 分离」口径)——管理面**不是**可嵌入面,零宿主来源白名单(与插件链路的 `frame-ancestors` 白名单形态**结构性互斥**);另带 `nosniff` / `no-referrer` / COOP+CORP `same-origin` / `cache-control: no-store`。控制台页 = **最小只读页**(语义化 DOM + 表格 caption + `role="status" aria-live="polite"`、零内联脚本样式、零视觉改版、**不进 vm-ui 交付面**)。
- **理由**:
  - **不新增 kind 是封闭集的纪律表达**:十值集是「安全事件」的语义边界,运维读取事实塞进去会让「审计账 = 安全事件」这一前提失效(D-API-90 的封闭性正是为了让消费方可以穷举)。若主控判定「管理面读取必须进账」,那是**契约面改动**(新 kind = 新版本 + 消费方穷举表更新),应按 D-API-59 复开流程走,不在实现期顺手加。
  - **审计严格性放在披露点**:审计的目的是「谁读走了什么」;**只有真正下发数据的路径需要「无账不下发」**;把拒绝路径也做成硬依赖,等于把日志可用性接入认证面(可用性攻击面)。
  - **`frame-ancestors 'none'`**:管理面若可被任意页面 iframe 嵌入,等于给出一个「用运维已登录浏览器读成绩」的点击面;`none` 同时是「本面不在插件链路」的结构性声明。
- **红灯位置**:`test/server.test.ts` 审计与限流 5 例(成功查询落账:系统主体 + 绑定租户 + 有界结局 / **审计失败 ⇒ 503 且零数据下发**(断言存储确被读过)/ 存储不可用 ⇒ 503 且失败查询也落账 / 429 冻结形态 / 503 常量形态);`test/server.test.ts` 运维面 3 例(healthz / readyz 探针失败 503 且失败方不透出 / **指标名 ⊆ 白名单 + 标签有界 + 零秘密零租户标识符**);安全头与只读页 2 例(CSP `frame-ancestors 'none'` + 全响应头 / 页面语义化 DOM + 零内联 + 凭证零持久化);`test/compose-topology.test.ts` 1 例(`audit_log` 四类授权全假 + 误写被拒);`test/audit-shutdown-rate-limit.test.ts` 14 例(受控日志**字段表精确锁定** + 拒绝态记录**不带**租户 + 文案 / 字段零凭证材料机检 + 失败注入 ⇒ 恒拒;停机序列**顺序** / 步骤失败 ⇒ 退出码 1 且不续跑 / **不重入**(第二次返回同一 Promise)/ 超时看门狗 / 默认三路触发器(**只触发 IPC 通道**,不向测试进程投真信号);频率闸窗口滚动 / 键独立 / 桶表上限驱逐 / 过期清扫 / 上限常量锚点)。
- **影响面**:新建 `apps/admin/src/audit/admin-audit.ts`(端口 + 受控日志实现 + 内存实现)、`src/metrics.ts`(三指标族,名称白名单机检)、`src/rate-limit.ts`、`src/http/security-headers.ts`、`src/console/page-source.ts`、`src/server.ts`、`src/runtime.ts`、`src/index.ts`、`apps/admin/README.md`(包级落地事实与待应用片段指针);测试面新建 `apps/admin/test/audit-shutdown-rate-limit.test.ts`、`test/persistence-adapters.test.ts`、`test/runtime.integration.test.ts`;`docs/user/出题人指南.md` / `docs/user/宿主平台接入指南.md` 需各回填一句(**待应用文本见末节**,两文件当前由其它 WP 占用)。

---

## 本工作包的红灯与证据摘要

| 面 | 证据(命令 / 文件 / 读数) |
|---|---|
| 类型面 | `pnpm --filter @stackmaster/admin typecheck` **exit 0**(`tsc -b` + `tsc -p tsconfig.test.json`) |
| 服务级门禁(零容器) | `pnpm --filter @stackmaster/admin test` → **9 files passed / 2 skipped(11);87 passed / 9 skipped(96)**;跳过 = 两个容器门控文件(`compose-topology.test.ts` 5 例 + `runtime.integration.test.ts` 4 例,`SESSION_API_IT=1` 才运行) |
| 容器门控门禁(**实跑**) | `SESSION_API_IT=1` + 真实 PostgreSQL 容器实跑 → **11 files passed / 96 passed(0 skipped)**:`compose-topology.test.ts` **5/5**(最小授权面 + `audit_log` 误写被库层拒 + RLS ENABLE/FORCE + 仅 SELECT 政策 + 三面真跑 + GUC 缺失零行)+ `runtime.integration.test.ts` **4/4**(**全装配路径**:真实连接池 / 真实 RLS / 真实三表连接 / 真实 HTTP 三面读数 / 停机句柄序 / 空白名单 fail-closed 态)。**入口形态差异如实登记**:`pnpm --filter @stackmaster/admin test:integration` 在本机因**并发 WP 的 app 拓扑已占用 deps 端口 19000**(MinIO `Bind for 0.0.0.0:19000 failed`)而无法 `up --wait`,故本次用**仓库外**临时配置直连**已在运行的真实 PostgreSQL 容器**(15432)跑同一批用例;`test:integration` 入口与 `vitest.config.ts` 未改动,在无端口冲突环境即为标准入口 |
| 覆盖率(本包 src) | **IT 形态**:`94.33% lines / 93.83% stmts / 83.11% branches / 92.3% funcs`;**零容器形态**:`89.93 / 89.52 / 81.33 / 86.32` —— 两种形态**都高于仓库 80% 门槛**(故新增 `apps/admin` 不拖累根 `vitest.coverage.config.ts` 的整体门槛)。唯一 0% 文件 = `src/index.ts`(进程引导;与 verifier `index.ts` 同款分工:装配面由 IT 覆盖、引导面不测) |
| 依赖边界(线上树) | **改前 / 改后各跑一次(同一份线上配置,改前 = 临时还原 `pathNot`)**:改前 `dependency-cruiser --config <还原配置> packages apps` **exit 3** —— `challenge-schema-dependents-restricted` 3 条(`apps/admin/src/challenges/registry-contract.ts` / `apps/admin/test/registry-contract.test.ts` / `apps/admin/dist/challenges/registry-contract.js` → `packages/challenge-schema/dist/index.js`),即**规则真实可红灯且 admin 的依赖边真实存在**;改后 `pnpm lint:deps` **exit 0** —— `✔ no dependency violations found (1351 modules, 4291 dependencies cruised)` |
| 依赖边界(自测红灯) | `pnpm lint:deps:self-test` **exit 0** —— `依赖边界反例自检全绿:20 组边期望全部满足`(**16 → 20**;新增反例13 admin→未登记包〔`admin-workspace-deps-allowlist`〕、反例14 plugin-dev→challenge-schema〔扩白名单后仍真实可红灯〕、正控15a/15b admin→challenge-schema / protocol 放行;另把反例2 收紧 `fromSuffix` 并断言**不**命中 admin 的规则,证明规则作用域不串味) |
| Lint(本包) | `pnpm exec eslint --config tooling/eslint.config.mjs apps/admin` **exit 0** |
| Lint(全仓) | `pnpm lint` **exit 0**(复跑时并行 WP 在 `packages/vm-ui/test/geometry/mount-instruction-view.mjs` 的浏览器全局告警已被其自行收口;**WP-79 对 `packages/**` 零改动** ⇒ 与本包无交集) |
| 构建 | `pnpm build` **exit 0** —— `Tasks: 13 successful, 13 total`(含 `@stackmaster/admin:build`) |
| 只读三重证明 | 端口面(`Object.getOwnPropertyNames(prototype)` 精确断言,`AdminReadStore` 只有 `ping` / `listChallenges` / `queryVerdicts` / `readScoresPage`)+ 语句面(护栏矩阵 4 正 / 14 反 + 源码机检 + **假 PoolClient 断言"发给驱动的那条 SQL == 护栏归一化返回值"且 `set_config('app.tenant_id',$1,true)` 恒为事务局部**)+ 授权面 / 政策面(容器门控真 PG:`has_table_privilege` INSERT/UPDATE/DELETE 全假、`relforcerowsecurity` 全真、`audit_log` 误写被拒、GUC 缺失零行) |
| 审计出口 | 受控日志字段表精确锁定(有界枚举 + 主体 + 时刻 + 绑定租户);拒绝态记录**不带**租户;文案 / 字段零凭证材料(机检 `authorization` / `Bearer` / `sha256` / `credential` 零命中);**零新增审计 kind**;审计失败 ⇒ 503 且零数据下发(容器门控 / 服务级双向断言) |
| 契约一致性 | 导出载荷逐字过 `HostScoresResponseSchema.parse()`;顶层键恰 `items`+`nextCursor`;逐条键集 = `Object.keys(HostScoreRecordSchema.shape)`;越界 `verdict` 字面 ⇒ 抛出(不下发) |

## 如实登记的限制与遗留

1. **compose 接线与 `admin_ro` 建角未落地(待主控应用)**:`apps/session-api/compose/app.yaml` / `deps.yaml` / `db-roles-init.sql` 三处属**其它 WP 占用面**,本包**未改**,只新增 `compose/admin-db-init.sql`(最小授权面,零 `CREATE ROLE`)+ `migrations/009_admin_row_level_security.sql`。**两段待插入片段与精确插入位见末节**;在片段应用前,**容器门控用例的 `beforeAll` 自行引导角色**(`ADMIN_ROLE_BOOTSTRAP_SQL`),故门控用例可独立复跑。
2. **`pnpm-lock.yaml` 被改动**:`apps/admin` 加入工作区后 `pnpm install` 更新了锁文件(**不在本包显式允许编辑清单内,但不可避免**),改动面仅新增 `apps/admin` 的依赖登记。
3. **WP-78 实现面尚未落盘**:`HostScoresResponseSchema` 契约已在 `packages/protocol`,但 `apps/session-api` 侧 `host/scores` 实现尚未出现 ⇒ 本包的 PG 投影**按契约与既有表形态(004 迁移)写出**,待 WP-78 落盘后需**对照复核一次**(字段口径 / `verdict` 来源列 / `decidedAt` 取值列);契约面已由「逐字 parse」机检锁死,**偏差只会在数据口径而非契约形态**。
4. **管理面查询不进审计账需主控追认(D-API-136 第 1 条)**:若主控判定该读面必须进 `audit_log`,则属**契约面改动**(`AUDIT_EVENT_KINDS` 十值封闭集增项 + 库层 CHECK + 消费方穷举表),应复开 D-API-59 流程,而非实现期加 kind。
5. **审计失败的 503 文案与存储失败同族但不同串**(`audit unavailable` vs `storage unavailable`):这是刻意保留的运维可诊断性(两类不同的可用性故障),但与宿主面「503 只有一种文案」不完全同形——**是否收紧为单一冻结文案留主控裁决**。
6. **频率闸是单进程内存固定窗口**(`AdminRateLimiter`,按客户端 IP):与 D-API-50 / D-API-124 的 Redis 共享计数**不同载体**(管理面是运维面,不做分布式预算);若后续要求多副本一致,属**载体演进**(端口已在,内存实现可换 Redis)。
7. **用户面文档两处待回填**(`docs/user/出题人指南.md` / `docs/user/宿主平台接入指南.md`,当前由其它 WP 占用,本包未改):现文仍写「管理后台不在 MVP 范围」/「归后续 WP 立项」,与 WP-79 落地事实**已冲突**,待应用文本见末节。
8. **控制台页无自动化 a11y 真机门禁**:本包以**结构机检**(语义化 DOM / `label` 关联 / caption / `aria-live` / 零内联脚本样式)固定 a11y 基线;**axe 真机扫描未接**(管理面不在 vm-ui 交付面,拒绝为它引入假绿 harness)。若主控要求,可后续并入 `apps/plugin-dev` 之外的独立 e2e 面。
9. **未落地**:管理面**写操作**(题目上下架 / 重判 / 封禁等)按分支 A **明确不做**;若后续需要,须先定「管理面写操作的身份 / 审计 / 双人复核」模型(属新 WP)。
10. **容器门控入口的环境依赖(如实登记)**:`pnpm --filter @stackmaster/admin test:integration` 走 `compose/deps.yaml up -d --wait`,本机复跑时**并发 WP 的 app 拓扑已占用 19000(MinIO)/ 15432(PostgreSQL)**⇒ `up --wait` 必然失败(**非本包缺陷**);本次实跑以仓库外临时配置直连已在运行的真实 PostgreSQL 容器,用例面完全一致。**无端口冲突环境(CI linux)下标准入口即可**。
11. **`src/index.ts`(进程引导)无自动化覆盖**:与 verifier / session-api 同款分工——**装配面**由容器门控 `runtime.integration.test.ts` 覆盖(**100% lines**),**引导面**(读 env → 装配 → 装触发器 → 听端口)不测;本包 src 覆盖率在 IT 形态与零容器形态**双形态均 > 80%**。

---

## 待应用片段(主控执行面;三个文件均属其它 WP 占用面,本包未改)

> **执行序是硬语义**(与既有缺陷 ① 同源:`CREATE POLICY ... TO <角色>` 要求角色**先于任何迁移**存在):
> `db-roles-init`(建角,**含 `admin_ro`**)→ `session-api`(迁移 **001 ~ 009**,009 内 `CREATE POLICY ... TO admin_ro` 要求角色在场)→ `admin-db-init`(对五个只读表 GRANT)→ `admin`。
> **片段 ① 与 ② 若不改,`session-api` 会在迁移 009 处以 `role "admin_ro" does not exist` 失败**(现象与缺陷 ① 同形),且 `deps.yaml` 的健康探针在只建 2 个角色时 `grep -qx 3` 恒假 ⇒ `session-api` 永不启动。

### 片段 ① `apps/session-api/compose/db-roles-init.sql`

**(a)** 头部口令口径行(现第 34 行)追加 `admin_ro`:

```diff
- -- session_app = session-app-dev,verifier = verifier-dev,与 compose/app.yaml /
- -- integration.env 的连接串一致)。
+ -- session_app = session-app-dev,verifier = verifier-dev,admin_ro = admin-ro-dev,
+ -- 与 compose/app.yaml / integration.env 的连接串一致)。
```

**(b)** 建角 DO 块(现第 43–45 行之后、`END` 之前)插入第三个角色:

```sql
  -- WP-79:信任域 4 管理面只读角色(独立凭证;与 session_app / verifier 不共享)
  IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'admin_ro') THEN
    CREATE ROLE admin_ro LOGIN PASSWORD 'admin-ro-dev';
  END IF;
```

**(c)** 只读不变量断言(现第 57 行)纳入 `admin_ro`(**必须**,否则被提权的 `admin_ro` 会静默绕过 RLS):

```diff
-    WHERE rolname IN ('session_app', 'verifier') AND (rolsuper OR rolbypassrls)
+    WHERE rolname IN ('session_app', 'verifier', 'admin_ro') AND (rolsuper OR rolbypassrls)
   ) THEN
-    RAISE EXCEPTION '角色 session_app / verifier 不得为超级用户或 BYPASSRLS 角色(行级租户策略强制前提,D-API-101)';
+    RAISE EXCEPTION '角色 session_app / verifier / admin_ro 不得为超级用户或 BYPASSRLS 角色(行级租户策略强制前提,D-API-101)';
```

### 片段 ② `apps/session-api/compose/deps.yaml`(db-roles-init 健康探针)

角色数由 2 变 3(**不改则 `session-api` 永不 healthy**),现第 132–134 行:

```diff
-          psql -v ON_ERROR_STOP=1 -tAc "SELECT count(*) FROM pg_roles
-          WHERE rolname IN ('session_app', 'verifier')
-          AND NOT rolsuper AND NOT rolbypassrls" | grep -qx 2
+          psql -v ON_ERROR_STOP=1 -tAc "SELECT count(*) FROM pg_roles
+          WHERE rolname IN ('session_app', 'verifier', 'admin_ro')
+          AND NOT rolsuper AND NOT rolbypassrls" | grep -qx 3
```

### 片段 ③ `apps/session-api/compose/app.yaml`

**(a)** 依赖服务三宿(现第 48–55 行;管理面入 `admin-net`,与 `verifier-net` 同为独立网络域):

```diff
   postgres:
     networks:
       - default
       - verifier-net
+      - admin-net
   minio:
     networks:
       - default
       - verifier-net
```

**(b)** 新增两个服务(建议插在 `verifier-minio-init` 之后、顶层 `networks:` 之前,现第 242/243 行之间):

```yaml
  # ── WP-79:管理面最小只读面(信任域 4;独立凭证 / 独立网络域 / 独立部署)──
  # 权威口径 docs/项目计划书.md:807(管理面临时凭证不得复用会话凭证或宿主令牌)
  # 与 :715(frame-ancestors 分离)。数据面 = 自有只读角色 admin_ro 直连 PG,
  # **不 import** session-api 任何内部实现;服务复用 = 契约复用(protocol)。
  admin-db-init:
    # 一次性最小授权(幂等;执行前置 = 迁移已应用 ⇒ session-api healthy)。
    image: postgres:16
    environment:
      PGHOST: postgres
      PGUSER: stackmaster
      PGPASSWORD: stackmaster-dev
      PGDATABASE: session_api
    volumes:
      - ./admin-db-init.sql:/init/admin-db-init.sql:ro
    entrypoint: ["psql", "-v", "ON_ERROR_STOP=1", "-f", "/init/admin-db-init.sql"]
    networks:
      - admin-net
    depends_on:
      session-api:
        condition: service_healthy
      session-api-db-init:
        condition: service_completed_successfully
    restart: "no"

  admin:
    image: stackmaster/session-api:dev
    command: ["node", "apps/admin/dist/index.js"]
    environment:
      ADMIN_HOST: 0.0.0.0
      ADMIN_PORT: "3200"
      # ── 独立凭证(摘要形态;明文只在运维侧持有,**不得复用**
      #    SESSION_API_HOST_BACKEND_TOKEN / 会话凭证)──
      # 合成 dev 值:明文 "admin-dev-credential-0123456789" 的 sha256。
      ADMIN_CREDENTIAL_SHA256: "REPLACE_WITH_SHA256_OF_DEV_CREDENTIAL"
      # ── 凭证 × 租户绑定白名单(O-MP-6 同源;空 = 数据面整体 404 同形)──
      ADMIN_TENANTS: "admin-tenant"
      ADMIN_POSTGRES_URL: postgres://admin_ro:admin-ro-dev@postgres:5432/session_api
    ports:
      - "13200:3200"
    networks:
      - admin-net
    depends_on:
      postgres:
        condition: service_healthy
      admin-db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3200/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 10s
    restart: "no"
```

**(c)** 网络域(现第 244–246 行)追加第二条独立网络:

```diff
 networks:
   verifier-net:
     driver: bridge
+  # WP-79:管理面独立网络域(postgres 双宿;session-api / verifier / 插件链路
+  # 均不入该网)。管理面零浏览器可达面:不进插件链路、不进 CORS 白名单。
+  admin-net:
+    driver: bridge
```

**(d)** `ADMIN_CREDENTIAL_SHA256` 的占位值生成(片段应用者执行;勿落库明文):

```sh
printf '%s' 'admin-dev-credential-0123456789' | sha256sum
```

### 待应用文本:用户面文档两处(当前由其它 WP 占用,本包未改)

**`docs/user/出题人指南.md:227`** —— 现文「管理后台(admin)不在 MVP 范围」与 WP-79 落地事实冲突,建议替换该句为:

```markdown
- **如实边界**:CDN 分发、生产密钥管理属生产部署面,MVP 范围内未做;当前发布形态 = dev/CI 拓扑内登记。管理面(`apps/admin`,WP-79 已落地)**只读**:题目登记列表 / 裁决查询 / 成绩导出三面,独立凭证与独立网络域、不入插件链路;**题目上下架等写操作仍未做**(分支 A 明确不做)。
```

**`docs/user/宿主平台接入指南.md:222`** —— 现文「管理后台(admin)面仍不在范围内…归后续 WP 立项」已失效,建议替换为:

```markdown
- **管理面(`apps/admin`)已落地,但与宿主面互不相通**:WP-79 交付的只读三面(题目登记 / 裁决查询 / 成绩导出)使用**独立凭证**(与本指南的宿主凭证、以及会话凭证三者不共享)与独立网络域,数据面走自有只读库角色 `admin_ro`,**不入插件链路**;`GET /host/scores` 这条路仍只认宿主凭证与 `SESSION_API_HOST_TENANTS` 绑定,管理面凭证在此接口同样 401(凭证域严格分离);
```
