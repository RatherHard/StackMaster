# 前端需求现状校验与不匹配清单

- **校验对象**:《前端交互需求拆解.md》的 44 条需求(FE-X 2 / FE-WS 7 / FE-MV 1 / FE-ST 10 / FE-IN 8 / FE-RG 4 / FE-FV 6 / FE-PB 6)。
- **校验基准(现有工作成果)**:以 `9ffaff7` 时点的仓库为准——后端阶段三已收口(WP-0~WP-8:`packages/protocol` 冻结契约、`apps/session-api` 编排器、`vm-engine` Rust 引擎、Compose 拓扑、可观测与限流);**前端零代码**。
- **校验方法**:逐条核对两件事——①前端实现是否存在;②协议/契约/题目包面是否具备该需求所需的数据与能力承载(前端无法凭空渲染服务端未下发的数据)。证据均给出来源文件。

## 一、总体结论

1. **前端实现整体缺位(G-M1)**:需求涉及的全部前端载体(`packages/vm-ui`、`packages/web-component`、`packages/embed-runtime`、`packages/react-wrapper`、`apps/plugin-dev`,见 `CLAUDE.md` 仓库结构规划)在仓库中不存在;`apps/` 仅有 `session-api`,`packages/` 仅有 `protocol` / `challenge-schema` / `challenge-compiler` / `session-core`。44 条需求无一有前端实现。
2. **协议面对需求的支撑分层明显**:19 条(A 类)协议面已支撑、可直接实现;15 条(B 类)可前端变通但有明确约束;8 条(C 类)协议/契约面无数据承载、前端无法变通,需服务端或 Schema 演进;**2 条(D 类)与已冻结决策直接冲突,须先做需求或协议决策**。
3. **最高优先级决策项**:FE-ST-05 / FE-FV-04(输入地址跳转任意地址视角)与投影决策 D3(字节窗口锚定区域起点、不支持玩家任选偏移)正面冲突;它同时制约 B 类中 8 条条目的完成度。`ADR-DC1`(2026-09-10 已评审接受,`docs/adr/ADR-DC1-调试克隆通道草案.md`)已为此定案第三条路径(调试克隆通道);实现落地前本清单缺口现状维持,相关条目验收转为拆解文档 §十二 的双档口径。
4. **正向面**:会话通道与动作面(12 动作、5 命令、认证 WSS、Cookie 凭证、断线 sync-projection、心跳/踢旧/背压/限流)与初始投影下发均已就绪,寄存器视图、VMA 列表、rip 锚点、指令步进、会话重置等条目的后端前提全部成立。
5. **【2026-09-11 阶段四落地回填】前端实现缺位(M1)已消除**:`packages/vm-ui`、`packages/web-component`(占位)、`apps/plugin-dev` 已落地并随阶段四验收(WP-F1~F9);A 类 19 条全部按解题模式档口径实现,B/C 类按拆解文档 §十二双档口径实现(调试模式档 = WP-40~44 调试通道 + WP-F8)。**分级口径说明**:本表 A/B/C/D 分级语义不变(协议/契约面对需求的支撑度);阶段四后原 B 类变通项的现状态 = **变通已实现 ≠ 契约演进完成**——如 M7 原生断点在调试通道侧已承载、但真实实例侧 ASLR(D-J8)等引擎契约演进项仍登记未实现;M2 描述包正式下发通道仍归阶段五(FE-ED-06/07 以夹具注入变通落地)。逐条现状态见《阶段四验收评审》§退出条件 1 勾稽表。

## 二、匹配分级定义

| 级别 | 含义 |
|---|---|
| **A 已支撑·待实现** | 协议/契约面已具备数据与能力,仅缺前端实现。 |
| **B 部分支撑·需变通** | 可在前端变通实现,但有明确约束/局限,或需对需求做语义解释。 |
| **C 无支撑·需演进** | 协议/契约面无数据或能力承载,前端无法变通,需服务端或 Schema 演进。 |
| **D 决策冲突** | 与已冻结决策直接冲突,须先决策(修改需求形态,或走协议版本演进推翻/扩写决策)。 |

## 三、逐条校验表

| 编号 | 需求摘要 | 现状 | 依据 / 关联缺口 |
|---|---|---|---|
| FE-X-01 | 组件化封装、自组件起步 | A | 纯前端工程形态,无后端依赖。 |
| FE-X-02 | 工作区唯一主体边界 | A | 同上。 |
| FE-WS-01 | 工作区容纳/管理/排布标签页 | A | 同上。 |
| FE-WS-02 | Hyprland+Niri 式滚动平铺 | A | 纯前端;交互规范未定义(拆解文档 Q1),但不依赖后端。 |
| FE-WS-03 | 顶部工作区菜单 | A | 纯前端。 |
| FE-WS-04a | 指令步进运行 | A | `step` 动作现成(`packages/protocol/src/session-action/action-object.ts`)。 |
| FE-WS-04b | 积木步进运行 | B | M8:无积木程序执行语义,仅可客户端展开原子动作逐步提交;粒度待定义;受每会话动作频率限流(D-API-50~53)约束。 |
| FE-WS-04c | 运行到断点 | B | M7:无地址断点动作;`run_to_event` 仅五类事件(`action-args.ts` `PauseEventSchema`);可用纯 `step` 循环 + rip 比对变通,受限流与往返延迟约束。 |
| FE-WS-05 | 重启测试环境 | B | M11:`reset` 动作存在,但终态会话确定性拒绝(`action-args.ts` `ResetArgsSchema` 注释);"测试环境"边界待定义。 |
| FE-MV-01 | 四种子视图容器 | A | 纯前端。 |
| FE-ST-01 | 栈视图默认渲染模式 | A | 窗口内 `bytesHex` 可渲染(`visible-memory-region.ts`)。 |
| FE-ST-02 | 栈视图行三段布局 | A | 同上。 |
| FE-ST-03 | 8 字节对齐偏移可调 | B | M3:窗口内重排可行;跨窗口边界无数据。 |
| FE-ST-04 | rsp/rbp 视角锚点 | B | M13:rsp/rbp 值来自 `visibleRegisters` ✓;但锚点行内容若在字节窗口外则无数据可显示。 |
| FE-ST-05 | 输入地址跳转任意地址 | **D** | M3:与 D3 冻结决策直接冲突(见下文详述)。 |
| FE-ST-06 | 字节检索 | B | M3:仅已下发窗口内字节可检索(默认每区域 256 字节)。 |
| FE-ST-07 | 地址行跳转链特殊显示 | B | M12:首段可由客户端解析 `bytesHex` 得到;延伸段需读目标地址内存,受窗口限制。 |
| FE-ST-08 | 跳转链代码区伪汇编延伸 | C | M4:投影无指令流,伪汇编无数据源。 |
| FE-ST-09 | 链上点击地址视角跳转 | B | M3:跳转行为可行;落点在窗口外则无内容。 |
| FE-ST-10 | 可见字符显示与链末延伸 | B | M12:行内渲染可行;链末延伸受窗口限制。 |
| FE-IN-01 | 指令视图默认渲染模式 | C | M4:投影仅 `controlFlow.currentInstruction` 单条(地址+文本),无指令列表、无伪机器码字节(`public-control-flow.ts`)。 |
| FE-IN-02 | 指令视图行三段布局 | C | M4:同上,伪机器码 16 进制无数据源。 |
| FE-IN-03 | 跳转语句延伸目标地址并点击跳转 | C | M5:投影不含跳转目标地址字段。 |
| FE-IN-04 | 函数列表 | C | M6:投影与公开描述包均无函数符号面(`public-types.ts` 布局仅 region 粒度)。 |
| FE-IN-05 | rip 视角锚点 | A | `currentInstruction.addressHex` 现成 ✓。 |
| FE-IN-06 | 指令视图地址跳转 | C | M4:指令流缺失,跳转无内容可看(叠加 M3)。 |
| FE-IN-07 | 字节、指令检索 | C | M4:无指令流可供检索。 |
| FE-IN-08 | 指令断点 | B | M7:纯 `step` 步进 + rip 比对可变通实现;原生断点动作需协议演进并论证回放语义。 |
| FE-RG-01 | 纵向显示所有寄存器 | B | M14:仅白名单寄存器有公开值(FLAG 保留区结构性排除,`投影语义规约.md:61`);"所有"须按白名单解释。 |
| FE-RG-02 | 寄存器行三段布局 | A | `visibleRegisters`(name+valueHex 大写十六进制)现成;特殊显示列内容另受 M12 约束。 |
| FE-RG-03 | 特殊显示点击复制剪贴板 | A | 纯前端行为(剪贴板 API 降级见拆解文档 Q8)。 |
| FE-RG-04 | 寄存器地址行跨视图标注 | B | 客户端以 `visibleRegisters` × `visibleRegions` 交叉计算可行;目标行须落在窗口内(M3)。 |
| FE-FV-01 | 自由视图默认渲染模式 | A | 同 FE-ST-01。 |
| FE-FV-02 | 自由视图行布局与特殊显示 | A | 同 FE-ST-02 / M12。 |
| FE-FV-03 | 对齐偏移可调 | B | M3:同 FE-ST-03。 |
| FE-FV-04 | 地址跳转任意地址 | **D** | M3:同 FE-ST-05。 |
| FE-FV-05 | 字节检索 | B | M3:同 FE-ST-06。 |
| FE-FV-06 | VMA 列表跳转 | A | `visibleRegions`(regionId/startAddressHex/byteLength/permissions)即 VMA 公开面;VMA 头部 = 窗口锚定起点,数据恒在下发 ✓——支撑最好的条目之一。 |
| FE-PB-01 | payload 页三区结构与拖拽搭建 | A | 纯前端。 |
| FE-PB-02 | 八类基础积木 | B | M9:积木 UI 纯前端;执行面只能客户端展开为原子动作(变量/循环等求值仅限公开投影可见数据);断点积木走 M7 变通。 |
| FE-PB-03 | 唯一起始积木 | A | 纯前端。 |
| FE-PB-04 | 出题者积木模块(交互接口) | C | M10:公开描述包 Schema 无任何积木/交互接口概念。 |
| FE-PB-05 | Blockly 选型 | A | 前端选型决策。 |
| FE-PB-06 | 悬停显示积木功能 | A | 纯前端。 |

**分级统计**:A 19 / B 15 / C 8 / D 2,共 44 条。2026-09-10 源文档增补带来 FE-WS-06/07(工作区模式切换、payload 状态跨模式共用),未纳入本表分级,随下轮校验回填。
**【2026-09-11 阶段四回填后口径】**共 54 条(44 + FE-WS-06/07 + FE-ED-01~08),分级更新为 **A 36 / B 17 / C 1 / D 0**:
- FE-WS-06/07:**A**(阶段四落地;WP-43 `debugMode` 契约面冻结 + WP-F8 实现,回填标注见拆解文档 §二);
- FE-ED-01~08:**A 6 / B 2**(FE-ED-06/07 因 M2 交付通道未建以夹具注入变通落地,拆解文档 §十三);
- 原 D 2 条(FE-ST-05 / FE-FV-04):ADR-DC1 定案(调试克隆通道)解除决策冲突,**双档落地**——调试模式档任意地址窗口全量满足(WP-40/41 调试通道),解题模式档维持 D3 窗口形态 → 计入 **A**;
- 原 C 8 条中 7 条(FE-ST-08、FE-IN-01/02/03/04/06/07):由调试通道承载(M4/M5/M6/M7 的 ADR-DC1 定案路径已实现,WP-40~41),调试模式档全量实现、解题模式档维持投影面约束 → 计入 **A**;FE-PB-04(M10 出题者积木):公开描述包交互接口声明面演进未启动,仍无承载 → 维持 **C**(开放,随 M2/演进统筹)。

## 四、全局缺口与重点不匹配项详述(M1~M14)

### M1 前端包整体零落地【全局】

- **关联**:全部 44 条。
- **现状**:规划中的 `packages/vm-ui`(投影渲染)、`packages/web-component`、`packages/embed-runtime`、`packages/react-wrapper`、`apps/plugin-dev` 均不存在;TS 侧仅有协议与后端包。
- **性质**:实现缺位,非设计冲突。
- **处置**:按 FE-X-01 自具体组件起步;依赖方向纪律(`lint:deps` 已在 CI 强制)对前端包落位已有既有轨道,无需新决策。

### M2 公开描述包的浏览器交付通道缺失【全局】

- **关联**:FE-PB-04 及一切需要题目静态面(标题/简介/VM Profile/encodingTable/hintLadder)的前端渲染。
- **现状**:`create_session` 响应仅含 `sessionId / revision / projection`,不含公开描述包(`session-command-response.ts`);嵌入协议消息为封闭五类型 hello/ready/height_changed/theme_changed/language_changed,无描述包载荷(`embed-message.ts`);iframe 嵌入面整体归阶段五(D-API-66)。
- **性质**:交付通道缺位(阶段五未启动),非契约冲突。
- **处置**:与阶段五嵌入面统筹;前端先行期可在 `plugin-dev` 开发壳内以测试夹具注入描述包。

### M3 任意地址视角 vs 投影决策 D3(字节窗口锚定区域起点)【D 类,最高优先级】

- **关联**:FE-ST-05、FE-FV-04(D 类直接冲突);FE-ST-03/04/06/09、FE-FV-03/05、FE-RG-04(B 类受限)。
- **现状**:可见区域的字节窗口**锚定区域起点,不支持玩家任选偏移窗口**(`visible-memory-region.ts` bytesHex 注释;`投影语义规约.md:73`:`window_bytes = min(region_len, maxBytesPerRange)`);窗口默认 256 字节、单策略上限 4096、单 revision 投影总预算 8192(`数据分类与秘密零驻留清单.md:351`)。这意味着:①地址跳转/拖拽到窗口外**无任何数据可渲染**;②rsp/rbp 锚点随执行移动,锚点处内容不保证在窗口内;③检索、偏移调整、跳转链延伸全部以窗口为界。
- **性质**:需求形态(自由探索内存)与已冻结的脱敏简化决策(D3,WP-3 冻结)正面冲突。
- **处置方向**(已定案:ADR-DC1 于 2026-09-10 评审接受;实现落地前本项缺口现状维持):
  0. **调试克隆通道(已接受,ADR-DC1)**:任意地址视角/检索/偏移在**调试模式档**内对调试克隆全量满足——克隆与真实实例共享公开条件集与布局结构(基址是否相同由题目 ASLR 开关决定)、秘密值由同一套 SeedDeriver 两个种子分别派生(调试侧经调试变体镜像零装载)、权威动作日志重放对齐状态,公开会话协议与 D3 一字不动;解题模式档维持 D3 窗口形态,验收拆双档(拆解文档 §十二);
  1. **需求降级**(留档备选,未采纳):视角/跳转/检索限定在可见区域已下发窗口内,VMA 列表跳转(FE-FV-06,天然匹配)成为主要导航手段;
  2. **协议演进**(留档备选,未采纳):引入"窗口重锚定"类只读查询动作,须重做 D3 的脱敏面论证,走协议版本演进 + golden fixture 跨语言同步。

### M4 指令流展示数据缺失(仅单条 currentInstruction)【C 类】

- **关联**:FE-IN-01/02/06/07、FE-ST-08。
- **现状**:投影代码面仅 `controlFlow.currentInstruction`(地址 + 服务端生成的伪指令文本,`public-control-flow.ts`;D5:该展示由服务端从展示数据单独生成,非可执行 IR 形态)。无指令列表、无伪机器码字节序列、无按地址取指令的通道。
- **性质**:数据承载缺失;演进受两条既有纪律约束——展示数据必须维持"非可执行 IR"边界(D5、ZR-B 系机检),且受单 revision 投影字节总预算 8192 约束。
- **处置方向**(已定案:ADR-DC1):指令流由**调试通道**承载——调试 worker 从调试克隆(代码区字节本身公开)生成完整伪指令流/伪机器码/跳转目标/函数表,公开投影的 `currentInstruction` 单条形态维持不动;解题模式档内指令浏览仍受投影面约束。留档备选(未采纳):公开协议演进"代码区展示窗口"或公开描述包预置静态展示数据,需走契约版本演进。两条路径下"D5 展示数据非可执行 IR"边界同样适用——下发展示形态,不下发引擎 IR 本体。

### M5 跳转目标地址缺失【C 类】

- **关联**:FE-IN-03、FE-ST-08 的跳转目标。
- **现状**:投影不含指令跳转目标的解析结果;客户端可从伪汇编文本自解析,但该文本来源仍是 M4 的缺失面。
- **处置**:随 M4 一并演进(展示数据附结构化目标地址字段);推荐随 M4 走 `ADR-DC1` 调试通道,由调试 worker 在生成指令流时附带。

### M6 函数列表缺失【C 类】

- **关联**:FE-IN-04。
- **现状**:投影无函数符号面;公开描述包内存布局仅 region 粒度(`public-types.ts` `PublicRegionSpec`),无函数边界/名称。
- **处置方向**:公开描述包扩展函数表(7.1 字段分类 + 双包 Schema + golden fixture),或服务端从展示数据生成;注意函数名值来源须满足 I-10(公开符号表/服务端静态模板)。ADR-DC1(已接受)下函数表先由调试通道承载;公开描述包扩展保留为解题模式档的可选演进。

### M7 断点机制不存在【C 类(原生)/ B 类(变通)】

- **关联**:FE-IN-08、FE-WS-04c、FE-PB-02 断点积木。
- **现状**:12 动作与全部协议文档中无断点概念;`run_to_event` 仅五类事件(read/write/call/ret/exception,`action-args.ts` `PauseEventSchema`)。
- **变通**(现协议内可行):payload 执行若采用纯 `step` 客户端编排,每步后比对 rip 与断点地址即可暂停——语义成立,但每条指令一次 WSS 往返,受每会话动作频率限流(D-API-50~53)与延迟约束,且与 `run_to_event` 的连续运行语义不兼容(连续运行中无法插入客户端检查)。
- **原生方向(已定案:ADR-DC1)**:地址断点 / run-to-breakpoint 归**调试通道**承载——调试 worker 对调试克隆执行,判题输入零装载、调试交互不进权威日志,因此无回放语义包袱,无需动 12 动作冻结面与 verifier。指令断点(FE-IN-08)与断点积木(FE-PB-02)统一为调试执行流上的暂停点(断点积木在解题模式档以步进暂停变通)。"step 逐条比对 rip"变通保留为解题模式档内的降级路径。
- **【2026-09-11 已落地】**:`debug_run_to_breakpoint` 帧族(WP-40 冻结)+ 调试 worker 原生断点循环步进(引擎进程协议 §4.8,`budget` 确定性暂停)+ 指令视图行断点/菜单「运行到断点」(WP-F8,`sm-instruction-view.ts` / `sm-workspace-menu.ts`);集成证据 = `apps/session-api/test/debug/debug-channel.integration.test.ts`(attach→窗口→断点暂停→检索→展示数据,真实 vm-worker)。**变通已实现 ≠ 契约演进完成**:权威会话协议维持 12 动作冻结(零新增动作),断点仅在调试通道语义内;D-J8(真实实例侧 ASLR)仍为引擎契约演进登记项。

### M8 积木步进运行无执行语义承载【B 类】

- **关联**:FE-WS-04b。
- **现状**:积木程序不是协议概念;唯一可行解释是客户端把积木图编译为 12 动作的原子序列逐步提交。"一块积木 = 一步"的粒度、分支/循环在步进中的呈现均无定义(拆解文档 Q3)。
- **处置**:产品定义步进语义后按客户端编排实现;须与限流默认值对表(循环类积木可能展开大量动作)。**【2026-09-11 已落地】**Q3 定案 = 原子动作粒度(拆解文档 §十一 Q3 回填);WP-F6 交付编译器 + PayloadStepExecutor(`packages/vm-ui/src/payload/`),展开上限 256 / 求值步 4096 / 调用深 32 确定性报错,429 `budget_exhausted` 可解释呈现(光标不动、手动重试);测试 `packages/vm-ui/test/payload/`(编译器 / 执行器 / 标签页)。

### M9 积木程序语义无协议对应【B 类】

- **关联**:FE-PB-02(变量/列表/分支/循环/模块化/运算赋值/字符串操作积木)。
- **现状**:动作协议只有 12 个原子动作,无表达式求值、无变量承载;积木程序的求值数据源只能是公开投影(`visibleRegisters` / 窗口内 `bytesHex`),隐藏数据天然不可用(这是底线约束而非缺陷)。展开后动作数量受动作频率限流与资源预算约束。
- **处置**:前端实现积木→动作编译器;在题目包 `allowedActions` 现有裁剪机制内声明可用动作;编译器行为先写测试(TDD)。**【2026-09-11 已落地】**编译器定稿 = 无头 Blockly 序列化编译、错误即值;求值环境 = 公开投影只读快照(未知引用/窗口外确定性报错,零静默兜底);64 位无符号回绕、小端、字符串 UTF-8 定案;`allowedActions` 编译期裁剪标红(缺省白名单 = 12 动作裁去 `run_to_event`);测试 `packages/vm-ui/test/payload/compile*.test.ts`。

### M10 出题者积木模块无题目包承载【C 类】

- **关联**:FE-PB-04。
- **现状**:公开描述包(`public-types.ts` `PublicChallengeDescriptor`)无积木/交互接口字段;最接近的锚点是 `vmProfile.encodingTable` 的 `interface` 操作数(`public-types.ts:48-51`),但那是编码表 token 语义(字节权威执行模式的接口调用 token),与"积木交互接口"不是同一概念,仅可作设计参考。
- **处置方向**:公开描述包 Schema 演进新增交互接口声明(积木形态、能力边界、展示文本);走 7.1 字段分类、双包 Schema 校验、golden fixture 与版本演进;同时须回答拆解文档 Q7(下发通道随 M2)。

### M11 "重启测试环境"与 reset 动作的语义缝隙【B 类】

- **关联**:FE-WS-05。
- **现状**:`reset` 动作=重置会话到题目初始状态,但**终态会话确定性拒绝**(D1 约束,`action-args.ts`);提交(`submit`)后会话进入终态,菜单项若直接调 `reset` 将被拒绝。
- **处置**:产品定义边界(拆解文档 Q5):运行中= `reset`;终态= `close_session` + `create_session` 新建;前端按会话状态启用/禁用菜单项。**【2026-09-11 已落地】**Q5 定案 = M11 口径(拆解文档 §十一 Q5 回填);WP-F5 菜单实现 reset 终态禁用 + 「测试环境已结束,请新建会话」引导(`new-session-request` 事件 → 宿主 close + create 新流程,`sm-workspace-menu.ts`);测试 `packages/vm-ui/test/workspace/`(禁用矩阵与引导)+ `apps/plugin-dev/test/`(新建会话流程)。

### M12 跳转链/字符延伸的数据边界【B 类,受 M3/M4 支配】

- **关联**:FE-ST-07/09/10、FE-FV-02、FE-RG-03。
- **现状**:链首段可由客户端解析 8 字节 `bytesHex` 得到(端序按题目 `vmProfile.endianness`);延伸段需要读**目标地址**的内存(受 M3 窗口限制)或指令(M4 缺失)。`semanticHighlights`(buffer_start / return_address_slot 等,上限 32)可增强特殊显示的教学标注,现成可用。
- **处置**:B 类范围内先实现"窗口内跳转链";延伸能力随 M3/M4 决策跟进(ADR-DC1 已接受,调试模式档内可全量延伸,含代码区伪汇编与可见字符末端)。

### M13 rsp/rbp 锚点内容不保证在窗口内【B 类,受 M3 支配】

- **关联**:FE-ST-04。
- **现状**:rsp/rbp 当前值来自 `visibleRegisters` ✓,锚点跳转机制可行;但栈区域窗口锚定区域起点(默认 256 字节),执行推进后 rsp/rbp 指向的位置可能已在窗口之外,锚点行将无内容。
- **处置**:随 M3 决策;短期可在 UI 明示"该地址内容不在可见窗口"而非空白渲染。

### M14 "所有寄存器"与白名单裁剪【B 类,需求需按纪律解释】

- **关联**:FE-RG-01、FE-RG-04。
- **现状**:仅 `ProjectionPolicy.visibleRegisters` 白名单寄存器进入投影(上限 64,`limits.ts:61`);FLAG 保留区命名结构性排除(`投影语义规约.md:61`),秘密寄存器经编译期污点推导禁止入白名单。
- **性质**:这是安全底线的预期行为,不是缺陷;需求文字"所有寄存器"应按"白名单内全部寄存器"解释(拆解文档已按此口径标注)。
- **处置**:无需协议改动;UI 不得为白名单外寄存器保留占位(与隐藏区域"无占位"纪律同构)。

## 五、已对齐面盘点(现有工作对需求的直接支撑)

为避免"清单只有缺口"的误读,以下现有能力与需求直接对齐、可直接消费:

- **通道面**:认证 WSS(`GET /sessions/channel` 升级即认证、claims 锚定会话)、Cookie 凭证(HttpOnly + SameSite=Strict)、心跳/多端踢旧/有界背压/错误帧九类封闭矩阵、断线重连 `sync-projection` 全链路(阶段三 WP-5)——前端会话通道的协议与实现前提全部就绪。
- **动作面**:`step` / `run_to_event` / `pause` / `undo` / `reset` / `create_checkpoint` / `checkout_checkpoint` / `write_bytes` / `push` / `pop` / `call` / `ret` 12 动作 + 5 生命周期命令,覆盖工作区菜单除断点/积木步进外的全部运行选项。
- **投影面**:初始投影随 `create_session` 一次性下发(寄存器/区域/高亮/当前指令);`visibleRegions` 直接构成 FE-FV-06 的 VMA 列表;`visibleRegisters` 直接构成 FE-RG-01/02 的数据面;`currentInstruction.addressHex` 直接支撑 FE-IN-05;`semanticHighlights` 可直接增强栈/自由视图的教学标注。
- **工程面**:依赖方向 CI 强制、golden fixture 跨语言机制、覆盖率门禁为前端包落地提供既有轨道。

## 六、处置路径汇总

| 优先级 | 事项 | 类型 |
|---|---|---|
| P0 | ADR-DC1 评审已完成(2026-09-10 接受,M3 定案调试克隆通道);当前 P0 = §四实现前置演进项排期:独立调试通道与协议、调试变体镜像(编译器)、描述包 debugMode/ASLR 字段、SeedDeriver 基址派生 | 契约/引擎演进 |
| P0 | 启动前端包落地(M1),按 A 类 19 条先行:字节行渲染 → 寄存器视图 → 工作区容器 → payload 框架 | 实现 |
| P1 | 决策 Q3/Q5(积木步进粒度、重启边界),解锁对应 B 类条目的实现口径;Q4 已随 ADR-DC1 落定(断点归调试通道) | 产品定义 |
| P1 | M7/M11 的客户端变通实现与限流默认值(D-API-50~53)对表(M7 原生路径随 ADR-DC1 调试通道) | 实现 |
| P2 | M4/M5/M6 指令流展示面:由 ADR-DC1 调试通道承载(已定案;维持非 IR 边界与字节预算) | 契约演进 |
| P2 | M10 出题者积木的公开描述包 Schema 演进(随 M2 通道统筹;不在 ADR-DC1 覆盖内) | 契约演进 |
| P3 | M2 公开描述包交付通道,与阶段五嵌入面对齐 | 阶段规划 |

> 维护约定:本文按需求编号(FE-XX)与缺口编号(M1~M14)双索引;需求口径变更时先改《前端交互需求拆解.md》,再回填本文校验结论;D3/D5 等被引用决策的变更必须走协议版本演进纪律,不得在本清单内默认放行。
