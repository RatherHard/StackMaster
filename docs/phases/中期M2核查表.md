# 中期 M2 核查表(E-4 证据)

> **文档状态**:**定稿**(2026-09-17)。由 WP-75 #5 / #9 / #11 承接方起草,主控在文档波次(WP-77)内**并入正式验收材料**:逐条复核三态、更新行号(#6 / #7 / WP-76 承接项已收敛)、去「草稿」标记。
>
> **权威对照物**:`docs/develop/前端的交互和开发设计.md`(**83 行**,逐行核查)。该文件已于本中期由 WP-77 按实现现状重写(工作区模型 + payload 三区,D-MP-1 / D-MP-4),**故本表的行号以重写后的 83 行为准**(起草时的旧行号已全量重映射)。
>
> **三态口径**(WP-75 完成标准「核查表逐条『符合 / 已定案』」):
> - **符合** = 实现与文档表述一致(附实现落点);
> - **已定案** = 实现与文档表述不一致或文档未表述,但已由决策点(D-MP-*)定案,按定案执行(附决策点与执行落点);
> - **偏差已双向回填** = 实现与文档的偏差已确认,实现侧与文档侧 / 台账侧都已登记到同一结论(附两侧落点)。
>
> **覆盖边界**:本表覆盖设计文档的**交互与组件语义**面。#6 / #7 / WP-76 承接项已于 M2 波次收敛并并入本表;WP-80(M10 出题者积木声明面)属**后续里程碑**,相关行仍标注承接关系;**WP-74 的 terminal 效果面(扫描线 / 光标 / 标题栏)不属本表覆盖面**(设计文档无主题章节,其变量面与消费面登记于 `packages/vm-ui/README.md` 与 D-API-118),其状态见 §9。

---

## 1. 工作区组件(文档 :5-19)

| # | 核查项(文档表述) | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 1.1 | 工作区是前端最大唯一主体,容纳、管理、排布**固定窗口集** | :7 | **符合**(WP-77 已重写文档) | 实现 = **固定窗口集(D-MP-1)**:窗口集合 = 注册表 10 类型各恰一实例、常驻、无开 / 关生命周期;`WorkspaceLayoutModel.bindWindows`(窗口 id ≡ 类型键)+ `isWindowSetComplete` 不变量机检。文档侧已由 WP-77 重写为固定窗口集表述(commit `9161ba7`),**原「标签页可开可关、同类多开」表述已退场** ⇒ 起草时的「偏差」状态已消解 |
| 1.2 | 排布采用类似 Hyprland + Niri 的滚动平铺 | :9 | **已定案(D-MP-1)** | 列式滚动条带 + 焦点列相机 + 三类拖拽落点 + 列宽五档 / 重置:`src/workspace/layout-presets.ts`(P0/P1/P2 单一来源)、`layout-camera.ts`、`layout-divider.ts`;E2E `e2e/workspace-layout.spec.ts`(8 例)、`e2e/workspace-windows.spec.ts`(3 例) |
| 1.3 | 工作区顶部存在管理工作区的菜单 | :13 | **符合** | `src/workspace/sm-workspace-menu.ts`(`menu.aria`);菜单组 = 窗口 / 布局 / 模式 / 运行(\| submit) |
| 1.4 | 菜单含运行选项:指令步进、积木步进、运行到断点 | :15 | **符合** | `menu.step`「指令步进」/ `menu.payloadStep`「积木步进」/ `menu.runToBreakpoint`「运行到断点」(`catalog-zh-CN.ts:59-66`);i18n 双目录齐备。「运行到断点」可用性 = `mode==="debug" && breakpointCount>0 && #sessionActionable && !#isTerminal`(WP-76 落地) |
| 1.5 | 菜单可重启测试环境 | :16 | **符合** | `menu.reset`「重启测试环境」+ 终态 / 无会话 / 断线三态禁用理由(`menu.resetTitle*`) |
| 1.6 | 菜单支持解题 / 调试模式切换 | :17 | **符合** | `menu.modeToggleToDebug` / `menu.modeToggleToSolve` + `aria-pressed`;门槛 `debugModeAvailable`(`sm-workspace-menu.ts:146`) |
| 1.7 | 调试模式由题目声明启用(**默认启用**,可显式关闭,ADR-DC1) | :17 | **偏差已双向回填**(WP-75#9 收敛) | 归一化口径 = `debugMode === undefined ? true : value`(`challenge-descriptor.ts:451-452`)+ session-api 门控 `debugMode !== false`;**开发壳夹具通道此前按 `=== true` 判真(缺省 false)**,与正式通道相反 ⇒ WP-75#9 收敛:`apps/plugin-dev/src/main.ts` 新增 `DEBUG_MODE_DEFAULT` / `normalizeDebugMode`,两通道缺省语义一致(commit `2da0779`);用例 `test/descriptor.test.ts` 补「缺省即启用」断言(17 → 20 例) |
| 1.8 | 两种模式**共用** payload 搭建窗口状态 | :19 | **符合** | `test/workspace/sm-workspace-mode.test.ts`「FE-WS-07:payload 窗口状态两模式共用」断言模式切换前后 `sm-payload-tab` **元素同一性**(状态共用;换绑数据源不重建元素) |
| 1.9 | 内存视图窗口随模式切换**换绑数据源** | :19 | **符合** | 同一测试文件(M1 定案「换绑数据源、布局零副作用」,`layoutSnapshot` 深度相等断言);调试档装配修复见 WP-70(`createDebugDataSource` 的 `projectionProvider`) |

## 2. 内存视图窗口组件(文档 :21-24)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 2.1 | 内存视图四类:栈 / 指令 / 寄存器 / 自由 | :23 | **符合** | `STACK_TAB_TYPE` / `INSTRUCTION_TAB_TYPE` / `REGISTERS_TAB_TYPE` / `FREE_TAB_TYPE`(`src/workspace/tab-registry.ts`);指令视图窗口类型在解题档呈现「调试档解锁」引导(不伪造数据) |
| 2.2 | 解题档 = 服务端公开投影,**受公开投影字节窗口约束** | :24 | **符合** | `ProjectionDataSource`(D3 窗口外语义:未下发 = `byte: null`、不占位不报错);`byte.jumpOutsidePublic`「在可见窗口之外(仅窗口内可达)」 |
| 2.3 | 调试档 = 调试通道;任意地址跳转 / 全内存检索 / 指令视图 / 指令断点全量可用 | :24 | **符合**(承载面另见 §9) | `DebugDataSource`(`prefetchWindow` / `searchBytes` / `instructionStream` / `functions` / 断点集);`menu.modeToggleTitleToDebug` 明示「调试通道承载任意地址 / 指令流 / 断点」;`byte.jumpPrefetching` / `instr.prefetching` 呈现拉取语义。**注**:该档的真机端到端此前被服务端两处既有缺陷阻塞(`/sessions/debug-channel` 未挂载 = 已修 `a35bb01`;变体构建 `DebugVariantBuildError` = 诊断中),详见 §9 与 D-API-120 |
| 2.4 | (红线)浏览器侧零权威执行、零判题 | :24 | **符合** | 双档数据源均只读公开投影 / 调试通道已下发数据;断线横幅 `menu.bannerBody` 明示「不做任何本地 VM 执行降级」;what-if 横幅 `menu.whatifTitle`「调试通过 ≠ 提交通过」保留 |

## 3. 栈视图(文档 :26-39)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 3.1 | 默认纵向排列、高处 = 低地址、8 字节一行 16 进制 | :28 | **符合** | `byte.tableAria`「内存十六进制字节(8 字节一行,高地址在下)」;`ROW_BYTES = 8`;虚拟列表 `sm-window-list` |
| 3.2 | 每行三段:左 = 8 字节中最低位字节的地址 / 中 = 16 进制 / 右 = 特殊显示 | :29 | **符合** | `byte-view.ts` 行三槽(`.row-address` / `.row-hex` / `.row-special`,同行三列 `grid-template-columns: 16ch 26ch 1fr`),行地址 = `row.addressHex`(行首 = 最低位字节);表头 `byte.colAddress` / `byte.colHex` / `byte.colSpecial`。**地址展示层恒小写**(`render/hex.ts`;仅寄存器 `valueHex` 恒大写)—— 帮助手册 §2.3 #10 勘误已按此回填 |
| 3.3 | 可调 8 字节对齐的偏移 | :30 | **符合** | `byte.offsetGroupAria` / `byte.offsetLabel` + 增 / 减入口;`src/views/byte/alignment.ts` |
| 3.4 | 默认视角 = rsp 位置,可切 rbp;拖拽后可回锚 | :31 | **符合** | `byte.anchorOutside` / `byte.anchorRewind`(「回锚 {register}」+ `aria`);锚点随投影寄存器值三级解析(见 `byte-tab` 视图接线) |
| 3.5 | 支持输入地址跳转任意地址 | :32 | **符合** | `byte.jumpAria` / `byte.jumpPlaceholder`(「0x1004 或偏移(十进制)」)+ `byte.jumpUnrecognized` 可解释错误文案 |
| 3.6 | 支持字节检索 | :33 | **符合** | `byte.searchAria` / `byte.searchPlaceholder` / `byte.searchNoHits` / `byte.searchSummary`(+ `byte.searchWindowNote`「仅已下发窗口字节」= 解题档窗口口径如实呈现) |
| 3.7 | 特殊显示:地址 → 横向跳转链,最长三段,循环打回;超三段可点击在下方展开竖向 | :36 | **符合** | `JUMP_CHAIN_HORIZONTAL_LIMIT = 3` / `JUMP_CHAIN_EXPANDED_LIMIT = 32` / `chainLimitReached`(展开入口)/ `loopBack`(SVG 回环箭头);`views/chain/resolve.ts` + `sm-jump-chain.ts` |
| 3.8 | **行右段链实际呈现** | :29-39 | **偏差已修复并登记**(原「新发现偏差,待派单」) | **缺陷**:`sm-workspace.ts` 以 **kebab 裸特性** `start-address-hex=${...}` 挂链,而 `sm-jump-chain` 的 `@property({type:String}) startAddressHex` 未声明 `attribute:` ⇒ Lit 缺省观察名是全小写 `startaddresshex` ⇒ 属性恒空、`render()` 直接空渲染 ⇒ **栈 / 自由视图行右段链恒空**,`extendable` / `extendHandler`(WP-76 伪汇编延伸入口)成为**不可达路径**。既有用例只断言裸特性(`test/workspace/sm-workspace.test.ts:872`),故长期未暴露。**修复**(commit `2f851a7`):绑定改为属性面 `.startAddressHex=`;断言升级为「属性值 + 链 shadow 内 `.chain` / `.chain-address[data-address]` 存在」。**登记 D-API-117**;同族全仓扫描确认**仅此一例**死绑定(`byte-tab.ts` 的 `view-kind` 合法,其消费方显式声明了 `attribute:`) |
| 3.9 | 特殊显示:链延伸到代码区 → 追加一条伪汇编 | :37 | **符合**(WP-76 落地) | 纯函数 `chainCodeEndAddress(segments, regions)`(只保留落在**可执行区域** `permissions.includes("x")` 的最后一跳地址)+ chip **三值形态** `data-pseudo-asm-source`:`debug` 真指令文本 / `no-coverage` 调试档未覆盖 / `solve` 解题档引导文案;**只读不可点、绝不伪造指令**;每条链**恰一个** chip,与 `visible-run` 延伸段为**兄弟节点**。**真机**:`solve` 形态已通过;**`debug` / `no-coverage` 两形态真机待复验**(被 D-API-120 缺陷 2 阻塞,不得宣称达成) |
| 3.10 | 特殊显示:点击地址发生视角跳转 | :38 | **符合**(链组件侧) | `viewport-jump`(`detail.addressHex` / `withinWindow`)+ 工作区 `#onViewportJump`(窗口内滚动、窗口外反馈);`sm-jump-chain` 自身不含滚动逻辑(组件只发事件) |
| 3.11 | 特殊显示:8 字节**均为可见字符** → 右侧显示为可见字符;链末满足可见条件则多延伸可见字符 | :39 | **符合(等价形态)+ 已定案(#11 清理)** | 行级「可见字符」由 `byte-view` 的**逐 cell** 特殊显示承担(`renderSpecialDisplayCell`,每字节一格,可见字符原样呈现);链末串形延伸由 `visibleRunAt` + `renderVisibleRun`(引号包裹 `data-visible-run`)承担。**WP-75#11 定为「清理」**(commit `484e2a7`):删除自 M1 起无生产调用方的 `visibleRunOfRow`(整行串形)及其出口面 / 用例,理由见 `src/views/chain/visible-run.ts` 模块头。文档侧「行级整行串形」表述 WP-77 已按等价形态回填 |

## 4. 指令视图(文档 :41-50)——已由 M2 波次收敛

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 4.1 | 一条指令一行;左 = 最低位字节地址 / 中 = 伪机器码 / 右 = 指令语句(伪汇编) | :43-44 | **符合** | `instr.colAddress` / `instr.colBytes` / `instr.colText`;`instr.tableAria`「伪指令流(一条指令一行,高地址在下)」 |
| 4.2 | 伪汇编列**右侧单独对齐** | :44 | **符合**(WP-75#7 收敛) | `.row-text { text-align: end }`(旧值 `text-align: start`)+ 单元格机检锚 `data-col-align="end"`;jsdom 层以构造样式表 CSSOM 实测,**无回退到 start 的路径**。**真机 computed 值待复验**(被 D-API-120 缺陷 2 阻塞在 attach 之前,如实标注未编造) |
| 4.3 | 跳转语句(jmp / call)向右延伸目标地址,点击跳转 | :45 | **符合** | `instr.jumpTargetTitle`「跳转到 {target}」+ `instr.jumpAria` / `instr.outOfCoverage` |
| 4.4 | 函数列表(按地址)可跳转指定函数 | :46 | **符合** | `instr.functionTable`「函数表({count})」+ `DebugDataSource.functions_list` |
| 4.5 | 默认视角 = rip;拖拽后可切回 rip | :47 | **符合**(WP-75#7 收敛) | rip 三级回退已达;WP-75#7 补「**初始视角锚定**」(`#initialAnchorApplied`,dataSource 变更即复位;首次建行时按 rip 锚定一次)与「**未暂停时也可回锚**」语义 + 断言 |
| 4.6 | 支持输入地址跳转 | :48 | **符合** | `instr.jumpPlaceholder` / `instr.prefetching` / `instr.jumpUnrecognized` |
| 4.7 | 支持字节、指令检索 | :49 | **符合** | `instr.byteSearch*`(全内存)+ `instr.textSearch*`(已推送覆盖面),命中汇总 `instr.byteHitsSummary` / `instr.textHitsSummary` |
| 4.8 | 可在一条指令上添加断点,后续运行到此处暂停 | :50 | **符合**(WP-76 联动落地) | 指令视图增删地址断点并广播 `breakpoints-changed`;payload 断点经 `#mergePayloadBreakpoints`(**add-only 幂等**)并入同一调试断点集;运行到断点全链路(含 `debug_paused(reason=breakpoint)` → 指令视图 `anchorAddressHex` 命中 + `.paused-row`)。**两个暂停分面互不覆盖**:行断点由 `DebugDataSource` 自身持有,payload 单步暂停以**独立行**呈现(`data-client-step-pause`) |
| 4.9 | 寄存器命中面在指令视图行左缘的交叉标注 | :58(寄存器视图注) | **符合**(WP-75#6 收敛) | `sm-instruction-view.ts` 新增 `registerHits` 注入 + 行左缘 `<sm-register-annotation>`,复用 `views/register/cross-annotation.ts` 与工作区注解缓存挂点;`SmRegisterAnnotation` / `SmRegisterView` **契约零改动**。全链路断言含行左缘 `data-registers="RIP"` |

## 5. 寄存器视图(文档 :52-58)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 5.1 | 纵向显示**所有**寄存器 | :54 | **符合** | 全量渲染 `dataSource.registers()`,服务端白名单结构性排除(M14:前端不留占位行);空态 `reg.empty` |
| 5.2 | 每行三段:寄存器名称 / 值(16 进制)/ 特殊显示 | :55 | **符合** | `reg.colName` / `reg.colValue` / `reg.colSpecial`;值恒 `0x` + 大写(`normalizeValueHex`) |
| 5.3 | 特殊显示形式**与栈视图相同**,但点击地址**不跳转、复制到剪贴板** | :56 | **已定案(D-MP-3),本批落地** | 实现:`sm-register-view.ts` 特殊显示列 = 区域引用 `→ regionId`(保留原信息与 `data-region`)+ **复用 `<sm-jump-chain>` 只读形态**(与栈视图行右段同族,锚 = 寄存器值命中地址);链上地址点击 → `stopPropagation()` 截停 `viewport-jump`(工作区跳转处理器收不到 ⇒ **不跳转**)+ 复制该地址到剪贴板。剪贴板不可用 / 写入被拒 → 降级(**不静默失败**):选中该地址芯片文本 + 行内 `role="status"` 提示「已就绪手动复制」(`reg.copyFallback`,自动消隐)。测试:`test/views/register/sm-register-view.test.ts`(11 → 15 例)。**关键决策**:绑定**必须**用属性面 `.startAddressHex=`(见 3.8 / D-API-117)。**遗留**:chip tooltip 仍为「跳转到 {address}」而点击实为复制(语义微瑕,需新增 `chain.copyTitle` 键,登记未做) |
| 5.4 | 寄存器值为地址 → 任意视图中该地址对应行最左侧标注寄存器名,点击展开寄存器值 | :58 | **符合**(字节视图 + 指令视图) | `crossAnnotateRegisters` + `renderRegisterAnnotationCell` + `sm-register-annotation`(工作区行左缘 `lead` 槽位);「任意视图」的**指令视图**覆盖面已由 WP-75#6 补齐(见 4.9) |

## 6. 自由视图(文档 :60-67)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 6.1 | 默认纵向、8 字节一行 16 进制;可调对齐偏移;输入地址跳转;字节检索 | :62-66 | **符合** | 与栈视图共用 `sm-byte-view`(同一组件、同一行三槽、同一对齐 / 跳转 / 检索入口),差异仅在数据源区域选择(`byte.regionSelectAria` + VMA 列表)与默认锚点 |
| 6.2 | 特殊显示形式**和栈视图完全相同** | :63 | **符合(同源)** | 行右段由同一工作区行装饰(`#renderRowJumpChain`)产出 ⇒ 与栈视图**同源**;**同 3.8 缺陷亦同源修复**(一次修复同时覆盖栈 / 自由视图) |
| 6.3 | 存在 VMA 列表(按地址排列),可跳转指定 VMA 头部 | :67 | **符合** | `vma.heading` / `vma.ariaScoped` / `vma.windowCaption` / `vma.truncatedSuffix`;`src/views/byte/vma-list.ts` |

## 7. payload 搭建窗口(文档 :69-77)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 7.1 | 三区方位 | :71 | **已定案(D-MP-4)+ 文档已回填** | 定案 = **以帮助手册 §7.1 为现状权威、修订设计文档与实现对齐**(不改实现)。文档侧已由 WP-77 回填(commit `dcbcfd7`):原文「左侧积木列表侧栏 + 顶部菜单栏 + 主体搭建空间」已改为「左栏积木画布(含工具箱)+ 右栏自上而下三区(工具栏 / 程序区 / 执行输出区)」,与帮助手册 §7.1 及 `sm-payload-tab` 渲染面一致 |
| 7.2 | 基础通用积木:变量 / 列表 / 分支 / 循环 / 模块化函数(有返回值)/ 运算赋值 / 字符串操作 / 断点积木 | :72 | **符合** | `PAYLOAD_TOOLBOX_CATEGORIES`(`payload/compiler/blocks.ts`)十类:会话动作 / 变量 / 列表 / 分支 / 循环 / 函数 / 运算与赋值 / 字符串 / 公开投影读取 / 断点 |
| 7.3 | 起始积木为唯一构建入口 | :73 | **符合** | `PAYLOAD_START_BLOCK_TYPE`(单一起始;`public-api.test.ts` 固定导出面) |
| 7.4 | 出题者提供的积木模块(交互接口) | :74 | **偏差已双向回填(未实现,WP-80 承接)** | 属**后续里程碑** M3:WP-80「M10 出题者积木最小声明面」已登记(§2.3 #4);本表不代判 |
| 7.5 | 积木技术选型 Blockly | :75 | **符合** | 无头 Blockly + 序列化 JSON 输入编译(`payload/compiler/*`);暗色画布对比度修复见 WP-74 前置项② |
| 7.6 | payload 状态在解题 / 调试模式间共用 | :76 | **符合** | 同 1.8(FE-WS-07 元素同一性断言) |
| 7.7 | 断点积木:调试模式 = 原生暂停点;解题模式 = 步进暂停变通 | :76 | **符合**(WP-76 落地) | **调试档「原生暂停点」已兑现**:断点积木编译期解析出 `addressHex`(**非协议面**字段,来源 = 公开投影 rip)→ `breakpointAddresses()` → 工作区 `#mergePayloadBreakpoints` 并入 `DebugDataSource` 断点集 → 「运行到断点」可用 → 命中后 `debug_paused(reason=breakpoint)`。**解题档变通语义保留**(客户端步进暂停,不新增服务端语义)。**边界**:解析不到地址时**该键不出现**、落「无地址形态」且不报错 ⇒ 解题档语义与既有测试零变化;两个断点积木解析到同一地址时按首现顺序**去重**。承载定案见 **D-API-119** |
| 7.8 | 鼠标悬停积木显示功能说明 | :77 | **符合** | Blockly 积木 `tooltip` 面(`payload/compiler/blocks.ts` 定义面) |

---

## 8. M2 波次对核查表的增量(三态变更一览)

| 项 | 行 | 三态变更 |
|---|---|---|
| WP-75#5 寄存器特殊显示升级 | 5.3 | 「偏差」→「**已定案(D-MP-3)**」并落地实现 + 用例(11 → 15 例) |
| WP-75#6 交叉标注补指令视图 | 4.9 | 「偏差(未收敛)」→「**符合**」 |
| WP-75#7 伪汇编右对齐 + rip 初始锚定 | 4.2 / 4.5 | 两项「偏差(未收敛)」→「**符合**」(真机 computed 待复验) |
| WP-75#9 夹具通道 debugMode 缺省语义 | 1.7 | 「偏差」→「**偏差已双向回填**」(实现归一 + 用例);正式通道侧原本即符合 |
| WP-75#11 `visibleRunOfRow` 无调用方 | 3.11 | 定为「**已定案(清理)**」:删除死导出面,行级可见字符语义归逐 cell 特殊显示 + 链末串形延伸 |
| WP-76 伪汇编延伸 | 3.9 | 「未实现」→「**符合**」(`solve` 形态真机已绿;`debug` / `no-coverage` 待复验) |
| WP-76 断点联动 | 4.8 / 7.7 | 「归 WP-76」→「**符合**」(全链路集成测试 3 例绿) |
| **3.8 新发现偏差**(jump-chain 死绑定) | 3.8 / 6.2 | 「待主控派单」→「**偏差已修复并登记**」(D-API-117,commit `2f851a7`) |
| WP-77 文档回填 | 1.1 / 7.1 | 「文档侧待回填」→「**已回填**」(`9161ba7` 工作区重写、`dcbcfd7` payload 三区) |

## 9. 遗留 / 待复核(如实登记,不代判)

1. **真机复验阻塞(D-API-120 缺陷 2)—— 已解除(2026-09-17 订正)**:接线后 `debug_attach` 曾恒返回 `internal_error`(服务端日志 `DebugVariantBuildError`)⇒ 调试档真机端到端不可达。**根因不在通道实现而在题目形态**(E2E / 演示拓扑唯一驱动的种子题登记为 IR 模式,而调试变体契约要求字节模式,ADR-DC1 条款 2),已修于 commit `c295c87`。**当前状态**:受影响项 3.9 的 `debug` 形态、4.5 的真机锚定、4.8 / 7.7 的真机断点联动**已获真机验证**(`breakpoint-linkage.spec.ts` 第 1~6 步真机绿:首个暂停 `breakpoint@0x401000` / 指令行可见 / 锚点行入视口 / `.row-text` `text-align:end` / 行断点联动 / payload 断点并入);本表相关行**不再受此阻塞**。**仍开放**:调试档伪汇编 chip(第 7 步)因「调试实例对齐源」(动作日志只在 `submit` 落库 ⇒ 未提交会话克隆恒为种子初态,真机取证 `targetRevision:1` 实测对齐 `revision:0`)未达,待主控定案两条修法;该项归属 WP-76 勾选项,不影响本表三态判定。(缺陷 1 —— 生产入口漏传 `debugChannel` 致路由 404 —— 已修,commit `a35bb01`。)
2. **WP-74 terminal 效果面**:扫描线 / 光标闪烁 / 终端式标题栏的实装属 WP-74 后半;**本表不含该项判定**(设计文档无主题章节)。其契约(C1~C9)单一来源 = `apps/plugin-dev/e2e/helpers/decoration.ts`,变量面与消费口径登记于 D-API-118 与 `packages/vm-ui/README.md`。
3. **5.3 的 chip tooltip 语义微瑕**:只读链 chip 的 tooltip 仍为「跳转到 {address}」而点击实为复制;修正需新增 `chain.copyTitle` 键(zh / en 双目录)并同改 `sm-jump-chain.ts`,已登记未做。
4. **7.4(WP-80 / M10 出题者积木声明面)**:属后续里程碑,按 WP-80 退出条件验收。
5. **核查方式说明**:本表的实现侧证据来自源码落点 + 组件用例 + i18n 双目录键面 + 全链路集成测试;**真机结论仅在明确标注处引用**(3.9 `solve` 形态、WP-76 的 1 passed);未标注真机结论的行其判定**不含真机视觉 / computed 值**。行号以 83 行的重写后文档为准。
