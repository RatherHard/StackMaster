# 中期 M2 核查表草稿(E-4 证据)

> **文档状态**:草稿。由 WP-75 #5 / #9 / #11 承接方(2026-09 波次)撰写,供主控在文档波次
> (WP-77)并入正式验收材料 / 修订 `docs/develop/前端的交互和开发设计.md` 时取用。
> **本文件不修改任何既有文档**;`docs/develop/前端的交互和开发设计.md` 的措辞修订归 WP-77。
>
> **权威对照物**:`docs/develop/前端的交互和开发设计.md`(75 行,逐行核查)。
> **三态口径**(WP-75 完成标准「核查表逐条『符合 / 已定案』」):
> - **符合** = 实现与文档表述一致(附实现落点);
> - **已定案** = 实现与文档表述不一致或文档未表述,但已由决策点(D-MP-*)定案,按定案执行(附决策点与执行落点);
> - **偏差已双向回填** = 实现与文档的偏差已确认,实现侧与文档侧/台账侧都已登记到同一结论(附两侧落点)。
>
> **超出本稿范围的项**:#6(交叉标注补指令视图)、#7(指令视图右对齐 + rip 锚定)、WP-76(#2 伪汇编延伸 / #3 断点联动)、
> WP-79 / WP-80 / WP-82 相关行一律标注「由并行 agent 承接,以主控复核为准」,本稿只如实登记**核查当时**观察到的现状,不代其判定。

---

## 1. 工作区组件(文档 :5-13)

| # | 核查项(文档表述) | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 1.1 | 工作区是前端最大唯一主体,容纳、管理、排布工作**标签页** | :7 | **偏差已双向回填** | 实现侧 = **固定窗口集(D-MP-1 定案)**:窗口集合 = 注册表 10 类型各恰一实例、常驻、无开/关生命周期,`WorkspaceLayoutModel.bindWindows` + `isWindowSetComplete` 不变量机检;文档侧仍写「标签页可开可关、同类多开」⇒ 文档重写归 WP-77(§二 M2 表 WP-77 行已登记)。两侧结论同一:模型以「窗口」为准 |
| 1.2 | 排布采用类似 Hyprland + Niri 的滚动平铺 | :8 | **已定案(D-MP-1)** | 列式滚动条带 + 焦点列相机 + 三类拖拽落点 + 列宽五档 / 重置:`src/workspace/layout-presets.ts`(P0/P1/P2 单一来源)、`layout-camera.ts`、`layout-divider.ts`;E2E `e2e/workspace-layout.spec.ts`(8 例)、`e2e/workspace-windows.spec.ts`(3 例) |
| 1.3 | 工作区顶部存在管理工作区的菜单 | :9 | **符合** | `src/workspace/sm-workspace-menu.ts`(`menu.aria`);菜单组 = 窗口 / 布局 / 模式 / 运行(\| submit) |
| 1.4 | 菜单含运行选项:指令步进、积木步进、运行到断点 | :10 | **符合** | `menu.step`「指令步进」/ `menu.payloadStep`「积木步进」/ `menu.runToBreakpoint`「运行到断点」(`catalog-zh-CN.ts:59-66`);i18n 双目录齐备 |
| 1.5 | 菜单可重启测试环境 | :11 | **符合** | `menu.reset`「重启测试环境」+ 终态 / 无会话 / 断线三态禁用理由(`menu.resetTitle*`) |
| 1.6 | 菜单支持解题 / 调试模式切换 | :12 | **符合** | `menu.modeToggleToDebug` / `menu.modeToggleToSolve` + `aria-pressed`;门槛 `debugModeAvailable`(`sm-workspace-menu.ts:146`) |
| 1.7 | 调试模式由题目声明启用(**默认启用**,可显式关闭,ADR-DC1) | :12 | **偏差已双向回填**(本批 #9 收敛) | 归一化口径 = `debugMode === undefined ? true : value`(`challenge-descriptor.ts:451-452`)+ session-api 门控 `debugMode !== false`;**开发壳夹具通道此前按 `=== true` 判真(缺省 false)**,与正式通道相反 ⇒ 本批 WP-75#9 收敛:`apps/plugin-dev/src/main.ts` 新增 `DEBUG_MODE_DEFAULT` / `normalizeDebugMode`,两通道缺省语义一致;用例 `test/descriptor.test.ts` 补「缺省即启用」断言 |
| 1.8 | 两种模式**共用** payload 搭建标签页状态 | :13 | **符合** | `test/workspace/sm-workspace-mode.test.ts`「FE-WS-07:payload 窗口状态两模式共用」断言模式切换前后 `sm-payload-tab` **元素同一性**(状态共用;换绑数据源不重建元素) |
| 1.9 | 内存视图标签页随模式切换**换绑数据源** | :13 | **符合** | 同一测试文件(M1 定案「换绑数据源、布局零副作用」,`layoutSnapshot` 深度相等断言);调试档装配修复见 WP-70(`createDebugDataSource` 的 `projectionProvider`) |

## 2. 内存视图标签页组件(文档 :15-18)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 2.1 | 内存视图四类:栈 / 指令 / 寄存器 / 自由 | :17 | **符合** | `STACK_TAB_TYPE` / `INSTRUCTION_TAB_TYPE` / `REGISTERS_TAB_TYPE` / `FREE_TAB_TYPE`(`src/workspace/tab-registry.ts`);指令视图窗口类型在解题档呈现「调试档解锁」引导(不伪造数据) |
| 2.2 | 解题档 = 服务端公开投影,**受公开投影字节窗口约束** | :18 | **符合** | `ProjectionDataSource`(D3 窗口外语义:未下发 = `byte: null`、不占位不报错);`byte.jumpOutsidePublic`「在可见窗口之外(仅窗口内可达)」 |
| 2.3 | 调试档 = 调试通道;任意地址跳转 / 全内存检索 / 指令视图 / 指令断点全量可用 | :18 | **符合** | `DebugDataSource`(`prefetchWindow` / `searchBytes` / `instructionStream` / `functions` / 断点集);`menu.modeToggleTitleToDebug` 明示「调试通道承载任意地址 / 指令流 / 断点」;`byte.jumpPrefetching` / `instr.prefetching` 呈现拉取语义 |
| 2.4 | (红线)浏览器侧零权威执行、零判题 | :18 | **符合** | 双档数据源均只读公开投影 / 调试通道已下发数据;断线横幅 `menu.bannerBody` 明示「不做任何本地 VM 执行降级」;what-if 横幅 `menu.whatifTitle`「调试通过 ≠ 提交通过」保留 |

## 3. 栈视图(文档 :20-33)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 3.1 | 默认纵向排列、高处 = 低地址、8 字节一行 16 进制 | :22 | **符合** | `byte.tableAria`「内存十六进制字节(8 字节一行,高地址在下)」;`ROW_BYTES = 8`;虚拟列表 `sm-window-list` |
| 3.2 | 每行三段:左 = 8 字节中最低位字节的地址 / 中 = 16 进制 / 右 = 特殊显示 | :23 | **符合** | `byte-view.ts` 行三槽(`.row-address` / `.row-hex` / `.row-special`),行地址 = `row.addressHex`(行首 = 最低位字节);表头 `byte.colAddress` / `byte.colHex` / `byte.colSpecial` |
| 3.3 | 可调 8 字节对齐的偏移 | :24 | **符合** | `byte.offsetGroupAria` / `byte.offsetLabel` + 增 / 减入口;`src/views/byte/alignment.ts` |
| 3.4 | 默认视角 = rsp 位置,可切 rbp;拖拽后可回锚 | :25 | **符合** | `byte.anchorOutside` / `byte.anchorRewind`(「回锚 {register}」+ `aria`);锚点随投影寄存器值三级解析(见 `byte-tab` 视图接线) |
| 3.5 | 支持输入地址跳转任意地址 | :26 | **符合** | `byte.jumpAria` / `byte.jumpPlaceholder`(「0x1004 或偏移(十进制)」)+ `byte.jumpUnrecognized` 可解释错误文案 |
| 3.6 | 支持字节检索 | :27 | **符合** | `byte.searchAria` / `byte.searchPlaceholder` / `byte.searchNoHits` / `byte.searchSummary`(+ `byte.searchWindowNote`「仅已下发窗口字节」= 解题档窗口口径如实呈现) |
| 3.7 | 特殊显示:地址 → 横向跳转链,最长三段,循环打回;超三段可点击在下方展开竖向 | :30 | **符合** | `JUMP_CHAIN_HORIZONTAL_LIMIT = 3` / `JUMP_CHAIN_EXPANDED_LIMIT = 32` / `chainLimitReached`(展开入口)/ `loopBack`(SVG 回环箭头);`views/chain/resolve.ts` + `sm-jump-chain.ts` |
| 3.8 | **行右段链实际呈现** | :29-32 | **偏差(新发现,待主控派单)** | 链组件能力齐备,但 `sm-workspace.ts:1707` 以 **kebab 属性** `start-address-hex=${...}` 挂链:Lit 的属性名缺省为「全小写、不加连字符」(`@lit/reactive-element` `__attributeNameForProperty` = `name.toLowerCase()`),故 `startAddressHex` 观察属性名是 `startaddresshex`,kebab 属性落不进属性面 ⇒ **栈 / 自由视图行右段的链恒空渲染**。既有用例只断言裸属性(`test/workspace/sm-workspace.test.ts:872`),故长期未暴露。WP-75#5 实现侧实证(属性形态 → `chain.startAddressHex === ''` 红灯;属性面 `.startAddressHex` → 绿)。**`sm-workspace.ts` 不在 WP-75#5 的允许清单,未触碰**,修法与承接方由主控定 |
| 3.9 | 特殊显示:链延伸到代码区 → 追加一条伪汇编 | :31 | **偏差已双向回填(未实现,WP-76 承接)** | `docs/phases/中期任务分解.md` WP-76 行(§2.3 #2)已登记;本稿不代判 |
| 3.10 | 特殊显示:点击地址发生视角跳转 | :32 | **符合(链组件侧)** | `viewport-jump`(`detail.addressHex` / `withinWindow`)+ 工作区 `#onViewportJump`(窗口内滚动、窗口外反馈);`sm-jump-chain` 自身不含滚动逻辑(组件只发事件) |
| 3.11 | 特殊显示:8 字节**均为可见字符** → 右侧显示为可见字符;链末满足可见条件则多延伸可见字符 | :33 | **符合(等价形态)+ 已定案(#11 清理)** | 行级「可见字符」由 `byte-view` 的**逐 cell** 特殊显示承担(`renderSpecialDisplayCell`,每字节一格,可见字符原样呈现);链末串形延伸由 `visibleRunAt` + `renderVisibleRun`(引号包裹 `data-visible-run`)承担。**WP-75#11 定为「清理」**:删除自 M1 起无生产调用方的 `visibleRunOfRow`(整行串形)及其出口面 / 用例,理由见 `src/views/chain/visible-run.ts` 模块头;文档侧如需把「行级整行串形不实现」写成明文,归 WP-77 |

## 4. 指令视图(文档 :35-44)——由并行 agent 承接,以主控复核为准

| # | 核查项 | 行 | 三态(登记态) | 核查当时观察 |
|---|---|---|---|---|
| 4.1 | 一条指令一行;左 = 最低位字节地址 / 中 = 伪机器码 / 右 = 指令语句(伪汇编) | :38 | 符合 | `instr.colAddress` / `instr.colBytes` / `instr.colText`;`instr.tableAria`「伪指令流(一条指令一行,高地址在下)」 |
| 4.2 | 伪汇编列**右侧单独对齐** | :38 | **偏差(未收敛,归 #7)** | `sm-instruction-view.ts:291` 为 `text-align: start`(核查当时);#7 由并行 agent 承接 |
| 4.3 | 跳转语句(jmp / call)向右延伸目标地址,点击跳转 | :39 | 符合 | `instr.jumpTargetTitle`「跳转到 {target}」+ `instr.jumpAria` / `instr.outOfCoverage` |
| 4.4 | 函数列表(按地址)可跳转指定函数 | :40 | 符合 | `instr.functionTable`「函数表({count})」+ `DebugDataSource.functions_list` |
| 4.5 | 默认视角 = rip;拖拽后可切回 rip | :41 | **偏差(未收敛,归 #7)** | rip 三级回退已实现(`sm-instruction-view.ts:385-390`);「初始视角锚定 / 未暂停也可回锚」语义与断言归 #7 |
| 4.6 | 支持输入地址跳转 | :42 | 符合 | `instr.jumpPlaceholder` / `instr.prefetching` / `instr.jumpUnrecognized` |
| 4.7 | 支持字节、指令检索 | :43 | 符合 | `instr.byteSearch*`(全内存)+ `instr.textSearch*`(已推送覆盖面),命中汇总 `instr.byteHitsSummary` / `instr.textHitsSummary` |
| 4.8 | 可在一条指令上添加断点,后续运行到此处暂停 | :44 | 符合(联动归 WP-76) | 指令视图已能增删地址断点并广播 `breakpoints-changed`(`sm-instruction-view.ts:542-545` / `:575-581`);与 payload / 调试执行流的联动暂停归 WP-76(§2.3 #3) |
| 4.9 | 寄存器命中面在指令视图行左缘的交叉标注 | :52(寄存器视图注) | **偏差(未收敛,归 #6)** | #6 由并行 agent 承接 |

## 5. 寄存器视图(文档 :46-52)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 5.1 | 纵向显示**所有**寄存器 | :48 | **符合** | 全量渲染 `dataSource.registers()`,服务端白名单结构性排除(M14:前端不留占位行);空态 `reg.empty` |
| 5.2 | 每行三段:寄存器名称 / 值(16 进制)/ 特殊显示 | :49 | **符合** | `reg.colName` / `reg.colValue` / `reg.colSpecial`;值恒 `0x` + 大写(`normalizeValueHex`) |
| 5.3 | 特殊显示形式**与栈视图相同**,但点击地址**不跳转、复制到剪贴板** | :50 | **已定案(D-MP-3),本批落地** | 实现:`sm-register-view.ts` 特殊显示列 = 区域引用 `→ regionId`(保留原信息与 `data-region`)+ **复用 `<sm-jump-chain>` 只读形态**(与栈视图行右段同族,锚 = 寄存器值命中地址);链上地址点击 → `stopPropagation()` 截停 `viewport-jump`(工作区跳转处理器收不到 ⇒ **不跳转**)+ 复制该地址到剪贴板。剪贴板不可用 / 写入被拒 → Q8 降级(**不静默失败**):选中该地址芯片文本 + 行内 `role="status"` 提示「已就绪手动复制」(自动消隐)。测试:`test/views/register/sm-register-view.test.ts`(11 → 15 例) |
| 5.4 | 寄存器值为地址 → 任意视图中该地址对应行最左侧标注寄存器名,点击展开寄存器值 | :52 | **符合**(字节视图) | `crossAnnotateRegisters` + `renderRegisterAnnotationCell` + `sm-register-annotation`(工作区行左缘 `lead` 槽位);「任意视图」中的**指令视图**覆盖面归 #6(并行 agent) |

## 6. 自由视图(文档 :54-61)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 6.1 | 默认纵向、8 字节一行 16 进制;可调对齐偏移;输入地址跳转;字节检索 | :56-60 | **符合** | 与栈视图共用 `sm-byte-view`(同一组件、同一行三槽、同一对齐 / 跳转 / 检索入口),差异仅在数据源区域选择(`byte.regionSelectAria` + VMA 列表)与默认锚点 |
| 6.2 | 特殊显示形式**和栈视图完全相同** | :57 | **符合(同源)+ 同 3.8 缺陷** | 行右段由同一工作区行装饰(`#renderRowJumpChain`)产出 ⇒ 与栈视图**同源同缺陷**(见 3.8) |
| 6.3 | 存在 VMA 列表(按地址排列),可跳转指定 VMA 头部 | :61 | **符合** | `vma.heading` / `vma.ariaScoped` / `vma.windowCaption` / `vma.truncatedSuffix`;`src/views/byte/vma-list.ts` |

## 7. payload 搭建标签页(文档 :63-71)

| # | 核查项 | 行 | 三态 | 证据 / 落点 |
|---|---|---|---|---|
| 7.1 | 三区:左侧积木列表侧栏 / 顶部菜单栏 / 主体搭建空间 | :65 | **已定案(D-MP-4)** | 定案 = **修订设计文档与实现对齐**(帮助手册为现状权威,不改实现);文档修订归 WP-77(§二 WP-77 行)。本稿不重复判定实现侧形态 |
| 7.2 | 基础通用积木:变量 / 列表 / 分支 / 循环 / 模块化函数(有返回值)/ 运算赋值 / 字符串操作 / 断点积木 | :66 | **符合** | `PAYLOAD_TOOLBOX_CATEGORIES`(`payload/compiler/blocks.ts:532+`)十类:会话动作 / 变量 / 列表 / 分支 / 循环 / 函数 / 运算与赋值 / 字符串 / 公开投影读取 / 断点 |
| 7.3 | 起始积木为唯一构建入口 | :67 | **符合** | `PAYLOAD_START_BLOCK_TYPE`(单一起始;`public-api.test.ts` 固定导出面) |
| 7.4 | 出题者提供的积木模块(交互接口) | :68 | **偏差已双向回填(未实现,WP-80 承接)** | §2.3 #4 / WP-80「M10 出题者积木最小声明面」已登记;本稿不代判 |
| 7.5 | 积木技术选型 Blockly | :69 | **符合** | 无头 Blockly + 序列化 JSON 输入编译(`payload/compiler/*`);暗色画布对比度修复见 WP-74 前置项② |
| 7.6 | payload 状态在解题 / 调试模式间共用 | :70 | **符合** | 同 1.8(FE-WS-07 元素同一性断言) |
| 7.7 | 断点积木:调试模式 = 原生暂停点;解题模式 = 步进暂停变通 | :70 | **偏差已双向回填(部分未实现,WP-76 承接)** | 解题档变通语义已实现(客户端步进暂停);调试档「原生暂停点」需断点积木携带地址并真实并入调试断点集 ⇒ WP-76(§2.3 #3);本稿不代判 |
| 7.8 | 鼠标悬停积木显示功能说明 | :71 | **符合** | Blockly 积木 `tooltip` 面(`payload/compiler/blocks.ts` 定义面) |

---

## 8. 本批(WP-75 #5 / #9 / #11)对核查表的增量

| 项 | 行 | 结果 |
|---|---|---|
| #5 寄存器特殊显示升级 | 5.3 | 三态由「偏差」→「**已定案(D-MP-3)**」并落地实现 + 用例 |
| #9 夹具通道 debugMode 缺省语义 | 1.7 | 三态由「偏差」→「**偏差已双向回填**」(实现侧归一 + 用例);正式通道侧原本即符合 |
| #11 `visibleRunOfRow` 无调用方 | 3.11 | 三态定为「**已定案(清理)**」:删除死导出面,行级可见字符语义归逐 cell 特殊显示 + 链末串形延伸 |

## 9. 遗留 / 待主控复核

1. **3.8(新发现偏差,不在 #5/#9/#11 允许清单内)**:`sm-workspace.ts:1707` 的 kebab 属性绑定使栈 / 自由视图行右段跳转链**恒空渲染**;修法极小(`.startAddressHex=${...}` + 把 `test/workspace/sm-workspace.test.ts:872` 的断言升级为「属性值 + 链 shadow 内芯片存在」)。承接方由主控定(候选:#6/#7 的 agent、WP-76)。
2. **4.2 / 4.5 / 4.9**:#6 / #7 未收敛项,由并行 agent 承接;本稿只登记核查当时现状。
3. **3.9 / 4.8 / 7.4 / 7.7**:WP-76 / WP-80 承接项,按各 WP 的退出条件验收。
4. **文档侧待回填(WP-77)**:①工作区「标签页」表述改固定窗口 / Niri 布局;②payload 三区方位按 D-MP-4 修订;③寄存器特殊显示(点击复制)与指令视图右对齐 / rip 锚定的表述落实;④本稿 3.11 的「行级整行串形不实现」如判定需明文;⑤`packages/vm-ui/README.md` 结构表 `visible-run` 行(现写「`visibleRunOfRow`(行内全可见字符)」)需随 #11 清理删除该函数名(README 不在 WP-75#5/#9/#11 允许清单,未触碰)。
5. 核查方式说明:本稿的实现侧证据来自源码落点 + 组件用例 + i18n 双目录键面;**未使用 Playwright / E2E**(本波次 E2E 端口与 compose 拓扑归另一 agent 独占),三态判定不含真机视觉结论。
