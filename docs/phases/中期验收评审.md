# 中期验收评审(UI 重设计 × 完整可用版本 · WP-70 ~ WP-83 收口 · 2026-09-17)

> 本文件是中期计划(`docs/中期计划.md`)的验收评审,沿用阶段二~六验收评审的**勾稽表形态**:
> E-1~E-8 逐条「达成 / 部分达成(附遗留登记)」+ 可复跑证据(命令 + 归档路径)+ 门禁汇总 + 遗留移交清单。
> 权威来源仍是 `docs/项目计划书.md`;本文件与计划书冲突时以计划书为准。

---

## 一、中期定位与范围

中期计划的目标是「UI 重设计 × 完整可用版本」,里程碑三分:

| 里程碑 | 工作包 | 主题 |
|---|---|---|
| M1 可用性地基 | WP-70 ~ WP-73 | 固定窗口工作区模型(全窗口常驻、无关闭)、Niri 式列式布局、主题机制扩展(terminal 预设) |
| M2 终端风格与交互补全 | WP-74 ~ WP-77 | 终端主题全组件落地、交互偏差收敛、断点联动与伪汇编延伸、文档回填批 |
| M3 完整可用收口 | WP-78 ~ WP-83 + 本评审 | 宿主闭环(成绩只读接口 + 最小管理面)、出题人面(M10 声明面 + canary 收口)、试用与数据(部署 + 采集最小面 + 报告模板)、主 chunk 体积拆分(收口补派) |

**M3 的进入前提**:M1 / M2 已收口(M1 事实段与 M2 事实段分别登记于 `CLAUDE.md`)。M3 的验收判据 = 中期验收定义 **E-1 ~ E-8**(`docs/中期任务分解.md` §五)。

**主控定案(本评审的执行前提,2026-09-17)**:`docs/中期计划.md` §五 与 §五.2 —— D-MP-5 取**分支 A**、D-MP-6 取 **`authorBlocks`**、D-MP-7 取 1~2 高校社团试点;O-MP-1 ~ O-MP-6 逐条定案(**O-MP-5 域名 / 主机项因主控暂无法提供,按阻塞登记**)。收口补派 **WP-83**(主 chunk 体积拆分),以消除 `docs/中期任务分解.md` §九风险表中唯一无承接的悬空义务。

---

## 二、交付清单(WP-70 ~ WP-83)

| WP | 内容 | 里程碑 | 状态 |
|---|---|---|---|
| WP-70 | 调试档数据源投影接线修复 | M1 | **完成**(`5b1b7f4` 同批;装配路径集成测试已补,红线先行 4 failed → 绿) |
| WP-71 | 固定窗口工作区模型 | M1 | **完成**(D-MP-1;`bindWindows` 一次性绑窗,窗口 id ≡ 类型键;`openTab`/`closeTab` 全退场) |
| WP-72 | Niri 式布局交互 | M1 | **完成**(三张预设表为默认布局唯一来源;分隔条 / 相机 / 三类拖拽落点 / 五档列宽) |
| WP-73 | 主题机制扩展(terminal) | M1 | **完成**(`SM_THEME_PRESET_VALUES` 三值;`EMBED_THEMES` 冻结面零改动;D-API-110) |
| WP-74 | 终端风格全组件落地 + 三浏览器矩阵 job | M2 | **完成**(16 组件零硬编码色;效果面三件包在 `no-preference` 内;axe 真机九面;`e2e-matrix` job 落地) |
| WP-75 | 交互偏差收敛批 | M2 | **完成**(含 (a1) 产品侧落地与两处死选择器族缺陷修复) |
| WP-76 | 断点联动 + 跳转链伪汇编延伸 | M2 | **完成**(D-API-117/119/120;**遗留 = 调试克隆对齐源**,已由 M3 派单,见 §六 遗留第 6 项) |
| WP-77 | 文档回填批 | M2 收尾 | **部分达成** —— 交付面完成;「依赖 M3 的后半部分」三项已按 M3 口径移交并**均有实体承接 WP**(§六 遗留第 7 项) |
| WP-78 | 成绩同步只读接口(契约先行 + 宿主凭证) | M3 | **完成**(8 子项 7 项完成;第 7 子项 compose 用例**代码面完成、本机不可取证** —— 环境阻断,见 §六 遗留第 19 项。证据:protocol 425 passed / session-api 653 passed / 契约冒烟 exit 0 / build 12/12) |
| WP-79 | 最小管理面(分支 A:落地 `apps/admin`) | M3 | **进行中**(本评审定稿时仍在执行;已见产物 `apps/admin` 36 文件 + `docs/develop/decisions-m3/WP-79.md` 21 KB;状态以收口轮终稿为准) |
| WP-80 | M10 出题者积木最小声明面 | M3 | **进行中**(本评审定稿时仍在执行;已见 `packages/challenge-schema/src/common/author-blocks.ts`、`author-blocks*.test.ts`、`canvas-sprite-filter.test.ts` 等在途产物) |
| WP-81 | canary 契约收口(XS-CANARY-CORR) | M3 | **进行中**(本评审定稿时仍在执行;定案面 lane 已派出,**片段尚未产出**,`docs/develop/decisions-m3/WP-81.md` 未落盘) |
| WP-82 | 试用环境部署 + 十五章采集最小面 + 报告模板 | M3 | **文档面完成**(部署指南 + 报告模板 + 暴露面,`24edb43`;D-API-137~140);**采集面进行中**(本评审定稿时仍在执行)。**O-MP-5 阻塞**见 §六 遗留第 8 项 |
| WP-83 | 主 chunk 体积拆分(收口补派) | M3 | **完成 · 义务关闭**(主 chunk **1,379.02 → 428.65 kB,−68.9%**;两处同改;`web-component` 单产物登记为结构性限制;D-API-146~148) |

---

## 三、门禁证据汇总(可复跑)

> **取证形态**:下表以 **2026-09-17 M3 收口轮主控实跑**为准(命令 + 关键读数 + exit code 逐行给出);标 ⚠ / ⛔ 者为**未实跑或环境阻断**,按纪律**明文登记为未实测,不以他人报告或历史值冒充本轮通过**。环境:**Node v24.20.0**(CI 为 Node 22)、pnpm 11.25.0、仓库根 `F:\StackMaster`;`NODE_OPTIONS=--max-old-space-size=8192`(本机 `lint:deps` 在 Node 24 上无此设置会 V8 OOM,属**本机约束而非 CI 缺口**)。

| 面 | 命令 | 结果 |
|---|---|---|
| TS 构建图与类型 | `pnpm build` / `pnpm typecheck` | ✅ **exit 0**:`build` **13 successful / 13**(12 cached,1.551s);`typecheck` **18 successful / 18**(14 cached,3.491s) |
| Lint 与依赖边界 | `pnpm lint` / `pnpm lint:deps` / `pnpm lint:deps:self-test` | ✅ 三者 **exit 0**;`lint:deps` = `no dependency violations found (1357 modules, 4300 dependencies cruised)`;`self-test` = **依赖边界反例自检全绿:20 组边期望全部满足**(含 WP-79 新增 `apps/admin` 四条:反例 13 `admin→未登记包`、反例 14 `plugin-dev→challenge-schema` 白名单外仍红灯、正控 15a `admin→challenge-schema` 放行、正控 15b `admin→protocol` 放行,并附「apps→允许清单全量」零误报对照组) |
| TS 测试 | `pnpm test` | ✅ **exit 0**,**Tasks 26 successful / 26**(23 cached)。逐包:`session-api` **72 files / 757 passed | 13 skipped**;`vm-ui` **70 / 866**;`challenge-compiler` **9 / 127**;`admin` **9 passed | 2 skipped(11 files)/ 87 passed | 9 skipped**;`web-component` **9 / 82**;`embed-runtime` **4 / 62**;`plugin-dev` **2 / 28**;`session-core` **28**;`react-wrapper` **1 / 8** |
| 覆盖率(完整门禁形态) | `SESSION_API_IT=1 pnpm test:coverage` | ⛔ **门禁形态本机不可达(端口冲突,非代码缺陷)**;✅ **补充形态已实跑且四维过门槛**。 · **门禁形态失败原样**:`docker compose -f apps/session-api/compose/deps.yaml up -d --wait` → `driver failed programming external connectivity on endpoint session-api-deps-redis-1 …: Bind for 0.0.0.0:16379 failed: port is already allocated`(占用者实证 = `session-api-app-redis-1`,运行中 app 拓扑);**因 abort 于 vitest global setup,全部 project 未执行 ⇒ 报告 0% 且四维全 `ERROR: … does not meet global threshold (80%)`,这是 abort 后果而非真实覆盖率**。 · **补充形态(无 IT 前缀,非门禁)`pnpm test:coverage` = exit 0**:`All files` **stmts 87.89 / branch 80.98 / funcs 87.28 / lines 88.48** ⇒ **四维全部 ≥ 80 门槛**(2804 passed | 145 skipped)。 · **由此可判**:整体 ≥80% 的**真实覆盖率已取得正向证据**;门禁形态仅缺「含容器门控 IT」这一形态的合成数,清偿载体 = CI linux 完整拓扑。 · **附带订正**:`vitest.coverage.config.ts:16-18` 注释称 unit-only 形态 branches ≈ **79.1%** 低于门槛,**该注释已过时**(现测 **80.98%** 已越线,系 M3 新增测试抬升)⇒ 登记为待订正项。 |
| 区间证据(历史) | —— | `admin` 覆盖 IT **94.33% lines / 83.11% branches**(WP-79 lane 报告);WP-82 采集面净增源码约 700 行 / 测试约 1000 行(其自身未单独跑覆盖率)。 |
| 契约 fixture | `pnpm fixtures:manifest --check` | ✅ **exit 0**:`--check 通过:254 个 fixture 与清单一致`(WP-80 重算后为 **254**;WP-80 报告中的「243 摘要 / 11 拒绝码」为**分列口径**,二者合计 254,非冲突) |
| 跨语言契约冒烟 | `pnpm build && pnpm smoke:contract` | ✅ **exit 0**:`Schema 编译 21 个,有效样例 67 接受,非法样例 143 拒绝,摘要比对 254 条,serde/schemars 消费 15 项,private-bundle 消费 10 项` |
| 产物隔离扫描 | `pnpm scan:public` | ✅ **exit 0**:`R8 隔离扫描通过:5 个公开包已扫描,0 违规,3 条豁免`(3 条豁免均为 `packages/protocol/dist/**` 的**既有 allowlist 条目**,已在输出中逐条具名 ⇒ 无一为新增豁免面) |
| Rust 双 profile | `pnpm test:rust` | ✅ **exit 0**,13 个 test target 全 `ok`(**0 failed / 0 ignored**);debug / release 双 profile 均由该入口覆盖 |
| 引擎纪律 | `node tooling/check-engine-discipline.mjs` | ✅ **exit 0**:`引擎确定性纪律门禁全部通过(ENG-1~6)`,含反例 crate 被拒 + **无配置对照绿灯**(证明红灯归因于禁用清单而非误报)+ 干净 crate 零误报 |
| Compose 集成 | `pnpm --filter @stackmaster/session-api test:compose` | ⛔ **本机环境阻断,不可达**(遗留第 19 项):运行中拓扑的镜像为 **M3 前构建**,且 docker registry 经 `127.0.0.1:7897` 代理被 actively refused ⇒ **镜像无法重建**。该门禁在本机是「不可达」而非「未做」,**清偿载体 = CI `ubuntu` 拓扑**;**在取得该证据前不得声称 compose 全链路通过** |
| 容器门控 IT(真实 PG / Rust worker) | `SESSION_API_IT=1 pnpm --filter @stackmaster/session-api test:integration` | ⚠ **部分实跑**:WP-82 采集面以「**去掉 compose 生命周期、对接已在场的同镜像 / 同 init / 同端口 PG**」的临时配置复跑,实得 **21 passed | 1 skipped**(真库 RLS 红矩阵 / append-only / kind CHECK / 两段式保留期 / 采集→聚合全链路 / 幂等重放 / 口径等价 / 零标识落库扫描**全绿**);WP-79 的 `admin` 覆盖 IT 由该 lane 报告 94.33% / 83.11%。**完整形态(含 compose 生命周期)受遗留第 19 项阻断** |
| E2E(本地 chromium) | `pnpm --filter @stackmaster/plugin-dev test:e2e` | ⚠ **本轮只对 axe spec 实跑**(见下行);**全量 E2E 未在本轮实跑** |
| axe 真机 | `e2e/axe-contrast.spec.ts`(归档 `e2e/reports/axe/<运行当日>/`) | ✅ **exit 0 / 4 passed**(两次复跑一致,15.1s / 15.2s):**9/9 可达面 `violations = 0`**;归档 `apps/plugin-dev/e2e/reports/axe/2026-09-17/` 的 **9 个 JSON 全部在本轮修复后重写**(mtime 落在同一运行窗口)、`summary.md` **31 行**(比修复前多 1 行,补回 shell-terminal 面)⇒ **归档内部一致**。**证据强度保留**:3 个 terminal 面的「0」仍主要建立在 axe **无法自动判定**之上(`plugin-iframe` terminal 面 `colorContrastCheckedNodes = 7` / `incomplete color-contrast×132`),非正向通过(遗留第 14 项) |
| 三浏览器矩阵 | `E2E_MATRIX=1 …` / CI `e2e-matrix` job | ⛔ **未取得绿**:`29 passed / 12 failed / 10 skipped`,12 个失败**逐条为 `[webkit]` 连接面**(单一可复现根因 = 跨源 WS 认证面),`ts-gate` **间歇红**(红 run 34 / 37,绿 run 32 / 38)。**该面属认证面改动,须走契约 + 安全审查,不在收尾轮改** ⇒ 遗留第 1 项 |
| miri | `pnpm test:miri` | ⚠ **本机未跑完(非红灯、非缺陷)** —— 正常启动(vm-core 167 tests),逐条 ok 通过 `arch` / `decode` / `exec` / `instr` / `judge::hidden` **全部用例**后,**停滞在 `judge::predicate::tests::constant_cost_predicate_count_under_mutation`,≥25 分钟零输出**,按纪律记「本机未跑完」。 · **机制由 CI 自证**:`.github/workflows/ci.yml:265` 的 rust-miri job **显式 `--skip constant_cost_predicate_count_under_mutation`**,且只跑七个子模块子集(`arch:: decode:: memory:: state:: registers:: judge::predicate::`);`:262-263` 注释明示该用例「Miri 下 >30 分钟」⇒ **本地全量入口结构性不可完成**。 · 清偿载体 = **CI rust-miri job(子集形态)** |
| fuzz | `pnpm fuzz:smoke` | ⛔ **exit 101 —— 本机环境缺失**:`cargo fuzz --version` → `error: no such command: fuzz`(nightly 存在,cargo 1.100.0-nightly,但 **cargo-fuzz 未安装**)。`vm-engine/fuzz/Cargo.toml:16-18` 自述「构建与运行仅由 rust-fuzz CI job 承载」。 · 清偿载体 = **CI rust-fuzz job**(`-max_total_time=60 -max_len=16384 --target x86_64-unknown-linux-gnu`,四点限时冒烟) |

---

## 四、退出条件逐条评审(E-1 ~ E-8)

> **填表口径(2026-09-17 收口轮)**:逐条给「**达成 / 部分达成**」判定 + **可复跑证据**;凡未实跑或环境阻断,一律写明并**同时登记遗留项编号**,不以沉默代替结论。总账见 §七.2(达成 3 / 部分达成 5)。

- **E-1**(WP-71 / WP-72)固定窗口集与 Niri 布局:**达成**。固定窗口集(D-MP-1):窗口集合 = 注册表 10 类类型**各恰一实例、常驻**,**无开 / 关状态**,`bindWindows(bindings, columns?)` 一次性绑窗(**窗口 id ≡ 类型键** ⇒ 单实例结构性保证),`openTab` / `closeTab` / 类型内序号 / 关闭入口 / 空态引导**全部退场**;「接入会话」与「布局」解耦(模式切换只换绑数据源,`layoutSnapshot` 深度相等有断言)。Niri 式布局:三张预设表(P0 5 列 / P1 3 列 / P2 单列)为默认列排布**唯一来源**、经 `bindWindows` **单点注入**;`isWindowSetComplete` 不变量与「10 类型各恰一次、无重无漏」**机检固定**;`selectLayoutPreset(viewportWidth)` 纯函数选档;分隔条(`role=separator` + `tabindex=0`,pointer + 方向键)、焦点列相机纯函数 `cameraScrollLeft`(按 `prefers-reduced-motion` 降级)、三类拖拽落点、列宽五档 + 重置入口**仅在菜单「布局」组**(标题栏零控件);视口外降级 = `content-visibility: auto` + `contain-intrinsic-size: auto 9rem`(零 JS)。**证据**:vm-ui 62 files / 766 passed(M1 收口);E2E `workspace-layout.spec.ts`(8 例)+ `workspace-windows.spec.ts`(3 例);axe 真机 `violations = 0`。
- **E-2**(WP-70 / WP-75 / WP-76)装配路径与断点联动:**达成(附一条已派单遗留)**。WP-70:调试档投影接线修复 —— `DebugDataSourceAssemblyOptions` 显式解构,工作区缺省装配传 `projectionProvider`;**装配路径集成测试已补**(既有测试经 `debugDataSourceFactory` 测试接缝绕开真实装配路径 = 该缺陷漏网原因,已登记在测试文件头)。WP-75/WP-76:跳转链**死绑定**修复(D-API-117,链恒空 ⇒ 相关节点在 M1 归档里根本不在场)、跳转链伪汇编延伸(D-API-119)、调试通道两处既有实现缺陷修复(D-API-120,均属「测试接缝绕开生产装配」缺陷族);`(a1)` 产品侧落地后**真机实测首个暂停 `breakpoint@0x401000` + 该地址指令流 16 条**。**证据**:M2 收口 vm-ui 64 files / 806 passed;真机 IT(真实 Rust worker)`test/debug` + `test/scan` 99 passed。**遗留**:调试克隆「对齐源」一般缺陷(未提交会话的克隆恒为种子初始态)⇒ 已登记为 §六 遗留第 6 项并由 M3 **派出实现 lane**(先红后绿 + 冻结契约零改动 + 同 revision 多次 attach 逐字节一致)。
- **E-3**(WP-73 / WP-74)主题三预设 + axe 真机 + `e2e-matrix` 实跑归档 + reduced-motion:**部分达成**。已达成:三预设 token 结构断言;axe 真机九面 `violations = 0`(归档 `e2e/reports/axe/2026-09-17/`);reduced-motion 断言(含 no-preference 正面对照)。**未达成/须限定**:① `e2e-matrix` job 已**实跑**但**未绿**,唯一红点 = WebKit 跨源 WS 认证面(见 §六 遗留第 1 项);② **axe 的「零 violations」在 3 个 terminal 面上证据强度显著偏低** —— 逐面读归档 JSON 实测(`colorContrastCheckedNodes` vs `incomplete[].count`):light / dark 插件面 = **132 通过 / 7 无法判定**,壳面 light = 146 / 7;而 **terminal 插件面 = 7 通过 / 132 无法判定**、**壳面 terminal = 19 / 134** ⇒ terminal 三面的「0 violations」主要建立在 **axe 无法自动判定**之上,不是正向通过(详见 §六 遗留第 14 项)。**⚠ 2026-09-17 M3 收口轮真机复跑:本项状态由「已达成」下调为「★ 收口轮复跑报出 1 条 violations,已派出修复 lane;修复验证前不得声明 axe 九面全 0」** —— 复跑命令 `E2E_SKIP_COMPOSE=1 pnpm --filter @stackmaster/plugin-dev exec playwright test e2e/axe-contrast.spec.ts --project=chromium`,**两次独立运行逐字一致(确定性,非 flaky)**,exit code = 1:面 = `plugin-dev-shell` / light,规则 = **`scrollable-region-focusable`**,impact = **serious**,1 节点,target = `["sm-workspace","sm-payload-tab",".output-log"]`,html = `<ol class="output-log" aria-live="polite">`,failureSummary = 「Element should have focusable content / Element should be focusable」;源码锚 = `packages/vm-ui/src/payload/sm-payload-tab.ts:~831` 渲染该 `<ol>`,`:277-281` 的 `.pane ol { flex:1; overflow:auto }` 使其成为**滚动容器却无 `tabindex`、亦无可聚焦后代**。**归因纪律**:该面在 M3 前最后一次归档(23:08)为 0 violations,而 `sm-payload-tab.ts` 的 mtime(23:35)与 vm-ui dist 重建(23:38)**均晚于**该归档 ⇒ **不能排除是 M3 期改动引入**,但**同样不能据时间戳断言是 WP-83 拆分引入**(无 diff 证据,且 23:08 归档能否覆盖该节点尚未证实)⇒ **交由修复 lane 以 `git show HEAD:` 取上一版归档做只读对比后定论**;在定论前,本项**既不宣布「拆分引入」也不宣布「既往既有」**。**连带证据缺口**:该失败使 **`plugin-dev-shell` / terminal 面本次未被扫描**(用例在同 spec 内 shell light 处先失败),故归档目录内**并存 8 份本次测量 + 1 份 23:08 的旧文件**,而 `summary.md` 只列已扫描面(8 行)⇒ **归档目录自相矛盾**,已登记为遗留第 13 项的**新增子面**(见下)。**并行面**:iframe 6 面(light/dark 各 2 + terminal 各 2)与 `plugin-degraded` light 面本次均 **0 violations**,其中 **iframe terminal 面 `colorContrastCheckedNodes = 7` / `incomplete = 132` 与旧归档逐数一致** ⇒ 遗留第 14 项的「terminal 面证据强度偏低」结论**经复跑再次确证,未改善**。**✅ 同日修复并复验通过(commit `dd68764`)**:修法 = 给该 `<ol class="output-log">` 加 `tabindex="0"`(axe 对 `scrollable-region-focusable` 的标准修法),并把**同渲染点、同成因**的 `.program-list`(受**同一条** `.pane ol { flex:1; overflow:auto }` 支配、只因 shell 面程序为空而本次不在场)一并加;不动 `aria-live` 语义、不加可聚焦后代、不改 CSS 几何、零新增依赖,且**无需更新任何测试断言**(无测试断言该 `<ol>` 的 `outerHTML` / 属性枚举)。**复跑两次均 exit 0 / 4 passed**,`violations` **9/9 面全为 0**(含此前因前置断言先失败而**未扫描**的 `plugin-dev-shell` terminal 面,现 `passed(19)` / `color-contrast×134`);归档 **9 个 JSON 全部在第二次运行窗口内重写、无旧文件残留**,`summary.md` 31 行(补回 shell-terminal 行)⇒ **遗留第 13 项 ⑤ 的「归档自相矛盾」在本轮被实际消除**。**归因结论(有据,`dd68764` 提交信息与 lane 取证)**:①「M3 在渲染路径引入源码回归」= **否**(缺 `tabindex` 是**既有缺陷**,WP-80 对该文件的 diff 未触及 `<ol>` / `.pane ol`);②「门禁一直在场但从未触发」= **是**(axe-core 4.13.0 的该规则要求 `getScroll(node,13)` 即**真实溢出 ≥13px** 才命中);③**把 needle 推过判定线的触发条件 = M3/WP-83 的惰性宿主** —— 硬证据:绿跑制品为 23:05:18 构建(**无** `sm-payload-tab-host`)、红跑为 23:38:29 构建(**含**该宿主),而中间窗口内 vm-ui 唯一结构变更 = `9557396` 的 WP-83 惰性宿主(在 `.tab-content` 与 `<sm-payload-tab>` 之间插入一层**自身无任何样式**的宿主);且**节点集恒定**(`colorContrastCheckedNodes` 在绿归档 / 红跑 / 修复后复跑**均为 146**)⇒ **变的是几何,不是内容**;产物差异**恰为 26 字节 = 两处 ` tabindex="0"`**,同批 `sm-workspace` chunk 两侧**同为 428,999 B** ⇒ **转绿可归因到本修复,而非「重建顺带修好」**。**⚠ 但具体几何机制未直接测量**(归档不记 `scrollHeight` / `clientHeight`,亦未在 pre-WP-83 制品上复现)⇒ 该机制属**高置信推断,非已证实事实**;**不得因 axe 转绿即判定惰性宿主无几何回归** ⇒ 已登记为**遗留第 30 项**。**⇒ E-3 的 axe 方在本轮恢复为「九面 `violations = 0`」,且本轮归档与 M3 终稿树对应;但「零 violations」在 3 个 terminal 面上的证据强度仍偏低(遗留第 14 项不变),且 `e2e-matrix` / WebKit 面仍未绿(§六 遗留第 1 项)⇒ E-3 总体仍为「部分达成」。**
- **E-4**(WP-75 / WP-77)设计文档核查表与文档回填:**部分达成(定稿时仍在收敛)**。已达成:WP-77 交付面的设计文档核查与回填批次;**依赖 M3 的三项回填已有两项落地** —— ① `docs/user/宿主平台接入指南.md` §七「成绩只读同步接口」章节随 **WP-78** 重写(§7.1/§7.2/§7.3;`docs/contracts/版本策略.md` §二补三行 + 缺口登记,`docs/contracts/数据分类与秘密零残留清单.md` §6.11 + 总表升 v1.16);② **`admin` 面悬空声明已消除**(WP-79 改 `README.md:34` / `:63-64`、`CLAUDE.md` 仓库结构与依赖方向两处;主控另修两处**用户面**状态行 —— `docs/user/出题人指南.md` 与 `docs/user/宿主平台接入指南.md` 原写「管理后台(admin)不在 MVP 范围」/「归后续 WP 立项」,而 `apps/admin` 已落地 36 文件,**该表述已与磁盘事实矛盾** ⇒ 改写为「只读最小面已落地(D-MP-5 分支 A;三面全 GET、独立凭证 + 独立只读角色 `admin_ro`),完整管理后台仍不在范围」,commit `1a5c787`;全仓扫描确认无残留同类悬空表述)。**未达成**:③ **WP-80** 的 M10 出题者积木声明面章节(出题人指南 + 出题人文档 HTML)与 **WP-81** 的 canary 行(出题人指南 §十五、`1B-虚拟机.html` B4、`T4-排查手册.html`、`30-精确参考.html`)—— 两条 lane 定稿时仍在执行 ⇒ 以收口轮终稿为准。
- **E-5**(WP-78 / WP-79)成绩只读接口与最小管理面:**部分达成(定稿时仍在收敛)**。**WP-78 完成** —— `GET /host/scores` 只读面 + **新契约族** `HostScoresResponse`(`HOST_SCORES_PROTOCOL_VERSION = 1`;信封恰两键 `items` / `nextCursor`,记录恰七键;终态耦合由 `superRefine` + 生成管线 `if/then` **双侧锁定**);20 个 golden fixture(valid ×4 / invalid ×16,含 `judging-detail-tamper` / `hidden-test-hit-detail-tamper` / `tenant-id-echo-tamper` / `internal-stack-and-run-id-tamper` 四类**泄漏面反例**);宿主凭证复用 `hostBackendTokenMatches`(**进程内仍只有一处实现**,零副本);租户 = **凭证 × 白名单**(`SESSION_API_HOST_TENANTS`,空白名单 ⇒ 面整体 **404 同形**,fail-closed 防枚举);查询参数白名单恰三个、超批量上限 **400 不钳制**;游标 = `verdicts.id` keyset(**禁时刻游标**),并**如实登记「不承诺并发插入零遗漏」**(用户文档已给 `submissionId` 去重 + 终态边界的正确对账用法);PG 侧三表内连接 **SELECT 列表恰七列,私有列在 SQL 层即不出现**(非「读了再脱敏」);**零写入面、零 DDL、零新角色、零新 GRANT**(复用既有三表 RLS 政策与 `session_app` 权限)。**证据**:protocol **19 files / 425 passed**、session-api **63 passed | 12 skipped / 653 passed | 95 skipped**、定向 `host-scores-routes` **46 passed**、`pnpm build` **12/12**、`pnpm smoke:contract` exit 0(65 收 / 143 拒 / 252 摘要 / serde 15 项 / private-bundle 10 项)、`fixtures:manifest` 重算 241 摘要、两包 typecheck exit 0、`lint:deps` 0 违规。**未达成**:第 7 子项 **compose 全链路用例本机不可取证**(代码面就绪、容器门控 24 skipped)⇒ 见 §六 遗留第 19 项(**环境阻断,非「未做」**,清偿载体 = CI ubuntu)。**WP-79 进行中**:`apps/admin` 已见 36 文件 + 决策片段 21 KB;定稿时 lane 仍在执行 ⇒ 以收口轮终稿为准(不含在本次判定内)。
- **E-6**(WP-80 / WP-81)M10 声明面与 canary 收口:**部分达成(按主控 R-4 口径裁决)**。**WP-80 完成(声明面)** —— 公开描述包新增**顶层可选**字段 `authorBlocks`(顶层字段 **16 → 17**,不进 `required`;形状 = 1–16 × `{id, displayText, interfaceId, slots(≤4), actions(≤8)}`;`kind ∈ {address,immediate,length}`;`type` ∈ **12 公开动作冻结枚举**;`args` 值 = 字面量(≤64)或 `{slot: <本模板槽键>}` 结构化引用 ⇒ **非图灵完备**,不构成任意宿主执行**);新增四条检查器规则 `XS-BLOCK-IFACE-REF` / `XS-BLOCK-SLOT-FORM` / `XS-BLOCK-ARG-ALLOW` / `XS-BLOCK-NO-EFFECT`(后者**递归扫描**效果原语与私有键名,含展示文本)+ `authorBlocks[].id` 纳入既有 `XS-ID-UNIQUE`;`CHALLENGE_PACKAGE_SCHEMA_VERSION` **维持 1**;双语 golden fixture(扁平有效样例 ×2,反例内联于测试 —— 扁平目录语义会把每份 JSON 当有效样例);`fixtures:manifest` 重算 **243 摘要 / 11 拒绝码**,`smoke:contract` 有效样例 **67 接受**。**证据**:challenge-schema **8 files / 268 passed**;vm-ui **70 files / 866 passed**(M2 收口 806 ⇒ 净增 60 例);`typecheck` 18/18;`build` 13/13;`scan:public` **0 违规**(3 条豁免均为 `packages/protocol/dist/**` 既有 allowlist);`lint` / `lint:deps` exit 0。**同时清偿 M1 遗留项「深色画布光栅精灵可见性」**(选项 (a):新增第 **21** 枚 token `--sm-canvas-sprite-filter`,真机合成对比度 dark **1.84 → 3.07** / terminal **1.82 → 3.04**,`.blocklyZoom` 当前在场数 **0** ⇒ 缩放半边属**前向兼容惰性规则,已如实登记不冒充已验收**)。**WP-81 定案面完成、实现待派单** —— 定案 = **(c) 双层语义分离**(哨兵槽模式 T / 参考值模式 R),**决定性取证** = 引擎自身 canary happy-path 测试即用 `visibility:"public"` + `containsSecret:false`(`session_lifecycle.rs:848-870`)⇒ 冲突的准确表述是「**检查器只承认引擎唯一拒绝的形态,引擎唯一接受的形态恰是检查器唯一拒绝的形态**」,实质 = **规则面缺一种模式,不是可见性语义有争议**;(a)、(b) 两案已逐条点名否决((a) 隐藏区字节玩家**永远写不到** ⇒ canary 恒不触发 = **安全剧场**;(b) 的字面形态**不可实现**,且只有「放宽 pair-rules hidden 前置 + 放宽 `project.rs` fail-closed」这一读法才真让 hidden 对象进浏览器 ⇒ 正面触碰红线第 1/4 条与 I-3/I-9);**canary 值是否为判题秘密 = 分模式**(模式 T 的期望值**不是另存秘密**,而是装载期从初始内存截取(`exec.rs:288-301`)⇒ 槽在可见区时**结构性不是判题秘密**;模式 R 才是 SERVER_ONLY);**CH-05 切换 = 不切换**(观察判据未达成 + 会改一道题的裁决语义)。**实跑 3 项 / 待跑 6 项严格分开标注**(E1 现语义红灯 1 passed;E2 引擎在 public+非秘密上装载成功;E3 双侧 `canary_slot_not_visible` 断言)。**⇒ E-6 判定 = 「能力可表达 + 门禁可验」口径下:声明面达成,canary 定案达成;canary 的「三侧语义一致」须待 WP-81a 落地**(P6 跨语言互证机检已由主控裁决**纳入 WP-81a 的完成标准** —— 现状 `vm-engine/**` 对 `XS-CANARY-CORR` **零测试引用**,这正是冲突活到 M3 的原因)⇒ **实现面归属 = 待派单(遗留第 22 项 / WP-81a)**。
- **E-7**(WP-82)试用环境公网可访问 + 采集最小面 + 报告模板:**部分达成**。**已达成的三条**:①**报告模板**(`docs/phases/中期试用报告模板.md`,按计划书第十五章 13 项观察项组织,每项标注数据来源与判据,并与 D-API-107 每题观察点对齐);②**部署与暴露面**(`docs/user/试用环境部署指南.md`;运维端点暴露面定案 = D-API-104 选项②反代准入,TLS 终止 / 域名 / 最小授权角色连线;**纪律 = 未实测不给配置片段**);③**采集最小面(实现子集)完成** —— 新增 `teaching_events` 表(`migrations/008_teaching_events.sql`):`ENABLE` + `FORCE ROW LEVEL SECURITY` + 租户绑定政策(谓词**逐字同 007**)+ **两段式保留期**(仅租户枚举读放行,**零跨域 DELETE 政策行**、零 `USING (true)`)+ UPDATE/TRUNCATE 双触发器 + `kind` **三值库层 CHECK** + `UNIQUE(tenant_id, kind, source_ref)` 幂等锚;三类**服务端可派生**口径(会话创建 / `verdicts` 成绩方向(冻结 11 值之 `success`,SQL 层 + 派生层**双层过滤**)/ `action_log` 已接受 `undo` 动作计数),口径字面**单源**(`kinds.ts` 常量以**绑定参数**注入,SQL 文本内零字面);**零学习者标识**(无 `user_id`、无原始 `session_id`;`subjectDigest = SHA-256(tenantId‖0x00‖sessionId)`**只在库内**用于幂等与分组,**端口零返回位** —— DDL 面 / 端口面 / 库内 SELECT 列表 / 落库文本扫描 / 聚合输出纪律机检 / 编译期**六重结构性**断言);**「提示使用」= v1 暂不可采集**(登记 `not_collectible_v1` + 原因 + 触发条件;库层 kind 封闭集**不含** `hint_used` ⇒ 伪造计数值**结构性不可写入**;`CLIENT_REPORTED_TEACHING_KINDS = []` ⇒ **零客户端自报**);聚合 = **受控查询**(冻结**九键** `TEACHING_AGGREGATE_FIELDS` + 单一组装点 + `assertTeachingAggregateDiscipline` **八类机检**且八类逐条有红灯反例;PG 侧逐会话分组**在库内**完成、SELECT 不含摘要);**三条硬约束逐条守住**:审计 kind **零新增**(十值封闭集不变,机检语句面零 `audit_log` + 全文零审计 kind 字面)、采集面**不经 `/metrics`**(机检零 `prom-client` / 零 metrics 导入)、采集失败**不影响会话主链路**(采集器**永不抛错**,折叠为**旁路账** `failed=true` + 可选 `onFailure` 回调;审计账 fail-closed **不动**)。**证据**:session-api 整包 **757 passed | 119 skipped / 0 failed**;新增面 **86 passed | 21 skipped**;容器门控 IT(真库)**21 passed | 1 skipped**(真库 RLS 红矩阵 / append-only / kind CHECK / 两段式保留期 / 采集→聚合全链路 / 幂等重放 / 口径等价 / 零标识落库扫描**全绿**);`typecheck` / `lint` / `lint:deps`(1351 模块 / 4292 依赖零违规)/ `build`(13/13)**全 exit 0**。**未达成**:①**生产运行时接线未做**(`runtime.ts` 不在该 lane 授权面)⇒ 采集面当前**仅经单点装配函数交付**,**尚未接入生产装配**(接线量 ≈ runtime 增一行 + 一个运维调用入口;已按 D-API-120 缺陷族纪律缓解 —— 单测与 IT **都经该单点装配函数**装配,**不存在「测试接缝绕开生产装配」**)⇒ 遗留第 27 项;②**公网可访问证据仍受 O-MP-5 阻塞**(见 §六 遗留第 8 项)⇒ 该条**仍未达成**,不得因采集面完成而认为 E-7 全绿;③覆盖率未在该 lane 复跑(明文登记为未实测)。
- **E-8**(全部 WP)门禁全绿 + 新增面 TDD:**部分达成 —— 须分层表述,不得笼统称「门禁全绿」**。**① 静态与测试面(本轮主控实跑,全绿)**:`build` 13/13、`typecheck` 18/18、`lint`、`lint:deps`(**1357 模块 / 4300 依赖,零违规**,模块数较 WP-82 前的 1351/4292 上升 = `apps/admin` + `src/teaching/**` 的新增面纳入巡航)、`lint:deps:self-test`(**20 组边**期望全满足,含 WP-79 四条新增正反控)、`scan:public`(5 个公开包,**0 违规 / 3 条既有豁免**)、`fixtures:manifest --check`(**254 与清单一致**)、`smoke:contract`(**21 Schema / 67 接受 / 143 拒绝 / 254 摘要比对 / serde 15 / private-bundle 10**)、`test:rust`(13 target 全 `ok`,0 failed)、`test`(**26/26 tasks**,含 `session-api` 757 passed、`vm-ui` 866 passed、`admin` 87 passed、`challenge-compiler` 127 passed),**全 exit 0**。**② 真机面(本轮实跑,全绿)**:axe 真机 **9/9 面 `violations = 0`**(两次复跑一致;本轮并**实际修掉**一条 serious 违规,见 E-3),归档内部一致。**③ 未取得绿的面(明文登记,不计入「全绿」—— 三条均已定名,且**无一条是产品缺陷**)**:(a) `SESSION_API_IT=1 test:coverage` **门禁形态本机不可达** —— 失败在 vitest global setup 的 `deps.yaml up`,原始行 `Bind for 0.0.0.0:16379 failed: port is already allocated`(占用者 = 运行中 app 拓扑的 `session-api-app-redis-1`);**因 abort 于 setup,四维报 `ERROR … does not meet global threshold` 是 abort 后果,不是真实覆盖率**;清偿载体 = CI linux 完整拓扑。**但补充形态 `pnpm test:coverage`(无 IT)已 exit 0 且四维 `87.89 / 80.98 / 87.28 / 88.48` 全过 80 门槛** ⇒ 覆盖率要求**已有正向证据**;(b) `test:miri` **本机未跑完** —— 停滞于 `judge::predicate::tests::constant_cost_predicate_count_under_mutation`,而 CI 的 rust-miri job **本就显式 `--skip` 该用例**且只跑七子模块子集 ⇒ **本地全量入口结构性不可完成**,清偿载体 = CI;(c) `fuzz:smoke` — `error: no such command: fuzz`(**cargo-fuzz 未安装**),该冒烟由 CI rust-fuzz job 承载。**另**:`E2E_MATRIX` / CI `e2e-matrix` 仍未绿(webkit 12 格红 + `ts-gate` 间歇红)⇒ 与上述三条合为 **E-8 不能判为达成的直接原因**。**④ 新增面 TDD 逐工作包**:WP-78 / WP-79 / WP-80 / WP-81(定案面含 E1~E3 实跑)/ WP-82(采集面 **86 passed** + 容器门控 IT **21 passed**)/ WP-83 均按测试先行交付,且**均在测试文件头或决策条目中登记了「测试接缝 vs 生产装配」风险**(D-API-120 缺陷族纪律)。**⇒ E-8 判定 = 「本机可实跑的门禁全绿;受环境阻断与 CI 承载的四项未取得绿」⇒ 部分达成**,清偿载体全部指向 **CI `ubuntu` 拓扑**(遗留第 1 / 19 项)。

---

## 五、决策点结账

### 五·一、D-MP-1 ~ D-MP-7

| 编号 | 决策 | 结账 |
|---|---|---|
| D-MP-1 | 工作区窗口集合口径 | M1 已定案(主控,2026-09-16):全部类型常驻、不关闭、各单实例;落地为固定窗口集 + `bindWindows` 单点注入(D-API-111/112/113) |
| D-MP-2 | terminal 主题在嵌入协议的承载 | 已定案并落地:`data-sm-theme` additive 扩值 + 宿主 appearance 映射为 dark;`EMBED_THEMES` 三值**零改动**(D-API-110) |
| D-MP-3 | 寄存器特殊显示形态 | 已定案并落地:复用 `sm-jump-chain` 只读形态 + 点击复制 |
| D-MP-4 | payload 三区方位 | 已定案:修订设计文档与实现对齐,不改实现 |
| D-MP-5 | admin 最小面形态与去留 | **M3 定案(主控,2026-09-17)= 分支 A**:落地 `apps/admin` 只读最小面(题目登记列表 / 裁决查询 / 成绩导出),独立凭证 + 独立网络域 + 入 compose,**不入插件链路**;WP-78 的 API 不移。落实见 WP-79 |
| D-MP-6 | M10 最小面形态 | **M3 定案(主控,2026-09-17)= 参数化动作模板**,公开字段名 `authorBlocks`;`slots[].kind ∈ {address, immediate, length}`;`actions[].type` 必须 ∈ `SESSION_ACTION_TYPES`(12 公开动作)⇒ 子集闸;不做图灵完备积木平台。落实见 WP-80 |
| D-MP-7 | 试用对象与渠道 | **M3 定案(主控,2026-09-17)= 1~2 个高校社团 / 新生赛试点**;具体渠道与 O-MP-5 域名同批确认 |

### 五·二、开口项 O-MP-1 ~ O-MP-6(全部定案)

| 编号 | 定案 | 影响面 / 结账 |
|---|---|---|
| O-MP-1 | 教学事件载体 = **新增 `teaching_events` 表**(迁移 + RLS 租户政策 + 保留期) | 与安全事件账 `audit_log` 分离 ⇒ 免重开 D-API-59 论证(审计 kind 保持十值封闭集)。v1 采集 = 服务端可派生三类(题目开始 / 通过 / 回退次数);**「提示使用」v1 如实登记「暂不可采集」** |
| O-MP-2 | `e2e-matrix` 三浏览器常态范围 | M2 已按推荐口径常态执行(chromium 必跑 + firefox / webkit 矩阵 spec) |
| O-MP-3 | 试用环境默认主题 = **terminal**,保留 light 切换 | 嵌入形态默认随宿主 appearance(不变)。影响 E-3 / E-7 的试用观察口径 |
| O-MP-4 | M10 公开字段命名 = **`authorBlocks`** | `interfaces` 被 `D2-NO-HIDDEN-IN-PUBLIC` 禁用于公开包;定名前不得动 Schema |
| O-MP-5 | 试用环境域名 / 证书来源与主机 | **主控暂无法提供 ⇒ 按阻塞登记**:E-7「公网可访问」记**部分达成**;部署文档与 TLS 配置形态本次仍落地(零秘密入仓);证书走 ACME 不入仓 |
| O-MP-6 | 宿主凭证 × 租户绑定 = **显式租户白名单配置 `SESSION_API_HOST_TENANTS`** | 凭证 → 租户集合绑定;查询参数不得决定租户;白名单缺失或空 ⇒ 宿主成绩面**整体 404 同形**(fail-closed,防枚举)。**限制**:多宿主共用一个令牌时无法按宿主分权 ⇒ 该形态是「单宿主 / 单租户或少量租户」部署口径;每宿主独立凭证作为后续演进登记 |

---

## 六、遗留移交清单(未完成项按「仍开放 / 触发条件 / 归属后续」登记,不得以沉默代替结论)

> 本清单 = 中期任务分解 §二 M3「收口 · 中期验收评审」遗留移交清单的**收口态**;逐项已在正文登记,此处为汇总收口面。
> **执行视图见 `docs/phases/中期遗留清单.md`**(面向派单与跟踪:分组、优先级、下一步动作、派单建议、编号双射映射)。**两条纪律**:① 涉及「要不要做 / 归谁 / 什么条件触发」以该清单为准,涉及「为什么这样判 / 证据是什么」以本节为准;② 任一侧增删项或改状态**必须同批改另一侧**(不得只写一边)。**注**:本节项号是**稳定标识空间**,与物理顺序**不完全一致** —— 第 **9 / 10** 两项物理上排在文末(第 32 项之后),引用时**一律以项号为准,不以行序为准**。

1. **WebKit 跨源 WS 认证面缺陷(= E-3 唯一剩余阻塞)** · **仍开放** · 触发条件 = 在**独立来源 iframe** 内、向**跨源** API 发起**认证 WSS** 升级(现拓扑必现:矩阵稳定 `29 passed / 12 failed`,12 格逐条为 webkit 连接面)· 归属 = **须先定案修法方向**(① 短时一次性 ticket 走 query;② `Sec-WebSocket-Protocol` 携带 token;③ 先 fetch 换 ticket 再升级),且因涉及**冻结凭证语义**(`authenticateSessionCredential` / Cookie `Path=/sessions`)与 golden fixture,**必须走契约更新 + 安全审查** · **两个未定分项**:WebKit 该策略的具体分支(ITP / SameSite / WS 握手专属省略)、HTTP 面同凭证在 webkit 是否可用(决定方案 ③ 是否可行) · **已逐项核对 WP-78~83:无实现 WP 覆盖认证 / 传输面** ⇒ 本清单承接,不虚构承接面。
   **机制已读盘取证为 file:line 锚点(2026-09-17 M3 收口期复核,零代码改动)**:升级认证走凭证 preHandler 的 Cookie 呈递 —— `apps/session-api/src/wss/wss-plugin.ts:104-116`;通道 URL 由公开 API `baseUrl` 派生 —— `packages/vm-ui/src/client/transport.ts:145`(`WSS_CHANNEL_PATH = "/sessions/channel"`)+ `:148` 起的 `resolveWebSocketUrl`。二者合起来即「插件页 `:5174` 的独立来源 iframe → 跨源 API `:13000` 的认证 WSS 升级,握手需带 API 源 Cookie」;WebKit 第三方 Cookie 策略使握手不带凭证 ⇒ preHandler 判无凭证返回 **401**(与 run 31 起服务端日志取证一致:六次 `GET /sessions/channel` 全 401、`responseTime ≈ 0.3ms`)。
   **口径纠正(勿沿用旧归因)**:M2 期曾登记「webkit 为环境级阻断、非产品缺陷」,**该归因已被推翻** —— ubuntu 的 `e2e-matrix` job 跑起来后,webkit 在 375/768/1024/1440/1920px 的嵌入面与壳面共 10 格以同一形态失败(`connection-status` 恒 `reconnecting`),而 chromium / firefox 同格全绿。清偿载体已用尽,**须按真实 webkit 兼容缺陷处置**。
2. **`ts-gate` 间歇红(vm-ui 包)** · **仍开放** · 触发条件 = **不确定**(同一 head 复跑可绿:红 run 34 / 37,绿 run 32 / 38)· 已查明**非逐用例失败而是套件级 / 进程级**(零逐用例注解,而配置式 `github-actions` reporter 已本地实证生效)· 归属 = 等**「失败日志 → 公开注解」桥**(`76170c7`)在其下次红时给出失败文本后定名处置。**定名之前,`ts-gate` 的绿不得视为稳定绿。**
3. **`descriptor.spec.ts:88`** · **已定案并实施(2026-09-17;定案 = (b) 断言 / 就绪口径敏感,非产品缺陷)** · **原挂账口径「视口 / 断言几何敏感」被推翻**:该用例不设视口、零几何断言,失败点在 `page.evaluate` 内的**首次 `client.sendAction`** 抛 `not_connected`(「动作通道未连接」)。真因 = **就绪门槛竞态** —— 壳状态行「会话已创建并连接」在 `client.connect()` 之后**立即**写入(`apps/plugin-dev/src/main.ts:399-403`),而 WSS 升级是**异步**的,该文案**不是**连接就绪信号;探针实测「壳文案置位 → `connection-status=connected`」= chromium ≈ **11 ms** / firefox ≈ **1249 ms** ⇒ chromium 碰巧够用、firefox 不足。**取证**:修复前 firefox `--repeat-each=3` = **3 failed / 6 passed**;修复后 chromium + firefox `--repeat-each=3` = **18 passed / 0 failed**;firefox 全量(含矩阵格)= **43 passed / 11 skipped / 0 failed**;firefox 320px 格 = 2 passed。**修法**:共用夹具 `apps/plugin-dev/e2e/fixtures.ts` 的 `createSessionViaForm` 增第 5 步权威就绪门槛 `await expect(menuStatus(page,"connection-status")).toHaveText("connected")`(结构选择器 + 枚举值,**零像素依赖**);`:88` 声明行锚点刻意保留,取证理由写在该用例体内。**副作用(须登记)**:该门槛被**全部 E2E** 共用 ⇒ webkit 矩阵格的失败位置由「下游 `step-button` 恒 disabled」**前移到夹具层**(`Expected "connected" / Received "reconnecting"`);**红格数量与单一根因(遗留第 1 项的跨源 WS 401)不变,变的是失败位置与文案** ⇒ 相关历史口径按此订正。
   另一项 `browser-matrix.spec.ts:78`(320px)**已改判为真实缺陷并修复**(`03853cf`,真凶 = Blockly 挂到 `document.body` 的文本测量画布);M3 复核**未回退且有效**:`packages/vm-ui/src/payload/blockly-theme.ts:132-133 / 138-146 / 155-156`(文档级离屏规则,`:155-156` 在分支**之前**无条件注入 = 该修复的关键点)+ 调用点 `sm-payload-tab.ts:336` + 反向锁定测试 `test/payload/measure-canvas-doc-style.test.ts:98`;firefox 320px 实测 2 passed。**不再挂账**。
4. **深色画布光栅精灵可见性**(M1 遗留) · 归属 = **WP-80**(M3 中唯一触及 Blockly 画布与 `src/payload/blockly-theme.ts` 主题接缝者)· 先决条件 = 须先定 token 名与值,或把主题锚镜像进 shadow 树。· **✅ 已闭环(WP-80 落地,2026-09-17)**:修法选择 = **新增第 21 枚主题 token `--sm-canvas-sprite-filter`**(第二选项「把主题锚镜像进 shadow 树」未采用 —— token 路线改动面更小且与既有 `--sm-*` 消费纪律一致);**light `none` / dark·terminal `brightness(1.6)`**;**真机实测合成对比度 dark 1.84 → 3.07 / terminal 1.82 → 3.04**;`theme-terminal.test.ts` 的精确键集断言已由 20 同步为 **21**。**如实登记的边界**:`.blocklyZoom`(缩放光栅精灵)当前**在场数 0** ⇒ 该半边属**前向兼容惰性规则**,**不冒充已验收**。决策 = **D-API-130**(并取代 D-API-110「定案 1:零新增 token 硬约束」的绝对表述)。执行视图见 `中期遗留清单.md` §七。
5. **M2 末轮登记的既有缺陷** · 归属 = 本条款 · 明细:① 指令视图**每条指令行实测行高 113px**(修法 = 收窄模板空白,但会动表格视觉);② `chain.copyTitle` tooltip;③ `DEBUG_MODE_DEFAULT` 跨包物理共享(现为重复定义)。**已核对无实现 WP 覆盖** ⇒ 本轮补派并处置。· **✅ 三项全部闭环(2026-09-17)**:① **指令视图行高 113.38px → 20.8px**(修法 = 收窄模板空白,**零 CSS 改动**;一屏行数 **1.74 → 9.47**;锚点行列表内可见比例 **0.852 → 1.0**);② **`chain.copyTitle` tooltip 已修**(`<sm-jump-chain>` 新增 `copyMode` property;组件仍只派发 `viewport-jump`,**事件契约零改动**;`chain.copyTitle` 走双 catalog i18n parity 机检);③ **`DEBUG_MODE_DEFAULT` 跨包物理共享 ⇒ 定案 = 保留重复 + 跨包恒等机检**(物理共享面在本席位不可达:依赖边界规则 + 文件锁,且 JSON Schema 对该字段只有文字描述、无机器可读 `default`;**机检 9 例含 4 组反例自检**,反例① 即真实历史缺陷形态「按 `=== true` 判真」)。决策 = **D-API-142/143/144**。**衍生遗留**:① 的 E2E 断言收紧见第 20 项 / `中期遗留清单.md` P4-1。执行视图见 `中期遗留清单.md` §七。
6. **调试克隆「对齐源」一般缺陷** · **仍开放** · 现象 = 调试实例 = attach 时按 `origin.revision` **重放权威动作日志**得到的克隆,而动作日志**只在 `submit` 时落库**(`session-manager.ts#persistActionLogDelta`)⇒ **未提交会话**的克隆恒为**种子初始态** · 触发条件 = **任何非种子题、或玩家已改动内存的未提交会话**(现演示题已用「种子预置返回地址」修掉**症状**,故当前不红;**一般解未修**)· 归属 = 实现 WP 待定(候选:对齐源改用**在途会话的权威动作日志** `SubmitReference.actionLog`)。· **✅ 已定案并实施(2026-09-17,遗留 lane 交付)**:候选**被采纳并落地** —— 对齐源 = **在途权威动作日志**(`SubmitReference.actionLog`,经 `getSessionSummary.acceptedActionLog` 读取)⊕ **已落库基线前缀**;`origin.revision` **收紧为精确对齐点**;该 revision **不可得时在 spawn 前冻结拒绝**(`invalid_input_format` / 稳定文案 `"revision is not available"`),**不静默退回种子态**。写入面 = `apps/session-api/src/sessions/debug-alignment.ts`(新)+ `session-manager.ts` + `src/debug/debug-channel-{constants,orchestrator}.ts`。**决策 = D-API-145**。**衍生遗留**:拒绝文案的可解释性(第 25 项)、`action_log` 保留窗 / 裁剪契约面(第 26 项),均见 `中期遗留清单.md` P4-3 / P3-3。
7. **M2 WP-77 三项文档收尾中「依赖 M3 的后半部分」** · 各自在 M3 侧均有实体承接 WP:① 宿主指南「成绩只读同步接口」章节 ⇒ **WP-78**;② 出题人指南 M10 声明面 ⇒ **WP-80**;③ `README.md` / `CLAUDE.md` 的 `apps/admin/` 声明 ⇒ **WP-79**。· **✅ 三项均已回填**:① 宿主指南「成绩只读同步接口」章节已成文(WP-78);② 出题人指南 **M10 声明面已回填**,`authorBlocks` 落在 **6 处**(`:9` / `:197` / `:199` / `:206` / `:230` / `:288`,含「两层校验、客户端更弱」的客户端能力边界说明);③ **`apps/admin` 已在磁盘落地** ⇒ 原「悬空声明」**被事实追平**,无需再改为「预留位」;用户面两处状态行已由主控重写为「**只读最小面已落地(D-MP-5 分支 A),完整管理后台仍不在范围**」(`1a5c787`),并已全仓扫描确认无残留旧表述。另:`docs/README.md` 索引回填见第 12 项(已解决)。执行视图见 `中期遗留清单.md` §七。
8. **试用环境公网可访问证据(O-MP-5)** · **仍开放(阻塞)** · 触发条件 = **主控提供域名与主机**(证书走 ACME 自动签发,不入仓)· 归属 = 后续运维面。**本次已落地**:`docs/user/试用环境部署指南.md`(部署拓扑 / TLS 待执行形态 + 补取证清单 6 条 / 最小授权角色连线 / `/metrics` 等运维端点按 D-API-104 择**②反代准入** / 环境变量与秘密注入 / 部署与回滚步骤 / 公网暴露面盘点(端口 P1~P8 + 端点 E1~E9 + 凭证 C1~C10)/ 试用前安全自查清单)、`docs/phases/中期试用报告模板.md`(13 项观察项逐项「数据来源 / 判据 / v1 可得性」+ D-API-107 八题 × 3 项对齐表 + 启动判据 + 隐私合规自查)。**未落地**:反代 / TLS 实配置(不给未实测配置片段 = 反伪造纪律)、真实可访问证据。· 影响 = E-7 该项记**部分达成**(报告模板与部署形态不受影响)。
11. **采集面覆盖缺口:24 个观察点中 7 项在 v1 结构性不可采集**(WP-82 文档面暴露的新登记项) · **仍开放** · 明细与共同原因:① 提示使用等级(服务端**无记录入口**,`hintLadder` 只存在于公开描述包);② 动作级 16 错误码分类与重复错误率、③ 协议错误分类 —— 共同原因 = **被拒动作不入 `action_log`**(`action_log` 只记已执行动作);④ 断线恢复率(无重连计数族,`sync_projection` 不落库);⑤ 探针行为、⑥ `inaccessible_address` 分布(④⑤⑥ 同属「无对应计数族 / 无落库面」);⑦ 溢出越界长度(CH-05)待取证(需 `action_log.write_bytes` 参数 × 题目布局登记面联表,**布局元数据可得性未核实** ⇒ 标「待补取证」而非填数) · 归类 = 24 格中 **12 可采集 / 2 部分可采集 / 7 v1 暂不可采集 / 2 人工抽样 / 1 待补取证** · 归属 = **采集面扩展工作包(待主控派单)**;触发条件 = 首轮试用数据回看后确认这些观察项对教学结论不可替代。**v1 处置**:不靠客户端自报、不伪造计数(纪律已写入报告模板 §六 填报纪律)。
12. **`docs/README.md` 索引未回填新增文档行**(试用部署指南 / 试用报告模板 / 本评审) · **本轮收口内解决**(归收口,非遗留) · 见 §二 交付清单与收口提交。
13. **axe 归档存在「同日复跑覆写 + 逐次非确定性噪声」的双重隐患**(M3 收口期发现,登记为证据卫生项) · **仍开放** · 现象:归档目录按**运行日期**生成(WP-74 落地,`e2e/axe-contrast.spec.ts` 用 `RUN_DATE`),但**同一日内二次测量会覆写同一目录** ⇒ M2 收口(2026-09-17)与 M3 收口(同日)的测量落在同一 `reports/axe/2026-09-17/`;且归档 JSON 内含**每次运行都变的字段**(会话 `esid` + Lit 模板标记 `?lit$<hash>$`)⇒ 即使测量结果完全相同也会产生伪 diff。 · **2026-09-17 主控读盘核实(结论:本次未构成证据损坏)**:`summary.md` 与 HEAD 提交版**字节相同**(零 diff),8 个 JSON 的差异**逐条仅为 `esid` 与 `lit$` 标记** ⇒ 实质结论(九面 `violations = 0`)未变。 · 触发条件 = 任一次 axe 复跑(每次都会产生噪声 diff)· 归属 = **证据面改造(待主控派单)**:推荐修法 = 归档前**规范化**(剥离 `esid` / Lit 标记)使归档**确定性**,并给同日复跑加**序号目录或显式覆盖开关**(默认不覆盖)。 · 同类先例:M2 期已因「归档被硬编码为 WP-55 日期、每次跑 axe 都覆写该历史目录」而把 `REPORT_DIR` 改为按日期生成(commit 记录于 `docs/phases/中期任务分解.md` WP-74 末段)——**本条是同一族缺陷的剩余面**:日期化消除了跨日覆写,**未**消除同日覆写与非确定性噪声。 · **补充核实(2026-09-17,收口轮)**:`summary.md` 在 `git status` 中**显示为 modified 但 `git diff` 为空** —— 该「修改」纯属**行尾符归一化(CHLF/CRLF)伪影**,**内容与 HEAD 提交版逐字相同**。这一现象本身也是噪声源之一:归档目录里**混有内容零差异却出现在 `git status` 的文件**,会误导「归档被改动过」的判断。⇒ 修法建议追加第 ④ 条:归档写入时**统一行尾符**并纳入上面的「规范化」步骤。 · **收口轮处置**:WP-83 落地(23:14)后主 chunk 拆分改变了 payload 窗口的挂载时机(同步 → 惰性宿主),而现存 `2026-09-17/` 归档由 23:08 的测量产生 ⇒ **该归档只代表拆分前形态**。**主控裁示**:待 `packages/vm-ui` 全部在途改动落地后**一次性复跑 axe 并覆写 `2026-09-17/`**(使归档与 M3 终稿树对应),同时在总结中**显式标注归档对应的树状态**;本条「归档缺变更身份锚」的根治仍归待派单改造项。 · **2026-09-17 收口轮再增四项(实测暴露,均由 axe 复跑 lane 以只读方式取证)**:⑤ **归档可自相矛盾** —— `summary.md` **不记录未扫描面**,只列已扫描的 `faceRecords` ⇒ 当某面因同用例内**前置断言先失败**而未被扫描时,目录里会**留着该面的旧文件而汇总里没有对应行**(本次 shell light 先红 ⇒ `plugin-dev-shell-terminal.json` 仍是 23:08 的旧文件,而 summary 只有 8 行)⇒ **归档目录内部不一致,且单看 summary 无法察觉**。修法:`summary.md` 增「未扫描面清单」段并把该段纳入归档校验。⑥ **面矩阵口径与实际不符** —— spec 头注 `:41` 与 `apps/plugin-dev/README.md:260-262` 均称面矩阵 = **10 份 JSON**(6+2+2),而按代码**最多只写 9 份**(`degraded terminal` **有意从不扫描**,见 M2 登记),本次因失败只写 8 份 ⇒ 文档/注释口径有误。⑦ **声明与产物不一致的「死字段」** —— per-face JSON 的 `AxeFaceResult` **声明了 `axeVersion`**,但落盘 JSON **没有该字段**(axe-core 4.13.0 的 `results.version` 为 `undefined`,`JSON.stringify` 直接丢弃)⇒ 类型声明比产物「多一个字段」,属**声明面未被机检覆盖**的实例(与遗留第 24 项「镜像表静态互证」同族)。⑧ **README 复跑命令与门禁范围不一致** —— `README.md:242` 的 axe 复跑命令**捆绑**了 `e2e/reduced-motion.spec.ts`,而门禁 spec 只是 `axe-contrast.spec.ts` ⇒ 按 README 复跑会**多跑一个 spec**,复跑口径与门禁口径不同源。
14. **terminal 三面的 axe color-contrast 自动判定覆盖度倒挂(「薄绿」)** · **仍开放(证据强度项)** · 现象(M3 收口期**逐面读归档 JSON 实测**,归档 `e2e/reports/axe/2026-09-17/`,字段 = `colorContrastCheckedNodes` 与 `incomplete[].count`):

| 扫描面 | violations | 自动判定通过 | 无法判定(axe incomplete) |
|---|---|---|---|
| plugin-iframe light / dark × registers / stack(4 面) | 0 | **132** | 7 |
| plugin-dev-shell light | 0 | **146** | 7 |
| plugin-degraded light | 0 | 2 | 0 |
| **plugin-iframe terminal × registers / stack(2 面)** | 0 | **7** | **132** |
| **plugin-dev-shell terminal** | 0 | **19** | **134** |

    · **判据**:九面 `violations = 0` 这一陈述本身为真,但**不能据此认为 terminal 预设的对比度已被验证** —— 该预设下 axe 只能自动判定 7 / 19 个节点,其余 132 / 134 个节点落入 `incomplete`(引擎无法自动判定,属**人工复核项**)。light / dark 面是 132 / 7 的正向覆盖,terminal 面是 7 / 132 的倒挂 ⇒ **同一句「零 violations」在两类面上的证据强度不可等同**。M2 的登记(「9 个可达面 violations = 0」)未披露该分项,本评审补齐。
    · **性质**:非 M3 引入(归档 `summary.md` 与 M2 提交版**字节相同**,该分项 M2 期即存在,只是未被写入文档);亦非产品缺陷判定 —— 是**证据强度**. 上游 `color-contrast` 规则在存在**半透明覆盖层**时无法计算合成后对比度(terminal 预设的效果面:扫描线 `opacity ≤ 0.06` + `prefers-reduced-motion` 门控),此为 axe 的已知判定边界,不是「已通过」。
    · **触发条件** = 任何以「axe 零 violations」作为 terminal 预设可读性结论的场合 · **归属** = **证据强化项(待主控派单)**,候选修法:① 对 terminal 面做**关闭效果层后的对照组扫描**(证明倒挂由覆盖层引起而非配色本身);② 引入**人工 / 脚本化对比度核算**(按 token 前景 × 合成背景实算 WCAG 比值)补齐 `incomplete` 面;③ 在归档汇总里**强制分列**「自动判定通过 / 无法判定」两栏,使「零 violations」不再掩盖覆盖度。 · **不得以沉默结案**。
15. **chromium 全量 E2E 存在 1 个既有偶发红,会污染「全绿」结论** · **仍开放(既有偶发,非 M3 引入)** · 现象:chromium 全量门禁连跑三次均 **36 passed / 1 failed**,失败**恒在 `apps/plugin-dev/e2e/embed-protocol.spec.ts`**(host-mock 未达 ready 超时);该 spec **单独整文件复跑 = 9 passed**。 · **已证与本轮改动无关**:该 spec **不使用** `createSessionViaForm`(遗留第 3 项的修正在该夹具内),且**把该修正临时中和后基线对照复跑同为 36/1 且失败点相同**(`:228`)⇒ 判为**既有偶发**(与 M2 期登记的「非缺陷偶发红」同类)。 · 触发条件 = 长会话 / 高负载下跑该 spec · 归属 = 既有测试稳定性项;**验收纪律**:M3 的「chromium 全量绿」结论必须写明该格的处置(单独复跑取证或标注为已知偶发),**不得直接宣称全绿**。
16. **开发壳连接文案仍「乐观」**(遗留第 3 项取证时顺带发现) · **仍开放(候选增量)** · 现象:`apps/plugin-dev/src/main.ts:403` 的「会话已创建并连接」在**真正 `connected` 之前**写出(即在 `client.connect()` 之后立即写),该文案**不是**就绪信号,是遗留第 3 项红灯的根源面。 · 现状处置:E2E **已不再依赖该文案**做就绪判定(改取工作区菜单权威连接态),故**不再致红**;但文案本身仍对使用者产生误导。 · 修法 = 给 `SessionDemoClientLike` 加状态订阅面,使文案随权威状态更新。 · 触发条件 = 任何以该文案判断连接就绪的场合(含人工联调与后续新用例)· 归属 = 开发壳体验项(待派单);**影响面仅限开发壳,不影响交付产物**(`apps/plugin-dev` 是开发 / E2E 壳,非插件交付物)。
17. **共享工作区内的 PowerShell 文本往返导致静默文件损坏(事故,已自恢复;纪律条目)** · **已闭环但须留档** · **事实**:M3 并行期内,一个执行 agent 用 `Get-Content -Raw | Set-Content -NoNewline`(本机 legacy 代码页回写)改写 `packages/vm-ui/test/views/chain/sm-jump-chain.test.ts`,产生**非法 UTF-8**(≈198 非法字节位 / ≈99 受损字符:汉字末字节被 `?` 顶替并**吞掉其后 ASCII 字节**,造成「注释与下一行代码并到同一物理行、后半句被 `//` 吞掉」的**静默丢断言**)。 · **取证过程**:另一 agent 在 vitest 侧先观察到**解析期 PARSE_ERROR**(`Unterminated string @125`);主控两次读取该文件分别得到 `U+FFFD = 2` 与「无 U+FFFD」⇒ 说明**损坏真实存在、且在两次读取之间被作者修复**。**此处订正主控早前的一个判断**:曾据「第二次扫描无 U+FFFD」判为「并发写入的瞬时态、无文件损坏」,该判断**不成立** —— 同一时点确有损坏,只是已被修复;正确表述 = **「曾有真实损坏,已由作者自恢复」**。 · **恢复方式(穷尽恢复源后)**:git 工作树无 vm-ui 副本、vitest `results.json` 仅结果、`.claude` file-history 与 `.dsh` sessions/storages/undo-snapshots 均无正文、无 coverage HTML ⇒ 采用**宽松解码取证 + 逐点重建**,再以**测试全绿(17 例)**与断言真值(如 `值 0x1010`)双向校验;顺带回滚了同批被误改的 `byteLength: 0x2000`。 · **残留风险(必须留档)**:受损点的**中文注释 / 用例名文字为按上下文重建**(语义等价、**措辞可能与原文有差异**),代码与断言面经测试校验一致。该 caveat 已写入对应提交信息(`719d93a`)。 · **纪律启示(建议上升为规范)**:**共享工作区内禁止用 PowerShell 文本往返(`Get-Content -Raw` + `Set-Content`)改写工作文件** —— 该组合会按 legacy 代码页回写 UTF-8 内容且**不报错**;批量文本改写应经 Node / 编辑器 API 或显式 `-Encoding utf8` 完成。另:跨 agent 观察到「文件短暂非法」时,应首先怀疑**并发写入或工具回写**,而非直接判定为他人缺陷。
18. **`pnpm lint:deps` 在本机需显式放大 Node 堆** · **环境事实(非 CI 缺口)** · 现象:主控实跑 `pnpm lint:deps`(**未设** `NODE_OPTIONS`)= **exit 134**(V8 OOM / SIGABRT,栈为 V8 内部帧);设 `NODE_OPTIONS=--max-old-space-size=8192` 后正常。 · **与 CI 的关系**:`.github/workflows/ci.yml` 的静态门禁 job 在 **Node 22、未设 `NODE_OPTIONS`** 的前提下**当前为绿**;本机为 **Node v24.20.0**。⇒ **本项判为「本机环境约束」而非 CI 缺口**,**不新增 CI 配置**;成因未定论(候选:Node 24 默认堆策略差异,或本机多 agent 并发测试进程造成的真实内存压力),**不宣称成因**。 · 影响面 = 本机复跑门禁需带该 flag;归属 = 收口命令清单注明即可(已记入下方 §三)。
19. **WP-78 的 compose 全链路用例在本机不可取证(环境阻断)** · **仍开放 · 归属 = CI(`test:compose` / ubuntu 拓扑)** · 事实:WP-78 第 7 子项代码面完成(真实 PG + 真实 RLS + 真实 verifier 裁决,逐字段比对 + 跨域载荷机检零命中 + 401/404 逐字节),但**本机未执行**(容器门控 `SESSION_API_COMPOSE != 1` ⇒ 24 skipped)。 · **主控独立取证(第一手)**:① 运行中的拓扑 `session-api-app-*` 六容器健康,但其镜像是 **`stackmaster/session-api:dev`(M3 前构建)**,**不含** `/host/scores`;② `docker pull node:22-bookworm-slim` / 任何镜像拉取均由 docker daemon 经 **`127.0.0.1:7897`** 代理发出并被 **actively refused**(`connectex: No connection could be made because the target machine actively refused it`)⇒ **镜像无法重建**(`apps/session-api/Dockerfile` 依赖 `node:22-bookworm-slim` / `rust:1-bookworm`,本机均未缓存)。 · **推论(须注意的连带风险)**:WP-78 已把 `SESSION_API_HOST_TENANTS` 写入 `compose/app.yaml`,而**在跑的旧镜像不认识该保留键** ⇒ 一旦触发 `compose:app:up`(含 E2E 的 `global-setup` 自动拉起),旧镜像将按**未知保留键**规则**拒绝启动**;故并行期各 lane 的 E2E 均以 `E2E_SKIP_COMPOSE=1` 对接**已在运行**的旧拓扑。 · 判据:**E-5 的 compose 证据在本机「不可达」而非「未做」**,清偿载体 = CI ubuntu 拓扑;在取得该证据前,**不得声称 compose 全链路已通过**。
20. **`breakpoint-linkage.spec.ts:224-230` 仍是薄断言(承接自遗留第 5 项)** · **仍开放(承接项)** · 事实:M2 登记的「锚点行可见比例仅 0.14,`toBeInViewport` 真绿但很薄」其**产品侧成因已由 M3 遗留第 5 项 ① 修复**(指令行行高 113.38px → 20.8px,锚点行列表内可见比例 → 1.0),但 E2E 侧断言**仍是** `await expect(...).toBeInViewport();`(**无 ratio、无行高上界**),故该处**仍可能以薄绿通过**。 · 修法(已由遗留第 5 项作者给出,不在其写入面):改判 `toBeInViewport({ ratio: 0.9 })` + 断言锚点行 `boundingBox().height <= 28`。 · 触发条件 = 指令视图行高再次回归时该用例**不会红** · 归属 = **E2E 断言收紧项(待派单)** · 注意:验证该改动需跑 E2E(本机受第 19 项环境阻断制约)。
21. **`assemble.rs:1151` 把私有标识符 `objectId` 送进浏览器,而该面未登记在公开字段分类中** · **仍开放 · 高优先 · 归属 = 待派单(建议 WP-84)** · 事实:引擎在 `visibleObjects` 映射内以 `label: object.object_id.clone()`(`:1151`;`:1058` 注释自述「= 私有对象 `visibility = public` 的 objectId」)把**私有包内标识符**下发到浏览器投影 / 高亮标签。 · **定性(校准,勿夸大)**:该标识符与 v1.8 已被裁决为「**公开 ISA 引用**」的 `interfaceId` **同类**,可能本就属公开引用 ⇒ **不宣称已确认泄漏**。**真正确凿的问题是流程面**:分类清单自身硬门槛要求「**任何新增公开字段必须先在本文补论证,再进契约**」,而该面**属未登记公开面**;且**8 题全中**、**M3 前即存在**(非本里程碑引入)。 · 触发条件 = 该字段被用于任何**存在性 / 结构**推断时,即从「未登记」升级为「实质泄漏」· 处置方向(供 WP-84 三选一):`label` 改取**公开声明**或 `publicLabel`(推荐)/ 补分类论证并登记为公开引用 / 证明其不可推导性后显式豁免。 · **纪律**:本条**不得以沉默结案** —— 它是「分类清单声明了硬门槛、实现里却有一个面没走该门槛」的**门槛失效实例**,与 D-API-131~133 的「规则面缺模式」属同一类治理缺口。
22. **`阶段六扩展评估报告.md:69` 与 D-API-109 的「结构性不相容」措辞已被证伪,须改述** · **仍开放 · 归属 = 待派单(与 WP-81a 同批或收口轮后)** · 事实:该两处把 `XS-CANARY-CORR` 与引擎 canary 可见性要求描述为**结构性不相容**;WP-81 定案以实跑证据(`session_lifecycle.rs:848-870` 引擎 happy-path 即用 `visibility:"public"` + `containsSecret:false`)证明冲突实质是「**规则面缺一种模式**」,而非语义矛盾。 · 不改述的后果:台账内**并存两个互相否证的结论**,后来者会按已证伪的那个理解 canary 面。 · 修法:两处改述为「规则面缺模式(定案 = 双层语义分离)+ 指向 D-API-131 ~ 133 / WP-81a 实施清单」。
23. **`apps/admin` 的 PG 取列口径与 WP-78 成绩面适配器的「值面」一次性对照** · **仍开放 · 归属 = 待派单(低成本,可在 CI 跑 admin IT 时顺带)** · 事实:admin 导出的**形状与字段名**已被契约机检锁死(`HostScoresResponseSchema.parse()` 单出口 + 顶层恰两键 + 逐条字段集由 `HostScoreRecordSchema.shape` 派生),但**「哪一列喂 `verdict` / 哪一列喂 `decidedAt`」属自写 SQL 的语义选择**,与 WP-78 适配器**无机检互证**。 · 触发条件 = 两实现取列不一致时,**契约校验仍会全绿**(形状合法但语义错位) ⇒ 这是**契约机检覆盖不到的一类错**。 · 修法建议:加一条**同数据同结果**的等价性用例(同一 PG 夹具分别经 WP-78 路由与 admin 导出,逐条值比对),或把两处取列收敛为单一 SQL 片段(注意**不得**因收敛而引入 app→app 依赖)。
24. **`protocol` 动作参数名表 ↔ `challenge-schema` `AUTHOR_BLOCK_ACTION_ARGS` 的跨包漂移无机检** · **仍开放 · 归属 = 待派单** · 事实:两者**有意重复**(叶子包不依赖 protocol,依赖纪律 5.5 要求如此),现有断言**只在 challenge-schema 包内**(键集 ≡ 12 动作、并集 ≡ 参数名全集、必填 ⊆ 允许),**没有**任何检查把 challenge-schema 的这张表与 protocol 的**真实**参数名表对比 ⇒ 任一侧改名 / 增删参数时,另一侧**不会红**(服务端入站重校验只能兜住**运行期**,兜不住**静态漂移**)。 · **同族**:WP-78 转来的「`docs/contracts/版本策略.md` 契约类目表 ↔ `version.ts` 常量漂移无机检」为**同一族缺陷**(文档 / 镜像表与权威源之间无静态互证)⇒ 建议**合并为一个「镜像表静态互证机检」条目**立项。
25. **调试克隆「精确对齐点」拒绝时的用户可解释文案缺失** · **仍开放 · 归属 = 待派单(UX 面)** · 事实:遗留第 6 项把恢复会话的缺口语义由「静默落后对齐」收紧为**确定性拒绝**并在 spawn 前判定(**正确**,已批准),但对外只给通用 `invalid_input_format` / `revision is not available` + 受控日志 ⇒ 使用者(出题人 / 调试者)看到的是**与「revision 越界」完全同形**的错误,**无法区分「你要的位置不存在」与「你要的位置存在但无法重建」**。 · 张力(须登记而非默默承受):**同形是防枚举纪律的正确选择**,但本场景**非敌手面**(调试通道,操作者 = 出题人本人)⇒ 可考虑在**受控日志 / 出题人可见面**补充可解释原因,而**不改**浏览器面文案。 · 触发条件 = 出题人反馈「调试暂停对不上但不知为何」时。
26. **已落库动作日志缺「保留窗 / 裁剪」契约面定义** · **仍开放 · 归属 = 待派单(契约面)** · 事实:`action_log` 的 `listBySession` **无裁剪 / 保留窗参数**;当前无裁剪 ⇒ 对齐覆盖判定恒为「连续可达」。 · **前向兼容已具备**:一旦引入裁剪,遗留第 6 项的覆盖判定**无需改代码**即会正确拒绝(缺口自动落入「不可得」分支) —— 这是该实现的一个**正向性质**,已登记。 · 仍需立项的原因:裁剪策略本身(保留多少 / 按什么维度裁剪 / 与契约版本和回放义务的关系)**属契约面决定**,不能留给实现期默认值。
27. **教学事件采集面尚未接入生产运行时装配** · **仍开放 · 高优先 · 归属 = 待派单(小项)** · 事实:WP-82 采集面已完整交付(`teaching_events` 迁移 008 + `src/teaching/**` 12 文件 + 单测 6 + 容器门控 IT 1,**真库 IT 21 passed**),但 `src/runtime/runtime.ts` **不在该 lane 授权面** ⇒ 当前只有 `buildTeachingCollection(pool)` **单点装配**可用,**生产装配未持有一行**。 · 接线量 = runtime 增一行持有装配结果 + 一个**运维调用入口**(T0 无 cron,与 `TerminalSessionCleaner` / `purgeExpired` 同形)。 · **缓解(该 lane 已按 D-API-120 缺陷族纪律处理)**:单测与 IT **都经同一单点装配函数**装配,不存在「测试接缝绕开生产装配」⇒ 接线后不会重演 WP-70 / WP-79 的那类漏网;但仍**未达成「采集最小面启动」的运行期证据**。 · 触发条件 = 试用开始前必须接线,否则**采集面虽绿但一条数据也不会产生**。
28. **`teaching_events` 的 GRANT 落在迁移内,与部署面 `session-api-db-init.sql` 存在两处授权面** · **仍开放 · 归属 = 待派单(可改 `compose/**` 的 lane)** · 事实:008 内的 `GRANT` 是**迁移期唯一可行落点**(init 脚本**首启执行、早于迁移**,其 `ON ALL SEQUENCES` **覆盖不到新建序列** ⇒ 授权只能随表同批落迁移),理由已写入 008 文件头与 D-API-149。 · 风险:同一张表的授权面**长期分处两地**,后续改权限时**可能只改一处** ⇒ 建议由可改 `compose/**` 的 lane 在 `session-api-db-init.sql` 补**同批字面**并双向对齐(属 D-API-93「部署面义务」的延伸)。 · 触发条件 = 下一次调整 `teaching_events` 权限时。
29. **教学事件保留期天数以「端口参数」承载,尚未升为配置键** · **仍开放(低优先)** · 事实:`TEACHING_EVENT_RETENTION_DAYS = 180` 现由调用方传入,该 lane 为避免与**共享 `src/config.ts` 的在途 lane 冲突**而**刻意不新增配置键**(节制正确)。 · 遗留:与既有 `SESSION_API_SNAPSHOT_RETENTION_DAYS` **不同名族** ⇒ 运维面存在两套保留期配置形态。 · 修法:接线运行时(遗留第 27 项)**同批**升为配置键并纳入三道闸(required / known / 天花板)。 · 触发条件 = 遗留第 27 项接线时一并处理,**避免二次改动**。
30. **WP-83 惰性宿主(`<sm-payload-tab-host>`)在 payload 窗口 DOM 链上多插一层,其高度 / 宽度链未复核 —— axe 转绿**不代表**几何无回归** · **仍开放 · 归属 = 待派单(收口轮后)** · 由来:M3 收口轮真机 axe 抓出的 `scrollable-region-focusable`(serious)已修(`dd68764`),但归因取证显示**把 needle 推过 axe「真实溢出 ≥13px」判定线的触发条件正是该惰性宿主** —— 硬证据:绿跑制品(23:05:18)**无** `sm-payload-tab-host`,红跑制品(23:38:29)**含**之;窗口内 vm-ui 唯一结构变更 = `9557396`;**节点集恒定**(`colorContrastCheckedNodes` 三次均为 146)⇒ **变的是几何**。 · **为何必须单列**:①该宿主**自身无任何样式**,却插在 `.tab-content` 与 `<sm-payload-tab>` 之间 ⇒ 若中间层未正确传递高度约束,滚动 / 伸缩容器会**静默改变实际内容盒**;②本次仅是**一个**滚动容器被 axe 的 13px 阈值判红,**不代表其它几何面未变**;③**几何回归不触发任何现有门禁**(E2E 几何断言只覆盖被显式断言的少数元素)⇒ 属「门禁盲区内的静默漂移」。 · **复核方法(建议)**:在该宿主存在与不存在两种拓扑下,逐项记录 payload 窗口内容盒的 `clientHeight/scrollHeight/offsetHeight` 与关键子元素 `boundingBox`(= 本次 axe 取证缺的那一步直接测量);或对 `.tab-content → host → sm-payload-tab` 链补一条**高度传递**的结构断言。 · 触发条件 = 任何再动该链或 payload 面板几何的改动。
31. **WP-80 的「注入 E2E」未交付(`authorBlocks` 声明积木的端到端呈现无 E2E 覆盖)** · **仍开放 · 低风险 · 归属 = 待派单** · 事实:WP-80 完成标准含「E2E = 声明了模板的题目在插件壳中呈现对应积木并可用」,但**主控本轮实测核实** `apps/plugin-dev/e2e/*.ts` 对 `authorBlocks` **零命中** ⇒ **该 E2E 不存在**。 · 风险为何「低」:声明面**形状**已由 `challenge-schema` 268 例 + `strictness.test.ts` 锁定,编译面由 vm-ui 866 例覆盖,**`smoke:contract` 67 有效样例接受**亦已跨语言验证 ⇒ 缺的是**「经插件壳真机注入并呈现」这一段**;故登记为**覆盖缺口而非功能缺口**。 · 修法:`plugins-dev` E2E 增一例,用声明了 `authorBlocks` 的题目在壳内断言对应积木呈现且可用(可复用既有 E2E 题目登记流程)。 · 触发条件 = 任何改动 `blocks.ts` 注入路径或 `sm-payload-tab` 积木渲染的改动落地时。
32. **`docs/user/出题人文档/index.html` 的页数声明已订正,但同名文档的跨页计数与层内页数需复查** · **已订正主项 · 复查项仍开放(低优先)** · 事实:该页原写「共 **11** 页」,而其**自身**四层页数为 `1(第 0 层)+ 7(第 1 层)+ 5(第 2 层)+ 1(第 3 层)= 14`,磁盘实际 `docs/user/出题人文档/*.html` 亦为 **14 个内容页 + index.html** ⇒ 原计数为**陈旧值**,**已由主控订正为「共 14 页」**(E-4「无悬空声明」要求下必须订正,不订正即留悬空)。 · **复查项**:①第 1 层自述「7 页 32 个概念」而正文入口写「通读 1A–1E」(`:44`)⇒ 与 7 页(A–G)口径需确认是否有意(仅推荐前 5 页)还是陈旧;②同类「共 N 页 / N 个概念」计数声明是否还有其它页存在(本次只核了 index.html)。 · 触发条件 = 出题人文档下次内容变更时。
9. **`e2e-matrix` job 在 CI 实跑绿至少一次并归档(E-3 子项)** · **仍开放** · 现状 = job 已落地并通过静态校验、已**实跑**(run 31 起),当前唯一红点 = 遗留第 1 项(webkit 认证面)· 归属 = 随遗留第 1 项清偿后进行归档。
10. **主 chunk 体积拆分义务** · 原登记于 `docs/中期任务分解.md` §九风险表「主题改造推高 chunk 体积」行,**归属待派单**(M3 遗留清单外唯一无承接项)⇒ **主控 2026-09-17 派单 WP-83** · **✅ 已达成,义务 CLOSED**:主 chunk **1,379.02 → 428.65 kB(−68.9%)**、gzip **336.36 → 107.97 kB**;首屏静态图 1,396.71 → 452.51 kB;惰性 chunk 945.82 kB;Blockly 占拆分前主 chunk 的 **65.5% raw / 64.1% gzip**。**两处改动必需**(积木注册表 + 入口桶),**只改一处 = 假绿**;`packages/web-component` 的 `codeSplitting: false` 属**结构性上限**;**零新增依赖(含 devDependency)**。**衍生遗留**:惰性宿主的几何链复核见第 30 项 / `中期遗留清单.md` P0-3。执行视图见 `中期遗留清单.md` §七。

---

## 五·补、M3 执行期裁决(主控 2026-09-17;各 lane 转来事项逐条结账)

> 纪律:凡 lane 明文「留主控裁决」的,一律**在有据的前提下给出裁决 + 理由 + 是否附条件**;不裁决即等于悬空,不允许。

**WP-79(apps/admin)转来 8 项**
- **(a) 管理面读取不进 `audit_log`,需追认 —— 追认,不回改。** 理由:`audit_log` 的**十值封闭集**是**安全事件账**(D-API-59),而管理面只读查询属**数据事件**;把读操作塞进安全账会稀释其语义,且须改契约面(十值集增项 + CHECK + 消费方穷举)。**附条件**:管理面读取的**可审计性现由「受控 Pino 日志 + 指标计数 + 披露点 fail-closed」承载**;若将来出现**合规要求读操作入库**,须**复开 D-API-59** 走契约变更,而非在实现期加 kind。`admin_ro` 结构性不可触 `audit_log`(零 GRANT)这一事实同时是**好消息**:管理面不可能污染安全账。
- **(b) 503 两类文案(`audit unavailable` vs `storage unavailable`)是否收紧为单一冻结文案 —— 保留双文案,不收窄。** 理由:两者是**不同故障域**(审计通道不可用 ≠ 存储不可用),合并会**丢失可诊断性**;二者均不含秘密、不改变状态码、不泄露内部结构 ⇒ **不构成侧信道**(CLAUDE.md 侧信道约束针对的是「经长度 / 时序 / 分类差异间接泄露秘密」,此处是**故障原因分类**,非秘密分类)。**附条件(已满足)**:两文案必须是**冻结常量**且被测试逐字节锁定,不得运行时拼接。
- **(c) `pnpm-lock.yaml` 被改动 —— 接受并如实登记。** 主控独立核实:差异为 **34 行纯新增**,全部是 `apps/admin` 的 workspace 条目(workspace 链接 + **既有版本**),**零新增外部包** ⇒ 属「新增工作区成员的必要结果」,非依赖面扩张。
- **(d) WP-78 host-scores 落盘后需复核 admin 的 PG 数据口径 —— 判为「契约面已被机检锁定,值面留一次性对照」**。已锁定部分:admin 以 `HostScoresResponseSchema.parse()` 单出口 + 机检「顶层恰 `items`/`nextCursor`」+「逐条字段集 = `Object.keys(HostScoreRecordSchema.shape)` 派生」⇒ **形状与字段名不可能与 WP-78 漂移**。**未覆盖部分**:admin 自写的三表连接 SQL **取哪一列喂 `verdict` / `decidedAt`** 属**语义口径**,无机检保证与 WP-78 的适配器一致 ⇒ 登记为**遗留第 23 项**。
- **(e) 用户面两处文档回填 —— 已由主控完成**(`1a5c787`;文本与 WP-79 建议等价,并额外写明「两条通道凭证 / 端口 / 网络域均不共用,接不接 admin 都不影响平台侧对账」)。
- **(f) 频率闸为单进程内存固定窗口(键 = 客户端 IP,零凭证材料入键)—— 接受。** 理由:管理面为**单副本运维面**(非水平扩展面),内存固定窗口足够;零凭证材料入键符合「限流键不得承载凭证」纪律。**触发条件**:管理面出现**多副本部署**需求时,须迁 Redis 并登记为契约面演进(端口已在,可换)。
- **(g) 控制台只读页无 axe 真机门禁(仅结构机检)—— 接受该边界。** 理由:为它引入 harness 需注入产品路径不产生的样式表 ⇒ **正是 M2 已明确拒绝的「假绿 harness」**(与 `degraded terminal` 不可达格的处置同款)。**登记为限制项**:管理面控制台页的无障碍证据 = **结构机检(语义化 DOM / label 关联 / caption / `aria-live` / 零内联)**,**不等于**真机 axe 通过;如需真机门禁须单列载体。
- **(h) 容器门控入口的环境依赖 —— 已登记为遗留第 19 项**(同一根因:并发拓扑占端口 / 镜像不可重建),清偿载体 = CI。

**WP-80(M10 出题者积木)转来 6 项**
1. **`test/workspace/sm-workspace.test.ts` 一处修复的记账归属 —— 确认归 WP-83。** 该例红**由 WP-83 引入**(惰性宿主把 payload 窗口创建由**同步**改为**异步**,而该例仍按同步存在性 `querySelector("sm-payload-tab")` 后立刻读 `shadowRoot`)⇒ 属 **WP-83 的接缝债务**;修复改用宿主 `whenReady()` **是正确口径**(与 WP-83 对外承诺的就绪钩子一致)。记账:**债务归 WP-83,清偿动作记在 WP-80**(该文件在 WP-83 禁改清单内,由 WP-80 代为改动,已在 WP-83 的「行为差异」登记之外单独留痕)。
2. **`CLAUDE.md` 与 D-API-110 仍写 20 token —— 已完成回填**(`a900bce`),并**消除了矛盾声明**:D-API-110「定案 1:零新增 token 硬约束」的绝对表述由 **D-API-130** 取代(原文逐字保留 + 追加取代说明),口径为「纪律有效、20 枚是 M1/M2 时点事实、当前 21 枚」。
3. **客户端校验能力边界 —— 接受,不立项。** vm-ui 不依赖 `challenge-schema`(依赖纪律 5.5)⇒ `XS-BLOCK-IFACE-REF`(接口是否存在)**在客户端结构上不可实现**,客户端只做形状闸。**权威面 = 服务端(challenge-schema)+ 编译链**,该边界已写入 D-API-129 与出题人指南(「两层校验、客户端更弱」)。为客户端拦坏接口号而引入跨包依赖 = **为功能破坏边界**,不取。
4. **`AUTHOR_BLOCK_ACTION_ARGS` 有意重复 protocol 参数名表 —— 接受重复,不开 schema-only 子路径依赖。** 理由:同 3,5.5 是硬约束;为消重而引入跨包依赖属**为 DRY 破坏边界**。现有护栏已覆盖包内自洽(键集 ≡ 12 动作、并集 ≡ 参数名全集、必填 ⊆ 允许)+ 服务端入站按 6.2 重校验。**但**「protocol 参数名表 ↔ `AUTHOR_BLOCK_ACTION_ARGS` 的**跨包**漂移」**无机检**(现有断言只在 challenge-schema 包内)⇒ 登记为**遗留第 24 项**(与 WP-78 转来的「`版本策略.md` ↔ `version.ts` 漂移无机检」**同族**)。
5. **`fixtures:manifest` 243/11 与 `smoke:contract` 摘要 254 不同计数器 —— 登记为口径说明,无动作**(前者含拒绝码记录,后者含 Rust 侧消费项;数值与改动前一致)。
6. **`docs/user/出题人文档/index.html` 写「共 11 页」而实为 15 个 HTML —— 裁决 = 订正。** 理由:E-4 的判据是「**无悬空声明残留**」,陈旧计数正属「文档声明了但事实不是」;成本一行,不订正即留悬空。**已由主控订正**(见提交记录)。

**WP-81(canary 收口)转来 6 项**
- **R-1(公开包 `semanticHighlights` 声明 vs 运行期高亮两条独立来源、无互证)—— 裁决 = 以「补编译期互证规则」为主,保留声明面。** 理由:声明面是**出题人的教学表达面**,删除即削减能力;而 `basic.json:129-135` 的**悬空高亮**(声明指向 stack 而权威对象在 vault)证明真正缺的是**校验**——故新增规则「声明的高亮目标必须存在且可见」,把该缺陷**从运行期提前到编译期**。归属 = 与 **WP-81a** 同批或并入后续投影面 WP。
- **R-2(`assemble.rs:1151` `label := objectId` 把私有标识符送进浏览器)—— 主控已独立取证,登记为遗留第 21 项(高优先)。** 取证:`:1151` 确为 `label: object.object_id.clone()`,位于 `visibleObjects` 映射内(`:1058` 注释自述「= 私有对象 `visibility = public` 的 objectId」)。**定性须校准,不得夸大为「已确认泄漏」**:该标识符与既有被裁决为「**公开 ISA 引用**」的 `interfaceId`(v1.8)同类,可能本就属公开引用;**但**按分类清单**自身**的硬门槛(「任何新增公开字段必须先在本文补论证,再进契约」)它属**未登记公开面**,且 **8 题全中、M3 前即存在** ⇒ 以高优先遗留登记,**不以沉默结案**。归属 = 待派单(**建议 WP-84**,含 WP-81 另发现的公开包 `semanticHighlights` 与运行期高亮互证缺口)。
- **R-3(P6 跨语言互证机检是否纳入完成标准)—— 纳入,但纳入的是 WP-81a 的完成标准,不是 M3 收口的完成标准。** 理由:M3 内 WP-81 只结**定案面**(定案 + 论证 + 实施清单成文);「三侧语义一致」的可验证性依赖 WP-81a 落地。**附判据**:WP-81a 完成标准含「新增跨语言互证机检」(现状:`vm-engine/**` 对 `XS-CANARY-CORR` **零测试引用**,仅 `assemble.rs:661-662` 一句注释 —— 这正是冲突能活到 M3 的原因,**必须补机检**)。
- **R-4(E-6 canary 面达成口径)—— 采纳「能力可表达 + 门禁可验」,不含「有发布题在用」。** 理由:CH-05 切换已明示**不承诺**;把「有发布题在用」当判据会把收口**绑在产品决策上**,与 WP-81 自身论证(「收口有效性不应被产品决策挟持」)一致。⇒ **E-6 = 部分达成**(定案 + 论证 + 实施清单;**实现待派单**)。
- **R-5(`阶段六扩展评估报告.md:69` / D-API-109 的「结构性不相容」措辞)—— 裁决 = 必须改述**(该措辞已被 WP-81 定案**证伪**:冲突实质是**规则面缺一种模式**,不是可见性语义有矛盾)⇒ 登记为**遗留第 22 项**。
- **R-6(并发 lane 在 `数据分类与秘密零驻留清单.md` 同文件编辑的四点核对)—— 已核实通过**:`版本` 行 = **v1.18**(WP-81);变更记录中 **v1.17(WP-80)与 v1.16(WP-78)并存**,无覆盖;§12.2 / §12.3 两模式论述在位。**另注**:此后 WP-82 采集面又追加了一行版本记录,故该文件当前最新版本号以**收口轮末态**为准。

**遗留第 6 项(lane B1,调试克隆对齐源)转来 4 项**
1. **写入面偏离(`src/debug/**` + 新模块 `src/sessions/debug-alignment.ts`)—— 批准。** 理由:该目录是**缺陷唯一客体**,且无并发席位编辑;`runtime.ts` / rig / `packages/**` / `persistence/**` / `migrations/**` / `teaching/**` / `compose/**` 零触碰,共享装配文件零改动。
2. **恢复会话的缺口语义由「静默落后对齐」改为「确定性拒绝」—— 批准(有意 fail-closed)。** 理由:静默退回种子态会把「对齐失败」伪装成正常克隆,**是比拒绝更坏的失败模式**(违反「不得以沉默代替结论」)。**附登记**:面向用户的解释文案缺失(现仅受控日志 + 通用 `invalid_input_format`)⇒ **遗留第 25 项**。
3. **是否为 attach 重放加步数上限配置键 —— 不加。** 理由:不轻增配置键(与 WP-79 (f) 同款节制);重放为 N 条且 **attach 幂等 ⇒ 每会话仅一次**,已被既有的**每会话动作预算 + clientSeq 上限**兜住。**触发条件** = 出现 attach 放大面的资源观测异常或安全评审要求。
4. **「已落库日志保留窗 / 裁剪」是契约面缺口 —— 登记为遗留第 26 项。**(`listBySession` 无裁剪参数;裁剪引入后本项判定**无需改代码即正确拒绝**,属**前向兼容**。)

---

## 七、结论

**总判定:M3 六个工作包(WP-78 ~ WP-83)+ 四项遗留移交逐项交付,「收口 · 中期验收评审」成文;退出条件 E-1 ~ E-8 全部给出「达成 / 部分达成 + 遗留登记」,无一条以沉默结案。中期里程碑按「部分达成、有条件通过」结账** —— 判据见下,判据本身也接受复核。

### 7.1 按工作包结账

| WP | 交付事实(要点) | 判定 |
|---|---|---|
| **WP-78** 成绩同步只读接口 | `GET /host/scores`;复用导出的 `hostBackendTokenMatches`(**单进程内单份实现**);参数白名单恰 `cursor`/`limit`/`tenantId`(多余即 400);游标 = `verdicts.id` keyset(**并发插入下无零遗漏保证,已明示**);批量上限默认 500 / 天花板 5000,**超限即 400 而非截断**;限流键 = 字典序最小租户;**SQL 恰取七列**;**零 DDL / 零新角色 / 零新 GRANT**;`SESSION_API_HOST_TENANTS` 为空 ⇒ **404 同形** fail-closed | ✅ 达成(compose 全链路证据本机不可达 ⇒ 遗留 19) |
| **WP-79** 分支 A 最小管理面 | `apps/admin`(src 18 / test 11 + 辅助);**独立凭证** `ADMIN_CREDENTIAL_SHA256`(sha256 + `timingSafeEqual`);自有**只读**库角色 `admin_ro`(不得 `BYPASSRLS`);RLS 迁移 **009**;三只读面(题目登记 / 裁决查询 / 成绩导出)**全 GET**;闸序冻结 **401 → 429 → 404 → 400 → 查询 → 审计 → 派发**;**零 app→app 依赖**(新增 allowlist 边);闸序与披露点已逐条取证 | ✅ 达成(compose 证据环境阻断);**主控已手工落地三处阻断性 compose 片段并验证**(`docker compose config -q` exit 0) |
| **WP-80** M10 出题者积木声明面 | `authorBlocks` **顶层可选**(16 → 17);1–16 × `{id, displayText, interfaceId, slots≤4, actions≤8}`;**12 公开动作**冻结枚举;**非图灵完备**(`args` 只能是字面量或 `{slot}` 结构引用 ⇒ 不构成任意宿主执行);四条新规则 + `XS-ID-UNIQUE` 扩展;`CHALLENGE_PACKAGE_SCHEMA_VERSION` **维持 1**;fixtures 重算 **254**;并**清偿 M1 遗留的深色画布光栅精灵可见性**(第 **21** 枚 token,真机对比度 dark 1.84 → 3.07 / terminal 1.82 → 3.04) | ✅ 达成 |
| **WP-81** canary 契约收口 | 定案 = **(c) 双层语义分离**(哨兵槽模式 T / 参考值模式 R);(a)(b) 两案逐条点名否决((a) = **安全剧场**,(b) = 字面形态**不可实现**);**决定性取证** = 引擎自身 happy-path 即用 `visibility:"public"` + `containsSecret:false` ⇒ 冲突实质 = **规则面缺一种模式**,非可见性语义之争;**canary 值是否为判题秘密 = 分模式**;**CH-05 = 不切换**;E1~E3 实跑 / P1~P6 待跑**严格分列** | ⚠ **定案面达成;实现面待派单**(遗留 22 / WP-81a) |
| **WP-82** 试用环境部署 + 指标采集最小面 + 报告模板 | 部署指南成文(**未实测不给配置片段**)+ 报告模板成文(24 观察点可得性分类)+ **采集面实现**:`teaching_events`(迁移 **008**,`FORCE` RLS + 租户绑定谓词逐字同 007 + 两段式保留期 + UPDATE/TRUNCATE 触发器 + kind 三值 CHECK + UNIQUE 幂等锚);三类服务端可派生;口径字面**单源**(绑定参数注入);**零学习者标识(六重结构性断言)**;**「提示使用」v1 不可采集且库层无表达位 ⇒ 零伪造**;失败语义 = **旁路账**(采集器永不抛错,审计账 fail-closed 不动);聚合 = 受控查询(冻结九键 + 八类机检);真库 IT **21 passed** | ⚠ **部分达成**:公网可访问**未达成**(O-MP-5 阻塞);**生产运行时接线未做**(遗留 27) |
| **WP-83** 主 chunk 体积拆分 | 主 chunk **1,379.02 → 428.65 kB(−68.9%)**,gzip 336.36 → 107.97;首屏静态图 1,396.71 → 452.51;惰性 chunk 945.82 kB;**两处改动必需**(注册表 + 入口桶),单点改动 = **假绿**;`web-component codeSplitting:false` = 结构性上限;**零新增依赖**(含 devDependency) | ✅ 达成,**义务 CLOSED** |
| **遗留移交 4 项** | 遗留 3(descriptor.spec 定案)/ 遗留 5(vm-ui 三条缺陷)/ 遗留 6(调试克隆对齐源)/ 遗留 e2e-descriptor —— 逐项落地,其中遗留 6 把恢复会话缺口由**静默落后**改为**确定性拒绝** | ✅ 达成 |

### 7.2 退出条件总账(E-1 ~ E-8)

**达成 3 项**:E-1(工作区模型与布局)、E-2(终端主题与效果面)、E-4(文档与声明面一致性 —— 含本轮**订正两处矛盾声明**与陈旧计数)。
**部分达成 5 项**:E-3(主题三预设 + axe 真机 **9/9 面 0 违规**已恢复且归档一致,但 `e2e-matrix` / WebKit 未绿 + terminal 面证据强度偏低)、E-5(容器与 compose 全链路 —— **本机不可达**,清偿载体 = CI)、E-6(声明面 + canary 定案达成,**实现待派单**)、E-7(采集面与模板达成,**公网可访问未达成**)、E-8(本机可跑门禁**全绿**,四项受环境 / CI 承载未取得绿)。

### 7.3 遗留清单状态

**共 30 项,逐项均带「归属 + 触发条件 + 现状」,无悬空项**(收口要求「遗留清单无悬空项」已满足)。其中**高优先 3 项**:①**遗留 21** —— `assemble.rs:1151` 的 `objectId` 属**未登记公开面**(定性已校准,8 题全中、M3 前即存在);②**遗留 27** —— 采集面**未接入生产运行时**(不接线则**采集面虽绿但一条数据也不会产生**);③**遗留 30** —— WP-83 惰性宿主的**几何链未复核**,**不得因 axe 转绿即判定无几何回归**。

### 7.4 结账意见

**建议按「有条件通过」结账,三个条件**:
1. **由 CI `ubuntu` 拓扑取得 `test:compose` 与 `e2e-matrix` 的绿证据**(遗留 1 / 19)—— 这是 E-5 与 E-3 收口的**唯一清偿载体**,本机既不可达也不应视为「未做」;
2. **派单 WP-81a**(canary 实现 + P1~P6 + 跨语言互证机检)与**遗留 27 的采集面接线**(二者都是「已定案 / 已实现但**尚未生效**」的面 —— 这类面最容易被"已完成"的表述掩盖,故单列);
3. **处置三项高优先遗留**(21 / 27 / 30),其中**遗留 21 必须先于任何公开面扩展**处置。

**本里程碑的真实风险不在「做了什么」,而在两类「看起来绿了」的面**:①**证据强度不足的绿**(3 个 terminal 面 axe「0 违规」主要来自**无法自动判定**,非正向通过);②**接线未完成的绿**(采集面单测 + 真库 IT 全绿,但生产装配未持有一行)。二者都已在正文与遗留清单中**显式标出,不以沉默结案**。

**纪律记录(本轮自我纠正)**:
- 曾把 WP-79 / 80 / 81 / 82 在交付清单中**误写为「完成」而其 lane 仍在跑**,已在同一动作内改回「进行中」;
- 曾把 `sm-jump-chain.test.ts` 的异常判定为「并发写入瞬时态」,实为**主控自身用 PowerShell 文本往返写入非法 UTF-8** 导致断言被静默丢弃(已自修复并订正 `WP-83.md`,登记为遗留 17);
- 本轮真机 axe 复跑**推翻了 E-3 原有的「九面全 0」结论**(发现并修复一条 serious 违规),并**据磁盘事实订正了 D-API-110 与用户指南中两处已被证伪的声明**。⇒ 以上均按「**发现即订正、订正留痕**」处理,不静默抹除。
