# 阶段一验收评审(WP-6 收口 · 2026-09-05)

对照 `docs/phases/阶段一任务分解.md` §五"阶段退出条件"逐条评审;证据均为本日可复跑命令的真实输出。评审结论:**八条退出条件全部通过,阶段一可退出**。

---

## 一、退出条件逐条评审

### 1. 字段级分类与"秘密不进入浏览器"论证 ✅

- 证据:`docs/contracts/数据分类与秘密零驻留清单.md`(WP-1,v1.9);`VmState` / `PublicStateProjection` / `ProjectionPolicy` 全字段分类,公开字段逐条论证;`packages/*/schema/classification.json` 机检产物,`schema-drift` / `strictness` 测试断言分类与常量一致;
- 硬门槛(任何无法证明"秘密不进入浏览器"的字段设计不得进入下一阶段):WP-1 五个工作包子项全部完成,包括 `PublicStatus` 时点侧信道判定与派生字段粒度决策。

### 2. 秘密零驻留清单成文且可机检 ✅

- 证据:同上文件 §机检清单:9.2 禁止下发清单 + 13.5 四类检查项逐条成文;`tooling/scan-public-artifacts.mjs`(`pnpm scan:public`)已接线,R8 隔离扫描本日通过(1 个公开包,0 违规)。

### 3. 双包字段分离检查器就位,正反样例结论正确 ✅

- 证据:`packages/challenge-schema` 检查器(`src/server-only/checker/`,XS-* 规则族);`221 passed`(challenge-schema 测试套,含 checker 正反样例、strictness、cross-package-consistency、isa-reference-face、hardening);公开包 Schema 零 `$ref` / 无 `default` / 禁私有键,正反样例结论由测试固化。

### 4. 四类契约 Schema 冻结,Zod 校验通过全部样例 ✅

- 证据:`packages/protocol` 12 个测试文件 `226 passed`,覆盖 ActionRequest(12 动作)、ActionResponse、投影三契约、PublicError(16 码 + 能力矩阵)、EmbedMessage(5 消息类型)、EmbedTokenClaims、VerdictResult(11 结果);正反 fixture 齐备(TS 侧 40 有效 / 94 非法)。

### 5. golden fixture 建立:TS 全绿 + Rust 冒烟通过 + 规范化序列化规则成文 ✅(WP-6 本次交付)

- 规范化规则:`docs/contracts/规范化JSON序列化.md`(`stackmaster-canonical-json/1`,JCS 整数域子集);
- TS 参考实现:`packages/protocol/src/common/canonical-json.ts`,226 测试全绿;
- 摘要清单:`tooling/contract-smoke/canonical-digests.json`(134 fixture:127 摘要 + 7 拒绝码),`pnpm fixtures:manifest --check` 本日通过;
- TS 防漂移:`packages/protocol/test/canonical-manifest.test.ts`;
- **Rust 冒烟**(`tooling/contract-smoke`,`pnpm smoke:contract`):**Schema 编译 13 个,有效样例 40 接受,非法样例 94 拒绝,摘要比对 134 条,serde/schemars 消费 4 项**;语义层承接(四条 superRefine 不变式)与 TS 判定逐例一致;cargo clippy(`-D warnings`)/ fmt / test 通过。

### 6. 四类版本策略与 handshake 规则成文冻结 ✅

- 版本策略:`docs/contracts/版本策略.md`(WP-6):四类版本定义与关系、判题 / 回放六记录项、生产锁定五条;
- handshake:`docs/contracts/嵌入协议.md`(WP-5):时序图覆盖常规路径、opaque origin、版本协商失败、能力降级、超时与 iframe 重载;校验规则 V-1 – V-13 逐条可测。

### 7. ADR-8 产能决策完成并记录 ✅(WP-6 本次交付)

- `docs/adr/ADR-8-vm-core语言产能决策.md`:**维持 Rust,回退预案不触发**;产能证据(工具链 + 生态实证)、决策理由、阶段二 tripwire 成文;WP-1 的 `VmState` 归属悬置解除。

### 8. 计划书五项确认逐条复核 ✅

| 确认项 | 冻结载体 | 复核结论 |
|---|---|---|
| 产品边界 | 计划书 §二 / §三、11.1 / 11.2;CLAUDE.md"项目一句话 / 非目标";任务分解 §三范围表 | MVP 闭环(栈帧 / buffer / return address)未被扩大;阶段一未实现任何行为面,符合 T0 纪律 |
| 最小 DSL | `docs/contracts/最小DSL范围.md`(WP-4,v1.3):指令面 20 opcode、谓词封闭集、禁止项清单、"扩展 = 受审计内置 + Schema 约束参数" | 无任何脚本语言能力;声明面(自定义指令 / 接口)为封闭原语组合,已由 XS-* 规则机检 |
| 字段分类规则 | WP-1 分类表 + `classification.json` + challenge-schema 分类检查器(XS-PUBLIC / XS-* 族) | 规则可机检、正反样例结论正确(条件 3 同一证据) |
| 秘密零驻留清单 | WP-1 清单 + `pnpm scan:public` 扫描器 | 条目粒度可被 CI 直接引用(条件 2 同一证据) |
| 判题安全原则 | 会话动作协议语义 §六(服务端校验基线七条)、9.1 十一结果类型冻结、verifier 独立重放设计(版本策略 §三 / §四)、浏览器不信任基线(WP-5 V 规则族) | 服务端权威 + 独立重放 + 隐藏测试不泄露的三要素在契约层齐备 |

---

## 二、本日门禁汇总(可复跑)

| 门禁 | 命令 | 结果 |
|---|---|---|
| 构建 | `pnpm build` | 4 tasks 成功 |
| 类型 | `pnpm typecheck` | 通过 |
| 测试(TS) | `pnpm test` | protocol 227 + challenge-schema 221,全部通过 |
| Lint | `pnpm lint` | 0 错误 |
| 依赖边界 | `pnpm lint:deps` | 201 模块,0 违规 |
| 隔离扫描 | `pnpm scan:public` | 通过(0 违规) |
| 清单一致性 | `pnpm fixtures:manifest --check` | 134 fixture 一致 |
| Rust 冒烟 | `pnpm smoke:contract` + `cargo test`(tooling/contract-smoke) | 全绿;clippy `-D warnings` / fmt 通过 |

## 三、移交阶段二的挂起项(不阻塞退出)

1. **CI 流水线接线**:上述门禁以本仓脚本存在;GitHub Actions 工作流归阶段二工程化(任务分解 §三:编排器等归后续阶段);`fixtures:manifest --check` 与 `smoke:contract` 必须纳入;
2. **N-1 窗口时长**:协议版本窗口为实现期运维参数(阶段三,5.6);
3. **引擎进程协议版本号**:阶段二接线时登记(投影与错误契约语义 §5.3);
4. **semver 演进工具**:题目包 / 协议的版本迁移工具与 fixture 跨语言往返已就位,版本迁移工具归阶段二(双包Schema语义 §一移交项);
5. **私有包 fixture**:永不入 git(红线);Rust 冒烟消费的公开面 fixture 已覆盖 12/13 个落盘 Schema,`private-bundle.schema.json` 的 Rust 消费随阶段二 vm-runtime 包加载实现补冒烟(私有样例由测试期 builder 现场生成,不入库)。

## 四、结论

阶段一交付契约与规则(Schema、类型、分类表、fixture、策略文档),无可运行 VM / API / UI,符合阶段定位。八条退出条件全部通过,五项确认完成,ADR-8 已决策。**阶段一关闭,进入阶段二(后端 VM Core 与会话编排,Rust)。**
