# 阶段三验收评审(WP-8 收口 · 2026-09-10)

对照 `docs/phases/阶段三任务分解.md` §五"阶段退出条件(整体验收)"逐条评审,每条给出可复跑命令与测试锚点(结果数为当日实跑输出);13.3 / 13.5 阶段三条目核对表、门禁证据汇总与移交清单见 §二 ~ §五。评审结论:**十条退出条件全部通过,阶段三可退出**。

实跑环境:Windows 11(x64,Git Bash)、Node v24.20.0、cargo 1.98.1、Docker 29.1.3。TS 全量与集成测试均在本机实跑;Compose 集成另由 CI `compose-integration` job(完整 linux 拓扑,`WORKER_CARGO_PROFILE=release`)在每次 push 复验。

---

## 一、退出条件逐条评审(§五,十条)

### 1. 会话级命令 Schema、WSS 传输信封、会话凭证 claims 冻结并落 fixture;N-1 版本窗口与幂等窗口 TTL 配置化生效 ✅

- **契约冻结面**(WP-0):`SessionCommandRequest` / `SessionCommandResponse` 判别联合、`WssFrame` 三值封闭类型、`SessionCredentialClaims` / `EmbedTokenClaims` 七字段——全部经 `@stackmaster/protocol` Zod Schema + JSON Schema 落盘;`fieldClasses` 键集 === 顶层 `properties` 键集的 schema-drift 机检在 protocol 套件内(vitest);
- **fixture**:`pnpm fixtures:manifest --check` → `--check 通过:178 个 fixture 与清单一致`(WP-0 新增 44 个:命令请求 14 + 命令响应 12 + 帧 11 + 会话凭证 7);
- **配置化生效**:D-API-4 两键(`SESSION_PROTOCOL_N1_WINDOW_DAYS` / `IDEMPOTENCY_WINDOW_TTL_SECONDS`)过启动校验三道闸(`test/config.test.ts`);受理集合锚点 `SUPPORTED_SESSION_ACTION_PROTOCOL_VERSIONS` 启动登记(`src/index.ts`,装配测试 `test/runtime/assembly.test.ts`);
- **跨语言**:`pnpm smoke:contract` → `契约冒烟通过:Schema 编译 17 个,有效样例 40 接受,非法样例 94 拒绝,摘要比对 178 条,serde/schemars 消费 4 项,private-bundle 消费 10 项`;覆盖面不变的裁决与理由成文于 D-API-7。

### 2. embed token 签发 / 三方比对 / jti 单次消费 / 吊销与会话凭证链路端到端;红灯矩阵;AuthContext 真实实现 ✅

- 链路端到端:`test/auth/e2e-chain.test.ts`(签发 → 消费 → 会话凭证 → 动作通道);红灯矩阵逐条确定性拒绝且响应面统一 401 冻结形态(D-API-14):过期 token、已消费 jti 重放、跨租户 session、跨题目版本绑定、伪造签名、宿主无凭证签发、吊销后使用——`test/auth/consumption.test.ts` / `test/auth/credential-middleware.test.ts` / `test/auth/issuance-route.test.ts` / `test/auth/signer.test.ts` / `test/auth/stores.test.ts`;
- 身份派生:请求体自报身份字段被 strictObject 拒绝(身份只来自认证上下文,`test/auth/auth-context.test.ts`);
- `stubAuthContext` 已退场:session-core 接口不变,`createSessionAuthContext` 装配替换(D-API-52 装配面;`test/runtime/boot-recovery.test.ts` 内存同构栈 + 全链路测试均以真实 AuthContext 运行)。

### 3. REST 生命周期 + 认证 WSS 通道全链路可演示 ✅

- REST 面:`test/routes/lifecycle.test.ts`(create_session 201 + Cookie → sync → checkpoint → list → submit → close 全链路,响应全过冻结 `SessionCommandResponseSchema`,响应体零凭证字段);
- WSS 面:`test/wss/channel.integration.test.ts`(签发 → create_session → WSS 升级 → 动作 → `action_response` 过冻结帧 Schema 原样下发;心跳 / 空闲 / 踢旧 / 背压 / 帧超限 close 1009);
- 拓扑级演示:`test/compose/compose-full-chain.integration.test.ts` 7 passed(容器形态)——POST /auth/embed-tokens(完整 token 链路)→ create_session → WSS 动作 → 增量下发 → 断线重连 → sync-projection → checkpoint / undo / checkout → submit(裁决引用与动作日志落库可取回);断线重连对齐用例同时落 rig 级(`api-acceptance-13-3`)与拓扑级,重启恢复用例(2064ms)自快照续算 revision 并同输入恒同响应(I-4)。

### 4. 一切入站按冻结契约重新校验;校验失败响应面 = 冻结 PublicError;护栏触顶确定性 ✅

- 路由级契约重校验 + 零校验器细节(基线 #8):`test/routes/error-mapping.test.ts`(畸形请求返回冻结错误形态,Zod issue 只进受控日志且零 message);HTTP 状态 ↔ 冻结结果类型映射矩阵 413 / 401 / 404 / 409 / 422 / 429 / 503 / 504(D-API-32,`test/routes/error-mapping.test.ts` 全矩阵);
- 护栏:请求体字节(bodyLimit 413)、JSON 深度 / 数组 / 字符串长度、clientSeq 预算触顶 409 确定性——`test/routes/guards.test.ts` + `test/routes/error-mapping.test.ts`;
- WSS 帧:`maxPayload` 协议层 close 1009 + 结构护栏(`test/wss/channel.integration.test.ts`);WSS 错误帧矩阵常量装配期过冻结 Schema 自检(D-API-41,`test/wss/*`)。

### 5. 投影零合成;跨域载荷录制机检零命中 + 红灯反例;ZR-P4 时序统计面就位 ✅

- 投影零合成:响应 = 执行域产物原样转发(`test/wss/channel.integration.test.ts` 出站帧过冻结 Schema,ADR-7);权威 revision 只来自 worker(编排核心账本);
- 录制机检:`test/wp7/cross-domain-payload-audit.test.ts`(rig 级出站帧 + HTTP 响应体全量录制)+ Compose 客户端捕获(`test/compose`)——ZR-B9 共现 / ZR-B10 私有事件形态 / ZR-B5 快照魔数与信封标记 / ZR-B4 键名语料 / ZR-B1 / B6 语料(`scanSecretCorpus`)**全链路零命中**;红灯反例齐(`test/scan/cross-domain-payload-scanner.test.ts`,每违规类注入即检出);
- ZR-P4:`test/limits/zr-p4-frame-statistics.test.ts`(帧数 / 类型序列 / 帧长逐位置 / 归一化载荷逐字节无可区分差异;变更脚本第 1 帧检出的红灯反例同套件运行,D-API-57)。

### 6. 持久化面:表域 + append-only + Redis 四键域 + 对象存储;快照密文断言;重启自快照恢复(I-4)✅

- 表域与租户校验:`test/persistence/register-challenge.test.ts` / `recovery-plan.test.ts`(sessions 行形态、租户强制 WHERE);`test/runtime/assembly.test.ts`(装配序与迁移 fail-closed);
- append-only 数据库层强制(触发器 RAISE):`test/persistence/migrations-appendonly.integration.test.ts`(UPDATE / DELETE / TRUNCATE 三连红灯反例,11 tests);
- Redis 四键域 TTL 与 fail-closed 分级:`test/persistence/redis.integration.test.ts` + `idempotency-window.test.ts`(幂等窗口降级语义 / 同键同负载字节相同重放 / 异负载 conflict);
- 对象存储:`test/persistence/minio.integration.test.ts`(私有包专用桶、验签失败 / 版本冲突确定性拒绝);
- **快照密文断言**:`migrations-appendonly.integration.test.ts`(AES-256-GCM 整包加密落库,明文语料 `scanSecretCorpus` 零命中 + 红灯反例证明扫描器可检出;D-API-21 / 22);
- **重启恢复**:rig 级 `test/runtime/boot-recovery.test.ts`(active 行 → planRecovery → recover → adoptRecovered,fail-open 边界)+ 拓扑级 Compose 重启用例(kill 编排器 → 重启 → revision 自快照续算 → 同输入恒同响应 I-4,`test/compose`,与未重启孪生会话对齐)。

### 7. 限流与配额:四维触顶确定性拒绝 ✅

- `test/limits/rest-rate-limits.test.ts`(每租户 / 每用户 120/min 固定窗口触顶 429,响应体逐字节一致,租户 / 用户隔离互不挤占)、`test/limits/rate-gate.test.ts`、`test/limits/checkpoint-quota.test.ts`(checkpoint 数量 ≤ 256 / 快照字节预算 / 租户存储配额三预执行闸 + 粘性超限标记 + 重复触顶同形)、`test/limits/wss-action-rate.test.ts`(每会话动作频率与每连接令牌桶叠加)、`test/limits/session-recycling.test.ts`(断线保持到期回收幂等)、`test/limits/action-log.test.ts`(submit 锚点增量落库,拒绝不入账);
- 429 冻结形态与 D-API-50 数值表(默认值 + 天花板)已在规约 §三·七登记。

### 8. Compose dev 一键拓扑可用;Compose 集成测试全链路绿;k6 基线首采归档 ✅

- **一键起停(本机实跑)**:`compose:app:up`(up -d --build --wait,debug 档)全拓扑 4 服务 Healthy(session-api 发布 13000);随后 `SESSION_API_TOPOLOGY=container pnpm --filter @stackmaster/session-api test:compose` 全绿——镜像构建(缓存命中)→ vm-worker linux 二进制冒烟 → vitest `test/compose` **7 passed**(全链路 + 机检零命中 + 重启恢复)→ `down -v` 收尾(exit 0);CI `compose-integration` job 每次 push 以 release 档复验同一拓扑;
- **k6 基线首采(本机实跑,docker grafana/k6 对 compose 容器拓扑)**:`pnpm --filter @stackmaster/session-api k6:baseline` → 归档 `apps/session-api/k6/results/2026-09-09T223628898Z/`(逐场景原始 summary JSON + stderr 留档 + 采集前 / 后 /metrics 快照 + summary.md):
  - `action-rtt-wss`:**848 个已确认 WSS 动作**,动作 RTT **p50 6ms / p95 10ms / max 16ms**(106 会话);
  - `rest-lifecycle`:**228 个完整生命周期**,整环 **p50 61ms / p95 69.65ms**(五命令,http_req_duration p95 50.33ms);
  - `concurrent-sessions`:服务端 `session_api_live_sessions` 峰值 **6**(与场景阶梯设计一致),held 12 会话;
  - **不设通过阈值**(10.3 / 13.6,避免过早优化;数据作 T2 触发判据基线,D-API-73)。

### 9. 秘密零驻留扩展面复验:四处语料扫描零命中 + 红灯反例;ZR-T1~T4 通过 ✅

- 四处扫描面:编排器进程内存(投影缓存 / checkpoint 账本 / submit 引用,阶段二套件照常回归)+ 日志(`test/logging.test.ts` redaction 三层)+ PostgreSQL 行(快照密文断言 + 动作日志零秘密锚点,`migrations-appendonly.integration.test.ts`)+ Redis 键(token 载荷 / 幂等负载,`test/auth/stores.test.ts` / `idempotency-window.test.ts`)——零命中;红灯反例证明扫描器可检出(WP-3 / WP-7 反例集);
- 新增第五处(本 WP 扩展):`/metrics` 渲染输出——`scanSecretCorpus` 零命中 + 标识符形态值零出现 + 红灯反例(`test/metrics/metrics.test.ts`,D-API-71);
- ZR-T1~T4:`test/wp7/zr-tamper-matrix.test.ts` 全绿(重放 / 伪造成功标志 / 自制快照 / 伪造投影 / 幂等键同键异负载,服务端权威锚定零状态腐化,D-API-62)。

### 10. 覆盖率与门禁:TS 整体 ≥ 80%(含 apps);Rust 门禁不回退;13.3 / 13.5 核对表无缺项 ✅

- **TS 覆盖率**(新增根级入口 `pnpm test:coverage`,vitest projects 聚合 apps + packages;完整门禁形态 = `SESSION_API_IT=1 pnpm test:coverage`,exit 0):

| 范围 | statements | branches | functions | lines |
|---|---|---|---|---|
| **整体(门槛 ≥ 80)** | **88.71%** | **81.23%** | **89.78%** | **88.68%** |
| apps/session-api | 86.01% | 78.66% | 87.75% | 86.17% |
| packages/protocol | 96.34% | 95.65% | 96.30% | 96.28% |
| packages/challenge-schema | 92.04% | 86.03% | 90.28% | 91.83% |
| packages/challenge-compiler | 93.79% | 79.48% | 97.26% | 93.64% |
| packages/session-core | 76.19% | 68.09% | 91.67% | 76.00% |

  整体四维全部 ≥ 80%,门禁通过。个别包低于 80% 的缺口如实分析(见 §四),不构成整体门槛违规;
- **Rust 门禁**:`pnpm test:rust`(debug + release 全 workspace)exit 0,**636 passed**(318 × 双 profile,与阶段二 318 基线逐 profile 持平,零回退);`cargo --version` = cargo 1.98.1;ENG-1~10 门禁与 miri / fuzz / 跨平台由 CI rust-gate / rust-miri / rust-fuzz / determinism jobs 承载(本阶段 Rust 侧零改动);
- 测试规模:session-api **345 passed / 30 skipped**(单元 + 内存同构;30 skipped = 容器门控集成,纪律性跳过);集成(`test:integration` + `test/compose`)+ 全图 turbo test 12/12 任务绿;13.3 / 13.5 核对表见 §二 / §三,无缺项。

---

## 二、13.3 API 侧条目核对表(WP-8 第 5 条)

13.3 原文条目逐条 → 测试锚点;**iframe 面条目(handshake 超时、iframe 重载、宿主不支持能力、不受信任来源、自适应高度、主题与语言)归阶段五**(嵌入协议交付通道,D-API-66 排除说明);消息乱序 / 非法序列号的服务端面由传输层纪律承载(D-API-5:权威判定只在载荷层,`test/wss/channel.integration.test.ts` 帧序列用例)。

| 13.3 条目 | 阶段三测试锚点 | 状态 |
|---|---|---|
| 过期 revision | `test/wp7/api-acceptance-13-3.test.ts`(服务端权威锚定续算,I-4 孪生对齐;D-API-62) | ✅ |
| 重复幂等键 | 同上(缓存同形响应;同键异负载 conflict;窗口外重放仍确定性) | ✅ |
| 跨租户 session | 同上(REST 凭证绑定 401 + WSS 会话绑定错误帧双层) | ✅ |
| 断线重连与投影重同步 | 同上(重连凭证重验 → REST sync-projection 对齐 → 新锚继续)+ `test/compose` 拓扑级 | ✅ |
| 服务端重放一致性 | `test/routes/lifecycle.test.ts` / session-core 幂等套件(同输入恒同响应 I-4) | ✅ |
| 权限校验 | `test/wp7/api-acceptance-13-3.test.ts`(过期 embed token / 已消费 jti 重放 / 绑定不符 / 未认证 / 过期会话凭证 → 401 统一形态)+ `test/auth/*` 红灯矩阵 | ✅ |
| 限流 | `test/limits/rest-rate-limits.test.ts`(429 冻结形态逐字节确定)+ `test/limits/wss-action-rate.test.ts` | ✅ |
| 超时和资源限制 | worker 看门狗 timeout 错误帧(`test/routes/error-mapping.test.ts` 504 矩阵 / `test/routes/guards.test.ts`)、请求体 413、clientSeq 预算 409;帧超限 close 1009(`test/wss/channel.integration.test.ts`) | ✅ |
| 重复消息 / 消息乱序 / 非法序列号 | 载荷层幂等 + clientSeq 语义(`test/wss/channel.integration.test.ts`、session-core 套件);传输层 seq 不作拒绝依据(D-API-5) | ✅(服务端面) |
| 不受信任来源 / iframe 重载 / 宿主不支持能力 / handshake 超时 / 自适应高度 / 主题与语言 | —— | **归阶段五**(iframe 嵌入面) |

**结论:API 侧条目全部有可执行测试锚点,无缺项;iframe 面条目已显式标注归属。**

## 三、13.5 相关条目核对(阶段三子集)

| 13.5 条目组 | 落点 | 状态 |
|---|---|---|
| 构建产物检查(bundle / SourceMap / 网络响应零 flag / 隐藏测试 / IR / 私有包 / 快照 / seed) | `pnpm scan:public`(R8 隔离扫描通过:1 个公开包已扫描,0 违规)+ 通道录制机检(ZR-B / ZR-P 全绿,§一.5)| ✅(阶段三面)|
| 运行时检查(浏览器主线程 / Worker / IndexedDB 等) | ZR-R1~R5 | 归阶段四 / 五(规则已冻结)|
| 投影泄漏检查(字段白名单 / 随机秘密差分 / 长度·时序·错误分类无相关性) | 引擎差分套件(T-SC1 / T-SC4 / ZR-P6 / ZR-P7)+ 编排器通道级统计面(ZR-P4,`test/limits/zr-p4-frame-statistics.test.ts`)| ✅(通道级兜底 + 引擎面)|
| 客户端篡改检查(篡改状态 / 伪造投影 / 重放 / 伪造成功标志不影响权威状态) | `test/wp7/zr-tamper-matrix.test.ts` | ✅ |

## 四、门禁证据汇总(可复跑;2026-09-10 实跑)

| 门禁 | 命令 | 结果 |
|---|---|---|
| TS 类型(turbo 全图) | `pnpm typecheck` | 9/9 任务成功 |
| TS 构建(turbo 全图) | `pnpm build` | 全绿 |
| TS 测试(turbo 全图) | `pnpm test` | 12/12 任务成功(960 passed / 30 skipped 当日全量口径) |
| TS Lint | `pnpm lint` | 通过(0 error;新增 `apps/session-api/k6/**` 全局块) |
| 依赖边界 | `pnpm lint:deps` | ✔ no dependency violations found(561 modules,1800 dependencies) |
| 依赖边界反例自检 | `pnpm lint:deps:self-test` | 5 组反例 / 对照全绿 |
| **TS 覆盖率** | `SESSION_API_IT=1 pnpm test:coverage` | **exit 0;整体 88.71 / 81.23 / 89.78 / 88.68(≥ 80 四维)**;分表见 §一.10 |
| golden fixture | `pnpm fixtures:manifest --check` | 178 个 fixture 与清单一致 |
| 跨语言契约冒烟 | `pnpm smoke:contract` | Schema 17 / 接受 40 / 拒绝 94 / 摘要 178 / serde 4 / private-bundle 10 |
| 隔离扫描 | `pnpm scan:public` | 通过(0 违规,0 豁免) |
| **Rust 测试** | `pnpm test:rust`(debug + release) | **exit 0,636 passed**(318 × 双 profile;cargo 1.98.1) |
| Compose 集成 | `SESSION_API_TOPOLOGY=container test:compose` | 7 passed(含一键 up / vm-worker 冒烟 / down -v) |
| k6 基线 | `pnpm --filter @stackmaster/session-api k6:baseline` | 首采归档 `apps/session-api/k6/results/2026-09-09T223628898Z/`(§一.8) |
| Rust 纪律 / 覆盖率 / miri / fuzz / 跨平台 | CI `rust-gate` / `rust-miri` / `rust-fuzz` / `determinism` | 由 CI 承载(本阶段 Rust 侧零改动;本地 debug+release 双 profile 实跑持平) |

**fixture manifest 与 ZR / ENG 映射**:`fixtures:manifest --check` 全绿(§一.1);`秘密零驻留CI检查项映射.md` 终版(v1.8)——阶段三范围 🔜 归零,尚存 🔜 逐条标注归属(语料法随题目 fixture → MVP 期;浏览器响应面 / ZR-R1~R5 → 阶段四 / 五;ZR-P5 通道级断言锚点补登);D-API-70~73 已回填规约 §三·九与 §四登记表。

**覆盖率缺口如实分析**(整体达标前提下,个别包低于 80% 的分支缺口):
- `packages/session-core`(branches 68.09%):缺口集中于 `worker-binary.ts`(ensureWorkerBinary 的容器 / 平台产物解析分支——本机仅命中 host 路径)与 `worker-connection.ts` 的进程异常分支(spawn 失败 / 非预期退出的时序组合,部分仅在 CI 矩阵或故障注入下可达);行为面由进程级集成测试(6 passed,真实二进制)与阶段二 Rust 侧 318 测试对冲;
- `apps/session-api` branches 78.66%:缺口集中于 `runtime/runtime.ts` 全量装配错误路径(依赖不可达组合)与 WSS 背压 / 心跳的极端时序分支——前者依赖真实容器组合(IT 形态已覆盖主路径),后者由 `test/wss/backpressure.test.ts` 覆盖主干;
- 两处缺口均不影响整体门槛(88.71 / 81.23),阶段四 / 五联调引入的新故障路径按同一测试纪律回补。

## 五、范围边界复核与遗留移交

### 范围边界复核(任务分解 §三)

阶段三未实现明确不属于本阶段的任何内容:Lit UI / 字节视图 / 寄存器面板 / Payload 构造器 / 时间线 UI(阶段四)、iframe 插件 / embed-runtime SDK / postMessage 实现 / token 交付通道 / 自适应高度 / 主题语言(阶段五)、公开描述包 CDN 发布与签名 URL(阶段五,本阶段仅对象存储落点)、独立 verifier 服务与 verdicts / verifier_runs 写入 / 审计归档 / 行级租户策略(阶段六,仅预留表结构 + 最小审计写入面)、容器化 Worker 池(T1 / T2)、Playwright E2E 与 axe-core(阶段四 / 五)、题目内容批量制作(MVP 期)、vm-engine 零改动。

### 遗留与移交

**移交阶段四(教学 UI)**
1. iframe / UI 面的 ZR 运行时检查(ZR-B7 浏览器响应面、ZR-R1~R5)随 Playwright + CDP 基建落地(规则已冻结);
2. OpenTelemetry 全链路 span 为 T0 可选增量(D-API-72 登记):跨服务排障需求实际出现时,接入点为 manager 动作入口与 WSS 通道流水线;
3. /metrics 的生产暴露面收敛(内网段 / 反代准入 / 独立端口)与 Prom 抓取接线随部署面配置(D-API-70,端点语义不变)。

**移交阶段五(iframe 插件与嵌入安全)**
4. 13.3 iframe 面条目(handshake 超时、iframe 重载、宿主不支持能力、不受信任来源、自适应高度、主题与语言)——本评审 §二已显式排除并标注;
5. token 交付通道(宿主注入 / MessageChannel):session-api 侧签发 / 消费 / Cookie 卫生已就绪(D-API-11~17),交付通道为纯阶段五面;
6. 公开描述包 CDN 发布与签名 URL(对象存储 `public-descriptors` 桶落点已就绪)。

**移交阶段六(独立判题与扩展)**
7. verifier 裁决闭环:`submissions` 内部裁决引用 + 完整动作日志落库可取回(重放锚齐备),verdicts / verifier_runs 写入与隐藏测试最终裁决归阶段六;
8. `audit_log` 表域与审计归档:本阶段审计为进程内 append-only 内存实现(D-API-33 已知留白),kind 七值封闭集合(D-API-59)不得演进式扩张,PG 落库替换实现须保持 append-only 端口语义;
9. 行级租户策略与全面租户隔离(查询层强制已就位,D-API-20)、限流默认值的租户规模复核(D-API-50);
10. 幂等窗口 / route / rate 键域的 T1 多实例演进面(单实例进程内形态与跨实例聚合的边界已在 D-API-36 / 49 / 50 登记);
11. 引擎侧演进项(沿阶段二移交):fuzz 持续化加码、vm-runtime / projection 的 miri 扩展。

---

## 六、结论

阶段三交付**可运行、可持久化、可观测的权威 API 承载面**:契约冻结(会话级命令 / WSS 信封 / 会话凭证)→ session-api 工程载体 → 认证凭证链 → PG / Redis / MinIO 持久化与快照加密静止 → REST 生命周期 → 认证 WSS 通道 → 限流配额回收 → 跨域机检与 Compose 全拓扑 → 指标面 / k6 基线 / 验收收口。345 + 960 项 TS 测试、636 项 Rust 测试(双 profile)、Compose 全链路 7 用例、k6 首采归档、覆盖率整体四维 ≥ 80% 全部实跑通过;**十条退出条件全部通过,阶段三关闭,进入阶段四(教学 UI)。**
