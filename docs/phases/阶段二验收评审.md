# 阶段二验收评审(WP-9 收口 · 2026-09-09)

对照 `docs/phases/阶段二任务分解.md` §五"阶段退出条件(整体验收)"逐条评审;§六决策点与风险的 ADR-8 tripwire 在本评审做验收点复核。证据均为本日可复跑命令的真实输出。评审结论:**十条退出条件全部通过,阶段二可退出**。

---

## 一、退出条件逐条评审

### 1. 引擎三 crate 满足 safe-Rust 与确定性纪律且机器强制生效 ✅

- `#![forbid(unsafe_code)]`(ENG-1)、引擎三 crate `#![no_std]`(ENG-2,std::time / std::io / std::net / std::fs 结构性不可解析)、release `overflow-checks = true`(ENG-3,配置断言 + 运行时探针双强制)、依赖允许清单(ENG-4,时间 / 随机源 crate 全禁)、clippy 引擎禁用清单第二层(ENG-5)、fmt + clippy `-D warnings` + dev/release 双 profile 测试(ENG-6);
- 机器强制:`node tooling/check-engine-discipline.mjs` 本日全绿;配置类反例以 `--self-test` 六组违反样例证明可红灯,lint / 编译类反例以临时反例 crate 双向对照证明真实触发(本日自检通过);
- 证据落点:`docs/develop/秘密零驻留CI检查项映射.md` §二(ENG-1 ~ ENG-7 逐条含红灯反例位置),CI job `rust-gate` 接线。

### 2. 13.1 VM Core 测试清单全部落地;miri 无未定义行为;fuzz 通过;覆盖率 ≥ 90% ✅

- **13.1 逐条核对**:见本文 §二(20 条目 → 测试锚点一一映射,无缺项);
- **miri**(ENG-8,WP-9 新增):`cargo +nightly miri test -p vm-core` 本地全量通过(167 测试,nightly 1.100.0-nightly,x86-64-pc-windows-msvc);CI job `rust-miri` 接线;属性测试以 `RngSeed::Fixed` + `failure_persistence = None` 保证 miri 隔离模式纯净(无 OS 熵、无文件 IO);
- **fuzz**(ENG-9,WP-9 新增):`vm-engine/fuzz/` 三目标——`frame_command`(动作 / 命令解析器)、`challenge_bundle`(私有包 + 公开描述包双包管线,含 IR 装配复验)、`byte_decode`(字节模式取指译码,gadget 路径);管线与 worker 处理路径同构(严格 JSON → Schema + 语义承接 → serde 镜像 → 会话装配);目标源本地 `cargo check` 通过(MSVC 无 libFuzzer 运行时,cargo-fuzz 官方不支持 Windows;运行面由 CI job `rust-fuzz` 在 ubuntu 承载,每目标 `-max_total_time=60` 冒烟);语料纪律:不提交语料——私有题目包样本永不入 git;
- **覆盖率**(ENG-7):`cargo llvm-cov --workspace --exclude vm-worker --fail-under-lines 90` 本日通过;分级行覆盖 vm-core **98.34%** / vm-runtime **90.55%** / projection **95.14%**,全部 ≥ 90%(三 crate 合计 96.19%)。

### 3. 确定性证据:跨平台与 debug / release 一致;重放哈希逐字节一致;差分测试通过 ✅

- **跨平台与双 profile**(ENG-10,WP-9 新增):`vm-runtime/tests/determinism_vectors.rs` 把黄金脚本(13 动作,覆盖执行 / 暂停 / checkpoint / undo 链 / checkout / reset)的**状态哈希序列、revision 序列、checkpoint 确定性 ID、动作日志摘要硬编码为仓库常量**;向量在 x86-64 Windows stable 上 debug 与 release(overflow-checks = true)逐字节一致后固化,任一平台 / profile / 引擎语义漂移即红灯(语义按版本策略冻结:常量变化 = `vmEngineVersion` 必须演进);CI job `determinism`(`.github/workflows/cross-platform.yml`)在 x86-64 + ARM64 × debug + release 四组合断言;
- **重放一致性**:`golden_replay_hash_sequence_identical_across_three_runs`(活体 = 重放 = 重放 = 解析后重放,三路逐字节一致)+ 篡改检测逐条目捕获;WP-9 黄金向量与其互为确定性 / 跨环境两路;
- **投影差分**:`projection::differential`(T-SC1 长度变体 8/16/32/64 + 同长异值、T-SC4 异 seed、I-9 探针双布局)规范化序列化后逐字节相等,差异字段注册表空集;ZR-P6 coarse 零解释机检通过。

### 4. 契约消费:worker 对一切入站按冻结 JSON Schema 校验,结论与 TS 侧一致;协议版本登记完成且语义文档成文 ✅

- `worker_commands::action_request_fixtures_match_ts_side_verdicts`(37 个跨语言 fixture 全量走查,与 TS 侧锁定结论一致)、`canonical_digest_manifest_is_reproduced_by_engine_module`(摘要清单引擎侧复算)、`protocol_version_constants_match_ts_side`(引擎进程协议版本双侧防漂移);
- 语义文档:`docs/develop/引擎进程协议.md`(WP-1 冻结;WP-8 增补 D-F10 load.publicDescriptor);`ENGINE_PROCESS_PROTOCOL_VERSION = 1` 双侧登记。

### 5. 会话语义:revision 单调、undo = 内容回退版本前进、幂等窗口缓存响应字节相同、单会话串行——均有集成测试 ✅

- Rust 侧:`session_runtime::revision_monotonic_with_delta_zero_or_one`、`undo_rolls_content_back_and_revision_forward`、`checkout_resolves_ownership_and_restores_content`、黄金向量 revision 序列恒 +1;
- TS 编排核心(`packages/session-core`,15 测试 = 9 单元 + 6 真实二进制进程级):幂等重放以原始 clientSeq / baseRevision 重构请求按规范化序列化比较、字节相同返回缓存响应;clientSeq 高水位 + baseRevision 预检 + 单会话串行队列;崩溃替换恢复投影逐字节一致、revision 自快照续算。

### 6. 秘密零驻留:`VmState` / 私有包 / seed / 隐藏测试只存在于 worker 进程内;编排器进程内零命中;隔离扫描覆盖 Rust 产物 ✅

- session-core 集成测试对编排器自身生成面(投影缓存 / checkpoint 账本条目 / submit 引用)做 seed / flag 语料扫描零命中,带红灯反例证明扫描器可检出;编排核心对私有包只透传,崩溃恢复要求调用方重新提供装载参数;
- 快照驻留边界成文(会话编排语义规约 D-W8-11:worker 所有 SERVER_ONLY blob,阶段三加密静止);
- 隔离扫描:`pnpm scan:public`(ZR-B3 / ZR-B9,含 Rust 产物路径 / `.rlib` / `.rmeta` / `libvm_*` 符号 / 原生二进制扩展名)本日通过。

### 7. 资源面:9.1 Worker 资源清单全部实现并有测试 ✅

- 步数 / 调用深度(`steps_budget_exhaustion_is_resource_limit`、`call_depth_limit_is_resource_limit`)、谓词累计预算(跨 undo/reset 保留)、动作日志长度结构护栏、内存静态护栏、回退次数进程级累计;
- wall-clock 看门狗(引擎无时钟,worker 进程层实现,D-W8-4):`watchdog_kills_worker_on_action_deadline`(50ms 预算死循环 → 退出码 3、零响应帧)+ 编排器外层兜底 SIGKILL;
- 崩溃替换恢复:`snapshot_replace_recovery_restores_identical_projection`(kill → 两步 load + import_snapshot → 同输入恒同响应 I-4,投影逐字节一致);进程不复用 + 僵尸回收;
- 引擎 panic(catch_unwind)→ engine_error 安全终止退出码 1(`main.rs`;溢出按 G1/D1 在掩蔽域内模回绕属玩家语义,引擎内溢出只可能是缺陷——构建级验证即 ENG-3 release 探针,泛化端到端触发样例需人为注入 panic,不为此扩协议面)。

### 8. 最小闭环演示:创建会话 → 动作 → 投影增量 → undo / checkout → reset → submit(内部动作日志可重放)✅

- `session_lifecycle::session_lifecycle_full_chain`(全链路逐步 revision 断言)+ session-core 进程级集成(TS 编排器 spawn 真实 worker 全链路);submit 产出内部裁决引用,集成测试以全新 worker 重放该日志验证完整可重放。

### 9. 版本锁定语义生效 ✅

- `worker_commands::engine_version_mismatch_is_rejected_challenge_invalid`、`declared_engine_build_id_must_match_self_report`(版本策略 §四:声明了构建 ID 就必须一致,不一致宁可拒绝不近似执行);worker 启动自报版本 + 快照导入同锚。

### 10. 阶段二验收评审通过,ADR-8 tripwire 验收点复核有结论 ✅

- 本文档即评审记录;ADR-8 tripwire 验收点复核结论见 §四:**未触发,维持 Rust,回退预案关闭**。

---

## 二、13.1 VM Core 测试清单逐条核对(WP-9 第 1 项)

| # | 13.1 条目 | 测试锚点(crate::test) |
|---|---|---|
| 1 | 地址读写 | `vm_core::memory::{write_then_read_roundtrip, little_endian_typed_access, access_across_page_boundary_within_region}`、`exec::arithmetic_with_memory_operand_reads_arch_width` |
| 2 | 越界访问 | `memory::{unmapped_access_is_rejected, address_overflow_is_rejected, range_crossing_region_boundary_is_fully_checked}`、`state::assemble_propagates_register_and_memory_rejections` |
| 3 | 不可写和不可执行区域 | `memory::permission_matrix_read_write_fetch`(8 权限 × 读 / 写 / 取指全矩阵)、`memory::{code_region_write_is_runtime_fault_even_in_ir_mode, byte_mode_rejects_writable_code_region}`(W^X,D4.5) |
| 4 | little-endian | `memory::little_endian_typed_access`、`decode::immediate_inline_is_little_endian_arch_width`、`decode::memory_displacement_inline_is_twos_complement` |
| 5 | 栈帧创建和销毁 | `exec::ir_canonical_call_epilogue_full_trace`(call → 序言 → 局部内存 → leave → ret → exit 闭环逐步断言) |
| 6 | `push/pop` | `exec::{push_pop_use_fixed_8_byte_slots_at_both_widths, push_into_unmapped_stack_fails_memory_fault}` |
| 7 | `call/ret` | `exec::ir_canonical_call_epilogue_full_trace`、`exec::{ret_without_call_is_lenient_on_shadow_frame, call_depth_limit_is_resource_limit}` |
| 8 | `leave` | `exec::leave_with_corrupted_frame_reports_invalid_rip`(返回地址槽劫持红灯,G4) |
| 9 | archBits 双位宽矩阵(32/64 算术、移位、出界) | `arch::{construction_masks_high_bits_both_widths, shift_semantics_both_widths, add_is_modular_and_carry_is_exact, sub_is_modular_and_borrow_is_exact, property_all_ops_stay_in_masked_domain}`、`exec::{arithmetic_flags_matrix_both_widths, shift_amount_is_taken_modulo_archbits}`、`state::full_state_behavior_matrix_at_both_widths` + WP-9 proptest 五项(`prop_add_carry_matches_modular_oracle` 等,u128 oracle) |
| 10 | 自定义指令解释器(直线语义、恒定步数) | `exec::{custom_instruction_straight_line_constant_steps, custom_instruction_memory_access_uses_unified_rejection, custom_instruction_bitmask_micro_op}`、`instr::{custom_instructions_validate_green, custom_instructions_red_matrix}` + WP-9 谓词恒定成本 proptest(T-SC2 属性化) |
| 11 | 作者接口派发(`syscall`/`call`,效果原语封闭集) | `exec::{syscall_builtin_exit_codes_and_halting, syscall_interface_dispatch_runs_effects, call_interface_operand_is_engine_managed, interface_exit_effect_halts_with_code_zero, byte_mode_runtime_undeclared_syscall_is_invalid_action}`、`instr::interfaces_validate_green_and_red` |
| 12 | Canary | `exec::{canary_intact_allows_epilogue, canary_corruption_fails_leave_and_ret}`、`projection::error::canary_violation_echoes_player_written_value_not_canary`(回显玩家写入值非期望值)、`session_lifecycle::canary_spec_happy_path_assembles_slots` |
| 13 | reset | `exec::{reset_restores_initial_state_and_replays_identically, reset_preserves_cumulative_predicate_budget}`、`session_runtime::reset_restores_initial_content_and_keeps_cumulative_budget` |
| 14 | checkpoint | `session_runtime::checkout_resolves_ownership_and_restores_content`、`session_runtime::rollback_cumulative_budget_caps_undo_checkout_reset`、黄金回放 / 黄金向量脚本内 CreateCheckpoint / CheckoutCheckpoint(确定性 ID `cp-0001-ad7aa4a0d491fed4` 入向量) |
| 15 | 快照恢复 | `golden_replay::{snapshot_roundtrip_and_replace_recovery, snapshot_import_enforces_version_lock, state_form_rebuild_matches_and_rejects_layout_drift, snapshot_carries_terminal_status}`、`form_roundtrip::snapshot_roundtrip_carries_stage_state` |
| 16 | 事件重放一致性 | `golden_replay::golden_replay_hash_sequence_identical_across_three_runs`(三路逐字节一致)+ **WP-9 黄金向量** `determinism_vectors::golden_vectors_match_across_profiles_and_platforms` |
| 17 | 随机 seed 确定性 | `judge::seed::{policy_mutual_exclusion_matrix, same_seed_same_stream_and_different_seeds_diverge, derivation_summary_excludes_seed_value}`(seed 零驻留:派生摘要不含 seed 值) |
| 18 | 超时 | `protocol_integration::watchdog_kills_worker_on_action_deadline`(wall-clock 看门狗,进程级) |
| 19 | 指令预算 | `exec::{steps_budget_exhaustion_is_resource_limit, call_depth_limit_is_resource_limit}`、`judge::hidden::exhausted_step_budget_is_resource_limit`、`session_runtime::predicate_budget_exhaustion_surfaces_as_fault` |
| 20 | 崩溃替换恢复 | `session_lifecycle::snapshot_replace_recovery_restores_identical_projection`(进程级 kill → 恢复 → 同输入恒同响应 I-4;TS session-core 对偶集成测试) |

**结论:20 条目全部有可执行测试锚点,无缺项。** 引擎侧补充覆盖(miri / fuzz / 跨平台与双 profile / 溢出构建级验证)见 §一.2、§一.3 与映射文档 ENG-8 ~ ENG-10。

---

## 三、本日门禁汇总(可复跑)

| 门禁 | 命令 | 结果 |
|---|---|---|
| Rust 测试(dev) | `cargo test --workspace`(vm-engine) | **318 通过**(vm-core 167、vm-runtime 43、projection 54、vm-worker 54) |
| Rust 测试(release) | `cargo test --release --workspace` | 全绿(ENG-3 release overflow-checks 复验;黄金向量与 debug 逐字节一致) |
| 纪律门禁 | `node tooling/check-engine-discipline.mjs` | ENG-1 ~ ENG-6 全绿 + 六组反例自检通过 |
| 覆盖率 | `cargo llvm-cov --workspace --exclude vm-worker --fail-under-lines 90` | 通过(vm-core 98.34% / vm-runtime 90.55% / projection 95.14%) |
| miri | `cargo +nightly miri test -p vm-core` | 通过(无未定义行为;ENG-8) |
| fuzz 目标编译 | `cargo check`(vm-engine/fuzz) | 通过(运行面由 CI `rust-fuzz` 在 ubuntu 承载;ENG-9) |
| 跨平台 | `.github/workflows/cross-platform.yml` | 接线(x86-64 + ARM64 × debug + release;ENG-10;本地证据:x86-64 双 profile 逐字节一致) |
| TS 全量 | `pnpm build / typecheck / test / lint / lint:deps / scan:public / fixtures:manifest --check` | 全绿(阶段一面,回归未动) |
| 跨语言冒烟 | `pnpm smoke:contract` + contract-smoke 测试 | 全绿(阶段一面,回归未动) |

新增测试资产(WP-9):proptest 属性测试 5 项(掩蔽域算术 ×4、译码纯函数 ×1、谓词求值器 ×1、COW 一致性 ×1——按文件计 7 个 `prop_*` 函数)+ 确定性黄金向量 1 项 + fuzz 目标 3 个。

---

## 四、ADR-8 tripwire 验收点复核(WP-9 收口)

- **中期复核结论**(2026-09-07,WP-4 完成时):未触发,维持 Rust(记录见阶段二任务分解 §六);
- **验收点复核结论**(本日):**未触发,维持 Rust,回退预案(TS vm-core)关闭**。判据:
  1. WP-0 ~ WP-9 十个工作包全部按序交付,无范围让步;引擎关键路径(WP-3 → WP-4 → WP-5 → WP-6 → WP-7 → WP-8)一次通过全部门禁;
  2. 语言面保持"纯同步状态机子集"(无 async / 无 unsafe / 引擎三 crate no_std)未出现产能摩擦;WP-9 测试强化的增量工具(miri / fuzz / proptest)均为标准工具链,接入成本低于预期(1 个工作包日内完成);
  3. 实现严格落在冻结契约上,阶段二全程无契约返工;跨语言消费面(37 fixture、摘要清单、版本常量)与 TS 侧零漂移;
  4. 下一检查点:无(本 tripwire 为阶段二设计;阶段六 verifier 复用回放引擎时如出现产能摩擦,按 ADR-8 §四重启评估流程,契约与 fixture 不变)。

---

## 五、范围边界复核(任务分解 §三)

阶段二未实现明确不属于本阶段的任何内容:Fastify 路由 / 认证 WSS / embed token(阶段三,编排核心以可注入 `AuthContext` 替身交付)、PostgreSQL / Redis / MinIO 与会话持久化、ProjectionDelta 网络下发、Lit UI / 字节视图(阶段四)、iframe 插件(阶段五)、独立 verifier 服务(阶段六,回放引擎本体已在 vm-runtime 交付供复用)、容器化 Worker 池(T1/T2)、题目内容批量制作(MVP 期)。

---

## 六、遗留与移交阶段三

1. **快照加密静止**:D-W8-11 边界已成文(快照为 worker 所有 SERVER_ONLY blob),编排器存储侧加密归阶段三;
2. **fuzz 深化**:三目标冒烟(`-max_total_time=60`)为 CI 基线;持续 fuzzing(超时加长 / 语料积累 / OSS-Fuzz 式长跑)随阶段六 verifier 复用面扩展时加码;
3. **ARM64 runner 依赖**:cross-platform.yml 使用 GitHub 托管 `ubuntu-24.04-arm`(公开仓库免费);若仓库转私有导致 runner 不可用,回退方案为 `cross` 交叉编译 + QEMU 用户态模拟(黄金向量测试为唯一断言载体,无需全量套件);
4. **投影侧时钟判定**:ZR-P4(T-SC3 时序)统计面归阶段三编排器层(引擎面无时钟,结构前提已满足);
5. **miri 范围**:当前按任务分解要求覆盖 vm-core;vm-runtime(手写 SHA-256 / canon 解析器)与 projection 的 miri 扩展为低成本后续项(同工艺,无新门禁设计)。

---

## 七、结论

阶段二交付**可运行的权威执行域**:Rust VM 引擎四 crate(vm-core / vm-runtime / projection / vm-worker)+ TS 会话编排核心 + 三份实现语义权威规约(指令 / 判题 / 快照与回放 / 投影 / 会话编排)+ 测试与证据归档。318 项 Rust 测试(含 7 项 proptest 属性测试与跨 profile 黄金向量)全绿,覆盖率三 crate 全部 ≥ 90%,miri 无未定义行为,确定性证据(重放 / 差分 / 黄金向量)完备,秘密零驻留机检齐全。**十条退出条件全部通过,阶段二关闭,进入阶段三(投影层与权威 API)。**
