# 阶段五验收评审(WP-56 收口 · 2026-09-12)

对照 `docs/phases/阶段五任务分解.md` §五"阶段退出条件(整体验收)"逐条评审,每条给出证据指针与可复跑命令(门禁数字为本机实跑输出,来源形态逐项注明);门禁证据汇总、决策点定案记录与移交清单见 §三 ~ §六。评审结论:**九条退出条件通过;条件 8(13.4 浏览器矩阵)为环境依赖遗留 ⚠️——WebKit(Windows)12 格不可达登记 linux CI 复跑义务,屏幕阅读器人工抽样与 Windows 高对比度真机抽样两项待人工执行(沿阶段四 axe 先例登记性质与补测锚),不构成退出阻塞——阶段五可退出**。

实跑环境:Windows 11(x64,Git Bash)、Node v24.20.0、pnpm 11.25.0、Docker 29.1.3。**本机实跑(2026-09-12,本次取证)**:TS 构建 / 类型 / lint / 依赖边界(+反例自检)/ 产物隔离扫描 / golden fixture / 契约冒烟 / TS 全量测试(含 vm-engine Rust 双 profile,经 turbo vm-engine 任务)/ unit-only 覆盖率快证。**本机实跑(2026-09-11,WP-55 波次内,证据在仓)**:Playwright E2E 全量 21 用例(chromium 真实浏览器 + compose 全拓扑)、axe 真机报告(归档 `apps/plugin-dev/e2e/reports/axe/2026-09-11/`)、13.4 矩阵 chromium / firefox、`SESSION_API_IT=1` 覆盖率(1904 tests)。**CI 承载**:完整 linux 拓扑 IT 与 compose 套件由 `compose-integration` job 每次 push 复验;ENG-6 完整门禁(fmt + clippy)由 CI `rust-gate` 承载;WebKit 矩阵格按任务分解 §六原定口径归 CI linux runner 复跑(义务登记,§四条件 8)。**人工待办**:屏幕阅读器抽样、Windows 高对比度真机抽样(对象 / 步骤 / 判定标准已在 WP-55 决策草稿 §三登记)。

---

## 一、阶段定位与范围

**目标**(计划书 §十二阶段五原文):完成独立构建产物、postMessage 宿主协议、embed token、自适应高度、主题和语言、静态公开描述包加载、断线重连和错误恢复。

**范围构成**(《阶段五任务分解》§二,七工作包):

1. **WP-50 嵌入交付通道与描述包下发定案(前置)**:embed token 浏览器交付通道(D-API-75)、公开描述包发布清单(D-API-76)、嵌入面运维参数(D-API-77)、M2 下发端点服务端面;
2. **WP-51 embed-runtime 宿主侧 SDK 与 react-wrapper**:握手状态机、V-1~V-13 逐规则执行、port 转移与凭证信封、宿主模拟页(`apps/plugin-dev/host-mock/`);
3. **WP-52 插件 Shell 正式实现**:`packages/web-component` 占位退场,`<pwn-memory-vm>` iframe 内插件视图托管、自包含单产物、能力降级三行、demo 双端口拓扑;
4. **WP-53 主题与语言机制面(vm-ui 增量)**:设计 token 8 变量双套 + i18n 基础设施 + zh-CN/en 双目录全量抽取(~420 键);
5. **WP-54 M2 描述包正式下发接入**:客户端加载器(双闸护栏 + ETag 完整性闸)、FE-ED-06/07 转正式通道、题目静态面渲染、plugin-dev 双通道并存;
6. **WP-55 测试与门禁(横切)**:嵌入协议 E2E、axe `color-contrast` 真机补测、13.4 浏览器矩阵、机检与门禁接线、ZR-B7 收口;
7. **WP-56 验收评审与文档收尾**:本评审成文、D-API-78~82 增补、全部文档回填、门禁取证。

**边界外(未触碰,如实复核)**:

1. **CDN / 生产部署面**:CDN 厂商挂接、生产缓存策略、生产密钥管理未触碰(D-API-76 已铺 ETag / Cache-Control 路,边界裁决 4);
2. **UI 美化**:视觉重设计零触碰(2026-09-11 定案)——WP-53 只交付主题 / 语言机制面,light 变量值 = 现行硬编码值原样,像素级零变化;
3. **M10 出题者积木(FE-PB-04)**:公开描述包交互接口声明面 Schema 演进未启动(触发条件 = 产品决策,校验清单随附登记);
4. **协议演进**:嵌入协议消息类型 / 能力枚举 / 主题取值零扩展,`EMBED_PROTOCOL_VERSION` 维持 1;WSS 通道、12 动作、16 错误码、会话凭证链路、调试通道协议等既有冻结面零改动;vm-engine / 全部 Rust 源码零改动(本阶段零 vm-engine 触碰);
5. **阶段六**:verifier 独立判题、隐藏测试裁决、审计归档、全面租户隔离未触碰;submit 响应面维持 `{submissionId, revision}` 冻结形态。

---

## 二、交付清单(七工作包)

| WP | 范围 | 关键交付 | 测试证据 |
|---|---|---|---|
| 50 | 交付通道与契约定案(前置) | **D-API-75**(token 通道两形态并存:默认 a 宿主服务端注入引导配置 / 备用 b MessageChannel port 下发 opaque 形态;TTL 维持 3600 s / 604800 上限)、**D-API-76**(发布清单不立新契约:复用 `challenge_versions` 双包 SHA-256 摘要 + 端点动态复算;`GET /descriptors/:challengeId/:version`,无凭证 GET + CORS 精确白名单 fail-closed)、**D-API-77**(运维参数构造面:T_handshake 10000 ms、消息字节 65536 / 高度 100000 冻结常量、height_changed rAF 合流 ≤30/s、hello ≤3 次、控制消息 ≤10/s) | `test/routes/descriptor-routes.test.ts`(11 单测 + 红灯矩阵:未登记 404 同形 / 摘要篡改与超限 422 同形 / 字节级断言);容器门控集成 `test/persistence/descriptor-publish.integration.test.ts`(SESSION_API_IT,由 WP-55 IT 形态运行与 CI compose-integration 承载);session-api 400 passed 零回退 |
| 51 | embed-runtime 宿主侧 SDK 与 react-wrapper | `packages/embed-runtime` 框架无关 core(握手状态机 idle→awaiting-hello→ready→unavailable、V-1~V-13 逐规则 13 计数键、max-wins 版本协商、esid CSPRNG 22 字符、T_handshake 超时不可用标记、iframe 重载新 esid 旧值作废、port 转移与 `EMBED_PORT_CREDENTIAL_KIND` 凭证信封、V-10 滑动窗口限速;时钟 / 随机源 / 调度器全注入确定性)+ `packages/react-wrapper` 薄封装(EmbedFrame props+handle)+ 宿主模拟页(签发代理 `/host-api/embed-tokens` + 引导取回 `/host-api/embed-bootstrap`,全量 data-testid);**Q4 定案 = 分层同源**(协议解析器同源 + 无状态构件单实现双侧复用,编排按角色非对称) | embed-runtime 62 用例(覆盖 87.17/80.60/86.89/87.81)+ react-wrapper 8 用例;依赖边界收紧(唯一放行 react-wrapper→embed-runtime)+ 反例自检 |
| 52 | 插件 Shell 正式实现(占位退场) | `<pwn-memory-vm>` 正式实现(`src/plugin/` 五模块 + 装配):esid fragment 一次性读取、hello 重试预算(seq 递增)、ready 消费 + 宿主 origin 钉住(V-11)、T_handshake 超时静态降级(零反射面)、方向过滤 + V-12 丢弃计数;能力降级三行(§4.4);height_changed rAF 合流 + 护栏;`EmbedAppearanceController` 接线位;会话全流程(create_session → `<sm-workspace>`,断线横幅与 sync-projection 由 vm-ui 承接);自包含单产物 dist(约 1.5 MB,lit / vm-ui / protocol / embed-runtime 内联,`codeSplitting: false`);插件文档页零内联脚本(CSP `script-src 'self'` 真实运行证明);demo 双端口拓扑(5173 宿主模拟页 × 5174 插件独立来源,demo-override 增补 5174);**Q3 定案 = workspace 依赖 + vite 单产物**;stub 桩页退场 | 组件 76 用例(jsdom 全链路 + 产物机械断言 `test/artifact.test.ts` 零外部导入);宿主模拟页真实浏览器冒烟六步全 PASS(`host-mock/smoke-embed.mjs`);`scan:public` 零违规 |
| 53 | 主题与语言机制面(vm-ui) | **Q6 定案 = 8 变量功能对比度最小面**(light = 现行值原样 / dark = 功能对比度初值);注入 = `data-sm-theme` 文档级锚样式表 + 自定义属性继承穿透 shadow DOM(组件零硬编码颜色,机械护栏测试);auto 由 CSS `@media` 承担、vm-ui 零 JS 解析;**Q5 定案 = 全量抽取 zh-CN(默认,现行文案原样)+ en**(~420 键,类型强制同键集,既有 518 用例零回退);BCP-47 确定性降级(exact → 主子标签前缀 → zh-CN);`LocaleController` + MutationObserver 运行中切换;锚对接 `data-sm-theme` / `data-sm-language`(**web-component 源码零改动**,68 用例零回退) | vm-ui 541/541(既有 518 + 新增 23:目录完整性 / BCP-47 矩阵 / locale 响应式 / 锚消费与降级 / 主题三值与变量面 / axe 双主题零 violations);真机断言移交 WP-55 |
| 54 | M2 描述包正式下发接入 | 描述包客户端加载器 `packages/vm-ui/src/descriptor/challenge-descriptor.ts`(定位闸 → GET → 字节护栏 262144 B → ETag = 登记摘要完整性闸 → JSON 解析 → 深度 16 / 数组 256 / 字符串 4096 客户端护栏(与服务端 D-API-76 / D-API-31 同值双闸)→ 轻量结构校验(公开 Schema 16 字段锚)→ 强类型视图;失败折叠确定性原因码 + 缺席明示,零会话中断,网络失败至多一次显式重试);FE-ED-06/07 + debugMode 门控 + 静态面(标题 / 简介 / VM Profile / encodingTable,`<details>` 折叠零视觉重设计)由 `<pwn-memory-vm>` 装配管线并行获取、晚到即注入;plugin-dev 双通道(`?descriptor=formal` 经 `/descriptors` 反代,夹具通道缺省保留);**主控修补项如实登记**:WP-54 实跑期发现跨源 ETag 不可读缺口(CORS 未 expose),主控于 session-api CORS 装配增补 `exposedHeaders: ["ETag"]`(D-API-76 增补段,容器重建后本机实测响应头在场)——零源码改动纪律由主控履行,缺口关闭 | 加载器与组件两层篡改红灯齐(摘要不符 / ETag 缺失 / 超限 / 坏形态 / 404 / 网络失败,49 用例);夹具 ↔ 公开 Schema 形态一致性断言(`packages/challenge-schema/test/fixture-consistency.test.ts`,三份语料同绿 + 红灯反例);vm-ui 599 / web-component 76 / plugin-dev 25 / challenge-schema 234 全绿;E2E `descriptor.spec.ts` 3 用例正式下发全链路 |
| 55 | 测试与门禁(横切) | 嵌入面 E2E `e2e/embed-protocol.spec.ts`(9 用例,§5.1 映射表逐行)+ `e2e/helpers/embed.ts`;axe 真机补测 `e2e/axe-contrast.spec.ts`(chromium 全规则含 color-contrast,报告归档);13.4 矩阵 `e2e/browser-matrix.spec.ts`(E2E_MATRIX 门控);机检接线(scan:public 五包 / dependency-cruiser 16 组边自检 / 覆盖率新包计入);ZR-B7 浏览器响应面收口(映射文档 v1.10 🚧→✅) | E2E 全量 **21 passed / 0 failed**(既有 9 + 嵌入协议 9 + axe 3);矩阵 chromium 17/17、firefox 12/12、webkit(Windows)12 格环境不可达(如实登记);axe 六扫描面 × light/dark **零 violations**(dark 初值直通零校准);`SESSION_API_IT=1` 覆盖率 1904 passed,整体四维 **90.06 / 81.58 / 91.05 / 90.54** |
| 56 | 验收评审与文档收尾 | 本评审成文;权威 API 语义规约 **D-API-78~82** 增补(WP-51~54 实现期定案收编;WP-55 为测试基建口径,登记于其决策草稿,不入 D-API);嵌入协议状态行(实现归阶段五 → **阶段五已实现**)与移交节更新(冻结正文零改动);校验清单 M2 关闭(FE-PB-04 / M10 统筹登记随附);拆解 / 实施计划承接标注;web-component README 占位退场核对;plugin-dev README 宿主模拟走查路径登记;本分解条目全勾稽 + 风险表结果列;门禁取证(§三) | 本文 §三 / §四 |

---

## 三、门禁证据汇总(可复跑)

**本机实跑(2026-09-12)**:

| 门禁 | 命令 | 结果 |
|---|---|---|
| TS 构建 + 类型(turbo 全图) | `pnpm build && pnpm typecheck` | build 11/11 任务成功;typecheck 16/16 任务成功(零类型错误) |
| TS Lint | `pnpm lint` | 通过(eslint packages + apps,0 error) |
| 依赖边界 | `pnpm lint:deps` | ✔ no dependency violations found(**1085 modules, 3173 dependencies**;阶段四 977/2846 → 嵌入三新包纳入) |
| 依赖边界反例自检 | `pnpm lint:deps:self-test` | **16 组边期望全部满足**(WP-52 扩展:web-component→embed-runtime / →vm-ui 放行,→web-component / →react-wrapper 全禁红灯) |
| 产物隔离扫描 | `pnpm scan:public` | **R8 通过:5 个公开包已扫描(protocol / vm-ui / web-component / embed-runtime / react-wrapper),0 违规,3 条豁免**(protocol server-only 同构字段名既有豁免) |
| golden fixture | `pnpm fixtures:manifest -- --check` | **214 个 fixture 与清单一致**(嵌入契约零新增,阶段四基线保持) |
| 跨语言契约冒烟 | `pnpm smoke:contract` | **Schema 编译 19 个,有效样例 55 接受,非法样例 115 拒绝,摘要比对 214 条,serde/schemars 消费 15 项,private-bundle 消费 10 项**(与阶段四基线一致,冻结契约零漂移) |
| TS + Rust 全量测试 | `pnpm test` | **22/22 任务成功**。TS 合计 **1875 passed / 38 skipped**(38 skipped = session-api 容器门控 IT 与 compose 拓扑套件)。分包:session-api 400+38skipped / vm-ui 599 / protocol 329 / challenge-schema 234 / challenge-compiler 127 / web-component 76 / embed-runtime 62 / plugin-dev 25 / session-core 15 / react-wrapper 8;**vm-engine Rust 双 profile 684 passed**(debug 342 + release 342,经 turbo vm-engine 任务实跑,与阶段四基线 684 持平零回退——本阶段零 vm-engine 触碰) |
| TS 覆盖率(unit-only 快证) | `pnpm test:coverage` | **exit 0;整体 statements 88.45 / branches 80.57 / functions 88.30 / lines 88.95(≥ 80 四维)**。**形态差异如实登记**:本形态不含 IT 集成装配面;完整门禁形态 = `SESSION_API_IT=1 pnpm test:coverage`(见下行 WP-55 实跑数字,为权威口径),IT×coverage 组合不在本波次重跑,由 CI ts-gate / compose-integration 复验 |
| **TS 覆盖率(完整门禁形态)** | `SESSION_API_IT=1 pnpm test:coverage`(**2026-09-11 WP-55 实跑**,数字自 `docs/develop/阶段五WP55决策草稿.md` §四.4) | **1904 tests 全绿;整体四维 90.06 / 81.58 / 91.05 / 90.54(≥ 80,新包计入)**:embed-runtime 87.17/80.60/86.89/87.81、react-wrapper 93.88/93.75/88.89/95.56、web-component 87.73/79.53/78.64/89.54(分包不设单包门槛,沿 WP-8 登记) |
| **Playwright E2E** | `pnpm build` → compose 全拓扑(e2e global-setup 自动,含 demo-override + E2E override)→ `SESSION_API_HOST_BACKEND_TOKEN=<compose dev 合成值> pnpm --filter @stackmaster/plugin-dev test:e2e`(**2026-09-11 WP-55 实跑**,报告与运行日志在 `apps/plugin-dev/.tmp/`;本波次未重跑,不强制) | **21 passed / 0 failed**(chromium 真实浏览器,串行单 worker,compose 全拓扑 + 5174/5175 webServer):既有 9 用例零回退(WP-F7 六用例 + WP-54 descriptor 三用例)+ 嵌入协议 9 用例 + axe 3 用例;嵌入面 9 用例清单见 §四条件 1 |
| **13.4 浏览器矩阵** | `pnpm --filter @stackmaster/plugin-dev exec playwright install firefox webkit` → `E2E_MATRIX=1 SESSION_API_HOST_BACKEND_TOKEN=… pnpm --filter @stackmaster/plugin-dev exec playwright test e2e/browser-matrix.spec.ts --project=chromium\|firefox\|webkit`(**2026-09-11 WP-55 实跑**) | chromium **17/17 PASS**(六宽度 ×2 面 12 格 + 主题 dark / 键盘 / reduced-motion / forcedColors / 非根路径 5175 共 5 格);firefox **12/12 PASS**;**webkit(Windows)12 格环境不可达**(Playwright WebKit Windows 构建无 WebSocket 支持,REST 正常、WSS 停在 reconnecting)——linux CI 复跑义务,§四条件 8 |
| **axe 真机补测** | `e2e/axe-contrast.spec.ts`(chromium,报告归档 `apps/plugin-dev/e2e/reports/axe/2026-09-11/`,summary.md + 6 面 JSON) | 六扫描面 × light/dark **violations = 0**(全规则含 color-contrast);incomplete 3 处 = sm-vma-list h3 系统色 canvastext/canvas 配对(axe 引擎不可解析系统色,人工复核判定无需处置,dark 锚实机取样正确映射);**dark 变量初值直通零校准** |
| 容器门控集成(IT) | `SESSION_API_IT=1 pnpm --filter @stackmaster/session-api test`;CI `compose-integration` job | **本波次未单独重跑(如实登记)**——IT 面(含 WP-50 `descriptor-publish.integration`、WP-41~44 调试通道集成)已由 WP-55 的 `SESSION_API_IT=1` 覆盖率运行承载(1904 passed 含 IT),并由 CI compose-integration(完整 linux 拓扑,`WORKER_CARGO_PROFILE=release`)每次 push 复验 |
| Rust 纪律 / miri / fuzz / 跨平台 | CI `rust-gate` / `rust-miri` / `rust-fuzz` / `determinism` | 由 CI 承载;本阶段零 vm-engine 触碰,debug+release 双 profile 684 passed 随 `pnpm test` 实跑持平(见上);ENG-6 本机 rustfmt 工具链漂移沿阶段四 §三末登记(环境事项,CI linux `rust-gate` 为准) |

---

## 四、退出条件逐条评审(§五,十条)

### 1. 嵌入协议 v1 全实现:五消息类型、V-1~V-13 逐规则红灯矩阵、handshake 五路径 E2E 全绿;嵌入协议状态行更新为已实现 ✅

- **五消息类型与信封**:收发两侧均消费 `@stackmaster/protocol` 冻结 `EmbedMessageSchema`(零契约改动);宿主侧入站校验序 13 计数键(embed-runtime)+ 插件侧同构校验序(web-component)逐规则落地;
- **V-1~V-13 红灯矩阵**:embed-runtime 62 用例(V-1/V-1'/V-2~V-8/V-10 逐规则 + `state-hello-after-ready` / `unavailable-drop` 状态违规键)+ web-component 76 用例(插件侧 V-1'/V-5/V-7/V-8 + 方向违规)+ E2E 伪造消息矩阵(宿主自投递→`v1-origin-mismatch`;重复 hello→`state-hello-after-ready`;过期 seq→`v7-stale-seq`;方向违规→`v6-wrong-direction`;错 esid ready→`v5-session-mismatch`;协商失败→`v3-unsupported-version`;迟到消息→`unavailable-drop`)——两侧计数递增、零回复、会话不中断;
- **handshake 五路径 E2E 全绿**(`e2e/embed-protocol.spec.ts` 9 用例,§5.1 映射表逐行):①常规握手全链路(签发 → 嵌入 → 握手 → create_session → step → revision 前进,两侧计数零);②iframe 重载(§4.5:新 esid 轮换、旧值按 V-5 拒绝、从空状态重握手);③插件侧握手超时(§4.3 降级显示,静态文案逐字 + 零反射 + 重试入口);④宿主侧 T_handshake 超时(§4.5 不可用标记 + 控制面停止,create_session 零发生);⑤不受信来源与伪造消息矩阵(V-1~V-8+V-12);⑥⑦能力降级两行(§4.4);⑧版本协商失败(§4.3);⑨断线恢复嵌入侧呈现(见条件 6);
- **状态行更新**:`docs/contracts/嵌入协议.md` 状态行已更新为**阶段五已实现**(本 WP-56 交付,冻结正文零改动);D-API-78(分层同源)与 D-API-79(插件侧实现)登记实现语义。

### 2. embed token 浏览器交付通道按定案落地:V-13 合规、浏览器不解析 token、TTL 运维参数生效 ✅

- **定案落地 = D-API-75 两形态并存**:MVP 演示面走通道 a(签发代理 `POST /host-api/embed-tokens` 持 `SESSION_API_HOST_BACKEND_TOKEN` 环境变量 → 引导配置 `POST /host-api/embed-bootstrap` 以 esid POST 体换取 token;token 不进 URL query、不经 postMessage、不进日志);备用通道 b(opaque 形态)经 port 转移后下发(`data=null` 零载荷转移 + `EMBED_PORT_CREDENTIAL_KIND` 凭证信封),E2E 断言转移消息零载荷、凭证值零回显;
- **V-13 红灯可测**:端点契约即排除 query 形态取回(无实现面);E2E 常规握手用例断言 iframe fragment 只携带 esid、消息序列化体不含 token 形态载荷;运行时红灯在伪造消息矩阵落锚;
- **浏览器不解析 token**:web-component 对凭证零回显(`portCredentialReceived` 仅布尔面),`EmbedTokenClaims` 解析器不经任何浏览器包消费(dependency-cruiser server-only 边 + `test/artifact.test.ts` 零外部导入断言);
- **TTL 生效**:`SESSION_API_EMBED_TOKEN_TTL_SECONDS` 维持默认 3600 s / 上限 604800(D-API-19 原样,D-API-75 论证登记:防伪造 / 防重放锚点在签名 + jti 单次消费 + 会话绑定,TTL 只影响未消费滞留窗口)。

### 3. 独立构建产物:dist 自包含、非根路径部署、受限 CSP 兼容;宿主模拟页完整链路演示 ✅

- **自包含单产物**:`dist/index.js` 约 1.5 MB(lit / vm-ui / protocol / embed-runtime 全部内联,`codeSplitting: false`),机械断言 `test/artifact.test.ts`(零外部导入、零相对 chunk、组件注册面完整);
- **非根路径部署**:插件文档页相对路径引用产物;矩阵非根路径格 = plugin-site-server `--base-path` 形态拉起于 5175 子路径、完整嵌入链路 PASS(chromium);
- **受限 CSP 兼容**:零 eval、插件文档页零内联脚本——`test/artifact.test.ts` 静态自测 + demo 服务器 `Content-Security-Policy: script-src 'self'` 头真实运行证明 + E2E 断言 CSP 响应头在场且全功能不受限(嵌入面矩阵格每格断言);
- **宿主模拟页完整链路演示**:真实浏览器冒烟六步全 PASS(WP-52 `smoke-embed.mjs`:握手 → 工作区挂载 → config 接线 → 高度上报 → 主题切换 → 违规计数全零)+ E2E 常规握手用例(签发 → 嵌入 → 握手 → create_session → 动作 → 投影渲染);人工走查路径已登记于 `apps/plugin-dev/README.md`(本 WP-56 回填)。

### 4. 自适应高度 / 主题 / 语言:三能力降级矩阵逐项验证;主题三值(含 auto)与语言切换端到端生效 ✅

- **降级矩阵逐项**:未授予 auto_resize → iframe 高度保持初始 480px、零 height_changed(E2E);未授予 theme/language → 内置默认 light/zh-CN、宿主 `sendThemeChanged` 抛 `EmbedCapabilityNotGrantedError` 会话不中断(E2E + jsdom 能力降级矩阵组共用测试锚);`capabilities` 空数组 = 完全静态形态;
- **主题三值**:light/dark 由宿主 `theme_changed` 经 `EmbedAppearanceController` 落 `data-sm-theme` 锚(E2E dark 双文档可达 + 变量计算值命中 dark 集);`auto` = WP-52 按插件自身 `prefers-color-scheme` 解析为二值 matchMedia(jsdom 注入缝测试)+ vm-ui 独立形态 CSS `@media` 跟随(零 JS 解析);
- **语言切换**:`language_changed` → `data-sm-language` 锚 → 工作区菜单文本 zh-CN↔en 双向切换 + 未知标签("fr")确定性回退 zh-CN(E2E);BCP-47 降级矩阵 jsdom 全覆盖(精确 / 前缀 / 未知);
- **高度上报**:内容变化驱动 rAF 合流 + 每秒 ≤30 + `MAX_EMBED_HEIGHT_PX` 护栏(web-component 25 用例 handshake/height-reporter 套件;E2E height_changed 到达宿主断言;Playwright/Chromium Windows「按需出帧」环境口径如实登记于 WP-55 草稿 §一,非产品缺陷,真实浏览器人工走查为旁证)。

### 5. M2 正式下发:FE-ED-06/07 转正式通道、题目静态面渲染;哈希校验与尺寸护栏红灯齐;夹具对齐锚 = 公开 Schema 断言保留 ✅

- **正式通道**:`fetchChallengeDescriptor`(vm-ui 加载器)→ `GET /descriptors/:challengeId/:version`(D-API-76 端点)→ `?descriptor=formal` 或插件装配管线消费;hintLadder revealPolicy 双分支 / teachingNote 匹配 / debugMode 门控全部由正式下发数据驱动(ED 组件零改动);
- **静态面渲染**:标题 / 简介 / VM Profile 事实表 / encodingTable 三列表(`<details>` 折叠,零视觉重设计,新文案走 i18n 双目录);
- **双闸与红灯齐**:服务端(字节护栏 / SHA-256 复算比对 / 结构巡检,D-API-76)× 客户端(同值护栏 + ETag 完整性闸 + 轻量结构校验,D-API-82)——红灯逐项:摘要不符 / ETag 缺失 / 超限 / 坏形态 / 404 / 网络失败(加载器与组件两层 49 用例;E2E 未登记 404 → 缺席明示用例);
- **夹具对齐锚保留**:`packages/challenge-schema/test/fixture-consistency.test.ts`(夹具 / E2E 正式语料 / 基线三份对同一校验器同绿 + 红灯反例);plugin-dev 双通道并存(夹具通道缺省,开发态零依赖正式部署);
- **主控修补项(如实登记)**:WP-54 实跑期发现跨源 ETag 不可读缺口,主控于 session-api CORS 增补 `exposedHeaders: ["ETag"]`(D-API-76 增补段 + 断言锚;零契约改动——`ETag` 本就是端点承诺的响应头,增补的是 CORS 暴露面),缺口已关闭。

### 6. 断线恢复嵌入侧呈现:断线横幅 + 重连 sync-projection 在 iframe 形态贯通;零本地 VM 降级路径 ✅

- **E2E(嵌入协议第 9 用例)**:routeWebSocket 注入拦截插件 iframe 直连 session-api 的认证 WSS(close 1000 = 服务端空闲超时语义)→ **iframe 内**断线横幅(明示最近一次公开投影 + 零本地 VM 降级)→ 重连 connected + REST `sync-projection` ≥ 1 次 → **revision 不回退**;
- 沿阶段四既有机制(vm-ui 断线横幅 + SessionClient 指数退避 + sync-projection 对齐)在 iframe 形态贯通,web-component 只保证装配,零本地 VM 降级(结构性问题:浏览器侧不存在 VM 执行代码,`scan:public` 引擎标识零命中)。

### 7. axe `color-contrast` 真机补测完成并归档(阶段四条件 6 遗留关闭)✅

- **真机 chromium + axe-core 4.13,全规则启用含 color-contrast**,六扫描面 × light/dark **violations = 0**(插件 iframe 寄存器视图满内容 / 栈视图 / 降级面 / 壳面);报告归档 `apps/plugin-dev/e2e/reports/axe/2026-09-11/`(summary.md + 6 面 JSON);
- incomplete 3 处(sm-vma-list h3,light/dark 同节点)= 系统色关键字配对(`canvastext`/`canvas`),axe 引擎不可解析系统色数值——人工复核判定无需处置(平台按 color-scheme 保证可读性,dark 锚实机取样正确映射),逐条定案登记于 WP-55 草稿 §二;
- **dark 校准记录:零校准**——dark 变量初值直通,`color-contrast` 零 violations,Q6 反馈回路未触发;
- 真机发现的两处小修(标注展开态 `aria-expanded` 移真实 button 修 aria-allowed-attr + nested-interactive;壳页 tab-area 改 section 修 landmark 嵌套)已随 WP-55 交付,测试锚同步;
- **阶段四条件 6 遗留正式关闭**(补测锚 = `test/ed/axe.test.ts` 豁免清单,jsdom 豁免第 1 条于真机关闭)。

### 8. 13.4 浏览器矩阵:六宽度 × Chrome/Firefox/Safari、主题切换、键盘、reduced-motion、高对比度、屏幕阅读器抽样、非根路径、受限 CSP,报告归档 ⚠️(环境依赖遗留)

- ✅ **已达标面**:六宽度(320/375/768/1024/1440/1920)× 嵌入面 + 壳面,chromium **12/12**、firefox **12/12** PASS(每格:投影渲染可达 + 无横向溢出 + step → revision 前进 + 受限 CSP 头在场);附加维度 chromium 5/5:主题切换 dark(双文档 + 变量计算值)、键盘操作(Tab 序 16 步预算 + Enter 激活 → revision 前进)、reduced-motion(emulateMedia)、高对比度(forcedColors 仿真 + 无破版)、非根路径部署(5175 子路径完整嵌入链路);矩阵口径与执行记录登记于 WP-55 草稿 §三(持久证据 = 该表格 + 各 CI 复跑);
- ⚠️ **WebKit(Windows)12 格环境不可达**:Playwright 官方 WebKit Windows 构建不支持 WebSocket(12 格统一失败于 connection-status = `reconnecting`,REST create_session 201 正常)——嵌入面 iframe 直连认证 WSS 无降级路径。**性质 = 环境依赖(沿阶段四 axe `color-contrast` 先例登记)**:矩阵 CI linux 跑为任务分解 §六原定口径,webkit 格随 CI linux runner 复跑(义务登记,补测锚 = `e2e/browser-matrix.spec.ts` + WP-55 草稿 §三复跑命令);Safari 覆盖由同引擎 WebKit 承载,chromium/firefox 全绿 + webkit 格在 linux 的可跑性由 Playwright 平台支持矩阵担保;
- ⚠️ **屏幕阅读器人工抽样 + Windows 高对比度真机抽样待人工执行**:自动化不能替代抽样验证(WP-55 草稿 §三已登记抽样对象 / 步骤 / 判定标准五条与执行人回填位);替代证据 = forcedColors 仿真的自动化口径已绿 + axe 全规则零 violations + role/aria 结构断言在组件套件;
- **判定:不构成退出阻塞**——自动化可达面全绿,不可达 / 人工项均已登记性质与补测锚,沿阶段四先例处置(阶段四条件 6 的同类遗留已在阶段五关闭,本条遗留的关闭责任面 = CI linux 复跑 + 人工抽样执行,均已在登记处留有可执行口径)。

### 9. 机检与门禁:scan:public 零违规(含新包)、覆盖率 ≥ 80%(新包计入)、dependency-cruiser 新规则带反例、ZR-B7 收口、既有门禁不回退 ✅

- **scan:public**:5 个公开包(三新包计入)0 违规 3 豁免(2026-09-12 实跑);
- **覆盖率**:完整门禁形态(SESSION_API_IT=1)整体四维 90.06/81.58/91.05/90.54 ≥ 80,新包计入(embed-runtime / react-wrapper / web-component 分项见 §三);unit-only 快证 88.45/80.57/88.30/88.95 佐证(2026-09-12 实跑,形态差异注明);
- **dependency-cruiser**:1085 模块 / 3173 边零违规;规则修订(Q3 放行边两条)带反例自检 16 组边全绿(`lint:deps:self-test` 2026-09-12 实跑);
- **ZR-B7 浏览器响应面收口**:`秘密零驻留CI检查项映射.md` v1.10 状态 ✅——落点 = 嵌入面 V-12 失败静默 + 受控日志零内部细节,锚点 = E2E 伪造消息矩阵用例(两侧计数本地递增、零回复、零中断 + console error/warning 抽样为零)+ `pwn-degraded` 静态文案零反射断言;上游清单 §9.1 无新增条目;
- **既有门禁不回退**:TS 全量 1875 passed(阶段四 1625 → 阶段五 +250,零回退);Rust 684 passed 双 profile 持平(零 vm-engine 触碰);fixture 214 条 / 契约冒烟 19/55/115/214/15/10 与阶段四基线一致(冻结契约零漂移);E2E 既有 9 用例零回退;IT 面由 WP-55 IT 形态运行(1904 passed)与 CI compose-integration 承载。

### 10. 文档:D-API 增补、嵌入协议状态行、校验清单 M2 关闭、拆解与实施计划文档回填、本分解条目全勾稽、《阶段五验收评审》成文 ✅

本 WP-56 交付日全部完成(回填清单):

1. **权威API语义规约**:新增 §三·十二「嵌入实现面(阶段五 WP-51~54)」**D-API-78~82** 实现期定案收编(既有 D-API-1~77 零改动;D-API-76 的 ETag exposeHeaders 增补段已在 WP-50 期由主控登记;WP-55 的 E2E / axe / 矩阵口径为测试基建口径,不入 D-API,登记于其决策草稿);头部状态行 / 日期行增补;
2. **嵌入协议**(`docs/contracts/嵌入协议.md`):状态行更新(实现归阶段五 → **阶段五已实现**,登记 D-API-75~83 指针)、日期行、§八移交下游节阶段五行更新为已落地——**冻结契约正文零改动**(只动状态行 / 移交节的登记性文字);
3. **校验清单**(`docs/develop/前端需求现状校验与不匹配清单.md`):M2 条目关闭(FE-ED-06/07 正式通道、静态面、双闸、红灯);FE-ED-06/07 分级 B→A 收敛(统计 A 38 / B 15 / C 1 / D 0);FE-PB-04 / M10 统筹登记随附(触发条件 = 产品决策,未启动如实保持);
4. **拆解 / 实施计划回填**:`前端交互需求拆解.md` §十三 FE-ED-06/07 分级收敛措辞 + 阶段五承接标注;`前端实施计划.md` 头部补阶段五交付行;
5. **web-component README**:占位章节退场核对通过(WP-52 已改,无需补动);
6. **plugin-dev README**:宿主模拟页 × 插件文档页人工走查路径登记(5173/5174/签发代理环境变量/冒烟脚本命令);
7. **本分解条目全勾稽**:§二 WP-50~56 全部 `- [x]` + WP-51 补完成标准核验行;§六风险表逐行补「结果」列;
8. **README 索引**:`docs/README.md` phases/ 表登记本评审。

---

## 五、决策点定案记录

### 任务分解 §六决策点 Q1~Q6

| 决策点 | 定案 | 登记处 |
|---|---|---|
| Q1 token 交付通道形态 | **两形态并存,默认 + 备用**:默认 (a) 宿主服务端注入插件引导配置(POST 体以 esid 换取);备用 (b) MessageChannel port 下发(opaque 形态,port 转移为必需前置);**MVP 演示面定 (a)**(plugin-dev 壳 vite 代理层作宿主后端替身);TTL 维持 3600 s / 604800 上限 | **D-API-75**;WP-51 草稿 §二(wire 形态);`packages/embed-runtime`(port 信封)、`apps/plugin-dev/host-mock/`(演示面) |
| Q2 描述包发布清单契约 | **不立新契约**:复用 `challenge_versions` 双包 SHA-256 摘要 + 端点动态复算比对(摘要真实性上游由 Ed25519 验签担保);签名 URL 路线放弃(无 CDN 前提下契约面最小) | **D-API-76**;`apps/session-api/src/routes/descriptor-routes.ts`;`test/routes/descriptor-routes.test.ts` |
| Q3 web-component 与 vm-ui 装配形态 | **workspace 依赖 + vite 库模式自包含单产物**(占位期"不得依赖 vm-ui"边界解除);dependency-cruiser 放行边扩为 web-component→embed-runtime / →vm-ui,反例自检同步 16 组边;产物自包含纪律不变(机械断言) | **D-API-79**;WP-52 草稿 §一;`packages/web-component`(Q3 依赖面 + `test/artifact.test.ts`) |
| Q4 V 规则引擎落点 | **分层同源**:消息契约层消费 protocol 单一来源(天然同源);V 规则编排层中无状态构件(版本协商、esid 生成/校验、TypeRateLimiter、违规计数键)单实现双侧复用,宿主 / 插件独有编排按角色非对称各自持有(强行共享方向参数化引擎复杂度高于收益) | **D-API-78**;WP-51 草稿 §一;`packages/embed-runtime` 导出面 + `packages/web-component/src/plugin/` 消费面 |
| Q5 语言内置集与 i18n 抽取面 | **全量用户可见字符串抽取(~420 键),MVP 语言集 {zh-CN(默认 = 现行文案原样), en}**(真实译集非机翻);协议词与数据值不入目录(与契约同形性);zh-CN 键化使既有 518 用例零回退;BCP-47 降级确定性 | **D-API-81**;WP-53 草稿 §二;`packages/vm-ui/src/i18n/` |
| Q6 主题层最小面 | **8 变量功能对比度最小面**(light = 现行硬编码值原样像素级零变化 / dark = 功能对比度初值);系统色关键词不入变量面(随 color-scheme 自适应,这是收敛到 8 个的原因);注入 = `data-sm-theme` 锚 + 文档级样式表 + 自定义属性继承穿透 shadow DOM;auto 由 CSS `@media` 承担;对比度以 axe 真机门禁为唯一口径(真机零校准直通) | **D-API-80**;WP-53 草稿 §一;`packages/vm-ui/src/theme/theme-tokens.ts` |

### 实现期定案(五决策草稿收编;全部零契约改动)

| 定案 | 内容 | 登记处 |
|---|---|---|
| 构造参数解析(D-API-77 落地形态) | `createEmbedSession(options)` 单点解析:handshakeTimeoutMs clamp 3000–30000 / heightChangedMaxPerSecond 1–120 / controlMessageMaxPerSecond 1–120 / helloMaxRetries 0–10,非整数负数装配拒绝;maxHeightPx 只可收紧、超上限拒绝装配(与 D-API-77 逐字一致) | **D-API-78**;WP-51 草稿 §二.1 |
| 违规计数键与入站校验序 | 键以规则编号为前缀(`v1-origin-mismatch` … `v10-rate-limit` + `state-hello-after-ready` + `unavailable-drop`),双侧同词汇,E2E 同一断言词汇;入站序:不可用丢弃 → V-1/V-1' → V-2(parse 前字节上限)→ V-3~V-6 → 状态机分发;宿主 API 误用 = 同步类型化错误(不属于 V-12 反馈面) | **D-API-78**;WP-51 草稿 §二.4~6 |
| port 凭证信封 wire 形态 | port 转移消息 `data=null` 零载荷;信封 `{kind: "stackmaster:embed-credential", credential}`;转移后宿主控制面消息改走 port;supportedVersions fail-closed(声明版本必须有对应 Schema) | **D-API-78**;WP-51 草稿 §二.3 / §四 |
| 插件侧协议行为 | hello 窗口 = 1 + helloMaxRetries 发、固定间隔 `T_handshake/(N+1)`、不重试风暴;迟到 ready 仍完成握手撤降级(慢网韧性);V-2 双形态受理(结构化对象 + 字符串 JSON);V-10 双闸(入站外圈 + 出站 height 管道自限);本地计数键 `pwn-height-oversize` / `pwn-height-rate-limit`;降级文案常量表零反射面 | **D-API-79**;WP-52 草稿 §四 |
| demo 拓扑 | 宿主模拟页 5173(plugin-dev vite)× 插件文档页 5174(plugin-site-server 独立来源);引导取回端点对 `PLUGIN_SITE_ORIGIN` 白名单回显精确 ACAO(fail-closed);compose demo-override 增补 5174;业务数据走插件 ↔ session-api 直连不经宿主转发 | **D-API-79**;WP-52 草稿 §二;`apps/session-api/compose/demo-override.yaml` |
| 描述包加载器双闸数值 | 客户端护栏 = 服务端同值装配(maxBodyBytes 262144 / 深度 16 / 数组 256 / 字符串 4096);ETag(= 登记摘要)为完整性闸;失败折叠确定性原因码不进玩家 DOM;装配时序 = 并行获取晚到即注入、缺席不中断会话;plugin-dev 双通道(?descriptor=formal 经反代同源) | **D-API-82**;WP-54 草稿 §一~§五 |
| 主题注入机制与 auto 边界 | 文档级锚样式表(`ensureSmThemeStyles` 幂等)+ 自定义属性继承穿透 shadow DOM(组件零硬编码颜色机械护栏);auto 两条路径互不依赖(WP-52 resolvedTheme matchMedia / vm-ui CSS @media);独立形态 `<sm-workspace theme>` 属性最近锚优先 | **D-API-80**;WP-53 草稿 §一 |
| i18n 固化语义 | 状态字符串按生成时刻 locale 固化;标签页标题打开时刻求值;积木画布按 Blockly 注册时刻固化(运行中切换不追溯,演进项登记 §六);zh-CN 目录为键集事实源(en 类型强制同键集) | **D-API-81**;WP-53 草稿 §二.8 |
| E2E「按需出帧」环境口径 | Playwright/Chromium(Windows)跨源 iframe 按需出帧——height_changed rAF 管道首帧排队、交互后释放;对策 = 先驱动 iframe 内交互再断言;真实浏览器不受影响(非产品缺陷,后续波次沿用同一口径) | WP-55 草稿 §一(测试基建口径,不入 D-API) |
| axe 扫描口径 | 全规则启用含 color-contrast(jsdom 豁免第 1 条真机关闭);宿主页面结构三条规则禁用(标题 / landmark 组织由宿主页面承担,组件面正确性规则全程启用);扫描变体页由真实路由承载(route.fulfill 触发 LNA 拦截不可用);shell dark 面环境不可达(headless 无法真实绘制 root 暗色画布),dark 证据由嵌入面 iframe dark 扫描承载 | WP-55 草稿 §二(测试基建口径,不入 D-API) |

---

## 六、已知遗留与移交清单

### 环境依赖遗留(本阶段新增登记;性质 = 环境不可达 / 人工抽样,非缺陷)

1. **WebKit 矩阵 12 格 linux CI 复跑义务**(条件 8):Playwright WebKit Windows 构建无 WebSocket 支持,本机登记不可达;补测锚 = `apps/plugin-dev/e2e/browser-matrix.spec.ts` + WP-55 草稿 §三复跑命令(`--project=webkit`,linux runner);
2. **屏幕阅读器人工抽样**(条件 8):自动化不可替代;对象 / 步骤 / 判定标准五条已登记(WP-55 草稿 §三),待执行人 / 日期 / 结果回填;
3. **Windows 高对比度真机抽样**(条件 8):forcedColors 仿真自动化口径已绿,真机抽样待人工执行(回填位同上);
4. **IT 门控集成测试本机未单独重跑**:WP-50 的 `descriptor-publish.integration` 等容器门控集成已由 WP-55 `SESSION_API_IT=1` 运行(1904 passed)承载,并由 CI compose-integration 每次 push 复验——本评审登记为 CI 承载,不构成缺口。

### 移交阶段六(沿既有)

5. **verifier 独立判题与正式裁决结果展示、隐藏测试裁决、审计归档、全面租户隔离与行级策略、限流租户预算复核**(submit 响应面维持 `{submissionId, revision}` 冻结形态);阶段三移交项继续有效(OpenTelemetry 可选增量、/metrics 生产暴露面收敛、幂等 / route / rate 键域 T1 演进面)。

### 演进与登记项(非缺陷,均为已定案触发条件的后续项)

6. **积木画布 locale 固化语义**(WP-53):Blockly 积木定义按注册时刻 locale 固化,运行中语言切换不追溯(需 dispose + 重注入画布并处理 payload 状态跨模式保留)——MVP 以"创建时刻 locale"交付,演进项 = Blockly locale 机制接线;
7. **全局单 locale MVP 形态**(WP-53):单文档多工作区多语言需把 store 升级为按子树作用域(目录与 t() 无状态耦合,接口面已预留);
8. **lit-virtualizer 真实浏览器字节行缺陷**(WP-55 移交发现):与「按需出帧」环境模型同族(虚拟化布局依赖渲染帧派发),自研 `<sm-window-list>` 不受影响;修复方向登记归后续 vm-ui 波次;上游修复重试锚沿阶段四登记继续有效;
9. **D-J8 真实实例侧 ASLR / ZR-B13 非平凡复算 fixture / M10 出题者积木(FE-PB-04)**:沿阶段四登记继续有效(M10 随 M2 通道统筹——通道本身已由本阶段交付,Schema 交互接口声明面演进触发条件 = 产品决策,未启动如实保持);
10. **ENG-6 rustfmt 工具链漂移**:沿阶段四 §三末登记(本机环境事项,CI linux rust-gate 为准);
11. **测试基建口径沿用**:E2E「按需出帧」与 axe「宿主页结构规则禁用」两条口径(WP-55 草稿 §一/§二)供后续波次沿用;矩阵 spec 由 `E2E_MATRIX=1` 门控(默认全量套件不含)。

---

## 七、结论

阶段五交付**可运行、可嵌入、可部署的 iframe 插件与嵌入安全全量面**:契约定案先行(D-API-75/76/77:token 通道两形态、发布清单不立新契约、运维参数构造面)→ 宿主侧 SDK(embed-runtime 分层同源 V 规则引擎 + react-wrapper + 宿主模拟页)与插件侧 Shell(web-component 正式实现、自包含单产物、受限 CSP)以冻结契约为唯一耦合面并行交付 → 主题 8 变量与 zh-CN/en 全量 i18n(vm-ui 增量,零视觉重设计)→ M2 正式下发(客户端双闸加载器、FE-ED-06/07 转正式通道、静态面)→ 测试与门禁收口(嵌入面 E2E 9 用例 §5.1 映射逐行、axe 真机补测关闭阶段四条件 6 遗留、13.4 矩阵 chromium/firefox 全绿)。门禁数字:TS 1875 + Rust 684(双 profile,随 turbo 实跑)+ IT 1904(WP-55,SESSION_API_IT=1 形态)+ E2E 21;覆盖率完整形态整体四维 90.06/81.58/91.05/90.54 ≥ 80(三新包计入);`scan:public` 五包 0 违规;fixture 214 条与契约冒烟同阶段四基线(冻结契约零漂移)。既有冻结面(WSS 通道、12 动作、16 错误码、嵌入协议 v1、调试通道协议、vm-engine)零改动——本阶段是纯实现阶段:唯一契约登记动作 = D-API-78~82 对实现期定案的事后收编,全部注明零契约改动。

**九条退出条件通过;条件 8 存环境依赖遗留(WebKit Windows 矩阵格不可达 → linux CI 复跑义务;屏幕阅读器人工抽样与 Windows 高对比度真机抽样待人工执行),已登记性质与补测锚,自动化可达面全绿,不构成退出阻塞——阶段五关闭,进入阶段六(独立判题与扩展)。**
