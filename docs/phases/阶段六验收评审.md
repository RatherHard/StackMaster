# 阶段六验收评审(WP-68 收口 · 2026-09-13)

对照 `docs/phases/阶段六任务分解.md` §五"阶段退出条件(整体验收)"十一条逐条评审,每条给出证据指针与可复跑命令;MVP 验收(计划书 13.6 八条)逐条核验见 §四条件 8 引导的细表;门禁证据汇总、决策点定案与阶段二~五移交勾稽见 §三 ~ §六。评审结论:**十一条退出条件全部通过(其中条件 8 含"性能达标判定按 10.3 / 13.6 纪律留待试用期"注记、条件 10 含 cargo-fuzz 本机不可用由 CI rust-fuzz 承载与 miri 本机实跑启动未完由 CI rust-miri 承载的形态登记,均如实注明,不构成阻塞)——阶段六可退出,T1 退出条件(9.2 / 13.5 / 裁决可复现)达成,T3 扩展评估成文且不含扩展决策(分解 §三纪律)**。

实跑环境:Windows 11(x64,Git Bash)、Node v24、pnpm、Docker 29.1.3(compose host 拓扑 + IT 依赖服务形态)。**本机实跑(2026-09-13,本次取证)**:TS 构建 / 类型 / lint / 依赖边界 / 产物隔离扫描 / golden fixture / 契约冒烟 / TS 全量(turbo 24 任务;vm-engine Rust 双 profile 随 turbo 与 `pnpm test:rust` 双通道实跑)/ `SESSION_API_IT=1` 覆盖率(2281 tests)/ compose host 拓扑 35 用例;miri 本机实跑已启动、在证窗口内未完(单属性测试解释执行长耗时,如实登记,§三)。**本机不可达面(如实登记)**:cargo-fuzz 未安装(`pnpm fuzz:smoke` exit 101)——四个 fuzz 目标(含阶段六新增 `action_log_parse`)由 CI `rust-fuzz` job 每次 push 承载(60 s 限时冒烟 × 4);容器拓扑本机未跑(Docker registry 拉取间歇不可达,WP-62/63/65/67 先例延续)由 CI `compose-integration` job 完整 linux 拓扑承载。**CI 承载**:compose-integration(ts-gate 门禁 + 完整容器拓扑 IT 与 compose 套件 + verifier 构建)、rust-gate(ENG-6 fmt + clippy)、rust-miri(miri 无 UB 权威承载)、rust-fuzz(4 目标)、determinism(跨平台黄金向量)。

---

## 一、阶段定位与范围

**目标**(计划书 §十二阶段六原文):完成 verifier 重放规范化动作日志、隐藏测试裁决、审计日志、租户隔离和限流。在前述闭环稳定后,再评估堆、ROP、防护机制、信息泄露、更完整指令集、后端 WASM 或真实 ELF 实验模式。是否扩展以及优先级由实际教学数据、性能基准和安全评审共同决定。

**范围构成**(《阶段六任务分解》§二,九工作包):

1. **WP-60 裁决呈现通道与异步裁决语义定案(前置)**:Q2 = REST 查询面 `GET /verdicts/:submissionId`(D-API-83)、Q3 = pending → verdicted 单向状态机 + 重询限流新键(D-API-84)、Q3 队列 = PG 轮询 SKIP LOCKED + `verifier_runs` 状态机 + `log_digest` 绑定锚(D-API-85 / 86);
2. **WP-61 verifier 独立裁决服务(信任域 4)**:Q1 = 候选 (b) Node verifier + vm-worker `export_action_log` / `verify` 命令面(ADR-9;D-API-87~89)、服务本体管线与拒裁矩阵、compose 独立服务(独立凭证 + `verifier-net` 网络域)、fuzz 加码与 miri 评估;
3. **WP-62 隐藏测试裁决与正式裁决汇总**:`vm-core::judge` 判题驱动复用(零第二实现)、11 值汇总合成规则五条 + 7×11 矩阵、D-H2 输入槽边界维持、裁决域审计三值发射(D-API-94~96);
4. **WP-63 正式裁决结果呈现(vm-ui 增量)与 ZR-T4 收口**:工作区提交态升级 + `VerdictPoller` 重询、呈现路由实装与 Cookie Path 调宽(D-API-97~99)、ZR-T4 verifier 级复锚(D-API-100);
5. **WP-64 审计面完善与归档**:PgAuditSink + append-only 库层触发器(D-API-91)、kind 十值封闭集合定案(D-API-90,Q5)、归档对象存储往返(D-API-92)、REVOKE 角色治理(D-API-93);
6. **WP-65 全面租户隔离与限流租户预算复核**:PG RLS 全表域 + 连接层租户注入(D-API-101)、Q6 八键逐键定案 + RLS k6 前后对比(D-API-102)、Redis 键域 T1 复核登记(D-API-103)、`/metrics` 生产暴露面收敛(D-API-104);
7. **WP-66 容器级 Worker 隔离(T1)**:Q4 = 缺省进程池零回退 / 容器池显式启用(D-API-105)、执行形态接口与五配置键、语义保持测试面 + 冷启动 / 镜像量化(D-API-106);
8. **WP-67 安全测试收口(9.2 / 13.5)与裁决可复现**:四边界逐格证据表 + 裁决边界攻击面矩阵、13.5 四类取证、裁决可复现矩阵(T1 退出条件两条达成)、侧信道论证、T-SC 取证与 WP-68 移交、阶段五遗留义务核验(《阶段六安全测试收口》);
9. **WP-68 MVP 题目集、T3 扩展评估与验收评审**:题目集部分(8 道 + 26 语料 + T-SC1 逐题 + ZR-B13 接入 + D-J8 决策登记,D-API-107~109,commit 0386340);**本包(WP-68b)**:T3 扩展评估报告成文(《阶段六扩展评估报告》)、MVP 验收 13.6 与退出条件逐条核验(本评审)、全部文档回填(§四条件 11 清单)。

**边界外(未触碰,如实复核)**:

1. **T2**:多实例编排器 + Redis Streams、WSS 网关分离、投影二进制编码、K8s 零触碰;Redis 键域只复核登记(D-API-103),裁决队列以 PG 轮询承载(边界裁决 4 纪律维持);
2. **T3 实现(只评估)**:堆、ROP、防护机制、信息泄露类、更完整指令集、后端 WASM、真实 ELF 实验模式零实现——《阶段六扩展评估报告》为唯一产出,不含扩展决策(分解 §三:是否扩展与优先级由教学数据、性能基准、安全评审共同决定,计划书阶段六原文);
3. **admin 管理后台**(5.5 预留位):零触碰;裁决运营面以受控查询(GET /verdicts)+ 审计承接;
4. **生产部署面**:CDN 挂接、生产密钥管理(KMS / 快照密钥轮换)、多区域、Prom 抓取接线未触碰;D-API-93 / 104 / 105 的"生产连线义务"按登记不实现纪律维持;
5. **既有冻结面零改动**:submit 响应面 `{submissionId, revision}`、12 动作、16 错误码、嵌入协议 v1、调试通道协议、会话 WSS 通道、`VerdictQueryResponse` 五字段上限面全部原样;引擎进程协议 additive 演进(`export_action_log` / `verify`,§4.9)版本维持 1;引擎语义唯一变更 = `ReplayReport.final_status` additive 只读字段(ADR-9 §七评估:不新增 miri 面),`vm-core` 判题语义零变更。

---

## 二、交付清单(九工作包)

| WP | commit | 关键交付 | 主控修补登记(如实) |
|---|---|---|---|
| 60 | fc73c60 | **D-API-83**(REST 查询面 + 呈现链路 = session-api 读裁决域,verifier 零查询面)、**D-API-84**(单向状态机 + 重询限流键默认 30 + 非成绩方向不自动重试 + fail-closed 登记)、**D-API-85**(PG 轮询 SKIP LOCKED + run 状态机 + log_digest 绑定)、**D-API-86**(`VerdictQueryResponse` 契约冻结:清单 §6.10 逐字段论证先行,fixture 合法 6 + 反例 12,contract-smoke 接入) | 无 |
| 61 | 5bcf3fc | **ADR-9**(Q1 定案候选 (b);候选 (a) 否决按 ADR-8 tripwire 条款登记)+ **D-API-87~89**:verifier 服务本体(引用受理 → digest 复算 → 登记行 → 双包取回 → 哈希比对 → seed 互斥 → bundle lock → 一次性 worker verify → 幂等落库)、compose 独立服务(verifier-db-init / verifier-minio-init / verifier-net)、vm-worker `verify_replay` 9 用例、verifier 33 用例、CI 增量(verifier 构建 + fuzz 新目标) | **收尾修一处集成缺口**:adjudicator 廉价结构闸原要求包声明 `engineBuildId` 必为 string,而 private-bundle.schema 该字段非 required(compose 生命周期题目即不声明)⇒ 真实链路合法包被误判 `challenge_invalid`;修正 = 仅 `vmEngineVersion` 必需,构建 ID 缺席跳过 bundle lock 构建级比对(与 worker 侧 Option 语义同构),新增回归锚(缺 engineBuildId 包 ⇒ 正常 verify 报 success)+ compose 复跑全绿 |
| 64 | 2c72b13 | **D-API-90**(kind 七值 → 十值封闭集合,归档排除,双锚冻结)、**D-API-91**(PgAuditSink + `audit_log` 表 + BEFORE UPDATE/DELETE/TRUNCATE 触发器,红灯三连 + 触发器禁用路径拒)、**D-API-92**(归档双对象 + id 单调游标 + 批 ID 幂等 + 完整性校验 fail-closed)、**D-API-93**(session_app 最小授权角色 + verifier 角色 audit_log INSERT 预留) | 无 |
| 62 | f705c02 | **D-API-94~96**:11 值汇总合成规则五条(failed 粗化优先 / 隐藏失败方向 / won∧全过 ⇒ success / 非终态 ⇒ wrong_answer / 驱动错误 ⇒ engine_error)+ 7×11 矩阵逐格可测、`replay_with_final` additive 引擎扩展、verifier 侧审计三值发射(同事务原子)、`verdicts.detail` SERVER_ONLY 机检 | 无 |
| 63 | c76b7f8 | **D-API-97~100**:呈现路由实装(404 同形矩阵 / pending 三字段字节确定 / 429 逐字节 / 503 fail-closed)、重询限流装配 + Cookie Path 调宽(`credentialCookiePath="/"` 覆盖 /sessions 与 /verdicts 两族)、vm-ui 提交态升级(`sm-verdict` 锚 + `data-verdict-state` 三态 + i18n 双目录 14 键 + `VerdictPoller` 2500 ms 恒定节奏)、compose ZR-T4 verifier 级复锚(同日志双提交行裁决逐字面一致) | 无 |
| 65 | 77457c3 | **D-API-101**(`007_row_level_security.sql`:九表 + DEFAULT 分区 ENABLE + FORCE,政策按角色分立,verifier 放行政策非 BYPASSRLS,逐事务 SET LOCAL 注入,三生命周期例外窄面)、**D-API-102**(八键逐键维持定案 + RLS k6 同拓扑双采对比:动作 med 3 ms 零变化 / REST 环 +11 ms / 吞吐 ≈ −4%,教学规模预算内)、**D-API-103**(Redis 四键域复核)、**D-API-104**(/metrics 三选项成文) | 无(实现期发现:PG 对 DELETE 施加双重谓词 ⇒ 保留期清理改两段式,登记 D-API-101) |
| 66 | 6dba7fe | **D-API-105**(Q4 定案 + `WorkerLauncher` 端口三件 + 五配置键三道闸 + 镜像策略复用同锁镜像 + 预算单一真源接线)、**D-API-106**(四层测试:单测 shim 12 + 真实容器 IT 6 + 既有 461 单测 + compose 22 零回退;docker inspect 结构闸七面;冷启动 ≈ 0.26 s / 镜像 152,470,823 B ≈ 145 MiB / 2 GiB / 租户判据式) | 无(容器 IT 镜像门控跳过形态如实登记,CI compose-integration 由 run.mjs 先行构建同一镜像承接) |
| 67 | 3500819 | 《阶段六安全测试收口》:四边界逐格证据表 + 裁决边界攻击面矩阵五格、13.5 四类取证、裁决可复现矩阵八行(T1 退出条件两条达成)、侧信道论证、T-SC 移交登记、阶段五遗留义务四条核验、映射文档 v1.12;新增测试五位(verdict-routes 3 + compose 1 + runtime IT 1 + verify_replay.rs 1) | **收尾修**:compose 风暴用例初版未释放并发会话槽位(租户预算 8),后段用例撞预算闸(429 ≠ 201,实跑红灯)——修复 = 用例收尾显式 `POST /sessions/close` 释放槽位,复跑全绿;属测试间资源租约问题,非被测实现缺陷 |
| 68 | 0386340(题目集)+ 本 commit(收口) | **D-API-107**(8 道渐进式题目集:语料源构造式 DSL + 门禁 44 用例 + 26 语料裁决矩阵真实 worker 全绿 + compose 登记发布 6 用例 + T-SC1 逐题 8×5~7 变体)、**D-API-108**(D-J8 = 不启用真实实例 ASLR,机检入门禁)、**D-API-109**(ZR-B13 接入 draws = 514 非平凡);**WP-68b**:《阶段六扩展评估报告》+ 本评审 + 文档回填(§四条件 11) | **题目集制作期发现 canary 契约两侧冲突**(XS-CANARY-CORR ⟂ 引擎 canary 槽可见性公开):CH-05 以守护标记谓词承担教学、CH-07 派生面携带真实 canary 槽承载 ZR-B13、检查器对"隐藏区域 + canary 同区域"组合的假阳性修正(canary 比对推迟到阶段 B,与 WP-40 §七.2 冻结槽序一致,零契约变更);契约收口归 WP-1 §1.3 挂账(§六) |

---

## 三、门禁证据汇总(可复跑;本机实跑 2026-09-13)

| 门禁 | 命令 | 结果 |
|---|---|---|
| TS 构建 | `pnpm build` | **12/12 任务成功** |
| TS 类型 | `pnpm typecheck` | **17/17 任务成功**(零类型错误) |
| TS Lint | `pnpm lint` | 通过(eslint packages + apps,**0 error**) |
| 依赖边界 | `pnpm lint:deps` | **0 violations(1214 modules,3557 dependencies)**(verifier allowlist 边界与反例自检 `pnpm lint:deps:self-test` 既有绿维持) |
| 产物隔离扫描 | `pnpm scan:public` | **5 个公开包已扫描,0 违规,3 条既有豁免**(protocol server-only 同构字段名,ALLOWLIST 带原因与到期日) |
| golden fixture | `pnpm fixtures:manifest -- --check` | **232 个 fixture 与清单一致**(阶段五 214 → 题目集与裁决面增量后基线,零漂移) |
| 跨语言契约冒烟 | `pnpm smoke:contract` | **Schema 编译 20 个,有效样例 61 接受,非法样例 127 拒绝,摘要比对 232 条,serde/schemars 消费 15 项,private-bundle 消费 10 项**(与 WP-67 基线逐数一致,冻结契约零漂移) |
| TS 全量(turbo) | `pnpm test` | **24/24 任务成功**:session-api **549 passed / 91 skipped**(IT+compose 门控面;WP-68 题目集四套件 85 用例入列:门禁 44 + 裁决回归 26 + T-SC1 9 + ZR-B13 6)/ verifier 66 + 17 skipped / vm-ui 624 / protocol 374 / challenge-schema 234 / challenge-compiler 127 / web-component 76 / embed-runtime 62 / session-core 28 / react-wrapper 8 / plugin-dev 25——**TS 合计 2173 passed / 108 skipped**(WP-67 为 2088 / 102:增量 = 题目集 85,skipped 增量 = mvp compose 6,全部为门控面)|
| Rust 全量(双 profile) | `pnpm test:rust`(与 turbo vm-engine 任务双通道互证) | workspace debug + release 双 profile 全绿:**728 passed / 0 failed**(44 个 test-result 目标行;verify_replay 22 用例双侧;与 WP-67 持平零回退) |
| **TS 覆盖率(完整门禁形态)** | `SESSION_API_IT=1 pnpm test:coverage` | **2281 tests:2243 passed / 38 skipped**(skipped = compose 门控套件 35 + IT 形态单列面);**整体四维 89.96 / 81.31 / 90.94 / 90.49(≥ 80,exit 0 阈值机检通过)**;**verifier 包计入:apps/verifier/src = 91.17 / 83.33 / 90.19 / 91.44**(退出条件 10"verifier 新服务计入"兑现);session-api 84.25 / 82.97 / 75 / 84.46、web-component 85.41 / 68.46 / 74.35 / 87.87、embed-runtime 87.16 / 80.60 / 86.88 / 87.81 等(分包不设单包门槛,沿 WP-8 登记;门槛按整体四维判定) |
| miri | `pnpm test:miri` | **本机实跑已启动、在证窗口内未完(如实登记)**:零错误输出,慢因 = `judge::predicate::constant_cost_predicate_count_under_mutation`(256 轮引擎构造 × 谓词求值属性测试)在 miri 解释执行下为长耗时项(本机 Windows 解释执行显著慢于 CI linux runner;CI `rust-miri` job timeout 60 min 内完成)——**miri 无 UB 结论以 CI `rust-miri` 承载为权威**(ENG-8,阶段二起每次 push 实跑;沿阶段五 §三"miri 由 CI 承载"先例);本包引擎面唯一变更 = `ReplayReport.final_status` additive 只读字段,ADR-9 §七评估不在 UB 敏感面(miri 门槛对象 vm-core 的测试矩阵零变更) |
| fuzz | `pnpm fuzz:smoke` | **本机不可达(exit 101:cargo-fuzz 未安装,`no such command: fuzz`)——如实登记**;证据形态 = ①四个 fuzz 目标在仓(`vm-engine/fuzz/fuzz_targets/`:frame_command / challenge_bundle / byte_decode / **action_log_parse**,WP-61 新增);②CI `rust-fuzz` job 每次 push 逐目标 60 s 限时冒烟(gnu 目标 + ASAN);③fuzz 语料纪律(提交面样本不入 git,空语料起步)维持 |
| **Compose 全拓扑(host 形态)** | `pnpm --filter @stackmaster/session-api test:compose` | **35 passed / 0 failed**(Docker 29.1.3;3 文件 = compose-full-chain 23 用例含全链路裁决演示 / ZR-T4 / 重询限流 / 停机重启落库 / 审计落库 + worker-container 6 用例 + **mvp-challenge-set 6 用例**:8 题登记哈希一致 / 公开描述包下发 / CH-07 参考解生产链路首个 success / CH-04 crash 语料 program_crash / 跨域载荷机检零命中);host 缺省拓扑实跑——容器拓扑本机未跑(Docker registry 拉取间歇不可达,WP-62/63/65/67 先例延续如实登记),由 CI compose-integration 完整 linux 拓扑每次 push 复验 |
| k6 归档 | `apps/session-api/k6/results/`(2026-09-09 容器拓扑首采 + 2026-09-12 host 双采) | 动作 med 3 ms(RLS 前后零变化)/ REST 生命周期环 61 → 72 ms / 吞吐 ≈ −4%(D-API-102;扩展评估报告 §1.2 出处) |
| Rust 纪律 / 跨平台 | CI `rust-gate` / `determinism` | ENG-6(fmt + clippy)与跨平台黄金向量(x86-64 + ARM64 × debug / release)由 CI 承载;本机 rustfmt 工具链漂移沿阶段四 §三末登记(环境事项,CI 为准) |

---

## 四、退出条件逐条评审(§五,十一条)

### 1. verifier 独立裁决闭环 ✅

- **全链路**:submit → `submissions` + `verifier_runs` pending 同事务入队(D-API-88)→ verifier SKIP LOCKED 认领(D-API-85)→ 引用取回 → 六记录项完备性 → 双包取回与哈希比对 → bundle lock → 一次性 worker `verify`(`vm_runtime::replay` 同一实现,ADR-8)→ 隐藏测试 → `verdicts` / `verifier_runs` 落库(幂等)——compose「CH-07 参考解全链路:生产裁决闭环 success」+「CH-04 crash 语料 program_crash」+ full-chain 全链路演示用例实跑绿(mvp-challenge-set 6 + full-chain 23 用例,本机 host 拓扑 35/35);
- **独立部署(信任域 4)**:compose `verifier` 服务 + `verifier-db-init`(最小授权 PG 角色)+ `verifier-minio-init`(独立用户 GetObject 双桶策略)+ `verifier-net` 独立网络域(session-api 不入)——D-API-89;compose 拓扑承载,CI compose-integration 完整容器拓扑每次 push 复验(本机 host 形态 + 容器拓扑本机不可达如实登记,§三);
- **仲裁面红灯齐**:log_digest 篡改 ⇒ run failed 零 verdicts / 六记录项缺项 ⇒ challenge_invalid / 双包哈希不符 ⇒ run failed / 双实例并发消费不重复不丢失(compose 拒裁矩阵 + runtime IT)。

### 2. 裁决可复现(T1 退出条件)✅

《阶段六安全测试收口》§三矩阵八行全绿:①跨 verifier 重启(compose 停机提交 → 重启落库);②跨重启汇总一致(同引用两次独立裁决 `verdicts.detail` 逐字节一致);③跨部署双实例(runtime IT:SKIP LOCKED 分单、同日志同判同明细、双行 completed);④golden 三路一致延伸到裁决面(verify_replay.rs:三个独立 verify 进程结论 / digest / 报告逐字节一致);⑤~⑧独立性矩阵(won ≠ 强制 success / failed 粗化优先 / 非终态 ⇒ wrong_answer / 漂移 ⇒ replay_mismatch,逐格可测)。**六记录项完备**(缺项裁决无效 = challenge_invalid,版本策略 §三);**篡改任一字段 ⇒ replay_mismatch**(篡改矩阵三向);**同锁**(镜像同一性 + worker 装载闸 + verifier bundle lock 三层,版本策略 §四.4)。

### 3. 裁决呈现 ✅

呈现通道按 WP-60 定案契约冻结并实现:`VerdictQueryResponse` 冻结于 protocol(WP-1 §1.3 全流程,fixture 6 + 反例 12;contract-smoke 接入,本机实跑零漂移)+ `GET /verdicts/:submissionId` 实装(D-API-97,rig 级 18 用例矩阵)。**submit 响应面零改动**:`{submissionId, revision}` 冻结形态,契约冒烟 + 既有套件零回退实证。**伪造 won / 成功不改成绩**:ZR-T4 verifier 级复锚(compose 同日志双提交行裁决逐字面一致,D-API-100)+ 伪造行未决期呈现与诚实行逐字节同形(verdict-routes 伪造用例)。**裁决细节零公开面**:pending 恒三字段 / verdicted 五字段上限,`verdicts.detail` SERVER_ONLY 机检(D-API-96);**重询限流生效**:30/min 触顶 429 冻结形态逐字节 + 计数不回退 + 404 同形防枚举(compose + rig 双形态)。

### 4. 审计 ✅

`audit_log` PG 落库 **append-only 库层强制**(BEFORE UPDATE / DELETE / TRUNCATE 触发器红灯三连 + 触发器禁用路径拒,D-API-91);**kind 集合阶段六定案**(D-API-90:七值原样 + 裁决域三值入账 + 归档排除,十值封闭集合双锚冻结 = TS 常量机检 + 库层 CHECK,D-API-59 开口收口);**归档对象存储往返**(落库 → 归档 → 校验 IT + compose 双形态实跑绿;id 单调游标 + 批 ID 幂等 + 完整性校验 fail-closed,D-API-92);**REVOKE 角色治理**(session_app 两本 append-only 账仅 SELECT/INSERT,verifier 角色 INSERT 预留,红灯矩阵 compose 实跑,D-API-93;dev/host 角色治理降级如实登记,CI 始终完整容器拓扑)。

### 5. 租户隔离与限流 ✅

**PG 行级策略全表域启用** + 双层红灯矩阵:19 IT 用例(跨租户零行 / WITH CHECK 拒 / 旁路红灯 / 例外生效不外溢 / verifier 双向 / 生产仓储端到端)+ compose 用例(D-API-101;TDD 红灯 15 → 全绿,WP-65 实跑);应用角色无 BYPASSRLS(`rolbypassrls = false` 机检),逐事务 SET LOCAL 注入,跨租户与"不存在"同形态语义保持。**限流 / 配额默认值复核定案**:D-API-102 八键逐键(数值维持即定案,证据登记)+ 裁决重询键与前端轮询节奏对齐复核 + 容器资源预算判据式回填(D-API-105 兑现)。**`/metrics` 生产暴露面收敛**:D-API-104(端口发布面盘点 + 三选项成文,零端点语义变更)。

### 6. 容器级 Worker 隔离 ✅

**形态交付**:每会话一容器(`--network none` + `--read-only` + `--tmpfs /tmp` + cgroup CPU / 内存 / pids + `--cap-drop ALL` + no-new-privileges + 即弃命名,ADR-3 T1 行逐项)——docker inspect 结构闸七面实跑断言 + 行为级出网即败红灯(worker-container IT 6 用例,本机真实镜像实跑);**进程池 ↔ 容器池开关生效**(五配置键三道闸;缺省 process 零回退,container 显式启用 + fail-closed 探测,session-core 28 + session-api 461 单测 + compose 22 用例零回退,D-API-105;启用前置 = MVP 验收通过,边界裁决 1 登记不翻转——本评审 §四条件 8 即该前置的事实面);**强制终止与崩溃恢复语义零回退**:kill → forced → 容器名零命中(waitExit 即含 rm -f 汇合)/ 崩溃替换恢复 revision 续算 + 投影逐字节还原 / 看门狗退出码 3 两形态同判(D-API-106 语义保持表六行全 ✅)。

### 7. 安全测试收口(T1 退出条件)✅

**9.2 威胁模型测试**:四边界逐格证据表 + 裁决边界攻击面矩阵五格(裁决伪造 / 重放请求伪造 / verdict 重询滥用 / 侧信道观察,逐格可复跑锚)——《阶段六安全测试收口》§一;**13.5 秘密零驻留测试四类逐条证据**:构建产物(scan:public 0 违规 + verifier 产物不进浏览器可达面结构性论证)/ 运行时(verifier seed 零驻留 + 容器零跨会话留存 + 通道录制面零命中)/ 投影泄漏(ZR-P1~P8 逐条指向)/ 客户端篡改(ZR-T1~T4 全 ✅)——§二。**裁决与交互判定独立性矩阵绿**:§三后四行(won ≠ 强制 success / failed 粗化优先 / 一切以重放为准)逐格全绿。**T1 退出条件两条达成**(该文档 §三结语)。

### 8. MVP 验收(13.6)✅

**完整闭环演示** = compose 用例 + 题目集生产链路(mvp-challenge-set compose 6 用例:登记 → 描述包下发 → 会话 → 动作 → submit → verifier → verdicts → GET 五字段,CH-07 success / CH-04 program_crash);**6~10 道渐进式题目集** = WP-68a 8 道(D-API-107)。13.6 八条逐条核验:

| # | 13.6 条目 | 结论 | 证据锚 |
|---|---|---|---|
| 1 | 原始产品定位、教学动机、Web 插件形态和框架化扩展方向没有被削弱 | ✅ | 产品定位与教学因果链未被稀释:8 题集逐题对应十一章教学目标(D-API-107 清单表);Web 插件形态维持独立来源 iframe + 插件 ↔ session-api 直连(裁决数据零嵌入协议帧,V-9 不破,D-API-99);框架化扩展方向维持(公开 / 私有双包 + 版本化 + 宿主零权威语义);11.1 MVP 边界外能力零引入(§一边界复核 2) |
| 2 | 至少一条完整的「输入字节到目标条件」教学闭环可以演示 | ✅ | compose 全链路演示用例(提交 → GET pending 恒三字段 → GET verdicted 五字段)+ CH-07 参考解:payload 字节写入 → 栈投影变化 → `virtual_file_read` 目标条件 → verdict = success(生产链路首个 success);交互半环(打开题目 → 观察栈帧 → 构造 payload → 执行 → 投影 → 失败解释 → 回退重试)由既有 E2E 21 用例(2026-09-11 归档,阶段五评审 §三)+ compose 会话生命周期用例承载;11.1 完成标准句逐要素有锚(篡改浏览器内存不可获密不可改成绩 = ZR-T1~T4) |
| 3 | 题目可以通过受限格式加载、校验和版本化,公开/私有双包字段分离经过检查 | ✅ | 8 题全管线:D-API-23 Ed25519 签发链路登记(版本不可变、双包 SHA-256 摘要)→ compose ChallengeRegistrar 真实登记 6 用例(注册行可查且哈希与构造摘要一致);`loadChallengePair` 全量门禁 44 用例(Ajv Schema + WP-4 检查器全量 + 编译期校验);双包分离 = 字段分类检查器 + `scan:public` 0 违规 + 跨域载荷机检零命中(compose 用例);版本化 = challenge_versions 版本链(重复登记确定性拒绝) |
| 4 | 浏览器只持有公开投影,服务端持有全部权威状态和裁决职责 | ✅ | 投影白名单三重闭环 + ZR-P1~P8(安全收口 §二.3);快照 / 完整事件日志 / 私有包 / seed 仅服务端(分类表 §六);裁决职责 = 信任域 4 verifier 独立重放(ADR-9),呈现面只读裁决域 11 值(D-API-83);浏览器构建零引擎代码(scan:public + lint:deps 边界反例自检) |
| 5 | 失败结果能够解释,而不是只返回通用错误 | ✅ | 错误粗化 + 可解释拒绝:16 错误码冻结形态携带白名单解释字段(拒绝理由映射表机检 + fail-closed 红灯,引擎 error 面既有套件);题目级 publicErrorMapping(CH-01「写入被拒:权限与对齐事实」教学文案,D-API-107);verdict-closed-loop 语料含「写入只读代码区被拒并给出解释」断言;vm-ui 错误解释组件(sm-error-explainer 套件)+ ZR-P6 错误粗化一致性 ✅ |
| 6 | 关键状态可以单步、回退和重放(服务端会话能力) | ✅ | 12 动作契约:step / undo / checkpoint / checkout / reset / sync-projection 全链路用例(compose 会话生命周期 + IT);回放一致性 = golden 三路一致 + 逐项比对(快照与回放语义规约 §五)延伸到裁决面(verify_replay 三路一致);断线恢复 = 重连 sync-projection revision 不回退(E2E 第 9 用例归档);崩溃替换恢复投影逐字节还原(worker-container IT) |
| 7 | 运行时、插件协议、会话边界和秘密零驻留有对应测试 | ✅ | 运行时 = 13.1 引擎全套(miri / fuzz / 双 profile / proptest);插件协议 = 13.3 V-1~V-13 两侧红灯矩阵 + E2E 9 用例(阶段五归档);会话边界 = 13.3 API 验收套件 + 限流 / 跨租户 / 幂等矩阵;秘密零驻留 = 13.5 四类取证(安全收口 §二)+ ZR 28 条映射文档 v1.12/v1.15 |
| 8 | 性能数字经过目标网络和设备 benchmark 后再确定是否达标 | ✅(纪律维持形态,注记) | 纪律 = 「未经 benchmark 不确定达标」:k6 基线三轮归档(本机 Docker 拓扑实测,D-API-73 / 102)且**不设通过阈值、不作性能承诺**(十章 10.3 / 十五章纪律);真实目标网络与设备的 benchmark = 生产部署 / 试用期义务,登记于 k6 归档纪律注记与《阶段六扩展评估报告》§1.2(试用期观察启动判据同源)——"是否达标"的判定按原文留待 benchmark 数据,本阶段不虚判 |

### 9. 扩展评估成文 ✅

《阶段六扩展评估报告》(同日成文):T3 七项候选逐项三维评估——教学数据(D-API-107 每题观察点 + 十五章框架;MVP 试用未开始,如实为"待观察"形态 + 观察启动判据)/ 性能基准(k6 三轮归档 / 容器冷启动与镜像预算 / verifier /metrics 观察面,数字出处逐项标注,无独立裁决吞吐采样如实声明)/ 安全评审(9.2 四边界延伸:每项威胁面增量与前提条件)+ 触发条件建议;**最终是否扩展的决策不在本阶段产出**(分解 §三,报告如实声明);共性结论:七项全部暂不进入实施排期,优先级信号排序仅供决策参考。

### 10. 机检与门禁不回退 ✅

- **TS 全量**:24/24 任务,2173 passed(WP-67 2088 → +85 题目集用例,零回退);**Rust 双 profile**:728 passed 持平零回退;**IT**:`SESSION_API_IT=1` 形态 2281 tests(2243 passed / 38 skipped = compose 门控套件,由 test:compose 单列 35/35 补足);**E2E**:既有 21 用例归档(阶段五形态;嵌入面与本阶段 UI 增量由 vm-ui 624 用例含 sm-workspace-verdict 套件承载);**golden fixture**:232 条一致;**scan:public**:0 违规;**覆盖率 ≥ 80%(verifier 计入)**:整体四维 **89.96 / 81.31 / 90.94 / 90.49**,verifier 包 **91.17 / 83.33 / 90.19 / 91.44**(§三完整门禁形态实跑);
- **引擎纪律门禁**:miri = CI `rust-miri` 承载(本机实跑启动未完如实登记,§三;沿阶段五先例),确定性 = CI determinism(跨平台 × 双 profile 黄金向量)承载,fuzz = 四目标在仓 + CI rust-fuzz 每次 push 承载(本机 cargo-fuzz 不可用 exit 101 如实登记,§三)——**fuzz / 确定性 / ENG-6 三面由 CI 承载的形态沿阶段五 §三先例登记,不构成回退**;
- 契约面零漂移:smoke:contract 20/61/127/232/15/10 与 WP-67 基线逐数一致。

### 11. 文档 ✅

本 WP-68b 交付日全部完成(回填清单):

1. **D-API / ADR 增补**:D-API-107~109(WP-68a,题目集 / D-J8 / ZR-B13)已登记;ADR-9(WP-61)已成文;本包零新增 D-API(T3 评估与验收评审为文档产出,无新实现面决策)——权威API语义规约头部状态行增补 WP-68 收口指向;
2. **四份语义规约状态行(阶段六消费收口)**:判题语义规约(§八移交行"已交付"+ 状态行收口注记)、快照与回放语义规约(状态行收口注记)、引擎进程协议(状态行与 §八 / §九移交行 WP-61 已改,核对通过)、会话编排语义规约(§六移交行已关闭,核对通过);
3. **阶段二~五移交清单勾稽**:集中成表于本评审 §六(先例形态:阶段六安全测试收口 §六同构);
4. **docs/README 索引**:phases/ 表登记《阶段六扩展评估报告》与本评审两行;
5. **本分解条目全勾稽**:§二 WP-68 条目 3 / 4 勾稽 + 交付物与完成标准核验行 + §五退出条件旁注 + §六风险表结果列回填(ADR-8 tripwire / v1 seed D-J8 / verifier 吞吐三行);
6. **《阶段六验收评审》成文**:本评审。

---

## 五、决策点定案记录

任务分解 §六 Q1~Q6 全部定案并回填结果列(详表见分解 §六与对应 D-API / ADR):

| 决策点 | 定案 | 登记处 |
|---|---|---|
| Q1 verifier 进程形态 | 候选 (b):Node verifier + vm-worker 裁决重放命令面(additive,版本维持 1) | ADR-9;D-API-87~89 |
| Q2 裁决呈现通道 | REST 查询面;session-api 读裁决域,verifier 零查询面 | D-API-83 / 86 |
| Q3 异步裁决语义 | pending → verdicted 单向;重询限流 30/min;非成绩方向不自动重试;队列 = PG 轮询 SKIP LOCKED | D-API-84 / 85 |
| Q4 容器池默认形态 | 缺省进程池零回退;容器池显式启用(前置 = MVP 验收通过);判据 = 冷启动 0.26 s / 镜像 145 MiB / 2 GiB / 租户 | D-API-105 / 106 / 102 |
| Q5 审计 kind 集合 | 七值 + 裁决域三值 = 十值封闭集合;归档排除 | D-API-90 |
| Q6 限流与配额默认值 | 八键逐键维持(证据登记);RLS 成本 k6 双采归档 | D-API-102 |

---

## 六、已知遗留与移交清单

### 六·一、阶段二~五移交清单逐项勾稽(集中表;37 行逐行勾稽:已完成(前期阶段)9 / 本阶段完成 12 / 仍开放 12(含部分完成与"同参照"挂账行)/ 持续有效 2 / 同参照行 2——仍开放项全部为环境不可达、人工抽样、登记义务或已定触发条件的后续项,不构成阻塞)

| 来源 | 移交项 | 状态 | 勾稽结论 |
|---|---|---|---|
| 阶段二 §六.1 | 快照加密静止 | **已完成(阶段三)** | D-W8-11 + 快照密文信封 AES-256-GCM 静止(ZR-B5,IT 红灯反例) |
| 阶段二 §六.2 | fuzz 深化(verifier 复用面加码) | **本阶段完成(持续化仍开放)** | WP-61:`action_log_parse` 第四目标 + `pnpm fuzz:smoke` / CI rust-fuzz 各增 60s 冒烟;超时加长 / 语料积累 / 长跑 fuzzing 继续登记(触发 = 解析面演进) |
| 阶段二 §六.3 | ARM64 runner 依赖(GitHub 托管) | 持续有效(未触发) | cross-platform.yml 现状;转私有才触发,回退方案登记不变 |
| 阶段二 §六.4 | 投影侧时钟判定 | **已完成(阶段三)** | `zr-p4-frame-statistics.test.ts`(ZR-P4 / T-SC3 统计面) |
| 阶段二 §六.5 | miri 范围扩展(vm-runtime / projection) | 本阶段评估:不新增(低成本后续项继续登记) | ADR-9 §七:唯一语义变更为 additive 只读字段,不构成 miri 面扩张触发证据;复核触发条件登记 |
| 阶段三 §五.1 | iframe / UI 面 ZR 运行时检查 | **已完成(阶段四 / 五)** | ZR-B7 收口(阶段五)+ 通道录制面机检(阶段三既有) |
| 阶段三 §五.2 | OpenTelemetry 可选增量 | 仍开放(T0 可选,不构成义务) | D-API-72 登记不变;接入点(manager 动作入口 / WSS 流水线)登记;跨服务排障需求实际出现时接入 |
| 阶段三 §五.3 | `/metrics` 生产暴露面收敛 | **本阶段完成** | D-API-104(三选项成文;Prom 抓取接线仍属生产部署面配置,随部署执行) |
| 阶段三 §五.4 | 13.3 iframe 面条目 | **已完成(阶段五)** | embed-runtime 62 + web-component 76 用例 + E2E 嵌入协议 9 用例 |
| 阶段三 §五.5 | token 交付通道 | **已完成(阶段五)** | D-API-75 两形态并存 |
| 阶段三 §五.6 | 公开描述包 CDN 发布与签名 URL | 部分(发布面完成,CDN 挂接开放) | M2 正式下发 = D-API-76(阶段五);CDN 厂商挂接与缓存策略 = 生产部署面,继续登记 |
| 阶段三 §五.7 | verifier 裁决闭环(引用与重放锚) | **本阶段完成** | WP-61 / 62(退出条件 1 / 2) |
| 阶段三 §五.8 | `audit_log` 表域与归档(append-only 语义保持) | **本阶段完成** | WP-64(D-API-90~93;端口语义零改动) |
| 阶段三 §五.9 | 行级租户策略与限流租户规模复核 | **本阶段完成** | WP-65(D-API-101 / 102;D-API-20 开口收口) |
| 阶段三 §五.10 | 幂等 / route / rate 键域 T1 演进面 | 复核完成(实现归 T2) | D-API-103(四键域逐条;T2 触发条件重申) |
| 阶段三 §五.11 | 引擎侧演进(fuzz + miri) | **本阶段完成(同阶段二.2 / .5)** | 见上两行 |
| 阶段四 §六.1 | embed 正式化(web-component / embed-runtime / react-wrapper / token 通道 / 高度主题语言 / 断线呈现) | **已完成(阶段五)** | 阶段五 WP-51~54 + 退出条件 1~6 |
| 阶段四 §六.2 | M2 正式下发通道 | **已完成(阶段五)** | FE-ED-06/07 转正式(D-API-76 / 82) |
| 阶段四 §六.3 | axe `color-contrast` 真机补测 | **已完成(阶段五)** | 六扫描面 × 双主题零 violations,报告归档(阶段五条件 7 关闭) |
| 阶段四 §六.4 | 13.4 浏览器矩阵扩展 | 大部分完成(WebKit 格仍开放) | chromium 17/17 + firefox 12/12(阶段五);WebKit linux CI 复跑 + 人工抽样仍开放(见六·二) |
| 阶段四 §六.5 | verifier 独立判题与正式裁决展示 | **本阶段完成** | WP-60~63(退出条件 1 / 3) |
| 阶段四 §六.6 | 沿阶段三移交项 | 同阶段三行 | — |
| 阶段四 §六.7 | D-J8 真实实例 ASLR | **本阶段完成(决策登记);实现仍开放(登记形态)** | D-API-108:不启用,触发 = 产品决策进入实施排期(当前无排期);四件套演进面登记 |
| 阶段四 §六.8 | ZR-B13 非平凡复算 fixture | **本阶段完成** | D-API-109(draws = 514 非平凡,红灯必命中;阶段四 draws = 0 平凡一致性登记闭合) |
| 阶段四 §六.9 | lit-virtualizer 上游修复重试锚 | 仍开放(演进登记) | 自研 `<sm-window-list>` 在用不受影响;上游修复后重试锚保留 |
| 阶段四 §六.10 | ENG-6 rustfmt 工具链漂移 | 仍开放(本机环境事项) | CI linux rust-gate 为准;沿阶段四登记 |
| 阶段四 §六.11 | M10 / FE-PB-04 出题者积木 | 仍开放(触发 = 产品决策) | 未启动如实保持;通道(M2)已就绪 |
| 阶段五 §六.1 | WebKit 矩阵 12 格 linux CI 复跑义务 | **仍开放(挂账)** | WP-67 §六.1:ci.yml / cross-platform.yml 无 WebKit 步骤,如实再登记;补测锚 = `browser-matrix.spec.ts`(`E2E_MATRIX=1 --project=webkit`,linux runner) |
| 阶段五 §六.2 | 屏幕阅读器人工抽样 | **仍开放(挂账)** | 人工不可代理;对象 / 步骤 / 判定标准五条在 WP-55 草稿 §三,待执行人 / 日期 / 结果回填 |
| 阶段五 §六.3 | Windows 高对比度真机抽样 | **仍开放(挂账)** | forcedColors 仿真口径已绿;真机人工目测待执行(回填位同上) |
| 阶段五 §六.4 | IT 门控 CI 承载确认 | **已完成(WP-67 补足)** | CI compose-integration 确认 + 本机 IT 实跑补足(安全收口 §六.4) |
| 阶段五 §六.5 | 移交阶段六(verifier / 审计 / 租户隔离等) | **本阶段完成** | 本评审退出条件 1~7 |
| 阶段五 §六.6~7 | 积木画布 locale 固化 / 全局单 locale 演进项 | 仍开放(演进登记) | 接口面已预留;触发 = 多语言运行中切换需求 |
| 阶段五 §六.8 | lit-virtualizer 真实浏览器字节行缺陷 | 仍开放(同阶段四.9) | — |
| 阶段五 §六.9 | D-J8 / ZR-B13 / M10 | 同阶段四.7 / .8 / .11 | D-J8 决策登记完成、ZR-B13 完成、M10 开放 |
| 阶段五 §六.10 | ENG-6 | 仍开放(同阶段四.10) | — |
| 阶段五 §六.11 | 测试基建口径沿用 | 持续有效 | E2E「按需出帧」/ axe「宿主页结构规则禁用」口径沿用;矩阵 spec 门控形态沿用 |

### 六·二、本阶段遗留(如实登记;性质 = 环境不可达 / 人工抽样 / 登记义务,非缺陷)

1. **WebKit linux CI 复跑义务**(沿阶段五 / WP-67 继续挂账):CI 无 WebKit 步骤,本机不可达如实登记;补测锚与复跑命令不变;
2. **屏幕阅读器与 Windows 高对比度人工抽样**(继续挂账):人工不可代理;自动可达面全绿(forcedColors 仿真 + axe 全规则零 violations);
3. **容器拓扑本机未跑**(沿 WP-62/63/65/67 先例):Docker registry 拉取间歇不可达;CI compose-integration 完整 linux 容器拓扑每次 push 复验为承载面;verifier-db-init / verifier-minio-init / 角色治理红灯的容器形态取证在 CI 拓扑内实跑(host 形态角色治理降级沿 D-API-87 / 93 如实登记);
4. **cargo-fuzz 本机不可用**(exit 101):四目标由 CI rust-fuzz 承载(§三);本机补装 cargo-fuzz 后 `pnpm fuzz:smoke` 即可复跑(工具链事项);
5. **canary 契约收口**(WP-1 §1.3 挂账):XS-CANARY-CORR ⟂ 引擎 canary 槽可见性语义对齐(D-API-109 登记;CH-05 教学由守护标记谓词承担,不阻塞任何退出条件);
6. **真实实例 ASLR(D-J8)实现**(登记形态):D-API-108 = 不启用,触发 = 产品决策进入实施排期(当前无排期);
7. **T3 扩展**:七项候选全部暂不进入实施排期(《阶段六扩展评估报告》;观察启动判据 = MVP 上线试用);
8. **裁决吞吐专项采样**(ADR-9 §九.3 / 扩展评估报告 §1.2):`verifier_queue_depth` / `verifier_verify_duration_seconds` 观察面已就位,队列深度出现真实信号时启动采样(当前零积压信号);
9. **生产连线义务族**(登记不实现,沿 D-API-93 / 104 / 105):应用最小授权角色连接、`/metrics` 生产暴露面选项执行、容器池 sibling socket 挂载面——随生产部署面执行;
10. **Prom 抓取接线 / CDN 挂接 / KMS 密钥轮换**(生产部署面,分解 §三既有边界):未触碰,随部署面配置。

### 六·三、移交后续阶段

1. **T2(规模与性能)**:多实例编排器 + Redis 键域承载(D-API-103 触发条件)、WSS 网关分离、投影二进制编码、LISTEN/NOTIFY 复核(D-API-85 登记)、按表域分级 RLS 优化选项(D-API-102 预案)、verifier 吞吐采样与队列形态复核(ADR-9 §九)、日志分段重放(ADR-9 §六.3)、`audit_log` 在线裁剪(D-API-92 副本形态);触发 = 10.3 工程目标或并发容量真实瓶颈(benchmark 证据);
2. **T3(能力扩展)**:七项候选的触发条件与优先级信号 = 《阶段六扩展评估报告》§二各④格 + §三;进入实施排期的前提 = MVP 试用期教学数据 + 性能基准 + 安全评审三判据共同决定;
3. **试用期义务**:十五章指标采集启动(D-API-107 观察点开采);真实网络 / 设备 benchmark(13.6 条 8 注记);WebKit CI 复跑与人工无障碍抽样的执行回填。

---

## 七、结论

阶段六交付**可信裁决与安全收口的全量 T1 面 + MVP 验收闭环**:裁决契约先行(D-API-83~86)→ verifier 独立裁决服务(信任域 4,ADR-9 单一回放实现同锚)→ 隐藏测试 11 值汇总(7×11 矩阵逐格可测)→ 正式裁决呈现(V-9 不破、ZR-T4 verifier 级复锚)→ 审计 append-only + 十值封闭集合 + 归档往返 → PG RLS 全表域 + 八键限流定案 → 容器级 Worker 隔离(缺省进程池零回退 + 量化判据)→ 安全测试收口(9.2 四边界 + 13.5 四类 + 裁决可复现矩阵,T1 退出条件两条达成)→ 8 道 MVP 题目集(26 语料裁决矩阵 + T-SC1 逐题 + ZR-B13)→ T3 扩展评估成文(七项三维,不含决策)。门禁数字:TS 2173 passed(24/24 任务)+ Rust 728 passed(双 profile)+ IT 形态 2281 tests(覆盖率整体四维 89.96 / 81.31 / 90.94 / 90.49,verifier 包 91.17 / 83.33 / 90.19 / 91.44 计入)+ compose host 拓扑 35/35 + miri 零 UB(CI rust-miri 承载,本机实跑启动未完如实登记)+ scan:public 0 违规 + 契约冒烟零漂移 + fixture 232 条一致;fuzz 由 CI rust-fuzz 四目标承载(本机不可用如实登记)。既有冻结面(12 动作 / 16 错误码 / 嵌入协议 v1 / submit 响应面 / WSS 通道)零改动;引擎语义唯一变更 = `ReplayReport.final_status` additive 只读字段。

**十一条退出条件全部通过(条件 8 含试用期 benchmark 注记、条件 10 含 fuzz CI 承载与 miri 本机实跑启动未完的形态登记,均如实注明);MVP 验收(13.6)八条逐条证据齐;阶段二~五移交清单勾稽:37 行逐行核验——已完成(前期)9 / 本阶段完成 12 / 仍开放 12 / 持续有效 2 / 同参照行 2(仍开放项全部为环境不可达、人工抽样、登记义务或已定触发条件的后续项,不构成阻塞)——阶段六关闭。MVP 上线试用为 T1 全面启用(容器池切换)与 T3 扩展评估决策的共同前置;后续路线移交见 §六·三。**
