# 阶段四验收评审(WP-45 收口 · 2026-09-11)

对照 `docs/phases/阶段四任务分解.md` §五"阶段退出条件(整体验收)"逐条评审,每条给出可复跑命令与测试锚点(结果数为当日实跑输出);清单 A 类 19 条勾稽表、门禁证据汇总、决策点定案记录与移交清单见 §三 ~ §六。评审结论:**九条退出条件通过;条件 6(axe 对比度真机补测)为环境依赖遗留 ⚠️,不影响阶段退出,移交阶段五真机补测锚——阶段四可退出**。

实跑环境:Windows 11(x64,Git Bash)、Node v24.20.0、pnpm 11.25.0、cargo 1.98.1、rustfmt 1.9.0-stable、Docker 29.1.3。TS 全量、覆盖率(整体的 unit-only 形态)、`SESSION_API_IT=1` 真实 worker 集成、Rust debug+release 双 profile、Playwright E2E(真实 Chromium + compose 全拓扑)均在本机实跑;Compose 拓扑集成另由 CI `compose-integration` job(完整 linux 拓扑,`WORKER_CARGO_PROFILE=release`)在每次 push 复验;ENG-6 完整门禁(fmt + clippy)由 CI `rust-gate`(linux)承载(本机 rustfmt 工具链漂移见 §三末登记)。

---

## 一、阶段定位与范围

**目标**(计划书 §十二阶段四原文):完成公开投影渲染、结构视图、字节视图、寄存器、调用栈、Payload 构造器、diff、时间线、checkpoint、提示和错误解释,并验证键盘操作与基础无障碍体验。

**范围构成**(《阶段四任务分解》§一边界裁决):

1. **设计文档需求集**(FE-X/WS/MV/ST/IN/RG/FV/PB 44 条 + 源文档 2026-09-10 增补 FE-WS-06/07):按双档验收口径(拆解文档 §十二)落地——解题模式档(数据源 = 冻结公开投影,D3 窗口)+ 调试模式档(数据源 = 调试通道,全量语义);
2. **计划书超出设计文档部分**:结构视图、调用栈、diff、时间线、checkpoint、提示、错误解释、键盘与无障碍——以 **FE-ED-01~08** 编号在分解 §二(WP-F9)登记并回填拆解文档 §十三;
3. **ADR-DC1 扩展**(2026-09-10 评审接受,本阶段范围扩展):调试通道契约与后端(轨道 B,WP-40~44)、调试模式档(WP-F8)、工作区解题/调试模式切换(FE-WS-06/07)。唯一新增契约面 = 调试通道协议(WP-40 契约先行,`DEBUG_CHANNEL_PROTOCOL_VERSION = 1`);既有 WSS 通道、12 动作、16 错误码、公开投影族(D3/D5)零改动;
4. **边界外**(未实现,如实复核):iframe/postMessage/`embed-runtime`/`web-component` 实现/`react-wrapper`、embed token 浏览器面、自适应高度/主题/语言、**公开描述包正式下发通道(M2)**——归阶段五;verifier、隐藏测试裁决、正式裁决结果展示、审计归档、全面租户隔离——归阶段六;`packages/web-component` 本阶段仅建包占位零实现(1 个冒烟测试)。

---

## 二、交付清单(三轨道 15 个工作包)

### 轨道 A:前端落地(WP-F1~F7)

| WP | 范围 | 关键交付 | 测试证据 |
|---|---|---|---|
| F1 | 包脚手架与工具链 | `packages/vm-ui`(Lit 3.3.3 + Vite 8 库模式双入口,legacy 装饰器定案)、`packages/web-component` 占位、`apps/plugin-dev` 开发壳(vite publicDir 消费 vm-ui dist 的平台宿主拓扑);dependency-cruiser 前端规则(浏览器包只依赖 protocol 公开入口);三包 vitest+jsdom 与覆盖率纳入根口径 | 根 build/typecheck/lint 全绿;`test/public-api.test.ts` 导出冻结;`test/lit-virtualizer-compat.test.ts` 版本组合冒烟 |
| F2 | session-client 与数据源抽象 | `SessionClient`(REST 5 命令 credentials:include、认证 WSS 首帧版本锚定、clientSeq/baseRevision/idempotencyKey 账本、断线指数退避 + sync-projection 重对齐、truncated→sync、rAF 合帧每帧至多一次、传输层全注入);`ProjectionStore`;`MemoryDataSource` 双档抽象(公开档 D3 窗口语义,越界 = 窗口外 cell 不报错) | `test/client/`(全链路 mock fetch / mock WebSocket)、`test/datasource/projection-data-source.test.ts`、`test/render/`;WP-F2 新代码行覆盖 ≥ 96% |
| F3 | 字节视图核心(公开档) | `<sm-byte-view>` 8 字节行三段布局 + 高地址在下 + 虚拟列表 + rsp/rbp 锚点(M13 窗外明示)+ 对齐偏移 0..7 重排 + 窗口内导航/检索;`<sm-vma-list>` 侧栏(visibleRegions 直读) | `test/views/byte/` 65 测试;新代码行覆盖 98% |
| F4 | 寄存器视图与跳转链 | `<sm-register-view>`(纵向全白名单寄存器、点击复制 + Q8 降级)、FE-RG-04 交叉标注纯函数、`<sm-jump-chain>`(≤3 段横向 + SVG 回环 + 展开 + 链末可见字符延伸、viewport-jump 事件、小端定案) | `test/views/register/`、`test/views/chain/` 46 测试;新代码行覆盖 ≥ 91% |
| F5 | 工作区容器与菜单 | `<sm-workspace>` 列式滚动平铺(Niri 列间 + Hyprland 列内 + pointer 拖拽)、tab-type registry 四类登记(debug 占位)、`<sm-workspace-menu>`(step / reset 终态禁用 + 引导新建(M11 口径)/ 断线横幅 / connection-replaced 手动重连 / 拒绝错误 explanation 呈现)、跨视图集成(VMA 回路、交叉标注行左缘、跳转链行右段 + viewport-jump);byte-view 最小 diff(rowDecorator + scrollToAddress,零行为回归) | `test/workspace/` 285+8 测试;workspace 行覆盖 92.9%;plugin-dev 完整 demo |
| F6 | payload 搭建框架 | 积木 → 12 动作编译器(无头 Blockly 序列化编译、错误即值、客户端编译期求值、64 位回绕、UTF-8 定案、展开上限 256 / 求值步 4096 / 调用深 32、allowedActions 编译期裁剪标红)+ `PayloadStepExecutor`(Q3 原子动作粒度步进状态机;断点积木步进暂停、rejected 停驻手动重试、429 可解释呈现光标不动)+ `<sm-payload-tab>` 三区布局 + 八类积木 + 唯一起始积木 + 中文 tooltip + 「积木步进」菜单动作 + 第三方依赖审计(blockly 13.2.1 Apache-2.0 零运行时依赖) | `test/payload/` 70 测试;新代码覆盖 ≥ 92%;scan:public 含 blockly dist 零命中 |
| F7 | 测试与门禁(横切) | Playwright 最小集(plugin-dev 壳内、compose 全拓扑、串行单 worker、globalSetup 拉拓扑 + seed 出题、每用例唯一 challengeId)、issue-embed-token 开发脚本(凭证只走环境变量)、E2E 复跑/保留拓扑环境变量门控 | `apps/plugin-dev/e2e/` 6 用例真实浏览器全绿(§三门禁表);scan:public 纳入 CI(ts-gate 既有步骤,前端产物覆盖面随 vm-ui/plugin-dev dist 自动纳入) |

### 轨道 B:调试通道后端与契约演进(WP-40~44,ADR-DC1 §四)

| WP | 范围 | 关键交付 | 测试证据 |
|---|---|---|---|
| 40 | 调试通道契约冻结(契约先行) | `DEBUG_CHANNEL_PROTOCOL_VERSION = 1`、12 帧族封闭判别联合(5 请求 + 7 展示/错误)、连接级版本锚定同款自建;调试变体镜像 server-only 契约(与私有包 initialState 逐字段同构、结构性排除 judgingConfig/hiddenTests/seed、draws 规范槽序 = 基址槽→区域秘密槽→canary 槽、algorithmId 锁 splitmix64-stream-v1);16 schema 落盘、36 golden fixture、contract-smoke 扩 §2 实例同判 + serde/schemars 镜像;数据分类清单 v1.11 §6.8/§6.9;协议语义文档 `packages/protocol/docs/调试通道协议语义.md`(含 §九推送模型增补) | `test/debug-frame.test.ts` 帧族矩阵(12 正例 + 红灯)、`test/debug-variant-bundle.test.ts`;fixtures:manifest 214 条一致;smoke:contract 19 Schema / 摘要 214 / serde 15;既有 WSS / D-API-2 / 12 动作零 diff |
| 41 | 调试 worker 生命周期与通道承载 | vm-worker additive 帧族(load_variant / debug_query_state / debug_read_window / debug_apply_recorded / debug_step / debug_run_to_breakpoint / debug_search / debug_instruction_stream / debug_function_table;ENGINE_PROCESS_PROTOCOL_VERSION 维持 1)+ assemble_variant 零装载装配(无判题面、无 seed 解析;IR 模式确定性拒绝);session-api `GET /sessions/debug-channel`(Cookie 认证同模型、连接级版本锚定、幂等 attach)+ `DebugChannelOrchestrator`(attach → 权威动作日志逐条重放对齐,禁止真实快照恢复)+ 限额共用 `SessionActionRateLimiter` 同一每会话桶 + 空闲回收复用保持窗口 + `/metrics` 三指标族;引擎进程协议 §4.8、会话编排语义规约 §七(D-W41-1~6)、权威API语义规约 D-API-74 登记 | `test/debug/debug-channel.integration.test.ts`(SESSION_API_IT,真实 worker:attach 零装载+重放对齐→窗口→断点→检索→展示数据、权威日志零污染、IR 拒绝)、`test/debug/debug-channel.test.ts` 13 用例(锚定/方向/绑定/限额);cargo 双 profile 全绿;零装载三断言 |
| 42 | 调试变体镜像产出 | challenge-compiler `buildDebugVariantBundle`(布局与真实镜像逐字段同构、isHidden 区域与 canary 字节按 draws 规范槽序由调试种子派生、IR 模式确定性拒绝 XC-DEBUG-MODE-IR)+ TS SeedDeriver 移植(splitmix64-stream-v1 逐行对照 vm-core,BigInt u64 wrapping)+ 32+6 条跨语言黄金向量(TS/Rust/Python 三方一致;Rust 对偶测试 `seed_derivation_conformance.rs` 锚定 draw 值)+ ASLR 基址派生定案 D-J10(位宽掩蔽→页对齐→窗口取模)+ 生产 `DebugVariantProvider` 接线(node:crypto 16 字节瞬态种子,不落存储)+ 判题语义规约 §6.1(D-J8/D-J9/D-J10)登记 | `build-debug-variant` 单测 + 属性测试(同种子同序列);`test/debug/debug-variant-replay.integration.test.ts`(SESSION_API_IT:同种子双实例重放一致性 revision/rip/窗口/检索一致、权威日志零污染、IR 拒绝) |
| 43 | 公开描述包 Schema 演进 | `debugMode`(opt-out,缺省 true;策略枚举否决、红灯 fixture 锁定)与 `aslrEnabled`(缺省 false)顶层可选布尔;aslrEnabled=true 时 `startAddressHex` 语义修订为结构描述(Schema 形状不变)、投影仍携带会话真实地址;新检查规则 XS-ASLR-NOTICE;strictness 字段数 14→16;双包Schema语义 v1.6 §2.5、分类清单 v1.12 | challenge-schema 红灯克隆变异 + 规则矩阵测试;夹具 basic.json 携带 debugMode 绿灯;摘要清单再生 |
| 44 | 机检扩展 | ZR-B12(调试通道 S→C 出站帧内存字节字段逐值 ⊆ 变体镜像 `contentHex`;地址/展示文本按 Schema 位置白名单放行 + 走私字节边界检查)+ ZR-B13(TS SeedDeriver 按规范槽序复算基址/隐藏区域/canary 槽,与变体逐项比对);分类清单 v1.13 §9.1 双条目(先登记后落点)+ 映射 v1.9;扫描器/检查器纯模块零依赖;ci.yml compose-integration 步骤名携 ID | 单测 `test/scan/debug-frame-corpus-scanner.test.ts`(17)/ `debug-variant-derivation-checker.test.ts`(7)常规 CI;集成 `test/scan/debug-frame-corpus.integration.test.ts`(SESSION_API_IT,生产 Provider 真实变体 + 真实 worker 全程出站帧零命中);红灯反例见 §四条件 8 |

### 轨道 C:汇合与教学组件面(WP-F8 / WP-F9 / WP-45)

| WP | 范围 | 关键交付 | 测试证据 |
|---|---|---|---|
| F8 | 调试模式档落地 | `DebugDataSource` 填充(推送 + 显式拉取的本地缓存;regions = 公开投影结构映射、扩展面 prefetchWindow/searchAllMemory/step/runToBreakpoint/断点集合)+ `DebugChannelClient`(独立 WSS 消费端)+ `<sm-instruction-view>` 指令视图全量(FE-IN-01~08:三段布局 / rip 锚点三级回退 / jumpTarget 点击 / 函数表 / 双入口检索 / 行断点 / 运行到断点)+ 工作区解题↔调试模式切换(FE-WS-06:debugModeAvailable 门控、换绑重置、payload 页与状态保留 FE-WS-07、what-if 横幅常驻 ADR-DC1 条款 7)+ 跳转链全延伸(FE-ST-08/10,prefetch 驱动)+ ED 七组件工作区挂接 + 断点积木双档接线 + plugin-dev 夹具描述包注入(debugMode/hintLadder/publicErrorMapping,fail-soft) | `test/client/debug-channel-client.test.ts`、`test/datasource/debug-data-source.test.ts`、`test/views/instruction/sm-instruction-view.test.ts`、`test/workspace/sm-workspace-mode.test.ts`、`test/views/chain/sm-jump-chain-extend.test.ts`;plugin-dev `test/descriptor.test.ts` |
| F9 | 教学组件面(FE-ED-01~08) | 七组件属性驱动可独立实例化(`src/views/ed/`)+ 纯函数(`computeByteDiff` / `buildTimeline` / ed-types 本地结构类型——challenge-schema 前端禁 import 纪律)+ axe-core 无障碍套件(8 组件全跑、shadow DOM 穿透红灯反例、color-contrast jsdom 豁免登记);工作区挂接归 WP-F8 | `test/views/ed/` 七组件套件、`test/ed/`(computeByteDiff / buildTimeline / axe)79 测试全绿;新代码四维覆盖 95+/100 |
| 45 | 阶段验收评审与门禁收尾 | 本评审成文;全量门禁取证(§三);十条退出条件核验(§四);文档回填(权威API语义规约 D-API-74、拆解文档 FE-ED 系与 FE-WS-06/07 回填 + Q3/Q5 定案、校验清单 M7/M8/M9/M11 已落地措辞与分级统计更新、实施计划交付标注、本分解条目勾稽、README 索引) | 本文 §三 / §四 |

---

## 三、门禁证据汇总(可复跑;2026-09-11 实跑)

| 门禁 | 命令 | 结果 |
|---|---|---|
| TS 构建 + 类型(turbo 全图) | `pnpm build && pnpm typecheck` | 13/13 任务成功(typecheck 9 包;vm-ui vite 库模式构建 196 modules) |
| TS Lint | `pnpm lint` | 通过(eslint packages + apps,0 error) |
| 依赖边界 | `pnpm lint:deps` | ✔ no dependency violations found(**977 modules,2846 dependencies**;阶段三 561/1800 → 前端三包纳入) |
| 依赖边界反例自检 | `pnpm lint:deps:self-test` | 6 组反例 / 对照全绿 |
| TS 测试(turbo 全图) | `pnpm test` | **18/18 任务成功;TS 合计 1625 passed / 36 skipped**(36 skipped = 容器门控 + compose 套件纪律性跳过)。分包:session-api 389 / vm-ui 518 / protocol 329 / challenge-schema 229 / challenge-compiler 127 / plugin-dev 17 / session-core 15 / web-component 1 |
| **TS 覆盖率**(unit-only 形态) | `pnpm test:coverage` | **exit 0;整体 statements 88.61 / branches 80.62 / functions 88.70 / lines 88.71(≥ 80 四维)**。分包:vm-ui 94.52 / 84.96 / 96.51 / 94.57;protocol 96.93 / 94.30 / 96.55 / 96.88;challenge-schema 92.07 / 86.12 / 90.32 / 91.85;challenge-compiler 94.63 / 80.05 / 98.00 / 94.48;plugin-dev 95.37 / 77.78 / 88.89 / 96.26;session-api 78.29 / 72.36 / 76.99 / 78.61;session-core 76.19 / 68.09 / 91.67 / 76.00。**含前端包且整体四维 ≥ 80**;完整门禁形态 `SESSION_API_IT=1 pnpm test:coverage` 的 IT 装配面覆盖由 CI ts-gate / compose-integration 复验(本机未跑 IT×coverage 组合形态,unit-only 形态已达标) |
| 产物隔离扫描 | `pnpm scan:public` | **R8 通过:3 个公开包已扫描,0 违规,3 条豁免**(protocol server-only 变体镜像同构字段名 `containsSecret` / `isHidden` 误报豁免,WP-40 登记);blockly dist 与前端产物含内零命中 |
| golden fixture | `pnpm fixtures:manifest -- --check` | **214 个 fixture 与清单一致**(阶段三 178 → WP-40 调试契约 +36) |
| 跨语言契约冒烟 | `pnpm smoke:contract` | **Schema 编译 19 个(阶段三 17 → +2 调试契约),有效样例 55 接受,非法样例 115 拒绝,摘要比对 214 条,serde/schemars 消费 15 项,private-bundle 消费 10 项** |
| worker 构建 | `cargo build -p vm-worker --manifest-path vm-engine/Cargo.toml` | dev profile 构建成功(真实 worker 集成前置) |
| **真实 worker 集成** | `SESSION_API_IT=1 pnpm --filter @stackmaster/session-api test` | **50 测试文件 passed / 1 skipped;416 passed / 9 skipped**(9 skipped = compose 拓扑套件,`SESSION_API_COMPOSE` 门控,由 CI compose-integration 承载)。含:debug-channel.integration(2)→ attach 零装载 + 重放对齐 → 任意窗口 → 断点暂停 → 检索 → 展示数据;debug-variant-replay.integration(2)→ 双实例重放一致性;debug-frame-corpus.integration(2)→ ZR-B12/B13 零命中;持久化 / boot / wss 通道集成全绿。如实登记:首跑 1 用例(展示流推送时序断言)在全量并发负载下抖动红灯、单跑与复跑全绿(416/416);首次拉起 deps 与在跑 app 拓扑存在端口竞态(环境操作次序问题,非代码缺陷) |
| **Rust 测试** | `pnpm test:rust`(debug + release 全 workspace) | **exit 0,684 passed**(debug 342 + release 342,各 21 套件;阶段三基线 318 × 双 profile = 636 → 每 profile +24,零回退;cargo 1.98.1) |
| **Playwright E2E** | `compose:app:up` 后 `SESSION_API_HOST_BACKEND_TOKEN=… pnpm --filter @stackmaster/plugin-dev test:e2e` | **6 passed(chromium 真实浏览器,compose 全拓扑)**:创建会话(表单 → running/revision 0/connected)、寄存器投影渲染、栈视图字节行(虚拟列表)、指令步进 revision 前进、payload 三区冒烟、断线注入 → 横幅(零本地 VM 降级明示)→ 重连 → sync 对齐 revision 不回退;拓扑按 as-found 保留在跑(E2E teardown 判定外部托管跳过 down) |
| Rust 纪律 / 覆盖率 / miri / fuzz / 跨平台 | CI `rust-gate` / `rust-miri` / `rust-fuzz` / `determinism` | 由 CI 承载;本机 debug+release 双 profile 实跑持平零回退;`cargo build`/clippy 零告警(WP-41 实跑登记) |

**ENG-6 工具链漂移如实登记(环境事项,非本阶段引入)**:本机 `cargo fmt --check`(rustfmt 1.9.0-stable,2026-09-01 构建)对 **6 个 HEAD 既有文件**报 style-edition 格式漂移(`vm-worker/src/protocol/message.rs`——阶段二时代文件、`vm-worker/src/protocol/worker.rs`、`vm-worker/src/session/{assemble,variant}.rs`、`vm-worker/tests/debug_variant.rs`、`projection/src/display.rs`);本评审零代码改动,红灯集全部为提交历史既有内容——属 rustfmt 版本 style-edition 行为漂移,非阶段四代码质量问题。ENG-6 完整门禁(fmt + clippy `-D warnings`)由 CI `rust-gate`(linux 工具链)承载为准。

**fixture manifest 与 ZR / ENG 映射**:`fixtures:manifest --check` 全绿(214 条);`秘密零驻留CI检查项映射.md` v1.9(WP-44)——ZR-B12 / ZR-B13 双条目登记(单测 ✅ 常规 CI / 集成 ✅ SESSION_API_IT 门控);ZR-B13 集成层如实登记为声明面平凡一致性(现网字节 fixture 无派生槽,draws = 0;非平凡复算 fixture 见 §六演进登记)。

---

## 四、退出条件逐条评审(§五,十条)

### 1. 解题模式档:清单 A 类 19 条逐条验收(字节/寄存器/VMA/锚点/菜单动作对真实会话生效),凭证走 Cookie、动作走认证 WSS ✅

A 类逐条勾稽表(编号 → 实现文件 / 测试;实现均在 `packages/vm-ui/src`,测试在 `packages/vm-ui/test/`):

| # | 条目 | 实现文件 | 测试锚点 |
|---|---|---|---|
| 1 | FE-X-01 组件化封装 | 包结构分模块(workspace / views / payload / client / datasource / render / ed)+ web-component 占位 + plugin-dev 壳;依赖方向 dependency-cruiser `browser-packages-only-depend-on-protocol` | `lint:deps` 零违规;`public-api.test.ts` |
| 2 | FE-X-02 工作区唯一主体边界 | `workspace/sm-workspace.ts`(唯一工作区根;标签页创建/销毁/排布仅由工作区管理) | `test/workspace/sm-workspace.test.ts` |
| 3 | FE-WS-01 容纳/管理/排布标签页 | `sm-workspace.ts` + `workspace-model.ts`(打开/聚焦/关闭/拖拽换位/序号稳定) | `test/workspace/`(布局模型) |
| 4 | FE-WS-02 滚动平铺结合 | `sm-workspace.ts`(列间 Niri 水平滚动 + 列内 Hyprland 二叉分割 + pointer 拖拽) | 同上(双页并排 + 列间滚动可达 + 拖拽模拟) |
| 5 | FE-WS-03 顶部工作区菜单 | `workspace/sm-workspace-menu.ts`(打开分组注册表驱动 / 运行选项 / 状态呈现) | `test/workspace/sm-workspace-menu.test.ts` |
| 6 | FE-WS-04a 指令步进 | `sm-workspace-menu.ts` step 菜单动作 → `step` 动作(恰一条指令后暂停) | **真实会话**:E2E `session.spec.ts`「指令步进 → 投影 revision 前进」;`test/workspace/`(真实 SessionClient mock 全链路 step 帧形态) |
| 7 | FE-MV-01 四种子视图容器 | `workspace/tab-registry.ts`(stack / free / registers / debug + payload 五类登记,内存四视图独立标签页、可多开) | `test/workspace/`(注册表默认四类 / 可扩展 / 多开) |
| 8 | FE-ST-01 栈视图默认渲染 | `views/byte/rows.ts` + `byte-view.ts`(8 字节行、高地址在下、地址对齐) | `test/views/byte/` |
| 9 | FE-ST-02 行三段布局 | `byte-view.ts`(左段最低位字节地址 / 中段 hex / 右段特殊显示) | `test/views/byte/byte-view.test.ts` |
| 10 | FE-IN-05 rip 视角锚点 | `views/instruction/sm-instruction-view.ts`(paused → attach paused → RIP 公开值三级回退 + 回锚) | `test/views/instruction/sm-instruction-view.test.ts` |
| 11 | FE-RG-02 寄存器行三段布局 | `views/register/sm-register-view.ts`(名 / 0x 大写值 / 特殊显示) | `test/views/register/` |
| 12 | FE-RG-03 特殊显示点击复制 | `sm-register-view.ts`(clipboard 注入 + Q8 降级:选中文本 + 手动复制提示) | `test/views/register/`(成功 / 降级双路径) |
| 13 | FE-FV-01 自由视图默认渲染 | `byte-view.ts`(`view-kind="free"` 与栈视图共用默认形态) | `test/views/byte/` |
| 14 | FE-FV-02 自由视图行布局与特殊显示 | `byte-view.ts` + `render/special-display.ts` | `test/views/byte/` + `test/render/special-display.test.ts` |
| 15 | FE-FV-06 VMA 列表跳转 | `views/byte/vma-list.ts`(visibleRegions 直读、按地址有序、vma-select → showRegion 回路) | `test/views/byte/vma-list.test.ts`;E2E 寄存器/栈视图投影渲染 |
| 16 | FE-PB-01 payload 三区结构与拖拽搭建 | `payload/sm-payload-tab.ts`(左 Blockly 画布 light DOM / 右上原子步骤程序区 / 右下输出区) | `test/payload/sm-payload-tab.test.ts`;E2E `payload.spec.ts`(真实浏览器) |
| 17 | FE-PB-03 唯一起始积木 | `sm-payload-tab.ts`(起始积木预置、`deletable=false`) | `test/payload/` |
| 18 | FE-PB-05 Blockly 选型 | `google-blockly` 13.2.1 采纳(编译器仅消费无头 workspace 序列化);依赖审计登记(vm-ui README) | `test/payload/blocks.test.ts`(无头可实例化);scan:public 含 blockly dist 零命中 |
| 19 | FE-PB-06 悬停显示积木功能 | `payload/compiler/blocks.ts`(全部积木中文 tooltip,UI 与编译器唯一共享源) | `test/payload/blocks.test.ts`(tooltip 齐备) |

- **对真实会话生效**:E2E 6 用例跑在 compose 全拓扑真实会话上(创建会话 → Cookie 凭证交付 → 认证 WSS → step 动作 → 投影 revision 前进);组件级由 `test/workspace/` 的真实 `SessionClient` mock 全链路套件对冲(step / reset 帧形态、踢旧重连、rejected 呈现、投影回流刷新);
- **凭证走 Cookie**:SessionClient 全部请求 `credentials: "include"`(D-API-12;`test/client/session-client.test.ts` Set-Cookie 罐断言);**动作走认证 WSS**:`GET /sessions/channel` 升级即 Cookie 认证、首帧版本锚定(同上);
- D 类 2 条(FE-ST-05 / FE-FV-04)不在本条件口径内(解题档维持 D3;调试档全量满足见条件 3)。

### 2. 工作区:列式滚动平铺、多标签页、模式切换(FE-WS-06/07)、reset 终态禁用并引导新建 ✅

- 列式滚动平铺 + 多标签页多开 + pointer 拖拽:`test/workspace/sm-workspace.test.ts`(双页并排、列间滚动可达、拖拽换位含跨列与开新列、同类型多开、生命周期与空态);E2E 真实浏览器验证;
- **模式切换(FE-WS-06/07)**:`debugModeAvailable` 门控(plugin-dev 夹具描述包 `debugMode` 注入,未启用题目隐藏切换项)、切换 = 数据源换绑(锚点/滚动重置)、payload 页与状态保留、断点积木双档(解题档步进暂停 / 调试档并入调试断点)、client 换绑确定性退回解题模式——`test/workspace/sm-workspace-mode.test.ts`;
- **reset 终态禁用并引导新建(M11 口径,Q5 定案)**:终态(won/failed)菜单项禁用 + 「测试环境已结束,请新建会话」→ `new-session-request` → 宿主 `close_session` + `create_session`——`test/workspace/sm-workspace-menu.test.ts` 禁用矩阵 + `apps/plugin-dev/test/`(新建会话流程)。

### 3. 调试模式档:调试通道端到端(attach → 任意窗口 → 断点 → 全内存检索 → 指令流/函数表),what-if 纪律显式呈现,"调试通过 ≠ 提交通过"不可误读 ✅

- **端到端集成(真实 vm-worker 二进制,SESSION_API_IT 门控)**:`test/debug/debug-channel.integration.test.ts`——attach(零装载 + 权威动作日志重放对齐)→ `debug_window` 任意窗口 → `debug_run_to_breakpoint` 断点暂停 → `debug_search` 全内存检索 → `debug_instruction_stream` / `debug_function_table` 展示数据;权威日志零污染断言;IR 模式确定性拒绝;`test/debug/debug-variant-replay.integration.test.ts` 双实例重放一致性;
- **what-if 横幅呈现**:调试模式下工作区顶部常驻「调试通过 ≠ 提交通过(裁决以提交为准)」+ ASLR 地址差异提示语——`sm-workspace-menu.ts`(ADR-DC1 条款 7 UI 义务);`test/workspace/sm-workspace-mode.test.ts` 横幅显隐断言;
- **指令流/函数表推送模型**(协议语义 §九定案):attach 后推 `debug_function_table` 恰一次、每次暂停后紧跟推暂停落点 `debug_instruction_stream`(`DEBUG_CONTEXT_INSTRUCTION_ITEMS = 16` 服务端常量;协议 v1 无 C→S 拉取帧);推送帧不带 requestId(唯一例外 = attach 伴生函数表回显);发射点归一化 worker null 线形为键缺席后逐帧过冻结 `DebugFrameSchema` 自检——集成端到端断言 + 单测帧序/归一化/失败路径四用例(展示流推送落地提交 eec9695);
- **遗留登记(不影响本条件验收口径)**:真实实例侧 ASLR 基址派生 = 引擎契约演进项 **D-J8(登记未实现)**——调试侧已按 D-J10 落地,ASLR 开启题目调试实例呈「同结构、异基址」兄弟形态,教学语义由 what-if 横幅承接,verifier 零改动(判题语义规约 §6.1)。

### 4. payload 搭建:三区结构、八类积木、唯一起始积木、悬停提示;积木→动作编译器步进执行受 `allowedActions` 与限流约束 ✅

- 三区布局 / 八类积木(变量、列表、分支、循环、模块化、运算赋值、字符串操作、断点 + 增设会话动作与公开投影读取两类承载映射定案)/ 唯一起始积木(`deletable=false`)/ 中文 tooltip 悬停——`test/payload/blocks.test.ts`、`sm-payload-tab.test.ts`;E2E `payload.spec.ts` 真实浏览器三区冒烟;
- **编译器步进执行约束**:Q3 定案 = 原子动作粒度(每原子一步,循环/分支在原子边界暂停);`allowedActions` **编译期裁剪**(不在白名单 → 编译错误带 blockId 标红,缺省白名单 = 12 动作裁去 `run_to_event`);**限流约束**:429 / `budget_exhausted` → 执行器 error 态 + 可解释 `PublicError` 呈现、光标不动、手动重试(`test/payload/executor.test.ts` fake sink 时序;展开上限 256 防循环展开大量动作,与 D-API-50~53 对表);
- 求值数据源仅限公开投影(求值环境 = 公开投影只读快照;未知引用 / 窗口外地址确定性报错)——底线约束的 UI 侧落法(`test/payload/compile*.test.ts`)。

### 5. 教学组件面 FE-ED-01~08 按 WP-F9 口径验收 ✅

- FE-ED-01~07 七组件逐一交付并挂接工作区(结构视图 highlight-jump 联动 / 调用栈截断明示 / diff 前值不可知明示 / 时间线 checkpoint 去重 / checkpoint 两步确认与终态禁用 / 提示 ladder revealPolicy 双分支 / 错误解释能力矩阵缺席形态 + teachingNote)——`test/views/ed/` 七组件套件 + `test/ed/` 纯函数套件(79 测试全绿;新代码四维覆盖 95+/100);
- FE-ED-06/07 数据面 = 公开描述包 `hintLadder` / `publicErrorMapping`(Schema 字段现成),交付通道以 **plugin-dev 夹具描述包注入变通**(M2 正式通道归阶段五)——分级口径如实标注为 B(变通已实现 ≠ 通道正式化,见校验清单回填);
- FE-ED-08 见条件 6。

### 6. 键盘操作与基础无障碍验证通过(axe-core 报告归档)⚠️(环境依赖遗留)

- ✅ 已落地面:全视图键盘可达(Tab 序、Enter/Space 原生激活、`:focus-visible`)、语义化 DOM(嵌套列表 / ol / table+caption+scope / dl / 原生 button+input)、状态/截断/当前/锁定信息全部文本承载(非视觉唯一载体)、动画零;**axe-core 套件归档**:`test/ed/axe.test.ts` 8 组件代表性满内容挂载零 violations,含 light DOM + shadow DOM 双红灯反例(无名称按钮必须被检出,证明机检穿透 shadow DOM 真实生效);
- ⚠️ **遗留性质 = 环境依赖(登记)**:`color-contrast` 规则在 jsdom 下禁用豁免(jsdom 无布局引擎与真实 CSS 级联,对比度不可判定,任何结论都是环境伪影;豁免逐条登记于测试文件头,未整体跳过)——**真实浏览器对比度补测移交阶段五**(真机 Playwright + axe 报告归档;补测锚 = `test/ed/axe.test.ts` 豁免清单与本条登记);
- 计划书 13.4 的屏幕阅读器抽样验证、多宽度 / 多浏览器 / reduced-motion / 高对比度矩阵:自动化无障碍检查不能替代抽样验证,归阶段五嵌入面(E2E 浏览器矩阵扩展)一并承接。

### 7. 断线重连 `sync-projection` 演示;浏览器零本地 VM 降级路径 ✅

- **E2E 演示(真实浏览器 + 全拓扑)**:`disconnect-recovery.spec.ts`——服务端侧断开 → 断线横幅(最近一次公开投影 revision N + 重连中,attempt / retryDelayMs)→ 重连 → `sync_projection` 对齐,断言 **revision 不回退**;零本地 VM 降级在横幅文案中明示并由用例断言;
- 组件级:`test/client/session-client.test.ts`(指数退避重连 → sync 对齐 → 新 revision 继续;close 1000/1001/1009/1013 语义化分发、1008 踢旧不自动重连)、`sm-workspace-menu.test.ts`(断线横幅 / connection-replaced 手动重连);
- **零本地 VM 降级路径**:结构性问题——浏览器侧不存在任何 VM 执行代码(TS 构建图不含 vm-engine,`lint:deps` 反例自检 + `scan:public` 引擎标识零命中);断线时仅展示最近一次收到的公开投影(投影存储只读),重连唯一对齐路径 = REST `sync-projection`(D-API-47 纪律,`test/workspace/` 断线横幅套件)。

### 8. 机检:调试通道语料包含性断言零命中 + 红灯反例;`scan:public` 产物隔离零命中 ✅

- **ZR-B12(帧语料 ⊆ 变体镜像)**:S→C 帧内存字节字段(`debug_window_data.bytesHex` / `debug_search_results.hits[].bytesHex` / `debug_instruction_stream.instructions[].bytesHex`,封闭枚举 `DEBUG_FRAME_MEMORY_BYTE_POSITIONS`)逐值子串包含断言,语料全集 = 变体自身 `contentHex` + 玩家输入回显封闭集;单测 `test/scan/debug-frame-corpus-scanner.test.ts`(17 用例,常规 CI + compose-integration `vitest run test/scan` 同步承载);
- **ZR-B13(变体派生复算)**:TS SeedDeriver 按规范槽序复算基址 / 隐藏区域 / canary 字节,与变体 `contentHex` / `baseAddresses` / `draws` 逐项比对;单测 `test/scan/debug-variant-derivation-checker.test.ts`(7 用例;ASLR 开/关双形态、draws 记账 514/513 一致);
- **集成零命中**:`test/scan/debug-frame-corpus.integration.test.ts`(SESSION_API_IT;生产 Provider 捕获真实装载变体 × 真实 vm-worker,attach→window→step→search→instruction_stream→function_table 全程出站帧零命中 + ZR-B13 复算零命中);
- **红灯反例清单(与零命中同文件同套件)**:①注入占位秘密语料(`SECRET_FLAG_PLACEHOLDER` / `SEED_HEX_FIXTURE`)→ 必命中;②变体外合成随机字节 → 必命中;③越界地址帧 → `ZR-B12-address-outside-variant`;④文本走私超长十六进制串 → `ZR-B12-text-smuggled-bytes`;⑤错误种子复算 → 基址/隐藏区域/canary 内容必命中;⑥变体隐藏区域 / canary 槽单字节篡改 → 各自必命中;⑦算法标识改写 / ASLR 关携带 baseAddresses / canary 槽越界 → 各自必命中(ZR-B13 集成层错误种子红灯不可构造,如实登记为声明面平凡一致性,见 §六);
- **`scan:public` 产物隔离**:3 个公开包已扫描、0 违规、3 条豁免(protocol server-only 同构字段名误报,含原因登记);前端产物(vm-ui 含 blockly、plugin-dev)含引擎标识 / 私有面标记 / 节点内建零命中;
- 既有 ZR 面(ZR-B9/B10/B5/B4 通道录制、ZR-T1~T4 篡改矩阵、ZR-P4 时序统计)零改动照常回归(session-api 416 passed 含 `test/wp7/`、`test/scan/`、`test/limits/` 全套)。

### 9. 门禁:tsc / eslint / dependency-cruiser(含前端新规则)/ cargo 不回退 / 覆盖率 ≥ 80%(含前端包)/ Playwright 最小集全绿 ✅

全部数字见 §三门禁证据表:tsc 9 包零错误;eslint 通过;dependency-cruiser **977 模块 / 2846 边零违规**(含前端新规则 `browser-packages-only-depend-on-protocol` / `no-backend-dependency-on-browser-packages`,6 组反例自检全绿);**cargo 不回退**(test:rust 684 passed,双 profile 各 342,较阶段三 318/profile 增 24,零回退);**覆盖率整体四维 88.61 / 80.62 / 88.70 / 88.71 ≥ 80,含前端包**(vm-ui 94.52+,plugin-dev 95.37+);**Playwright 最小集 6 用例全绿**。ENG-6 本机 rustfmt 工具链漂移如实登记(§三末;环境依赖,CI linux `rust-gate` 为准,本评审零代码改动、红灯集全部为 HEAD 既有内容)。

### 10. 文档:调试通道登记权威 API 语义规约、数据分类清单调试通道行、拆解文档回填(FE-WS-06/07、FE-ED 系、分级统计更新)、实施计划承接标注、本分解条目全勾稽 ✅

本 WP-45 交付日全部完成(任务三回填清单):

1. **权威API语义规约**:新增 **D-API-74**(§三·十调试通道:独立端点 `GET /sessions/debug-channel`、`DEBUG_CHANNEL_PROTOCOL_VERSION = 1` 连接级锚定、帧族一览、限额共用 D-API-50~53 + `/metrics` 三指标、推送模型 §九、错误帧 `PublicError` 形态;引用 ADR-DC1 与协议语义文档)——**既有 D-API-1~73 条目零改动**;
2. **数据分类清单调试通道行**:v1.11 §6.8 / §6.9 两分类小节 + 总表两行(WP-40 交付);v1.12(WP-43 debugMode/aslrEnabled)/ v1.13(WP-44 ZR-B12/B13)接续;
3. **拆解文档回填**:FE-ED-01~08 新增 §十三(来源 = 计划书阶段四,逐条落地面与分级)、FE-WS-06/07 分级回填(A 类落地口径:debugMode 门控 / 换绑重置 / payload 状态共用 / 断点积木双档)、Q3 定案(原子动作粒度)、Q5 定案(M11 口径)、编号规则增补 FE-ED;
4. **校验清单回填**:总体结论增补阶段四落地状态与分级口径说明(变通已实现 ≠ 契约演进完成)、M7/M8/M9/M11 处置行补「已落地」定案措辞、分级统计更新(回填后口径 **A 36 / B 17 / C 1 / D 0**,共 54 条);
5. **实施计划承接标注**:头部承接标注核验 + 补「2026-09-11 阶段四 WP-F1~F9 全部交付」;
6. **本分解条目全勾稽**:§二轨道 A / WP-40~44 / 轨道 C / WP-45 全部 `- [x]` + 完成标准核验行;§六风险表逐行补「结果」列;
7. **README 索引**:`docs/README.md` phases/ 表登记本评审。

---

## 五、决策点定案记录(阶段四内裁决)

| 决策点 | 定案 | 登记处 |
|---|---|---|
| Q3 积木步进粒度 | **原子动作粒度**:积木图编译为 12 动作原子序列,每原子一步;循环/分支在原子边界暂停;断点积木 = 步进暂停点(解题档)/ 调试断点并入(调试档);数据源仅公开投影;submit 与积木执行无关 | 拆解文档 §十一 Q3 回填;vm-ui README WP-F6 节;`src/payload/executor.ts` |
| Q5 重启边界 | **M11 口径**:运行中 = `reset`;终态禁用 + 引导 `close_session` + `create_session` 新建 | 拆解文档 §十一 Q5 回填;`sm-workspace-menu.ts` |
| `debugMode` 字段语义 | **opt-out 布尔**(缺省 = true,题目默认启用调试;非布尔即拒);策略枚举形态否决(过度设计且留下未审计中间语义位),红灯 fixture 锁定 | 双包Schema语义 v1.6 §2.5;分解 §六风险表 |
| ASLR 基址派生 | **调试侧落地(D-J10 定案)**:首个 draw 到基址的映射写死五步(位宽掩蔽 → 页对齐 → 窗口取模;base 页对齐、`base + span ≤ 2^archBits`、`(raw, archBits, pageSize, span)` 确定性函数);**真实实例侧 = D-J8 引擎契约演进项登记未实现**(触发条件 = 真实实例启用 ASLR 的产品决策进入排期) | 判题语义规约 §6.1;`build-debug-variant.ts` + 黄金向量 |
| SeedDeriver 跨语言黄金向量 | **32+6 条三方一致**(TS / Rust / Python);Rust 对偶测试 `seed_derivation_conformance.rs` 锚定 draw 值;算法标识 `splitmix64-stream-v1` 常量引用不硬编码 | `challenge-compiler/test/golden/seed-derivation-vectors.json`;vm-worker tests |
| 调试变体镜像契约 | WP-40 §七定案:与私有包 initialState 逐字段同构;**结构性排除** judgingConfig / hiddenTests / seed 值字段(strictObject 即拒);draws 规范槽序 = 基址槽 → memoryRegions 数组序 → canarySlots 数组序(双方不重排);canary 期望值写入所属区域 `contentHex`(槽对象不携带值);整体 SERVER_ONLY,浏览器永不可见(server-only 子路径 + dependency-cruiser 强制) | `packages/protocol/docs/调试通道协议语义.md` §七 |
| 展示流推送模型 | **推送定案**(协议 v1 无 C→S 拉取帧):attach 后 `debug_function_table` 恰一次;每次暂停后紧跟暂停落点 `debug_instruction_stream`(maxItems = 16 服务端常量);推送帧不带 requestId(唯一例外 = attach 伴生函数表回显);发射点归一化 null 线形为键缺席后逐帧过冻结 Schema 自检;推送失败 error 帧连接不断 | 协议语义文档 §九;提交 eec9695 |
| 虚拟列表裁决 | **lit-virtualizer 2.1.1 → 自研 `<sm-window-list>`**:vite8 构建 + 嵌套 shadow DOM 下 scroller 模式 `rangeChanged` 不触发(最小复现呈创建顺序时序性缺陷,jsdom 冒烟无法暴露);字节/指令视图切换为自研确定性窗口化(流内 sizer + translateY 切片 + light DOM,512 行窗口);lit-virtualizer 依赖与兼容冒烟保留为上游修复后的回归重试锚 | vm-ui README「虚拟列表裁决」节 |

---

## 六、已知遗留与移交清单

### 移交阶段五(iframe 插件与嵌入安全)

1. **embed 正式化**:`packages/web-component` 实现(本阶段占位零实现)、`embed-runtime` postMessage 宿主协议、`react-wrapper`、embed token 浏览器交付通道、自适应高度 / 主题 / 语言、断线恢复的嵌入侧呈现;
2. **公开描述包正式下发通道(M2)**:FE-ED-06(hintLadder)/ FE-ED-07(publicErrorMapping)由夹具注入转正式下发;夹具与正式通道的对齐锚 = 公开 Schema(分解 §六风险表定案);
3. **axe `color-contrast` 真实浏览器补测**(条件 6 遗留,环境依赖):真机 Playwright + axe 报告归档;补测锚 = `test/ed/axe.test.ts` 豁免清单;
4. 13.4 浏览器矩阵扩展(多宽度 / Chrome-Firefox-Safari / reduced-motion / 高对比度 / 屏幕阅读器抽样)随嵌入面 E2E 基建承接。

### 移交阶段六(独立判题与扩展)

5. **verifier 独立判题与正式裁决结果展示**:submit 响应仅 `{submissionId, revision}`(本阶段 UI 只呈现「已提交」态);隐藏测试裁决、审计归档、全面租户隔离;
6. 沿阶段三移交项继续有效:OpenTelemetry 可选增量、`/metrics` 生产暴露面收敛、幂等 / route / rate 键域 T1 演进面。

### 演进登记(非阶段遗留缺陷,均为已定案触发条件的后续项)

7. **D-J8 真实实例侧 ASLR**(登记未实现):调试侧已按 D-J10 落地;真实实例基址派生触碰引擎装配参数面 / 动作日志地址语义 / verifier 派生复核 / 快照信封基址登记位,触发条件 = 产品决策启用真实实例 ASLR(判题语义规约 §6.1);
8. **ZR-B13 非平凡复算 fixture**:现网字节 fixture 无派生槽(draws = 0),集成层复算为声明面平凡一致性;含隐藏区域 / canary 槽的题目 fixture 进入批量制作期(MVP 期)时接入非平凡复算(映射 v1.9 如实登记);
9. **lit-virtualizer 上游修复重试锚**:依赖与 `test/lit-virtualizer-compat.test.ts` 兼容冒烟保留;上游修复后重试替换 `<sm-window-list>` 的裁决可重启;
10. **ENG-6 rustfmt 工具链漂移**(本机环境事项):rustfmt 1.9.0 style-edition 对 6 个 HEAD 既有文件报格式漂移;非本阶段引入,CI linux `rust-gate` 为准;本机跟进 = 工具链对齐或按 CI 基线执行 `cargo fmt` 一次性归零(须独立评审,不在本评审改动面);
11. **公开描述包交互接口声明面(M10 / FE-PB-04,校验清单唯一存留 C 类)**:出题者积木模块的 Schema 演进未启动,随 M2 通道统筹。

---

## 七、结论

阶段四交付**可运行、双档制、可解释的教学 UI 全量面**:轨道 A 前端落地(vm-ui 组件库 518 项测试、公开投影渲染四视图 + 工作区 + payload 框架)→ 轨道 B 调试通道契约与后端(12 帧族契约先行冻结、真实 worker 端到端集成、变体镜像与跨语言派生黄金向量、Schema 演进、ZR-B12/B13 机检)→ 轨道 C 汇合(调试模式档 + FE-ED 教学组件面 + Playwright 最小集)。门禁数字:TS 1625 + Rust 684(双 profile)+ IT 416 + E2E 6 全部实跑通过;覆盖率整体四维 88.61 / 80.62 / 88.70 / 88.71 ≥ 80(含前端包);`scan:public` 0 违规;fixture 214 条跨语言一致;机检零命中 + 红灯反例齐备。既有冻结面(WSS 通道、12 动作、16 错误码、D3/D5、verifier 回放面)零改动;唯一新增契约面 = 调试通道协议 v1(契约先行,fixture 与 Rust 消费面同步建立)。

**九条退出条件通过;条件 6 存一项环境依赖遗留(axe `color-contrast` 需真实浏览器补测,jsdom 结构性不可判定),已登记性质与补测锚,不构成阶段退出阻塞——阶段四关闭,进入阶段五(iframe 插件与嵌入安全)。**
