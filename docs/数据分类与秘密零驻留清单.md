# 数据分类与秘密零驻留清单(WP-1 · 已冻结)

| 项 | 值 |
|---|---|
| 版本 | v1.4(冻结) |
| 日期 | 2026-09-04 |
| 修订 | v1.4:由《Vm 模块设计冲突与整改方案》(G1/G3)发起——公开包 `vmProfile` 新增 `archBits`(32/64)位宽声明,全部架构值语义修订为"archBits 位宽的值,以 64 位容器承载,高位按位宽掩蔽"(§3.2、§4.2);区域类型增补作者自定义 `custom` 类与 VMA 4KB 倍数对齐论证(§12.2/§12.3);§12.6 新增检查器规则 `XS-ARCH-WIDTH` / `XS-MEM-PAGE-ALIGN` 条目;不改变任何分类标签、决策 D1–D6 或既有字段集合;v1.3:由 WP-4 发起,新增第十二章 题目双包字段级分类——公开描述包逐字段 `PUBLIC` 硬门槛论证(含初始投影排除集与题目可裁量预算公开面)、私有判题包整体 `SERVER_ONLY` 字段集合冻结、`declaredSeedPublicPaths` 路径语法、`secretSinkRegisters` 声明面与 FLAG 寄存器命名约定;v1.2:由 WP-3 发起,为投影与错误契约的 Schema 冻结补齐字段级论证——`PublicEvent` / `PublicCallFrame` 增补 presence-only `truncated` 标记,新增 §4.3(`ProjectionDelta` / `DirtyRange`)与 §4.4(`PublicError` 含解释字段);v1.1:revision 递增语义由"仅成功动作 +1"修订为"已执行(非拒绝)动作 +1"(论证见 1.3 变更记录);其余字段集合、分类标签与决策均不变 |
| 状态 | 阶段一 WP-1 交付物,已完成冻结 |
| 上游依据 | 计划书 9.2(威胁模型与数据分类)、6.2(状态模型与动作链路)、13.5(秘密零驻留与侧信道测试)、5.2(四信任域)、7.1(双包模型)、4.4(错误反馈) |
| 效力范围 | WP-2 至 WP-6 的全部契约字段设计;冲突时以计划书为准,其次以本文为准 |

**硬门槛**(阶段一任务分解原文):任何无法证明"秘密不进入浏览器"的字段设计不得进入下一阶段。本文第四章对每个公开字段逐条给出该论证;任何新增公开字段必须先在本文补论证,再进契约。

---

## 一、效力范围、分类标签与变更纪律

### 1.1 分类标签

本文用三个标签对每个跨域字段作唯一分类:

| 标签 | 含义 | 浏览器可见 | 机检含义 |
|---|---|---|---|
| `PUBLIC` | 可下发浏览器,可缓存于浏览器任意位置(IndexedDB、localStorage、Worker) | 是 | 出现在浏览器属预期;不存在于任何 `SERVER_ONLY` 标识符扫描命中集 |
| `SERVER_ONLY` | 禁止出现在任何跨域载荷:API 响应、WSS 帧、postMessage、回放下载、错误文本、公开回放 | 否 | 命中即违规(见第九章 ZR-B/R/P 条目) |
| `BOUNDARY` | 双向跨域传输,但属"玩家自己的输入或短期凭证",不是服务端秘密;服务端按 6.2 全部重新校验,不信任其内容 | 是(仅玩家自报内容) | 服务端不得读取其中未声明字段;Schema 解析必须剥离未知字段 |

判定次序:先问"该字段的值是否由服务端秘密派生"——是则 `SERVER_ONLY`;再问"该字段是否是玩家输入/短期凭证的回传"——是则 `BOUNDARY`;否则才可评 `PUBLIC`,且必须通过第四章论证。

### 1.2 与其他工作包的边界

| 工作包 | 从本文承接 | 不得做的事 |
|---|---|---|
| WP-2(动作协议) | 第六章信封字段分类;I-5 稠密序号规则 | 不得新增未分类的响应字段 |
| WP-3(投影与错误契约) | 第四、五章字段集合与子类型边界;第七章决策 D1–D6;第十章粗化矩阵 | Schema 冻结必须与本文字段集合一致并携带分类标签 |
| WP-4(双包 Schema) | 不变量 I-2 / I-3 的编译期检查义务;私有包 Schema 必须含 `declaredSeedPublicPaths`(seed 策略的显式公开路径声明,供 T-SC4 / ZR-P2 消费——此为对上游 WP-4 任务清单的增补项,按 1.3 记录);第十二章双包字段级分类(逐字段承接) | 公开包 Schema 不得能表达任何 `SERVER_ONLY` 字段 |
| WP-5(嵌入协议) | 第六章 `BOUNDARY` 凭证规则 | 不得把宿主自报身份当作认证 |
| WP-6(版本与 fixture) | 第十章差分测试依赖的规范化序列化规则 | fixture 必须覆盖 `PUBLIC` / `BOUNDARY` 样例与违规样例 |

### 1.3 变更纪律

- 本文标注"冻结"的字段集合与决策,变更须走契约变更流程:先改本文(补论证或改决策)→ 更新 `protocol` / `challenge-schema` 与 golden fixture → 评审通过 → 再改实现;
- 新增 `PUBLIC` 字段而未在本文第四章完成"秘密不进入浏览器"论证,视为违反硬门槛,CI 字段白名单校验(I-1)直接拒绝;
- 为未来能力预留字段(计划书风险表)被明确禁止:扩展靠协议版本号演进,不靠预留字段。

### 1.3.1 变更记录

| 版本 | 变更 | 论证 |
|---|---|---|
| v1.4(2026-09-04) | 由《Vm 模块设计冲突与整改方案》(P1 批次,G1+G3)发起。(1) G1/archBits:公开包 `vmProfile` 新增 `archBits` ∈ {32, 64} 位宽声明(Vm 模块设计硬规范:架构位宽由出题人指定,D1 裁决);架构值语义全文修订为"archBits 位宽的值,以 64 位容器承载,高位按位宽掩蔽,算术/移位/比较在掩蔽域内进行"(§3.2 `registers` / `instructionPointer`、§4.2 `PublicRegister.valueHex`);十六进制字符串的变长形态不变,**不新增任何投影字段**。(2) G3/VMA:区域类型枚举增补作者自定义 `custom` 类(必填 `publicLabel` 承载类型名,同 `SemanticHighlight.custom` 先例),区域大小与 `pageSizeBytes` 收紧为 4KB 的倍数(区域内对象如 canary 1–8 字节不受约束);两者均为作者声明的公开布局常量,I-10"公开布局常量"类论证补齐(§12.2/§12.3)。(3) §12.6 新增检查器规则 `XS-ARCH-WIDTH`(位宽域:私有初始值 / IR 立即数与位移 / 公开镜像值)与 `XS-MEM-PAGE-ALIGN`(公开/私有双侧页对齐)。协议投影契约、分类标签与既有字段集合不变 | 位宽是题目公开常量(`archBits` 随公开包下发),对秘密无派生关系,无新侧信道面;值域收紧属纵深防御——超位宽初值在引擎侧本应按掩蔽域归一,校验期拒绝使"出题人手写越界初值"成为编译期错误而非运行时静默掩蔽;4KB 对齐与自定义区域命名都是 7.1 公开布局的声明面,公开无危害;XS-ARCH-WIDTH 以跨包形态落地(私有包不复制 `archBits` 字段,位宽只从公开包 `vmProfile` 读取,避免双真相源漂移) |
| v1.3(2026-09-03) | 由 WP-4(题目双包 Schema)发起,新增第十二章 题目双包字段级分类:公开描述包 14 个顶层字段的逐字段 `PUBLIC` 硬门槛论证(7.1 七项清单逐一落位,含 `vmProfileVersion` 共享身份字段),`initialProjection` 的部分镜像排除集(`revision` / `callStackSummary` / `controlFlow` / `status` 结构性排除),题目可裁量预算公开面(`predicateEvalBudgetPerSession` / `rollbackBudgetPerSession` / `maxWriteBytesPerAction`),私有判题包整体 `SERVER_ONLY` 的 13 组字段集合冻结,`declaredSeedPublicPaths` 路径语法,`secretSinkRegisters` 声明面与 FLAG 寄存器命名约定(`^FLAG[A-Z0-9_]*$`),及 WP-4 检查器规则与本文条目的对照表(§12.6)。不改变任何既有字段集合、分类标签、决策 D1–D6 或机检条目 I-1–I-10 / ZR-* 的语义 | 见第十二章逐字段论证。要点:(1) `initialProjection` 只镜像作者可声明的子形状,四个排除字段全部是运行时派生量(I-5 会话计数 / D2 调用深度 / D5 服务端生成展示 / D1 会话态),静态描述包对其赋值即矛盾陈述;(2) 预算公开面是 D1 约束 5 的直接落位("预算的公开部分由公开描述包声明"),揭示上限降低探测收益,协议冻结常量不是题目可授权面;(3) FLAG 寄存器名称公开、值永不公开,且公开寄存器白名单(`vmProfile.registers`)不得含 FLAG 寄存器——该白名单是 `ProjectionPolicy.visibleRegisters` 的组装来源(第五章),否则 I-3 直接违规;(4) 私有包整体 `SERVER_ONLY`、无逐字段灰地带,与 3.1 规则 1(`VmState` 无契约只有禁令)同构 |
| v1.2(2026-09-03) | 由 WP-3(投影与错误契约)发起,为 Schema 冻结补齐论证面:(1) §4.2 `PublicEvent` 与 `PublicCallFrame` 字段集合各增补 presence-only `truncated` 标记——D2(调用栈截断"以不含计数的统一标记表达")与 D4(超限"聚合为带 truncated 标记的聚合事件")已 sanction 该标记,但字段集合表未列入,本变更是把已 sanction 的标记落进字段集合,非新增公开语义;(2) 新增 §4.3(`ProjectionDelta` / `DirtyRange`)与 §4.4(`PublicError` 含 `explanation` 解释字段)的逐字段分类与硬门槛论证——§6.2 已 sanction `projectionDelta` 与 dirty range、6.3 已冻结 E-1–E-6 可见性规则,字段级论证面此前缺位。不改变任何既有字段语义、决策 D1–D6 或机检条目 | (1) 两个标记都是 presence-only(`const true`,出现即"该元素是聚合/截断产物"),不含省略计数;存在性分别是公开事件列表(D4 聚合规则不读私有状态)与"可见调用栈深度 > 公开常量 64"(D2)的确定性函数,无隐藏信息。(2) `ProjectionDelta` 只描述可见区域内的可观察变化(§6.2 冻结),`PublicError` 解释字段全部落入第十章粗化矩阵允许面(值来源 ⊆ 玩家输入/可见内容/公开常量/静态模板,I-10),逐字段论证见 §4.3 / §4.4 |
| v1.1(2026-09-02) | §4.1 `revision` 与 I-5 的递增语义由"仅成功动作 +1(失败/拒绝不推进)"修订为"**已执行(非拒绝)动作 +1**";ZR-P5 写明判定基准。由 WP-2 安全评审(M-1)发起,走本条流程 | 原"成功"措辞把"动作进入执行"与"教学成功"混为一谈:已执行但失败的动作(如写可见但不可写区域的教学性 `memory_fault`、Canary 破坏触发 `failed`)会改变公开投影内容(`status`、`userVisibleError`、事件),若 revision 不前进,同一 revision 值将对应不同投影内容,破坏"revision = 公开投影版本"的对齐语义,`sync-projection`、幂等缓存与断线同步都无法以 revision 为版本锚点。侧信道硬门槛不受影响——两种读法下 revision 都仍是玩家操作史的确定性纯函数、增量恒定、不含隐藏跳变,I-5 的实质约束(稠密、单调、无隐藏信息)保持不变。判定基准:动作**进入执行**即 +1(含失败但已执行与 undo / checkout / reset / pause / create_checkpoint);进入执行前被拒绝(参数校验失败、`baseRevision` 过期、不可见地址、终态违规、幂等冲突、乱序序号)不推进。不涉及任何字段集合、分类标签或机检条目的增删 |

---

## 二、信任域与数据驻留总表

计划书 9.2 数据分类表扩充信任域与具体存储位置(域编号见计划书 5.2):

| 数据类别 | 浏览器可见 | 分类 | 存储位置 |
|---|---|---|---|
| 公开描述包 | 是 | `PUBLIC` | CDN(域 1,带哈希/签名);对象存储 `public-bundles` 回源 |
| 私有判题包(隐藏 flag、隐藏测试、完整 IR、seed) | 否 | `SERVER_ONLY` | 对象存储 `private-bundles`(静态加密、最小权限);域 3 进程内存 |
| 用户动作记录(受限动作请求) | 提交内容可见 | `BOUNDARY` | 浏览器(构造);PostgreSQL `action_log`(append-only,域 3);域 4 verifier 读取重放 |
| 私有 VmState、服务端快照、完整事件日志、私有目标条件 | 否 | `SERVER_ONLY` | 域 3 Worker 进程内存(权威);COW 快照 → PostgreSQL `checkpoints`;诊断 trace → 对象存储 `traces` |
| 公开投影 / ProjectionDelta | 是 | `PUBLIC` | 域 3 生成 → 域 2 通道 → 浏览器缓存(IndexedDB);服务端仅缓存最近 N 个投影(5.7) |
| 短期会话凭证(embed token、会话 token) | 是(短期持有) | `BOUNDARY` | 浏览器(仅会话期);Redis `token:{jti}`(域 2,带 TTL);PostgreSQL `sessions`(域 3) |
| 幂等键、限流计数、会话路由 | 否 | `SERVER_ONLY` | Redis `idem:{sessionId}:{key}`、`rate:*`、`route:*`(域 2,全部带 TTL) |
| 审计日志(含内部堆栈、文件路径、隐藏测试信息) | 否 | `SERVER_ONLY` | PostgreSQL `audit_log`(append-only,域 3/4);归档对象存储 |

驻留三原则(结构保证,非流程约定,计划书 5.2):

1. **秘密不出执行域**:`VmState`、私有题目包、隐藏测试、seed 只存在于域 3 Worker 进程内;
2. **跨域只传投影**:投影白名单过滤与脱敏在域 3 内完成后才离开;域 2 与网络通道上只有公开投影;
3. **构建期隔离可机器验证**:引擎 crate 与 `challenge-compiler` 不进浏览器构建图(CI 产物扫描,5.8 门禁 6)。

---

## 三、`VmState` 字段集合冻结(纯后端类型)

### 3.1 两条归属规则(本阶段冻结)

**规则 1:`VmState` 是纯后端类型,不进入 JSON Schema 契约。**
它不出现在 `@stackmaster/protocol` 包中,这正是其安全属性:不存在任何被认可的序列化路径,意味着任何跨域载荷中出现该类型或其字段名,按定义即违规,无需逐次判断"是否泄密"。契约侧不存在"私有两个版本的字段子集"这种灰地带——`SERVER_ONLY` 类型没有契约,只有禁令。

**规则 2:实现语言由 ADR-8 产能决策决定(WP-6)。**
本阶段只冻结字段集合——字段名、语义、不变量与相互关系(下表);vm-core 用 Rust 还是回退 TypeScript 不影响本表。回退时 `VmState` 仍不得进入 TS 构建图中任何浏览器可达的包(CI 导入图检查)。

### 3.2 字段集合(冻结,8 字段)

来源:计划书 6.2。内部形态为语义冻结,供阶段二实现;具体数据结构属引擎内部,可自行演化,前提是保持本表的分类与不变量。

| 字段 | 语义(冻结) | 为何 `SERVER_ONLY`(禁止下发论证) | 存储位置 |
|---|---|---|---|
| `registers` | 寄存器全集到架构值(archBits 位宽,64 位容器承载,高位按题目 `vmProfile.archBits` 掩蔽;v1.4)的映射 + FLAG 位 | 完整寄存器集含未白名单寄存器;其值可被私有逻辑写入秘密派生内容(解密中间值、隐藏地址)。公开子集经 `visibleRegisters` 白名单后另行下发(第四章) | 域 3 进程内存;COW 快照 |
| `memory` | 分页虚拟内存(VMA 为基本单元,区域大小为 4KB 的倍数,COW):代码区、全局区、堆区、栈区、题目关键区、作者自定义类型区域(6.1;v1.4 G3 整改) | 含隐藏区域与隐藏对象的全部字节;下发任何区域都等于下发原始快照的一个切片。可见区域字节经白名单 + `maxBytesPerRange` 截断后另行下发 | 同上 |
| `callFrames` | 调用帧内部栈:函数标识、返回地址、saved `RBP`、内部参数 | 深度与内容未被裁剪前不公开(见决策 D2 的裁剪规则);内部参数可含私有布局信息 | 同上 |
| `instructionPointer` | 当前执行位置(archBits 位宽架构值;v1.4) | 直接下发可暴露私有代码区域的存在与位置(MVP 中代码区恒公开,但类型本身不因 MVP 布局而降级)。公开形态是 `controlFlow.currentInstruction`(第四章) | 同上 |
| `privateEventLog` | 私有事件 append-only 日志:内部事件、隐藏判定事件、公开事件的全集来源 | 含隐藏测试触发记录与私有目标条件求值痕迹;序号空洞、条目数都是侧信道(决策 D4)。公开子集经白名单过滤后另行下发 | 同上;审计归档对象存储 |
| `constraints` | 运行时约束的权威执行状态:步数、内存、wall-clock、调用深度、动作日志长度、输出上限、超时,以及**私有谓词求值次数预算**(按会话累计,跨 undo/reset 不重置)与**回退/重置次数预算**(两维度为本阶段冻结新增,依据见 D1 约束 5) | 是服务端资源预算的完整集合;完整下发可帮助选手把动作精确顶到限额做探测。公开部分由公开描述包单独显式声明(7.1),不是对本字段的投影 | 同上 |
| `seedState` | 随机源状态:`strategyId`、版本、内部状态字节 | 种子值与状态字节即随机化秘密本身;泄露可复现全部随机行为。`strategyId` 与版本号同属私有包 seed 策略字段(7.1),**不进公开描述包**——策略元数据泄露搜索空间(如"栈基址在 N 个候选中随机"直接给出熵上界),与回退神谕(D1 约束 5)复合会降低穷举成本。公开面只允许文案声明"存在随机化"这一事实,不声明策略、候选空间或熵上界;seed 策略与运行环境版本写入服务端回放信息(6.3,域 3/4 审计记录) | 同上 |
| `status` | 权威 VM 状态:`running` / `paused` / `won` / `failed`;won/failed 为会话终态(D1 约束 5) | 这是私有目标条件的求值**结果来源**;公开的是其在动作响应边界上的投影 `PublicStatus`(决策 D1),不是本字段的实时读取通道 | 同上 |

寄存器名称集合(冻结,计划书 6.1):`RSP`、`RBP`、`RIP`、`RAX`、`RBX`、`RCX`、`RDI`、`RSI`、`RDX`、`R8`、`R9`、`R10`、`R11`、`R12`,加题目 VM Profile 声明的 FLAG 子集。扩展寄存器走 VM Profile Version,不靠本集合预留。

### 3.3 `VmState` 机检标识符清单

浏览器产物与跨域载荷扫描(第九章 ZR-B9 / ZR-R2)使用以下标识符(词边界匹配):

```text
类型名:  VmState  RuntimeConstraints  SeedState  VirtualMemory  VmEvent
字段名:  privateEventLog  seedState  instructionPointer
crate:   vm-core  vm-runtime  vm-worker  vm-engine  challenge-compiler
```

说明:不使用 `status`、`registers`、`memory`、`callFrames`、`constraints` 等通用词做独立命中词(误报率高);它们仅作为 `VmState` 引用关系的导入图检查对象。`CallFrame` 单独出现不违规(`PublicCallFrame` 是公开类型),命中词用 `VmState` 与字段名;`VmEvent` 词边界匹配不会命中公开的 `PublicEvent`,故可作独立命中词。浏览器 UI 代码亦不得以 `instructionPointer` 等被禁标识符命名导出符号(公开投影中的对应概念叫 `controlFlow.currentInstruction`)。

**共现形态规则**:真实 VmState 误下发序列化出来的恰恰是通用键名(`registers`、`memory`、`status` 等),而 `privateEventLog`/`seedState` 反而常为空或缺席——仅靠词边界扫描会漏检该形态。补充规则:单个 JSON 对象内同时出现 ≥ 3 个 VmState 字段名键(registers、memory、callFrames、instructionPointer、privateEventLog、constraints、seedState、status)即判定为 VmState 序列化违规;`privateEventLog` / `seedState` / `instructionPointer` 单独命中即违规。

---

## 四、公开投影字段级分类:`PublicStateProjection` 及子类型

### 4.1 顶层字段(冻结,7 字段)

Schema 细节(具体类型、可选性、数值边界)由 WP-3 冻结;本表冻结**字段集合、分类与硬门槛论证**。

| 字段 | 分类 | 浏览器可见 | 硬门槛论证:为何公开不泄露隐藏状态 | 存储位置 |
|---|---|---|---|---|
| `revision` | `PUBLIC` | 是 | 会话内单调计数器,值 = 已执行(非拒绝)动作数(v1.1 修订,论证见 1.3.1),完全由玩家自己的操作史决定;每个进入执行的动作 +1(含失败但已执行的教学反馈动作与 undo / checkout / reset;进入执行前被拒绝的不推进,响应携带当前值),相邻差恒定,不含隐藏跳变,无可推测信息 | 域 3 生成;浏览器可缓存 |
| `visibleRegions` | `PUBLIC` | 是 | 仅含 ProjectionPolicy 白名单区域;区域布局本身是公开描述包声明的公开信息(7.1),不是运行时推导;隐藏区域根本不出现在数组中(无"此处有隐藏区域"的占位);与隐藏对象不重叠由编译期检查保证(I-2)。字节内容截断与占位规则见决策 D3,截断行为对秘密无相关 | 同上 |
| `visibleRegisters` | `PUBLIC` | 是 | 仅含策略白名单寄存器;白名单集合是题目设计公开的一部分;"秘密汇寄存器不得进入白名单"由编译期检查强制(I-3),运行时由差分测试兜底(T-SC4);白名单寄存器的值属于教学因果链本身,是玩家要学习观察的对象 | 同上 |
| `callStackSummary` | `PUBLIC` | 是 | 决策 D2:内容裁剪为 `{index, functionLabel, returnAddressHex}`;MVP 中代码区恒公开,调用深度是公开程序的确定性行为,玩家可自行推导;深度上限 64 仅为资源护栏,截断标记不含计数;若未来出现非公开代码区(非 MVP),按 D2 回退规则整帧占位 | 同上 |
| `controlFlow` | `PUBLIC` | 是 | 决策 D2/D5:当前指令展示由服务端从展示数据生成,不是可执行 IR(7.3);MVP 代码区恒公开,故 `RIP` 展示恒可行;`pausedOn` 是玩家动作触发的暂停原因枚举,属教学反馈 | 同上 |
| `semanticHighlights` | `PUBLIC` | 是 | 高亮目标区域必须在可见区域内(I-2);标签文本值来源受 I-10 约束(公开描述包或服务端静态模板);高亮位置是公开布局与玩家写入的函数,不是隐藏判定结果的函数 | 同上 |
| `status` | `PUBLIC` | 是 | 决策 D1:`PublicStatus`(`running` / `paused` / `won` / `failed`)仅在动作响应边界同步下发;无进度字段、无轮询旁路;won/failed 本身是教学结果,属公开;判定成本与秘密内容无关(I-6),时序无可测差异 | 同上 |

### 4.2 子类型边界(冻结;Schema 归 WP-3)

| 子类型 | 字段边界(冻结) | 可见性规则 |
|---|---|---|
| `VisibleMemoryRegion` | `regionId`、`label`、`startAddressHex`、`byteLength`、`permissions`(r/w/x 子集)、`bytesHex`(≤ 2×`maxBytesPerRange` 字符)、`truncated` | `bytesHex` 仅含白名单区域字节;`byteLength` 是公开布局常量,不是运行时秘密的函数;`truncated` 为统一布尔标记,不含省略计数 |
| `PublicRegister` | `name`、`valueHex`(archBits 宽架构值:变长大写十六进制字符串,64 位容器承载、高位按题目 `archBits` 掩蔽;v1.4) | 名称 ∈ 白名单;值是教学观察对象 |
| `PublicCallFrame` | `index`、`functionLabel`、`returnAddressHex`、`truncated?`(v1.2) | 决策 D2;帧基址不直接下发,由玩家从可见 `RSP`/`RBP` 自行推导(教学价值);`functionLabel` 来源冻结:公开描述包符号表或服务端静态模板,禁止私有符号表;`truncated` 为 presence-only 标记(出现即 `true`):仅出现在深度超 `CALL_STACK_MAX_DEPTH = 64` 后展示的**最后一帧**上,表达"其后还有帧未展示",不含省略帧数——存在性是(可见调用栈深度, 公开常量)的确定性函数,深度本身是公开程序的确定性行为,无隐藏信息;若未来出现非公开代码区(非 MVP),整帧替换为统一占位帧(标签与地址一起隐藏),与 D5 的 `currentInstruction` 占位规则对齐 |
| `PublicControlFlow` | `currentInstruction: {addressHex, text}`、`pausedOn: enum \| null` | `text` 为服务端生成的伪指令展示文本;`pausedOn` ∈ `read` / `write` / `call` / `ret` / `exception`;若未来出现非公开代码区(非 MVP),`currentInstruction` 必须整体替换为统一占位符,不得只隐藏地址保留文本 |
| `SemanticHighlight` | `kind`、`targetRegionId`、`startAddressHex`、`byteLength`、`label` | 目标区域必须可见(I-2);`kind` 枚举冻结(`buffer_start`、`return_address_slot`、`saved_rbp_slot`、`canary_slot`、`custom`) |
| `PublicEvent` | `seq`、`kind`、`addressHex?`、`byteLength?`、`payloadHex?`、`truncated?`(v1.2) | `kind` ∈ `read` / `write` / `call` / `ret` / `syscall` / `exception`;`addressHex` 的省略/占位遵循 I-8(语境码为事件 `kind`);对不可见地址的访问不产生事件,其结果由 I-9 统一(统一拒绝,杜绝以"有无事件"探测隐藏区域存在性);`payloadHex` 值来源受 I-10 约束(玩家输入字节或可见区域字节);`seq` 为公开事件流独立稠密编号(D4);每动作事件数上限 256,超限按确定性规则聚合为带 `truncated` 标记的聚合事件——`truncated` 为 presence-only 标记(出现即 `true`),仅出现在聚合事件上,不含被聚合事件的计数;存在性由聚合规则决定,聚合只读取**公开事件列表自身**(确定性规则),不读取任何私有状态,无隐藏信息 |
| `PublicStatus` | `running` / `paused` / `won` / `failed` | 决策 D1;`rejected` 不属于投影,只属于 `ActionResponse.status` |

### 4.3 `ProjectionDelta` 与 `DirtyRange` 字段级分类(v1.2 增补;Schema 归 WP-3)

`ProjectionDelta` 是 `ActionResponse.projectionDelta` 的载荷形态(§6.2:整体 `PUBLIC`,增量只描述可见区域内的可观察变化)。所有可选字段遵循**存在性确定性**:字段在增量中出现当且仅当该字段在本次动作中发生了可观察变化,是 `(动作, 执行前可见状态)` 的确定性函数(I-4),禁止按隐藏状态分支选择形态。

| 字段 | 分类 | 硬门槛论证:为何公开不泄露隐藏状态 |
|---|---|---|
| `revision` | `PUBLIC` | 同 §4.1 `revision` 论证;契约要求非 null 增量的 `revision` 必须等于信封 `revision`(WP-2 语义文档 §2.2 冻结) |
| `dirtyRanges` | `PUBLIC` | 只描述**可见区域内**已发生的写入(§6.2 冻结);对不可见地址的写入被 I-9 统一拒绝,永不产生 dirty range;范围集合是"本次动作可见写入列表"经合并规则(相邻/重叠归并为极大连续区间)得到的确定性函数,合并只读取公开写入记录 |
| `changedRegisters` | `PUBLIC` | 白名单寄存器中值发生变化者的新值子集;"哪些寄存器变化"由已执行动作的公开语义决定(I-4),值是教学观察对象(同 §4.1 `visibleRegisters` 论证);秘密汇寄存器不得进入白名单由 I-3 强制 |
| `controlFlow?` | `PUBLIC` | 整体替换形态(存在即整体替换客户端缓存);存在性与内容同 §4.1 `controlFlow` / 决策 D5 |
| `status?` | `PUBLIC` | 整体替换形态;存在性与内容同 §4.1 `status` / 决策 D1 |
| `callStackSummary?` | `PUBLIC` | 整体替换形态;存在性与内容同 §4.1 `callStackSummary` / 决策 D2(含 §4.2 截断标记规则) |
| `semanticHighlights?` | `PUBLIC` | 整体替换形态;存在性与内容同 §4.1 `semanticHighlights`(目标区域必须可见,I-2) |

`DirtyRange` 子类型边界(冻结):

| 子类型 | 字段边界(冻结) | 可见性规则 |
|---|---|---|
| `DirtyRange` | `regionId`、`startAddressHex`、`bytesHex`、`truncated?` | `regionId` 必须引用白名单可见区域(I-2,公开布局);`startAddressHex` 是可见区域内写入的起始地址(公开布局或玩家写入位置);`bytesHex` 的新字节值来源 ⊆ {玩家输入, 服务端对可见区域的写入}(后者属 D3"写入可见性"的可见表面,符合 I-10);单 range 字节上限同 `VisibleMemoryRegion.bytesHex`(≤ `maxBytesPerRange`),单增量字节总预算 ≤ 8192(D3 冻结);`truncated` 为 presence-only 标记:合并后的写入总字节超出单 revision 预算时,服务端按地址升序承载至预算耗尽、对最后一个 range 打标记,不含省略字节数——存在性由(可见写入总体积, 公开常量)决定,无隐藏信息;客户端收到带标记的增量后应以 `sync-projection` 重新对齐(9.1 sanctioned 路径) |

### 4.4 `PublicError` 字段级分类(v1.2 增补;枚举与格式归 WP-3,可见性规则见 §6.3 E-1–E-6)

| 字段 | 分类 | 硬门槛论证:为何公开不泄露隐藏状态 |
|---|---|---|
| `code` | `PUBLIC` | 冻结枚举(E-1,WP-3 落地 16 值,覆盖 4.4 九类 + I-9 统一访问 + 协议级拒绝 + 预算 + 内部错误);枚举值本身是公开契约,出现与否是 (错误类别, 可见状态) 的确定性函数,与秘密内容无相关 |
| `message` | `PUBLIC` | 按 `(code, 可见状态)` 参数化的静态模板展开(E-5);参数值来源 ⊆ {玩家输入, 可见状态, 公开常量}(E-3 / I-10);值级违规(内部堆栈、文件路径、capability 名称、隐藏区域名、seed 信息,E-6)由 ZR-B7 语料扫描承接——Schema 宽度与值级扫描分工(WP-2 移交事项) |
| `addressHex` | `PUBLIC` | E-2 冻结:仅当地址落在**可见区域**时为真实地址;不可见时为 `null`(统一占位形态,M-3 抉择:选 JSON `null` 而非伪 hex 常量——伪 hex 会被客户端当真实地址渲染且无法与真实地址机检区分);字段**省略**与否、真值与占位的选择都是 `(code, 可见状态, errorDetailLevel)` 的确定性函数(I-8),逐 code 能力矩阵由 WP-3 冻结并落 Zod 机检;真实地址值来源 ⊆ {公开布局, 玩家输入} |
| `explanation.regionId?` | `PUBLIC` | 教学解释中涉及的可见区域标识;必须引用白名单可见区域(I-2),区域布局本身是公开描述包声明的公开信息 |
| `explanation.permissions?` | `PUBLIC` | 该可见区域的权限事实(r/w/x)——"为什么不在可执行/可写区域"的教学解释载体(10.1 educational 列);是公开布局常量 |
| `explanation.valueHex?` | `PUBLIC` | 被解释的值(如被当作返回地址弹出的 qword);值是玩家输入或可见区域内容的回显(E-3 / I-10),向玩家解释"该值被如何解释"(4.4)不引入任何非玩家自供信息 |
| `explanation.interpretedAs?` | `PUBLIC` | 解释方式(位宽 × 端序)枚举;由题目 VM Profile 公开元数据决定,不含策略或秘密信息 |
| `explanation.alignmentBytes?` / `explanation.expectedBytesLength?` | `PUBLIC` | 对齐要求与期望长度,来源 ⊆ {协议级公开上限, 公开描述包预算, 可见区域边界, 对齐常量}(E-3);隐藏缓冲区边界等私有数值禁止出现(由 T-SC1 / ZR-P8 信息流断言兜底) |
| `explanation.actualBytesLength?` | `PUBLIC` | 玩家输入的实际长度(玩家自供内容) |
| `explanation.hints?` | `PUBLIC` | 服务端静态模板教学提示(10.1 educational 列:检查 padding、little-endian、`RSP` 位置);文本值来源受 I-10 约束(静态模板),内容按 `(code, 可见状态)` 参数化,禁止谓词信息(E-4 / I-7)与 E-6 全部禁项 |

`explanation` 整体与 `addressHex` 的逐 code 允许面(能力矩阵)是 WP-3 冻结的确定性函数:`objective_not_met` 恒为零解释字段(E-4 / I-7:无已匹配/未匹配计数、无谓词标识、无部分匹配结果);`inaccessible_address` 的 `addressHex` 恒为 `null`(I-9 统一形态);协议级拒绝类 code(`stale_base_revision` / `stale_client_seq` / `idempotency_conflict` / `session_terminal` / `internal_error` / `budget_exhausted`)不带地址与解释字段。`coarse` 级载荷(正式判题与 verifier 语境)只含 `code` + `message`,解释字段整体缺席(10.1 矩阵),由生成端纪律与 ZR-P6 机检承接。

---

## 五、`ProjectionPolicy` 分类(SERVER_ONLY 配置类型)

`ProjectionPolicy` 决定公开投影的内容边界(计划书 6.2)。**整体分类:`SERVER_ONLY`。** 它驻留域 3,由公开描述包的可见布局与私有判题包的隐藏对象排除集组装而成;浏览器只接收它产生的效果(脱敏后的投影),永不接收策略本身。

| 字段 | 为何 `SERVER_ONLY`(整型配置不下发的论证) |
|---|---|
| `visibleRegions: string[]` | 单看是公开布局的镜像,但策略对象整体承载"排除集"语义:下发完整名单等于宣告"其余区域皆为隐藏",给出隐藏区域的存在性与命名空间。公开布局的唯一 sanctioned 来源是公开描述包(7.1) |
| `visibleObjects: string[]` | 同上;对象名单的补集即隐藏对象清单 |
| `visibleRegisters: string[]` | 白名单补集含"秘密汇寄存器"(I-3),下发名单可帮助玩家定位哪些寄存器被题目设计为敏感 |
| `maxBytesPerRange: number` | 本身非秘密,但随策略整体驻留域 3;其数值约束冻结见决策 D3 |
| `errorDetailLevel: "coarse" \| "educational"` | 属判题配置;泄露判定口径无直接危害,但按"整型不下发"原则处理,其效果体现在错误脱敏(第十章) |

Schema 归属:WP-3 将 `ProjectionPolicy` Schema 冻结进 `@stackmaster/protocol` 的 **server-only 子路径导出**,并打 `x-sm-class: server-only` 标签——**Schema 存在不等于可下发**;它供后端各包(challenge-compiler、session-api、verifier)跨语言一致性校验消费。CI 双重检查:(a) dependency-cruiser 禁止浏览器可达包(vm-ui、web-component、embed-runtime、react-wrapper)导入 server-only 子路径,防止 server-only 类型进入浏览器构建图诱发误序列化;(b) 任何浏览器可达响应 Schema 的字段不得引用 `ProjectionPolicy` 类型(第九章 ZR-P1 的组成部分)。

---

## 六、跨域信封字段分类(ActionRequest / ActionResponse / PublicError)

Schema 冻结归 WP-2 / WP-3;本表冻结**分类与可见性规则**,使"每个跨域字段在表中可查"成立。

### 6.1 `ActionRequest`(浏览器 → 服务端,`BOUNDARY`)

| 字段 | 分类 | 规则 |
|---|---|---|
| `protocolVersion` | `BOUNDARY` | 必须匹配服务端支持版本,否则拒绝 |
| `sessionId` | `BOUNDARY` | 不透明标识符;有效性由服务端会话表与 token 绑定校验,泄露危害限于会话劫持,由短期凭证与绑定缓解 |
| `clientSeq` | `BOUNDARY` | 玩家侧序号;服务端以其实现单会话串行确认,不作为权威序号(权威是服务端 revision) |
| `baseRevision` | `BOUNDARY` | 过期即拒绝(WP-2 冻结语义) |
| `idempotencyKey` | `BOUNDARY` | 防重;服务端生成映射,玩家值不承担安全性 |
| `action.type` / `action.args` | `BOUNDARY` | 玩家输入,即 9.2 表中"提交内容可见"类;服务端按契约重新校验类型、地址、区域权限、操作数、资源预算(6.2);Schema 解析剥离未知字段 |

### 6.2 `ActionResponse`(服务端 → 浏览器,`PUBLIC` 为主)

| 字段 | 分类 | 规则 |
|---|---|---|
| `requestId` | `PUBLIC` | **服务端生成**的请求关联 ID(冻结的 `ActionRequest` 六字段中无此字段,故不是客户端回显);客户端以 `idempotencyKey` 关联请求与响应。WP-2 若改为客户端携带,属契约变更,须走 1.3 流程并按 1.1 重新分类(应为 `BOUNDARY`) |
| `revision` | `PUBLIC` | 同 4.1 `revision` 论证 |
| `status` | `PUBLIC` | ∈ `running` / `paused` / `won` / `failed` / `rejected`;`rejected` 的具体原因走 `userVisibleError`,拒绝理由粒度受第十章粗化约束 |
| `projectionDelta` | `PUBLIC` | 增量形态;字段白名单同 `PublicStateProjection`(I-1);dirty range 只描述可见区域内的写入 |
| `publicEvents` | `PUBLIC` | 4.2 `PublicEvent` 规则 |
| `userVisibleError` | `PUBLIC` | 脱敏后错误;见 6.3 与第十章 |

### 6.3 `PublicError`(可见性规则冻结;枚举与格式归 WP-3)

| 规则(冻结) | 内容 |
|---|---|
| E-1 | `code` ∈ 冻结枚举(WP-3,覆盖 4.4 清单:输入格式、payload 长度、偏移不足/过长、端序、权限、非法 `RIP`、Canary 破坏、参数寄存器、目标条件未满足) |
| E-2 | `addressHex` 仅当落在可见区域时下发;否则统一占位符,且 `code` 保持不变——省略与否必须是 `(code, 可见状态, errorDetailLevel)` 的确定性函数(I-8),不得让"有无地址"本身成为信号 |
| E-3 | 携带的数值(长度、偏移、对齐、期望值)只能来自玩家输入或可见区域内容 |
| E-4 | "目标条件未满足"类错误零谓词信息:不含已匹配/未匹配计数、不含谓词标识、不含隐藏比较的任何部分结果(I-7) |
| E-5 | `message` 为按 `code` + 可见状态参数化的静态模板;educational 级才允许展开教学解释(第十章) |
| E-6 | 禁止出现:内部堆栈、文件路径、capability 名称(`virtual_file:*` 等)、隐藏区域名、seed 相关信息 |

---

## 七、派生公开字段粒度判定(决策记录,冻结)

派生公开字段 = 从私有状态派生其值的公开字段。本节记录粒度判定结论;每条以"结论 + 约束"形式成文,约束均落入第八章不变量或第九章机检条目。

### D1:`PublicStatus` 的 won/failed 公开时点与时间差侧信道

**问题**:won/failed 的公开时点是否通过时间差侧信道泄露隐藏目标条件?

**结论:在下列五条约束下无时间差侧信道,公开时点冻结为"动作响应同步携带"。** 约束全部可机检:

1. **无重询旁路**:won/failed 只在动作求值边界随响应下发;`sync-projection` 只重发最近一次已生成投影,不触发谓词重新求值,不构成可重询接口。提交(submit)的正式裁决走独立 verifier 异步链路(9.1),其时延由队列与重放主导,且裁决结果只取 11 种稳定结果类型,无部分匹配信息;
2. **恒定成本判定**:私有谓词求值的步数预算在题目资源预算内固定,与秘密值内容无关;隐藏字节比较禁止逐字节提前退出——提前退出会使"匹配前缀长度"成为可测信号。由单字节突变步数恒定测试强制(T-SC2,10.3);
3. **无进度字段**:不存在"已通过 N 项 / 共 M 项"类字段(I-7);失败响应只携带触发动作的可解释错误(E-4);
4. **交互与正式裁决分离**:交互会话的 `won`/`failed` 与正式裁决 `success`/`wrong_answer` 相互独立;前者是教学反馈,伪造它不改变成绩(ZR-T4),后者仅出自域 4 verifier;
5. **回退不可重询,神谕带宽受预算约束**:won/failed 为会话终态,`undo` / `checkout_checkpoint` / `reset` 在终态会话上以确定性错误拒绝;非终态会话的回退不重新求值历史判定;时间线回放与查看不受终态影响(它们只重放投影,不改状态)。交互式 won/failed 本质是设计内的验证通道(4.4"运行到结果"),无法也不应消除,其穷举带宽由 `constraints` 的**谓词求值次数预算**(按会话累计,跨 undo/reset 不重置,3.2)与每用户/每租户会话创建限流(9.2)共同限定,不构成无界神谕;预算的公开部分由公开描述包声明(7.1),完整预算属 `constraints` 私有。

### D2:`callStackSummary` 边界

**结论**:内容冻结为 `{index, functionLabel, returnAddressHex}`;深度上限常量 `CALL_STACK_MAX_DEPTH = 64`,超限截断,截断以不含计数的统一标记表达(不得显示"+N 帧",否则帧数成为隐藏控制流深度的信号)。**MVP 中代码区恒公开**,调用深度是公开程序的确定性行为,玩家可自行推导,故深度本身公开无泄露;含隐藏代码区/隐藏控制流的题目超出 MVP 范围,由 WP-4 检查器在题目校验期拒绝(公开描述包不得声明隐藏代码区域)。帧基址不直接下发,由玩家从可见 `RSP`/`RBP` 推导,保留教学价值。若未来引入非公开代码区(非 MVP),`functionLabel` 与 `returnAddressHex` 整体替换为统一占位帧(标签与地址一起隐藏),与 D5 的 `currentInstruction` 占位规则对齐。

### D3:`visibleRegions` 边界与 `maxBytesPerRange` 默认值策略

**结论**:

- **区域级**:仅白名单区域进入投影;隐藏区域无占位、无条目、无计数;可见区域与隐藏对象不重叠由编译期检查强制(I-2);相邻可见区域之间的"间隙"含义只有"不可见"——与公开布局的补集一致,不含额外信息;隐藏映射地址与未映射地址的访问结果由 I-9 统一,探测扫描无法区分"此处存在隐藏区域"与"此处未映射",故间隙不构成隐藏区域存在性信号;
- **字节级**:`maxBytesPerRange` 默认 **256**,单策略上限 **4096**,单 revision 投影字节总预算 **8192**(数值冻结;依据:MVP 教学视图 buffer 64 B + saved `RBP` + 返回地址 ≈ 128 B,256 留观察余量;4096 与总预算供 WP-3 落为 Schema maxima)。字节窗口锚定区域起点,不支持玩家任选偏移窗口(简化脱敏面;时间线回看走历史投影);截断统一 `truncated` 标记,不含省略字节数;
- **写入可见性**:对可见区域的写入(无论来源是玩家动作还是教学伪 syscall)都属于可见表面;私有逻辑向可见区域写入秘密内容属题目设计错误,由差分测试兜底检测(T-SC4)与编译期别名检查(I-2 的别名变体)拒绝。

### D4:`publicEvents` 过滤与编号

**结论**:公开事件是私有事件日志经白名单过滤后的子集;私有事件被丢弃时**不留占位**;`seq` 采用公开事件流**独立稠密编号**,禁止沿用私有日志序号——否则序号空洞可被用于计数隐藏事件(经典计数侧信道)。每动作公开事件数上限 256,超限按确定性规则聚合(聚合不依赖任何私有状态)。

### D5:`controlFlow` 与伪指令展示

**结论**:当前指令展示文本由服务端从展示数据单独生成(7.3),不是可执行 IR 的任何形态;MVP 代码区恒公开,`RIP` 展示恒可行;若未来引入非公开代码区(非 MVP 范围),`currentInstruction` 必须整体替换为统一占位符(地址与文本一起隐藏),不得只藏地址留文本。`pausedOn` 枚举(`read` / `write` / `call` / `ret` / `exception`)是玩家动作触发的暂停原因,属教学反馈,公开。

### D6:交互状态与正式裁决的关系

**结论**:交互会话的 `PublicStatus`(won/failed)与正式裁决(11 种结果类型,出自域 4 verifier)相互独立。浏览器伪造 won 状态只影响本地显示,不影响成绩与权威状态(ZR-T1/T4 验证);正式裁决只能由 verifier 对规范化动作日志独立重放产生。此分离使"前端显示"与"权威结果"在信任模型上彻底解耦。won/failed 的终态语义见 D1 约束 5。

---

## 八、机器可验证不变量(供 CI 与检查器直接引用)

| ID | 不变量 | 检查时机 |
|---|---|---|
| I-1 | 字段白名单:任何跨域载荷的字段路径 ⊆ 本文冻结的 `PUBLIC`/`BOUNDARY` 集合;未知字段即违规(解析层剥离 + Schema `additionalProperties: false`) | WP-2/3 Schema;阶段三投影实现;阶段五消息校验 |
| I-2 | 可见/隐藏不重叠:`visibleRegions` 与隐藏对象地址范围交集为空;别名变体:隐藏对象不得经由任何映射暴露到可见区域 | WP-4 编译期检查器 |
| I-3 | 秘密汇不可见:编译器从私有包秘密声明做**污点推导**,所有秘密派生值(寄存器、FLAG 位、派生地址)自动标记为秘密汇,禁止进入 `visibleRegisters` 白名单;作者显式声明仅作补充,不得放宽 | WP-4 编译期检查器 |
| I-4 | 一致性均匀性:相同(可见状态, 动作)必然产生字节相同的公开响应,与隐藏状态无关 | T-SC1(第十章) |
| I-5 | 稠密公开序号:`revision` 仅已执行(非拒绝)动作 +1(v1.1,论证见 1.3.1);`PublicEvent.seq` 独立稠密,无私有空洞 | WP-2 语义;阶段三运行时断言 |
| I-6 | 恒定成本比较:隐藏字节比较步数与候选内容无关(无提前退出) | T-SC2 |
| I-7 | 无进度泄露:任何公开结构不得携带隐藏判定的部分通过信息(计数、比例、命中掩码) | WP-3 Schema 审查;T-SC1 |
| I-8 | 公开可选字段可见性确定性:错误字段与 `PublicEvent` 地址/长度字段的省略/占位,是其语境码(错误 `code` 或事件 `kind`)、可见状态与 `errorDetailLevel` 的确定性函数 | 第十章粗化矩阵;ZR-P6 |
| I-9 | 统一不可见访问结果:对任何非可见地址(隐藏映射或未映射)的访问,其全部可观察结果(错误 `code`、占位符、事件形态、`revision` 行为、响应字节)与规范"未映射地址"完全一致;不可见访问不产生 `PublicEvent`,杜绝以探测行为绘制隐藏区域边界 | WP-2 动作语义;阶段二引擎实现;T-SC1 探针变体 |
| I-10 | 公开字节值来源约束:所有携带字节/数值的 `PUBLIC` 字段(`bytesHex`、`valueHex`、`payloadHex`、`returnAddressHex`、`semanticHighlights.label` 等)的值来源 ⊆ {玩家输入, 可见区域内容, 公开布局常量, 服务端静态模板};T-SC1 的允许差异字段注册表默认为**空集**,未来新增豁免须走 1.3 契约变更并在本条注册 | 阶段三投影实现;ZR-P8 |

---

## 九、秘密零驻留清单(可机检条目)

每条含:违禁/断言内容、检查对象、机检方法、落地阶段与责任包。**扫描器实现时必须直接引用本文条目 ID**(测试名、CI job 名、告警文案携带 ID),保持单一来源;条目方法本文冻结,扫描器数值参数(如语料格式)在其落地阶段确定并在该阶段文档中归档。

**9.2 禁止下发清单 → 条目映射**(逐项可追溯):

| 9.2 禁止项 | 落点 |
|---|---|
| 隐藏 flag | ZR-B1、ZR-R1 |
| 隐藏测试 | ZR-B2、ZR-R1 |
| 私有目标条件 | ZR-B2(谓词数据)+ E-4 / I-7(结构性)+ ZR-P3 |
| 私有 capability 返回值 | ZR-B11(内容语料)、ZR-B8(名称语法) |
| 完整 IR | ZR-B3 |
| 原始快照 | ZR-B5 |
| 完整事件日志 | ZR-B10(形态检测)、ZR-R2 |
| 内部堆栈和文件路径 | ZR-B7、ZR-R2 |

**扫描器自检纪律**:每条 ZR-* 条目落地时必须附带至少一个**必触发反例 fixture**(红灯样例),证明扫描器在真实违规样例上触发——glob 写错、快照抓取失败、语料缺失都表现为静默通过,与零命中不可区分,属无效控制。语料法(ZR-B1/B2/B4/B11)定位为兜底控制;主控制是结构性不变量(I-1、I-2、I-9、I-10)。

### 9.1 构建产物检查(13.5 第一类;检查对象:前端 bundle、SourceMap、静态资源、网络响应录制)

| ID | 违禁内容 | 机检方法 |
|---|---|---|
| ZR-B1 | 隐藏 flag 值 | 已知秘密语料扫描:CI 用含合成秘密(已知字节串)的 fixture 题目构建,对全部浏览器产物做精确与模糊(fuzzy,大小写/编码变换)匹配 |
| ZR-B2 | 隐藏测试与私有谓词数据 | 同上语料法;另扫描谓词参数字典(私有包编译期导出的秘密字符串集) |
| ZR-B3 | 完整 IR 与引擎代码 | 依赖图检查(dependency-cruiser / cargo workspace 声明):浏览器构建图不得含引擎 crate 与 `challenge-compiler`;产物符号表扫描 crate 名(3.3 标识符) |
| ZR-B4 | 私有题目包内容 | 私有包字段键名集合扫描;fixture 私有包内容语料扫描 |
| ZR-B5 | 权威快照 | 快照 blob 魔数/版本头字节模式扫描 |
| ZR-B6 | 服务端 seed 值 | fixture seed 语料扫描;`seedState` 内部状态字节模式 |
| ZR-B7 | 内部堆栈与文件路径 | 模式扫描:源码路径片段(`/src/`、`vm-engine/`、`.rs` 文件名)、堆栈帧语法(`at fn (file:line)`)出现在任何浏览器可达响应 |
| ZR-B8 | 私有 capability 名称 | 语法扫描:`virtual_file:` 等 capability 前缀 + 动态拼接形态(DSL 编译期已禁,此处兜底) |
| ZR-B9 | `SERVER_ONLY` 类型标识符 | 3.3 标识符清单词边界扫描(类型名 + 字段名)+ 共现形态规则(3.3) |
| ZR-B10 | 完整事件日志(形态) | 结构检测:任何数组载荷中出现 `kind` 不在冻结 `PublicEvent` 枚举(4.2)内的条目,或条目字段超出 `PublicEvent` 冻结集合,即违规 |
| ZR-B11 | 私有 capability 返回值 | fixture 虚拟文件内容语料扫描(与 ZR-B1 同法);`virtual_file:*` 类 capability 的读取结果不得以任何编码出现在浏览器可达产物 |

落地:ZR-B3/B9 可随 WP-0/CI 基线先行;ZR-B1/B2/B4/B5/B6/B8/B10/B11 随题目 fixture 到位(阶段二起)补齐语料。

### 9.2 运行时检查(13.5 第二类;检查对象:主线程堆、Worker 内存、IndexedDB、localStorage、Cache Storage、WebSocket 帧)

| ID | 断言 | 机检方法 |
|---|---|---|
| ZR-R1 | 秘密语料零命中 | Playwright + CDP 堆快照:运行完整教学闭环脚本后抓取堆快照与全部存储,对秘密语料扫描零命中 |
| ZR-R2 | `SERVER_ONLY` 结构零驻留 | 同上,对 3.3 标识符与 ZR-B5/B8 模式扫描 |
| ZR-R3 | 全通道零泄露 | 代理层录制**全部跨域载荷**:WSS 帧、HTTPS 响应体(create-session / submit / sync-projection)、postMessage 流量、回放与 trace 下载产物;断言各自 ⊆ 冻结 `PUBLIC`/`BOUNDARY` Schema(I-1)且无语料命中 |
| ZR-R4 | 存储零秘密 | 遍历 IndexedDB/localStorage/Cache Storage 全部键值,断言仅含 UI 状态、草稿与脱敏投影缓存 |
| ZR-R5 | 诊断 trace 受限导出 | 诊断模式 trace 导出(计划书 10.4)内容按公开回放同一 Schema 约束 + 语料零命中;诊断模式默认关闭,开启记录为审计事件 |

落地:阶段四(UI)/阶段五(插件)实现;规则本文冻结。

### 9.3 投影泄漏检查(13.5 第三类)

| ID | 断言 | 机检方法 |
|---|---|---|
| ZR-P1 | 字段白名单 | Schema 驱动:全部投影/增量/错误响应按冻结 Schema 校验,未知字段拒绝;`x-sm-class: server-only` 类型不得出现在任何响应 Schema 引用图 |
| ZR-P2 | 随机秘密差分 | T-SC4(第十章):不同 seed → 差异字段 ⊆ 声明 seed-public 字段集 |
| ZR-P3 | 确定性通道无相关性 | T-SC1:秘密变体下响应规范化字节完全相等 |
| ZR-P4 | 时序无相关性 | T-SC3:动作时延与秘密长度/汉明距离回归斜率为零(噪声带内) |
| ZR-P5 | 序号与计数 | 运行时断言 I-5:revision 增量 ∈ {0, +1},判定基准为"动作是否进入执行"(参数校验失败、`baseRevision` 过期、不可见地址、终态违规、幂等冲突、乱序序号等前置拒绝为 0,其余为 +1);`PublicEvent.seq` 连续;公开事件计数不含私有事件占位 |
| ZR-P6 | 错误粗化一致性 | 相同 `(code, 可见状态, errorDetailLevel)` 下错误载荷字节一致(I-8);错误参数仅来源于可见状态或玩家输入(信息流断言) |
| ZR-P7 | 恒定步数比较 | T-SC2(10.3):隐藏比较的谓词求值步数对候选内容恒定(单字节突变);引擎侧属性测试 |
| ZR-P8 | 公开字节信息流 | I-10 断言:测试插桩的污点追踪验证全部携带字节的 `PUBLIC` 字段值来源 ⊆ 允许源集合(玩家输入、可见区域、公开布局常量、静态模板) |

落地:ZR-P1 随 WP-3 Schema;ZR-P2–P8 随阶段二/三实现(测试框架先行,实现到位即启用)。

### 9.4 客户端篡改检查(13.5 第四类;Compose 集成测试形态)

| ID | 断言 | 机检方法 |
|---|---|---|
| ZR-T1 | 篡改前端状态不影响权威状态 | 集成测试:篡改浏览器侧状态后继续交互,服务端状态哈希与动作日志不受影响 |
| ZR-T2 | 伪造投影不被采信 | 客户端伪造投影/状态上报:服务端不读取(解析层剥离);后续响应与服务端权威投影一致 |
| ZR-T3 | 重放旧请求幂等 | 同 `idempotencyKey` 重放:返回缓存响应,动作日志不追加;旧 `baseRevision` 请求被拒 |
| ZR-T4 | 伪造成功标志不改成绩 | 客户端伪造 won/成功后 submit:verifier 独立重放裁决与未伪造会话一致 |

落地:阶段二/三集成测试(Compose 拓扑,5.8 门禁 7)。

---

## 十、侧信道判定规则

### 10.1 错误精度粗化策略(`errorDetailLevel`)

两级共同遵守:错误只能由 `(code, 玩家输入, 可见状态)` 确定性生成(E-1–E-6,I-8);禁止任何依赖隐藏状态的分支改变措辞、字段有无或数值。

| 允许暴露的内容 | `coarse` | `educational` |
|---|---|---|
| 错误码(冻结枚举) | ✓ | ✓ |
| 静态最小文案 | ✓ | ✓ |
| 目标地址(仅当落在可见区域,否则统一占位,E-2) | ✗ | ✓ |
| 玩家输入值的解释(如何被解释:little-endian qword 等) | ✗ | ✓ |
| 可见区域的权限/对齐事实(为什么不在可执行/可写区域) | ✗ | ✓ |
| 教学提示(检查 padding、little-endian、`RSP` 位置——4.4) | ✗ | ✓ |
| 隐藏区域地址/名称/存在性 | ✗ 禁止 | ✗ 禁止 |
| 私有谓词标识、参数、部分匹配结果、命中计数(E-4,I-7) | ✗ 禁止 | ✗ 禁止 |
| capability 名称、内部堆栈、文件路径、seed 信息(E-6) | ✗ 禁止 | ✗ 禁止 |

`coarse` 用于正式判题与 verifier 语境及策略要求的场合;`educational` 用于交互会话(默认),其展开解释是本项目教学价值核心(4.4),但解释字段全部由可见状态参数化。

### 10.2 确定性通道无相关性判定(T-SC1:秘密变体等价测试)

**原理**:本项目的确定性纪律(6.3)使投影长度、事件数量、错误序列成为 `(可见状态, 动作)` 的确定性函数——因此相关性判定可以做到**精确相等**,无需统计。

**方法**:取 fixture 题目,仅替换秘密值(长度变体 8/16/32/64 字节 + 同长不同值变体),其余(布局、代码、玩家动作脚本)完全相同;动作脚本必须包含**探针变体**——对若干可见区域外固定地址的读写动作,同步验证 I-9:隐藏映射地址与未映射地址在所有变体下的观测完全一致;逐动作回放,收集每步响应;**判定:全部响应经规范化序列化(WP-6 规则)后字节完全相等**。允许差异字段注册表(I-10)**默认为空集**;任何非空差异即违规。未来新增豁免字段须走 1.3 契约变更并在 I-10 注册。

**覆盖通道**:响应 JSON 字节长度(隐含投影长度)、`publicEvents` 数量与内容、错误 `code` 序列、`semanticHighlights` 集合、`callStackSummary` 深度、`truncated` 标记、探针动作的统一拒绝形态。

**落地纪律**:T-SC1 对 CI fixture 与**每道发布题目**(13.2 发布管线,针对其声明秘密生成变体)均为必过项——T-SC4 的 seed 差分对固定(非 seed)秘密无效,T-SC1 是唯一兜底。

### 10.3 恒定成本判定(T-SC2:单字节突变步数恒定测试)

**目的**:强制 I-6——隐藏字节比较不得逐字节提前退出;提前退出会使"最长匹配前缀长度"成为可测信号。这是时序之外的第二条通道:步数计数。

**方法**:

1. **fixture**:私有包含固定隐藏比较谓词(候选 vs 秘密,定长);
2. **突变**:对候选逐位置(0..len-1)、逐值(0x00–0xFF 抽样)生成突变候选,并覆盖全部前缀形态(全等、首字节差、尾字节差、位置 i 为首个差异);
3. **测量**:以**谓词求值步数**(指令预算消耗,引擎确定性指标)为主判据,动作时延为辅;
4. **判定**:全部突变候选的谓词求值步数集合方差为零;步数随差异位置或差异内容变化即违规。

**落地纪律**:引擎侧属性测试(proptest)随 CI 运行;每道发布题目对其谓词实现必跑(13.2 发布管线)。本测试是 10.4 时序判定的前提。

### 10.4 时序无相关性判定(T-SC3)

时序不是确定性通道,判定走统计方法,流程冻结、数值标定:

1. **基线标定**:对秘密不变的固定脚本测得动作时延分布(每动作 p50/p95),归档为噪声基线;同一脚本多次运行给出自然抖动带;
2. **变体测量**:秘密按长度(8/16/32/64 字节)与汉明距离(同长逐位翻转 1/2/4 字节)变体,重复固定脚本;
3. **判定**:对时延关于秘密长度/汉明距离做回归,斜率与 0 无显著差异,且各变体 p95 落在基线自然抖动带内。数值阈值(显著性水平、抖动带宽度)在实现期按基线标定并归档——方法与判定流程本文冻结,不留主观判断;
4. **约束**:判定前必须先完成 I-6(单字节突变步数恒定,T-SC2,10.3)——步数恒定是时序无相关的前提;时序修复优先靠消除内容相关分支,不靠注噪。

**触发纪律**:引擎或谓词实现变更时全量回归;题目发布管线按抽样运行(T-SC2 的步数恒定已覆盖主通道,时序测试作引擎级保障)。

### 10.5 随机秘密投影差分测试方法(T-SC4,计划书 9.2)

**目的**:证明"投影差异只来自被声明公开的随机化,随机秘密本身不显形"。

**方法**:

1. **同 seed 确定性(前提)**:同 seed 同脚本两次运行,投影序列规范化后字节一致(6.3);
2. **异 seed 差分**:同脚本不同 seed 运行,逐 revision 对投影做树 diff;**判定:差异字段路径 ⊆ 私有包 seed 策略显式声明的 `declaredSeedPublicPaths`**(例:公开宣告的随机栈基址)。其余任何字段路径出现差异即违规;
3. **实现形态**:属性测试(fast-check / proptest)随机生成秘密内容与 seed,断言上述两条;对每组 fixture 题目在 CI 运行,并纳入题目发布管线(13.2);
4. **秘密汇兜底**:随机秘密流入白名单寄存器/可见区域(违反 I-3 设计的题目)由此测试暴露为差异——差异即发布阻断。

### 10.6 修复纪律

- 任何一侧信道判定失败,修复方向是**结构性收窄**(白名单、统一占位、恒定成本),不是加噪声;
- **禁止**以随机填充/随机化响应内容掩盖设计泄漏——内容层随机化破坏投影确定性(6.3)与回放一致性;
- 时序噪声只允许在编排器层作为最后手段,必须归档说明并复测 T-SC3;
- 修复后全部 T-SC1–T-SC4 重跑。

---

## 十一、硬门槛对照结论与 WP-1 完成标准自查

**硬门槛逐字段结论**:`PublicStateProjection` 全部 7 个顶层字段、全部子类型(§4.2 七个子类型 + v1.2 增补的 `DirtyRange`)、`ProjectionDelta`(§4.3)、`PublicError` 含解释字段(§4.4)、`ActionResponse` 全部字段的"秘密不进入浏览器"论证见第四章/第六章,逐条通过;论证失败的字段不存在——`VmState` 与 `ProjectionPolicy` 整体归 `SERVER_ONLY`(第三、五章),没有部分下发的灰地带。

| WP-1 完成标准(阶段一任务分解) | 落点 | 状态 |
|---|---|---|
| 每个跨域字段在表中可查且有归属边界 | 第三、四、五、六章分类表(覆盖 `VmState` 8 字段、投影 7 字段 + 7 子类型、策略 5 字段、信封 12 字段) | ✅ |
| 清单条目粒度足以让 CI 扫描直接引用 | 第九章 ZR-B1–B11 / ZR-R1–R5 / ZR-P1–P8 / ZR-T1–T4(28 条),含 9.2 禁止项映射表与扫描器自检纪律;不变量 I-1–I-10 | ✅ |
| 每个公开字段能通过硬门槛论证 | 第四章逐字段论证列 + 决策 D1–D6 | ✅ |
| `PublicStatus` 时点侧信道判定 | D1(结论:五条可机检约束下无侧信道,含终态与神谕带宽约束) | ✅ |
| `callStackSummary` / `visibleRegions` 边界与 `maxBytesPerRange` 默认值 | D2、D3(默认 256 / 上限 4096 / 总预算 8192) | ✅ |
| `VmState` 字段集合冻结 + 两条归属规则 | 3.1、3.2 | ✅ |
| 9.2 禁止下发清单 + 13.5 四类检查项成文可机检 | 第九章(28 条,9.2 八项逐项映射 + 13.5 四类 1:1 落条) | ✅ |
| 错误精度粗化策略 | 10.1 矩阵 + E-1–E-6 | ✅ |
| 长度/事件数/错误分类/时序无相关性判定方法 | 10.2(T-SC1 精确相等 + 探针变体)、10.3(T-SC2 恒定步数)、10.4(T-SC3 统计流程) | ✅ |
| 随机秘密投影差分测试方法 | 10.5(T-SC4) | ✅ |

---

## 十二、题目双包字段级分类(WP-4 · v1.3 新增)

本章按计划书 7.1 双包模型冻结两个字段集合:**公开描述包**(整体 `PUBLIC`,逐字段硬门槛论证见 12.2)与**私有判题包**(整体 `SERVER_ONLY`,12.3)。Schema 落盘于 `@stackmaster/challenge-schema`(手写 JSON Schema 2020-12 + Ajv):公开面经包根导入;私有面(私有包 Schema 与字段分类检查器)仅经 `./server-only` 子路径供后端包(challenge-compiler、session-api、verifier)消费——镜像第五章 `ProjectionPolicy` 的"Schema 存在不等于可下发"纪律。双包均无 `BOUNDARY` 字段:双包都不是玩家输入或短期凭证的回传(1.1 判定次序第二步直接否决)。

### 12.1 分类总则

- 公开描述包 = 计划书 7.1 公开七项清单的逐项落位(12.2 表最右列给出一一对应);
- 私有判题包 = 计划书 7.1 私有七项清单的逐项落位(12.3 表同);
- **共享身份字段**:两包均携带 `schemaVersion` / `challengeId` / `challengeContentVersion` / `vmProfileVersion`——公开包内为 `PUBLIC`(12.2 逐字段论证);私有包内随整包 `SERVER_ONLY`,不因语义无害而获得独立下发路径;
- **机检分工**:字段混装检查(公开 Schema 不得出现私有包顶层属性名,元检查 XS-1)、公开包无隐藏区域表达位(D2-NO-HIDDEN-IN-PUBLIC)、capability 名称禁入公开包实例(ZR-B8-CAP-SCAN)由 `challenge-schema` 检查器承接,规则 ID 对照见 12.6;
- Schema 层的每层 `additionalProperties: false` 承接 I-1(未知字段即违规,不存在"忽略后接受")。

### 12.2 公开描述包字段级分类(冻结;Schema 细节归 `challenge-schema`)

| 字段 | 分类 | 硬门槛论证:为何公开不泄露隐藏状态 | 7.1 清单落位 |
|---|---|---|---|
| `schemaVersion` | `PUBLIC` | 格式信封版本常量(const 1),不含题目内容;演进走 7.4 版本号,无预留字段 | (信封) |
| `challengeId` | `PUBLIC` | 题目公开标识符;CDN 寻址与会话创建按 ID 引用是产品需求;题目是公开教学内容,标识符可预测无危害 | (信封) |
| `challengeContentVersion` | `PUBLIC` | 7.4 第 4 类版本;UI 展示与缓存失效消费;版本号不编码秘密 | (信封) |
| `vmProfileVersion` | `PUBLIC` | 7.4 第 3 类版本;寄存器白名单与 VM Profile 演进的版本锚点(UI 缓存失效与引擎侧 profile 匹配按版本号消费);版本号不编码秘密;与私有包同值(XS-ID-CORR) | (信封) |
| `locale` | `PUBLIC` | 语言标签,冻结枚举(v1 仅 `zh-CN`);无内容 | (信封) |
| `briefing` | `PUBLIC` | 纯作者撰写的教学文案,值来源 = 公开描述包自身(I-10"公开布局常量/静态模板"类);长度预算对齐协议公开文本纪律(`title` ≤ 128) | 题面、学习目标和教学文案 |
| `vmProfile` | `PUBLIC` | `registers` = 教学可见寄存器白名单声明("白名单集合是题目设计公开的一部分",§4.1 `visibleRegisters` 同论证),**不得含 FLAG 寄存器**(12.5);`flagRegisterNames` = FLAG 寄存器**名称**(12.5:名称公开、值永不公开);`endianness` / `pageSizeBytes` / `archBits`(32/64 位宽声明,v1.4 G1/D1)= 架构公开常量(位宽只决定值长度与掩蔽域,不含秘密派生信息);`canary.enabled` / `sizeBytes` = 教学目标与公开布局参数——canary **值**在私有包(I3 / XS-CANARY-CORR 互证) | VM Profile 的公开元数据 |
| `memoryLayout` | `PUBLIC` | §4.1 `visibleRegions` 同论证:"区域布局本身是公开描述包声明的公开信息(7.1),不是运行时推导";公开包结构上**无隐藏区域表达位**(无 `isHidden` / `visibility` 类字段)——隐藏区域只能存在于私有包(D2-NO-HIDDEN-IN-PUBLIC 元检查);v1.4 G3:区域 `kind` 增补作者自定义 `custom` 类(`publicLabel` 必填承载类型名,同 `SemanticHighlight.custom` 先例),区域大小与 `pageSizeBytes` 为 4KB 的倍数(区域内对象不受约束)——自定义类型名与对齐值均为作者声明的公开布局常量,值来源属 I-10"公开布局常量"类,与秘密无相关 | 可见区域布局和公开对象标签 |
| `allowedActions` | `PUBLIC` | 12 动作冻结枚举的**子集选择**;参数 Schema 单一来源是 `@stackmaster/protocol` 会话动作契约(5.6 契约单一来源),公开包不复述参数定义,避免第二真相源漂移 | 允许动作及参数 Schema |
| `resourceLimits` | `PUBLIC` | D1 约束 5 直接落位:"预算的公开部分由公开描述包声明,完整预算属 `constraints` 私有"。三个题目可裁量预算:谓词求值次数 / 回退次数 / 单动作写字节(≤ 协议 `MAX_WRITE_BYTES`)。揭示上限**降低**探测收益(玩家无须浪费动作顶限额试探);协议冻结常量(`MAX_PROJECTION_BYTES_PER_REVISION`、`CALL_STACK_MAX_DEPTH` 等)不是题目可授权面,公开包不得重新声明 | 资源限制的公开部分 |
| `hintLadder` | `PUBLIC` | 提示讲方法不给答案(与 E-4 / I-7 同纪律:不含谓词信息、不含隐藏比较结果);全量随包下发是产品决策(客户端渲染提示无服务端往返),`revealPolicy` 声明服务端展示策略 | 提示阶梯 |
| `publicErrorMapping` | `PUBLIC` | 错误码枚举本身是公开契约(E-1);`teachingNote` 为静态教学文案,不含谓词信息 | 公开错误映射 |
| `randomizationNotice` | `PUBLIC`(可选) | §3.2 `seedState` 行 sanction:公开面只允许文案声明"存在随机化"这一事实,**不声明策略、候选空间或熵上界**。作者侧纪律(题目评审承接),不做脆弱的语义机检;seed 差分(T-SC4 / ZR-P2)兜底 | (随机化存在性文案) |
| `initialProjection` | `PUBLIC` | 7.1"UI 所需的非秘密初始投影"——**部分镜像**,排除集见 12.2.1;公开值必须镜像私有初始态(XS-PROJ-VALUES),值来源属 I-10"可见区域内容"类且以 I-2 / I-3 检查通过为前提 | UI 所需的非秘密初始投影 |

### 12.2.1 `initialProjection` 排除集(冻结)

公开描述包的 `initialProjection` 只含**作者可声明**的三个子形状:`visibleRegions` / `visibleRegisters` / `semanticHighlights?`。投影的其余四个顶层字段被**结构性排除**(公开 Schema `additionalProperties: false` 使其不可表达;排除集由跨包一致性测试精确断言——`protocol` 新增顶层投影字段时测试失败,强制显式决策而非默认镜像):

| 排除字段 | 排除理由 |
|---|---|
| `revision` | I-5:会话内已执行(非拒绝)动作计数——静态描述包无从赋值,任何静态值都是矛盾陈述 |
| `callStackSummary` | D2:运行时调用深度,由初始 IR 执行状态派生;静态描述包不承载运行时派生量 |
| `controlFlow` | D5:`currentInstruction` 由服务端从展示数据单独生成;描述包若承载指令展示文本即逼近 IR 下发(7.3 禁令) |
| `status` | D1:运行时会话态;会话未创建时无 `status` 可言 |

**值一致性**:公开包声明的初始值(`bytesHex` / `valueHex`)**必须镜像**私有包 `initialState` 的对应切片(XS-PROJ-VALUES)——描述包不得自说自话出与权威初始态不一致的公开值;该一致性检查是"公开值来源"链条的闭合环节:先由 I-2 / I-3 证明所涉区域与寄存器和秘密无重叠,再要求公开值与私有初始态逐字节相等。

### 12.3 私有判题包字段级分类(冻结,整体 `SERVER_ONLY`)

**整体级论证**:计划书 7.1"私有判题包……只存在服务端";第二章总表已归类(对象存储 `private-bundles`,静态加密、最小权限;域 3 进程内存)。私有包没有任何 sanctioned 下发路径,任何字段出现在跨域载荷即违规——与 3.1 规则 1(`VmState` 无契约只有禁令)同构,故下表不再逐字段重复"为何不可下发"。

| 字段 | 语义 | 要点(义务与检查器锚点) |
|---|---|---|
| `schemaVersion` / `challengeId` / `challengeContentVersion` / `vmProfileVersion` | 共享身份字段 | 与公开包一致性由 XS-ID-CORR 强制 |
| `dslSchemaVersion` | 7.4 第 1 类版本(DSL Schema Version) | const 1;扩展走版本演进,无预留字段 |
| `vmEngineVersion`(必填) / `engineBuildId?` | 7.4 第 2 类版本 + 引擎构建 ID | 7.4"生产环境不得自动使用最新引擎,正式题目应锁定执行环境";§3.2"seed 策略与运行环境版本写入服务端回放信息"——同面 `SERVER_ONLY` |
| `declaredSeedPublicPaths`(必填,可空) | T-SC4 / ZR-P2 的判定基准 | 语法冻结见 12.4;空数组 = 显式声明"无公开随机化面" |
| `seedPolicy` | §3.2 `seedState` 的策略元数据来源 | `strategy` ∈ `fixed` / `server_random_per_session`;`fixed` ⇒ `seedHex` 必填(if/then);无 `algorithmId`(无冻结 PRNG,确定性由 `vmEngineVersion` 锁定) |
| `initialState` | `VmState` 的初始形态(3.2) | `registers`(全寄存器初始值,含 FLAG 值;初值落在题目 `archBits` 位宽域内,XS-ARCH-WIDTH)+ `memoryRegions`(含隐藏区域 `contentHex` 全字节;区域大小为 4KB 的倍数、类型枚举含 `custom`,XS-MEM-PAGE-ALIGN)——下发任何部分都等于下发原始快照切片(3.2 `memory` 行同论证) |
| `secretSinkRegisters?` | I-3 的作者显式声明面 | "作者显式声明仅作补充,不得放宽"(I-3 原文);声明项匹配寄存器命名模式;与公开白名单互斥由 I3-VISIBLE-REG 强制 |
| `secrets` | 隐藏 flag、隐藏字节和虚拟文件内容(7.1) | `flag` 8–128 字符;`virtualFiles` ≤ 8、单文件 ≤ 4096 字节;ZR-B1 / ZR-B11 语料源 |
| `privateObjects` | 私有对象和隐藏区域(7.1) | `visibility` ∈ `public` / `hidden` 与 `containsSecret` 布尔是 I-2 / I-3 检查器的输入;I3-SINK-HIDDEN:`containsSecret` ⇒ `hidden` |
| `judging` | 权威成功/失败谓词和隐藏测试(7.1) | `successCondition` / `failureConditions` = 封闭谓词枚举 + 三级布尔组合(深度为静态 Schema 属性,XS-NESTING);`hiddenTests[].expectedResult` ∈ 7 值可达判定枚举——隐藏测试是(输入, 预期判定)对,不是条件表达式,谓词数据不进任何公开面(ZR-B2);`challenge_invalid` / `replay_mismatch` / `cancelled` / `engine_error` 非可授权期望(D6 交互/正式分离) |
| `stages?` | 多阶段题目有限状态机(7.2) | 每状态:`allowedActions` / `preconditions` / `transitions` / `sideEffects` / `failureConditions` / `resourceBudget`;`resourceBudget.maxInstructionSteps` **必填**承接 7.2"循环必须有静态上限或动态指令预算";`sideEffects` v1 封闭仅 `grant_virtual_file`(有限副作用;capability 引用以结构化 `fileId` 字段表达,拒绝动态拼接,7.3) |
| `compiledIr`(必填) | 编译后的 IR(7.1) | ZR-B3:完整 IR 只进后端;信封 `{irFormatVersion const 1, entrypointIndex?, instructions ≤ 4096, labels ≤ 512}`;指令 = 21 opcode 封闭枚举 + 类型化操作数槽;逐 opcode 操作数合法性与 CFG 可达性归 challenge-compiler(阶段二) |
| `judgingConfig` | 题目专用判题配置(7.1) | `verdictRuleVersion`(7.4"判题规则版本")必填;`maxPredicateEvalSteps` **必填**(D1 约束 2 / I-6 / T-SC2 的配置前提:谓词求值预算存在才可断言恒定成本);`timeoutMsPerAction` / `maxTotalActionBytes` 可选 |

### 12.4 `declaredSeedPublicPaths` 路径语法(冻结)

- 必填数组,**允许空数组** = 显式声明"本题无公开随机化面"(合法形态:隐藏随机 canary 不产生任何投影差异);
- 路径为点分标识符序列(如 `visibleRegions.stack`);**根 ∈ `PublicStateProjection` 七顶层字段**;完整路径必须可解析到公开描述包 `initialProjection` 的具体叶子——声明不存在的路径 = 矛盾陈述,检查器 XS-SEED-DECL 拒绝;
- 语义:T-SC4 / ZR-P2 判定"异 seed 差异字段路径 ⊆ 本声明集合"(10.5);随机化只允许显形在被声明的公开路径上;
- 本字段随私有包 `SERVER_ONLY`:下发等于向玩家宣告"哪些投影字段随 seed 变化",直接缩小随机化搜索面。

### 12.5 FLAG 寄存器命名约定(XS-REG-FLAG,冻结)

6.1 冻结基集(`RSP`…`R12`)+ "题目需要的部分 FLAG"。本约定把 FLAG 子集的命名实例化:

1. 题目 FLAG 寄存器名称必须匹配 `^FLAG[A-Z0-9_]*$`;基集 14 寄存器名称不得匹配该模式(两个名称空间不相交,秘密汇可静态枚举);
2. **名称公开、值秘密**:`flagRegisterNames`(公开)只承载名称;FLAG 寄存器的**值**只能存在于私有包 `initialState.registers`,公开包任何字段不得携带(XS-PROJ-VALUES 校验公开初始值镜像);
3. **公开寄存器白名单(`vmProfile.registers`)不得包含 FLAG 寄存器**:该白名单是 `ProjectionPolicy.visibleRegisters` 的组装来源(第五章),FLAG 值一旦进入白名单即随投影下发(I-3 直接违规);`flagRegisterNames` 因此**不要求**是 `vmProfile.registers` 的子集(通常恰相反);
4. 效果:秘密汇寄存器集合 = `flagRegisterNames ∪ secretSinkRegisters`,可静态枚举,I3-VISIBLE-REG 机检可执行:`initialProjection.visibleRegisters ∩ 秘密汇 = ∅`。

### 12.6 WP-4 检查器规则对照(机检条目 → 落点)

扫描器与测试命名必须直接引用下表左列规则 ID(第九章自检纪律);每条规则落地时附带必触发红灯样例。

| 检查器规则(`challenge-schema`) | 本文 / 计划书依据 |
|---|---|
| I2-PUB-PAIRWISE / I2-PRIV-PAIRWISE / I2-PUB-MIRROR / I2-HIDDEN-NOT-PUBLIC / I2-OBJ-NOT-PUBLIC / I2-HIGHLIGHT | I-2(可见/隐藏不重叠;含公开镜像双射与高亮目标变体) |
| I3-SINK-HIDDEN / I3-VISIBLE-REG | I-3(秘密汇不可见;12.5 命名约定) |
| D2-CODE-PUBLIC / D2-NO-HIDDEN-IN-PUBLIC | 决策 D2(MVP 代码区恒公开;隐藏代码区校验期拒绝)+ §1.2 WP-4 行 |
| ZR-B8-CAP-SCAN | ZR-B8(capability 名称语法;公开描述包实例深扫描) |
| XS-ID-CORR / XS-ID-NO-PRIVATE / XS-ID-UNIQUE | 7.1(双包身份对应)、13.2(公开包不得经引用 ID 推导私有字段;标识符唯一性) |
| XS-REG-SUBSET / XS-REG-FROZEN / XS-REG-FLAG | §3.2 寄存器冻结基集 + 6.1 + 12.5 |
| XS-PROJ-GEOM / XS-PROJ-VALUES / XS-PROJ-REG | 7.1(UI 初始投影与公开布局、私有初始态一致)+ I-10 |
| XS-CANARY-CORR | 7.1(canary 公开元数据 ↔ 私有 canary 对象互证) |
| XS-MEM-TOTAL / XS-MEM-CONTENT | 13.2(过大内存布局拒绝;内容长度一致) |
| XS-ARCH-WIDTH(v1.4) | G1/D1(《Vm 模块设计冲突与整改方案》):archBits 位宽域——私有初始寄存器值、IR 立即数(无符号)与位移(有符号)、公开镜像值均须落在题目 `vmProfile.archBits` 声明的位宽域内;计划书 6.2(掩蔽域)+ I-10(公开镜像值同规则,作为 XS-PROJ-VALUES 的位宽前提) |
| XS-MEM-PAGE-ALIGN(v1.4) | G3/D2(《Vm 模块设计冲突与整改方案》):VMA 页对齐——公开/私有区域 `byteLength` 与 `pageSizeBytes` 均为 4KB 的倍数(区域内对象不受约束);计划书 6.1(VMA 基本单元)+ 13.2 |
| XS-SEED-DECL | §1.2 WP-4 行(`declaredSeedPublicPaths`)+ 10.5 + §3.2 |
| XS-STAGE-REACH / XS-STAGE-BUDGET | 7.2(状态机形态;循环上限) |
| XS-IR-LABEL | 13.2(未定义引用) |
| XS-PRED-REFS | 13.2(谓词引用可解析;越界访问) |
| XS-DUP-KEY | 13.2(重复键拒绝) |
| XS-NESTING(三级条件结构,深度静态) | 13.2(恶意嵌套)+ 7.3(布尔组合深度静态化) |
| 公开 Schema 元检查(XS-1) | §1.2 WP-4 行(公开包 Schema 不得表达任何 `SERVER_ONLY` 字段)+ I-1 |

### 12.7 硬门槛对照结论(本章)

公开描述包 14 个顶层字段的"秘密不进入浏览器"论证见 12.2(逐字段)与 12.2.1(排除集);私有判题包整体 `SERVER_ONLY`、无逐字段灰地带(12.3);`initialProjection` 的公开值镜像约束(XS-PROJ-VALUES)以 I-2 / I-3 检查通过为前提,闭合了"公开值来源"链条。本章新增的 `PUBLIC` 字面均未触碰既有第四章字段集合、决策 D1–D6 或机检条目 I-1–I-10 / ZR-* 的语义。
