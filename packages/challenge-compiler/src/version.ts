/**
 * @stackmaster/challenge-compiler 包版本(与 package.json 同步;非契约版本)。
 * 题目包契约版本由 challenge-schema 登记(dslSchemaVersion /
 * CHALLENGE_PACKAGE_SCHEMA_VERSION);编译器是契约消费方,不设独立契约版本。
 */
export const CHALLENGE_COMPILER_PACKAGE_VERSION = "0.1.0";
