[package]
name = "contract-smoke"
version = "0.1.0"
edition = "2024"

[dependencies]
jsonschema = "0.53.0"
schemars = "1.2.2"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
# 规范化序列化、严格解析与语义承接自 WP-1 起提升为引擎模块
# (vm-engine/vm-worker),本 crate 消费同一实现,禁止第二套实现。
vm-worker = { path = "../../vm-engine/vm-worker" }
